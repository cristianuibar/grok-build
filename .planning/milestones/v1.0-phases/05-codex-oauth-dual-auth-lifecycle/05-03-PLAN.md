---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/codex/mod.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/browser.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/device.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/claims.rs
  - crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs
  - crates/codegen/xai-grok-shell/src/auth/mod.rs
  - crates/codegen/xai-grok-shell/src/auth/flow.rs
  - crates/codegen/xai-grok-shell/src/auth/model.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
autonomous: true
requirements:
  - AUTH-02
user_setup:
  - service: ChatGPT account
    why: "Live browser smoke optional only; CI uses mock IdP / injected tokens"
    env_vars: []
    dashboard_config:
      - task: "Optional: enable device code in ChatGPT workspace security if using --device-auth live"
        location: "ChatGPT workspace security settings"
must_haves:
  truths:
    - "Mock/inject Codex login persists only providers.codex under explicit auth path / temp product home"
    - "Authorize URL includes public client_id, S256, localhost auth/callback, offline_access, originator bum"
    - "Browser loopback binds 127.0.0.1:1455 with 1457 fallback; redirect host string is localhost"
    - "Device flow uses Codex deviceauth usercode/token with pending/slowdown/exchange/denied coverage"
    - "State mismatch / OAuth error / token-exchange failure write nothing to providers.codex"
    - "expires_at derived from JWT exp when token response lacks expires_in; unparseable expiry treated conservatively"
    - "chatgpt_account_id maps to GrokAuth.organization_id; email when present"
    - "Codex login branches BEFORE xAI post-login managed-config sync (no Codex principal into xAI team config)"
    - "bum login --provider codex routes to Codex; bare login remains xAI-only"
    - "Re-login codex overwrites only codex; seeded xAI remains"
    - "Success copy Signed in to Codex; never prints tokens"
    - "Named login/device/authorize/state-failure lifecycle tests GREEN"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/codex/mod.rs
      provides: "Codex OAuth constants + login entry"
      contains: CODEX_CLIENT_ID
    - path: crates/codegen/xai-grok-shell/src/auth/codex/browser.rs
      provides: "axum loopback PKCE login"
      contains: auth/callback
    - path: crates/codegen/xai-grok-shell/src/auth/codex/device.rs
      provides: "deviceauth usercode/token poll"
      contains: deviceauth
    - path: crates/codegen/xai-grok-shell/src/auth/flow.rs
      provides: "run_cli_login provider dispatch; Codex early return before xAI managed sync"
      contains: provider
  key_links:
    - from: run_cli_login(--provider codex)
      to: codex browser/device login
      via: "provider match → persist mutate_provider(AuthProvider::Codex); return before post_login_sync xAI path"
      pattern: "AuthProvider::Codex|PROVIDER_CODEX|post_login"
    - from: token exchange success
      to: providers.codex GrokAuth
      via: "AuthMode::Oidc + refresh_token + organization_id + expires_at from expires_in or JWT exp"
      pattern: "organization_id|refresh_token|expires_at|AuthMode::Oidc"
    - from: authorize URL
      to: auth.openai.com oauth/authorize
      via: "PKCE S256 + state + localhost redirect"
      pattern: "code_challenge|localhost|1455|auth/callback"
---

## Phase Goal

**As a** bum user, **I want to** log in to ChatGPT/Codex via OAuth into the bum store, **so that** GPT models can use my ChatGPT subscription credentials without touching xAI credentials.

<objective>
Implement in-tree Codex ChatGPT OAuth (browser PKCE + device-code) and CLI `bum login --provider codex` that persists only `providers.codex` (AUTH-02; D-01, D-02, D-04, D-09..D-13). Addresses review: branch before xAI managed-config sync; JWT exp fallback; stronger device + OAuth failure tests.

Purpose: Fill reserved Codex slot with real OAuth lifecycle; Phase 4 routing already selects the slot.
Output: Mockable Codex login + CLI provider dispatch; login/device/state contracts GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
@crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
@crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs
@crates/codegen/xai-grok-shell/src/auth/device_code.rs
@crates/codegen/xai-grok-shell/src/auth/flow.rs
@crates/codegen/xai-grok-shell/src/auth/storage.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | `--provider codex\|xai`; bare login xAI-only |
| D-02 | Browser PKCE default; device flags for headless |
| D-04 | Re-login overwrites only targeted slot |
| D-09 | No stock import; `$BUM_HOME/auth.json` only |
| D-10 | In-tree mirror: ports 1455/1457, localhost redirect, public client_id |
| D-11 | Persist tokens + chatgpt_account_id → organization_id |
| D-12 | ChatGPT OAuth primary — not Platform API key |
| D-13 | Mock IdP tests; no live ChatGPT required |

**Discretion locks:**
- Module placement: `auth/codex/{mod,browser,device,claims}.rs`
- CODEX_AUTH_SCOPE = `https://auth.openai.com::app_EMoamEEZ73f0CkXaXp7hrann`
- Share generate_pkce via pub(crate) from oidc/protocol
- originator=bum on authorize URL
- Skip obtain_api_key Platform exchange (COVERAGE OPT-OUT)
- Injectable HTTP / token-exchange hooks for tests
- **CLI branch (review):** Codex path returns after Codex persist **before** any `managed_config::post_login_sync` / xAI team-config tail in run_cli_login
- **expires_at (review):** if token JSON lacks expires_in, parse access-token JWT exp; if unparseable, treat conservatively (short TTL or require refresh soon — document choice; never multi-day silent TTL that skips refresh)
- Typed AuthProvider at clap/flow boundary when Plan 01 left string Option

## Codex OAuth constants (must match RESEARCH)

| Constant | Value |
|----------|-------|
| Issuer | `https://auth.openai.com` |
| client_id | `app_EMoamEEZ73f0CkXaXp7hrann` |
| Ports | 1455 preferred, 1457 fallback |
| Redirect | `http://localhost:{port}/auth/callback` (host **localhost**) |
| Bind | `127.0.0.1:{port}` |
| Token | `{issuer}/oauth/token` |
| Device usercode | `{issuer}/api/accounts/deviceauth/usercode` |
| Device token | `{issuer}/api/accounts/deviceauth/token` |
| Device verify | `{issuer}/codex/device` |
| Scopes | `openid profile email offline_access api.connectors.read api.connectors.invoke` |

## UI-SPEC copy (login)

- Start: `Signing in with Codex...` / `Open this URL to sign in:` + indented URL
- Success: `✓ Signed in to Codex as {email}` or without email
- Port bind fail: UI-SPEC bind error suggesting `--device-auth`
- Never print tokens in errors

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codex module constants, PKCE URL, claims, mock exchange persist + failure isolation</name>
  <files>crates/codegen/xai-grok-shell/src/auth/codex/mod.rs, crates/codegen/xai-grok-shell/src/auth/codex/browser.rs, crates/codegen/xai-grok-shell/src/auth/codex/claims.rs, crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs, crates/codegen/xai-grok-shell/src/auth/mod.rs, crates/codegen/xai-grok-shell/src/auth/model.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-03 expiry + state failures)
    - crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
    - crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs
    - crates/codegen/xai-grok-shell/src/auth/jwt.rs
    - crates/codegen/xai-grok-shell/src/auth/storage.rs
  </read_first>
  <behavior>
    - codex_authorize_url_includes_pkce_and_localhost_callback GREEN
    - codex_login_persists_slot GREEN (inject/mock path; xAI sibling preserved)
    - claims map chatgpt_account_id → organization_id; expires_at from JWT exp when expires_in missing
    - codex_oauth_state_mismatch_writes_nothing GREEN
    - OAuth error / exchange failure does not write providers.codex
  </behavior>
  <action>
    Build Codex OAuth core (D-10, D-11, D-12, D-13; COVERAGE authorize/token/claims INTEGRATE). Addresses review: expiry JWT + security failure contracts.

    1. Create auth/codex/mod.rs with constants; re-export login entrypoints.
    2. Share PKCE from oidc protocol — do not hand-roll crypto.
    3. Authorize URL builder (RESEARCH). Validate state on callback; reject mismatch/missing state with no disk write.
    4. claims.rs: insecure JWT decode for exp/email/chatgpt_account_id → organization_id; set expires_at from expires_in if present else JWT exp; conservative fallback if both missing/unparseable.
    5. browser.rs axum loopback: bind 1455 then 1457; redirect localhost host + /auth/callback; form POST token; persist via public mutate_provider_store_or_prune(AuthProvider::Codex, ...) AuthMode::Oidc with key/refresh_token/expires_at/oidc_issuer/client_id/organization_id/email.
    6. Test inject path for GREEN without real ports when possible; unit-test authorize URL without network.
    7. GREEN codex_oauth_state_mismatch_writes_nothing and exchange-failure no-write tests.
    8. No Platform obtain_api_key. No ~/.codex. No clear xAI on codex login.
  </action>
  <acceptance_criteria>
    - auth/codex modules compile and wired
    - Authorize URL + login persist + state-mismatch GREEN
    - expires_at populated without relying solely on long TOKEN_TTL age fallback when JWT exp available
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_authorize_url_includes_pkce_and_localhost_callback -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_login_persists_slot -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_oauth_state_mismatch_writes_nothing -- --nocapture</automated>
  </verify>
  <done>
    Codex OAuth core persists only providers.codex with claims/expiry mapping and fail-closed state handling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex device-code multi-step + CLI login provider dispatch (no xAI post-login sync)</name>
  <files>crates/codegen/xai-grok-shell/src/auth/codex/device.rs, crates/codegen/xai-grok-shell/src/auth/flow.rs, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/device_code.rs
    - crates/codegen/xai-grok-shell/src/auth/flow.rs (run_cli_login, post_login_sync tail ~928-939)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (device multi-step + post-login branch)
  </read_first>
  <behavior>
    - codex_device_endpoints_use_deviceauth GREEN
    - codex_device_pending_slowdown_exchange_denied: mock pending → slowdown → success exchange OR denied/expired with no write
    - run_cli_login provider codex does not call managed_config post_login_sync with Codex principal
    - bare login still xAI; report_signed_in provider-aware; unknown provider UI-SPEC error
  </behavior>
  <action>
    Complete AUTH-02 vertical slice (D-01, D-02, D-04; UI-SPEC). Addresses review: device multi-step + branch Codex before xAI post-login sync.

    1. Implement codex/device.rs against deviceauth usercode + token poll; handle pending, slowdown, denied, expired; on success perform authorization-code exchange if required by Codex flow; persist only Codex slot.
    2. Extend run_cli_login with AuthProvider/option: None/Xai → existing xAI path including post_login_sync; **Codex → complete Codex login and return without calling managed_config::post_login_sync** (Addresses review concern: xAI-specific post-login on Codex).
    3. report_signed_in provider-aware UI-SPEC labels.
    4. Wire clap --provider and pager-bin Login arm (if not already from Plan 01).
    5. Device flags force device for selected provider.
    6. GREEN device URL + multi-step mock tests (usercode body, pending/slowdown, denied no-write, success persist).
    7. No dual logout (Plan 04). No Phase 6 gate.
  </action>
  <acceptance_criteria>
    - bum login --provider codex end-to-end compiles through pager-bin
    - Device multi-step tests GREEN; endpoints use deviceauth
    - Codex login path does not run xAI managed-config sync
    - Bare login still xAI
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager bum_login_provider_codex_parses -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_device_endpoints_use_deviceauth -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_device_pending_slowdown_exchange_denied -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_login_persists_slot -- --nocapture</automated>
  </verify>
  <done>
    CLI Codex login is a complete AUTH-02 slice with device multi-step proofs and no xAI post-login contamination.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/loopback → token exchange | OAuth code + state on localhost callback |
| auth.openai.com → process | Untrusted IdP JSON; parse fail-closed |
| CLI provider arg → flow dispatch | Allow-list xai\|codex only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Spoofing | codex/browser callback | high | mitigate | Cryptographic OAuth state compare; reject missing/mismatched state; no-write tests |
| T-05-07 | Information Disclosure | login errors / report_signed_in | high | mitigate | Never print access/refresh tokens; UI-SPEC |
| T-05-08 | Tampering | redirect URI | high | mitigate | Fixed allow-listed localhost redirect path/ports only |
| T-05-09 | Elevation of Privilege | cross-slot write | high | mitigate | mutate AuthProvider::Codex only; sibling preserve tests |
| T-05-10 | Spoofing | public client_id reuse | medium | accept | Documented Codex CLI practice; product lock ChatGPT OAuth |
| T-05-22 | Tampering | xAI managed config after Codex login | high | mitigate | Early return before post_login_sync for Codex |
| T-05-SC | Tampering | package installs | low | accept | No new packages; axum/reqwest/sha2 already in tree |
</threat_model>

## Note on user_setup

Live ChatGPT is optional smoke only. Automated AUTH-02 is mock/fixture only (D-13).

<verification>
Login persist + authorize + device multi-step + state-mismatch green; no xAI wipe; no Codex-into-xAI managed sync.
</verification>

<success_criteria>
- AUTH-02 automated proofs: Codex OAuth under bum store only; bare login xAI; --provider codex selects Codex
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-03-SUMMARY.md` when done
</output>
