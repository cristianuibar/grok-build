---
phase: 01-product-identity-isolated-home
plan: 05
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - crates/codegen/xai-grok-agent/src/discovery.rs
  - crates/codegen/xai-grok-shell/src/bundle.rs
  - crates/codegen/xai-grok-shell/src/extensions/bundle.rs
  - crates/codegen/xai-grok-shell/src/config/mod.rs
  - crates/codegen/xai-grok-shell/src/auth/manager.rs
  - crates/codegen/xai-grok-shell-base/src/util/changelog.rs
  - crates/codegen/xai-grok-voice/src/bin/voice_probe.rs
  - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
  - crates/codegen/xai-grok-pager-bin/Cargo.toml
  - crates/codegen/xai-grok-workspace/src/hub_auth.rs
  - crates/codegen/xai-grok-mcp/src/credentials.rs
  - crates/codegen/xai-grok-hooks/src/trust.rs
  - crates/codegen/xai-grok-shell/src/mcp_doctor.rs
autonomous: true
requirements:
  - ID-01
  - ID-03
must_haves:
  truths:
    - "Agent discovery does not treat stock ~/.grok user agents as first-class bum user state when product home is bum"
    - "User role/persona discovery uses bum product home; project-local cwd/.grok discovery preserved"
    - "bundled_root writes under product home (grok_home), never $HOME/.grok/bundled"
    - "Extension bundle tests use explicit product root or one process-wide BUM_HOME (no multi-scenario OnceLock fights)"
    - "Hermetic temp-home run: HOME+BUM_HOME set (Windows: USERPROFILE / platform home vars or gated assertion), GROK_HOME and CODEX_HOME trapped or removed; recursive zero product writes under .grok and .codex"
    - "Static gate covers xai-grok-shell production sources for stock-home product-root env reads with allowlist"
    - "Operational product-home guidance (hub_auth, mcp credentials, hooks trust, mcp_doctor path labels) points users at BUM_HOME / ~/.bum — not GROK_HOME / ~/.grok — without full chrome rebrand"
    - "User can build/run primary CLI as bum with documented cargo invocation"
  artifacts:
    - path: crates/codegen/xai-grok-agent/src/discovery.rs
      provides: "no stock-home legacy agent scan for product isolation"
    - path: crates/codegen/xai-grok-shell/src/bundle.rs
      provides: "bundled_root via grok_home product home"
      contains: grok_home
    - path: crates/codegen/xai-grok-shell/src/extensions/bundle.rs
      provides: "extension bundle tests sandbox via BUM_HOME or injected root"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-shell/src/config/mod.rs
      provides: "role/persona user discovery under product home"
    - path: crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
      provides: "hermetic temp-home isolation integration proof"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-shell-base/src/util/changelog.rs
      provides: "ChangelogManager product-home resolve uses BUM_HOME only or grok_home()"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-workspace/src/hub_auth.rs
      provides: "hub auth operational product-home labels use BUM_HOME / ~/.bum"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-mcp/src/credentials.rs
      provides: "mcp credential store path labels / missing-home errors use BUM_HOME"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-hooks/src/trust.rs
      provides: "hooks trust missing-home guidance uses BUM_HOME"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-shell/src/mcp_doctor.rs
      provides: "mcp_doctor diagnostic path labels use ~/.bum product home"
      contains: ".bum"
  key_links:
    - from: bum process under hermetic HOME + BUM_HOME
      to: filesystem product sinks
      via: "main startup path + writers under grok_home"
      pattern: "BUM_HOME|\\.bum"
    - from: bundled_root
      to: extension sync / agent init
      via: "grok_home().join(bundled)"
      pattern: "grok_home|BUM_HOME"
    - from: SubagentsConfig::resolve
      to: product home roles/personas
      via: "discover under grok_home not dirs::home_dir/.grok"
      pattern: "grok_home|\\.bum"
    - from: operational diagnostics / missing-home errors
      to: user-visible product-home guidance
      via: "BUM_HOME and ~/.bum path labels in hub_auth, mcp credentials, hooks trust, mcp_doctor"
      pattern: "BUM_HOME|\\.bum"
---

## Phase Goal

**As a** bum user, **I want to** run `bum` with a temporary home and know stock Grok/Codex dirs stay untouched, **so that** isolation is proven and identity is safe for later multi-provider auth phases.

<objective>
Close Phase 1 with: legacy stock-home read gating (agents + roles/personas), bundle writer cutover to product home (including extension bundle tests / OnceLock-safe sandbox), production GROK_HOME bypass cutover (changelog, voice_probe, auth diagnostic), operational product-home path labels (hub_auth, mcp credentials, hooks trust, mcp_doctor — not full chrome rebrand), hermetic recursive isolation proof with Windows home-var discipline, and a classified production grep gate including xai-grok-shell plus those guidance sites (per D-VERIFY, D-HOME, D-WRITERS, D-MIGRATE, D-PLUGIN, D-BIN, D-SCOPE). Addresses review concerns: bundle writer bypass; role/persona stock-home reads; extension bundle OnceLock multi-scenario tests; narrow grep; weak isolation test; Windows HOME-only hermetic gap; operational guidance still teaching stock home.

Purpose: Isolation without automated proof and without every production writer cut over regresses (PITFALLS half-rebrand / incomplete isolation).
Output: discovery + roles/personas + bundle (+ extensions/bundle tests) + bypasses + operational product-home labels + hermetic integration test + classified shell-inclusive stock-home gate + green `bum` build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@.planning/phases/01-product-identity-isolated-home/01-REVIEWS.md
@crates/codegen/xai-grok-agent/src/discovery.rs
@crates/codegen/xai-grok-shell/src/bundle.rs
@crates/codegen/xai-grok-shell/src/extensions/bundle.rs
@crates/codegen/xai-grok-shell/src/config/mod.rs
@crates/codegen/xai-grok-shell/src/auth/manager.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
@crates/codegen/xai-grok-pager-bin/Cargo.toml
@crates/codegen/xai-grok-shell-base/src/util/changelog.rs
@crates/codegen/xai-grok-voice/src/bin/voice_probe.rs
@crates/codegen/xai-grok-workspace/src/hub_auth.rs
@crates/codegen/xai-grok-mcp/src/credentials.rs
@crates/codegen/xai-grok-hooks/src/trust.rs
@crates/codegen/xai-grok-shell/src/mcp_doctor.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stock-home read gate — agents + roles/personas + bundle writer + extension tests</name>
  <files>crates/codegen/xai-grok-agent/src/discovery.rs, crates/codegen/xai-grok-shell/src/bundle.rs, crates/codegen/xai-grok-shell/src/extensions/bundle.rs, crates/codegen/xai-grok-shell/src/config/mod.rs</files>
  <behavior>
    - user_agent_dirs no longer appends literal HOME/.grok agents/bundled agents as legacy product state when product home differs
    - Project-local cwd/.grok agent and role/persona discovery remains (D-PLUGIN)
    - SubagentsConfig::resolve user-global roles/personas use product home (via grok_home or discover_*_in_dir under product home), not dirs::home_dir() which yielded $HOME/.grok/roles|personas
    - bundled_root() returns grok_home().join(bundled dir name), not dirs::home_dir().join(".grok").join(bundled)
    - Bundle unit tests and extensions/bundle.rs tests pass under product home without multi-scenario OnceLock contention
  </behavior>
  <action>
    Per RESEARCH, D-MIGRATE, D-WRITERS, D-PLUGIN (Addresses review concerns: bundle writer bypass; role/persona stock-home reads; legacy agent scan; extension bundle tests OnceLock multi-scenario):
    1. In `user_agent_dirs` (`discovery.rs`), remove or permanently gate off the legacy branch that adds `home.join(".grok")` agent directories when `grok_home` differs. Keep scanning the actual product home path. Keep project-layout discovery under cwd `.grok` (D-PLUGIN). Update unit tests so legacy HOME/.grok is NOT included when product home is HOME/.bum.
    2. In `xai-grok-shell/src/config/mod.rs` `SubagentsConfig::resolve`: replace the `dirs::home_dir()` then `discover_roles/personas(&home)` path (which resolves to `$HOME/.grok/roles|personas` via join(".grok")) with discovery under the **bum product home** — e.g. `discover_roles_in_dir(&xai_grok_config::grok_home().join("roles"))` and same for personas, or equivalent that does not reintroduce stock `.grok` under user home. Preserve project-local `discover_roles(cwd)` / `discover_personas(cwd)` for `cwd/.grok/...`. Keep bundled_root-based discovery after root is fixed.
    3. In `xai-grok-shell/src/bundle.rs` `bundled_root()`: derive root from `xai_grok_config::grok_home()` (or shell-base re-export) joined with the existing bundled dir name — never `dirs::home_dir().join(".grok")`. Update bundle module tests that assume stock path; use BUM_HOME / temp product home.
    4. **`extensions/bundle.rs` tests (mandatory file):** current per-test HOME guards + repeated `bundled_root()` calls fight the process-global product-home OnceLock once production resolves via `grok_home()`. Choose one of: (a) inject/pass an explicit product root into test paths so tests never re-resolve env after first pin; (b) set a single process-wide temp `BUM_HOME` for the whole test binary / serial module before first `bundled_root()`; or (c) split conflicting env scenarios into separate test binaries. Do not leave multi-scenario env mutation around OnceLock in one process. Prefer BUM_HOME over HOME-only for product-home isolation.
    5. Leave credential import / migration unimplemented (D-MIGRATE).
  </action>
  <verify>
    <automated>cargo test -p xai-grok-agent discovery -- --nocapture && cargo test -p xai-grok-shell bundle -- --nocapture && cargo test -p xai-grok-shell config -- --nocapture</automated>
  </verify>
  <done>
    Stock ~/.grok is not used for user agents, user roles/personas, or bundled cache writes; project `.grok` preserved; bundle + extensions/bundle tests green without OnceLock multi-scenario fights.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hermetic temp-home isolation integration test</name>
  <files>crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs, crates/codegen/xai-grok-pager-bin/Cargo.toml</files>
  <behavior>
    - Spawns built bum via env!("CARGO_BIN_EXE_bum") (prefer compile-time path)
    - Hermetic env: HOME=temp, BUM_HOME=temp/.bum, GROK_HOME=temp/.grok-trap (or explicitly removed), CODEX_HOME=temp/.codex-trap (or explicitly removed)
    - On Windows: also set/restore USERPROFILE (and any other platform home vars documented by test-support) to the temp home, OR explicitly cfg-gate assertions that depend on std::env::home_dir() tracking HOME alone
    - Invokes short non-interactive path that exercises product-home creation (prefer --version if main creates home; else minimal path that triggers grok_home)
    - Asserts product root under BUM_HOME exists or received expected writes
    - Recursively asserts trap dirs .grok / .codex under the hermetic home root are absent OR byte-identical to pre-run snapshot (not a filename allowlist only)
    - Disables network-ish knobs like test_env_cmd_tokio (telemetry/autoupdate)
  </behavior>
  <action>
    Per D-VERIFY (Addresses review concerns: hermetic traps; recursive zero writes; Windows HOME alone insufficient for home_dir; version-only weak without writer coverage — note bundle/updater covered by unit/integration elsewhere in phase):
    1. Add `crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs` with package test discovery; add dev-deps only if needed from workspace.
    2. Prefer `env!("CARGO_BIN_EXE_bum")` for deterministic binary path in package integration tests.
    3. TempDir for HOME; `BUM_HOME = home.join(".bum")`.
    4. **Windows hermetic discipline:** do not rely on `HOME` alone as proof of default/user home. Follow test-support (`env.rs`) guidance — set/restore `USERPROFILE` and any other platform-relevant home variables to the same temp root on Windows, **or** explicitly `#[cfg(windows)]` gate assertions that require `std::env::home_dir()` to track the temp HOME. Prefer setting platform vars so the binary under test stays fully sandboxed.
    5. Hermetic traps: either **remove** ambient `GROK_HOME` and `CODEX_HOME` from the child env, or set them to dedicated trap paths under the temp tree that must remain empty/unchanged. Never inherit developer ambient stock homes. Also ensure ambient `BUM_HOME` is not inherited unless intentionally set to the temp product root.
    6. Before run, optionally snapshot trap paths (nonexistent is fine). After run, walk trap roots recursively — any new file or content change fails the test. Also assert no product-state tree appeared at hermetic-home `.grok` or `.codex` if traps used alternate names.
    7. Run lightest main entry that still initializes product home (inspect main.rs for docs extract / version path). Creating home via `grok_home()` create_dir_all is acceptable for startup proof when combined with unit writer tests.
    8. Disable telemetry/autoupdate env knobs consistent with test-support.
    9. Do not require live network or authenticated extension sync in this binary test; bundle writer isolation is covered by Task 1 unit tests + production root change. Updater managed leaf covered in plan 01-02.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Hermetic isolation test proves temp-home bum run does not write under stock .grok/.codex traps (recursive); Windows uses USERPROFILE/platform home vars or gated assertions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Production bypass + operational product-home labels + classified gate + bum build</name>
  <files>crates/codegen/xai-grok-shell-base/src/util/changelog.rs, crates/codegen/xai-grok-voice/src/bin/voice_probe.rs, crates/codegen/xai-grok-shell/src/auth/manager.rs, crates/codegen/xai-grok-config/src/paths.rs, crates/codegen/xai-fast-worktree/src/db/mod.rs, crates/codegen/xai-grok-pager-bin/Cargo.toml, crates/codegen/xai-grok-workspace/src/hub_auth.rs, crates/codegen/xai-grok-mcp/src/credentials.rs, crates/codegen/xai-grok-hooks/src/trust.rs, crates/codegen/xai-grok-shell/src/mcp_doctor.rs</files>
  <action>
    Per D-WRITERS, D-HOME, D-BIN, D-PLUGIN, D-VERIFY, D-SCOPE (Addresses review concerns: narrow grep missing shell; auth manager product-home diagnostic; production bypasses; operational product-home guidance still teaching stock home):
    1. `ChangelogManager::from_env_home()`: live product-home resolve reads only `BUM_HOME` or pure `grok_home()`; never the legacy product-home env key. Update rustdocs.
    2. `voice_probe` `load_config`: BUM_HOME + fallback `HOME/.bum/config.toml` (or config SoT if already depended). No dual-read of the legacy product-home env key.
    3. `auth/manager.rs` diagnostic that reads/logs whether the legacy product-home env key is configured: cut over to BUM_HOME wording/key or remove the stale product-home diagnostic input so it does not teach the legacy key as product home.
    4. Confirm SoT + twin still BUM_HOME only; confirm managed leaf bum; confirm `[[bin]]` is bum with local run comment.
    5. **Operational product-home guidance (path labels only — not full UI chrome/telemetry; D-SCOPE):** Update user-facing / operational strings that teach the stock product home so they name bum product home instead. Scope is **labels and missing-home error text only** — do not rebrand unrelated Grok chrome. Mandatory files:
       - `crates/codegen/xai-grok-workspace/src/hub_auth.rs` — module/docs and missing-home errors that currently cite the legacy product-home env key or stock home path for auth.json; point at BUM_HOME / default `~/.bum` (and auth under product home). Keep actual path resolution via existing `grok_home()` / product SoT.
       - `crates/codegen/xai-grok-mcp/src/credentials.rs` (and crate root docs if they only restate the same store path) — store path rustdocs and missing-home error strings that currently cite the legacy product-home env key; use BUM_HOME / product-home wording. Leave wire/file basename `mcp_credentials.json` unchanged.
       - `crates/codegen/xai-grok-hooks/src/trust.rs` — missing-home error / path docs that currently cite the legacy product-home env key or stock home for trusted_folders; use BUM_HOME / `~/.bum` product-home wording. Path resolution stays on product SoT.
       - `crates/codegen/xai-grok-shell/src/mcp_doctor.rs` — diagnostic `path` labels that hardcode stock home config path → default bum product-home config path (`~/.bum/config.toml` or equivalent product-home display). Do not rewrite unrelated doctor logic.
       Do **not** expand into Phase 8 chrome (TUI help strings, telemetry docs, marketing). Project-layout `cwd/.grok` remains (D-PLUGIN).
    6. **Classified static gate** over production Rust sources including `xai-grok-shell` plus the operational guidance files above (not only config/fast-worktree/shell-base/voice). Two classes:
       - (A) Executable product-root env reads of the legacy product-home env key (`env::var` / `var_os` / equivalent) — fail on remaining hits under scanned trees.
       - (B) Wrong operational product-home guidance in the mandatory label files — fail when those files still teach stock product home via stock-home path-label strings or the legacy product-home env token in user-facing error/doc path guidance (after step 5 cutover). Prefer file-scoped greps on hub_auth, mcp credentials, hooks trust, mcp_doctor so Phase-8 chrome and isolation-proof comments elsewhere are not forced clean.
       Also fail on remaining production path joins that hardcode the stock product-home directory segment as **user product home root** under SoT/bundle/twin scanned trees (existing gate).
    7. Allowlist (do not require zero repo-wide hits): `#[cfg(test)]` / `tests/`, comments naming stock Grok for isolation proofs, project-layout cwd stock-plugin dirs / project config segment, intentional non-product-home knobs, Phase-8 chrome strings outside the operational label files. Document allowlist in the SUMMARY if any residual production matches are intentionally kept.
    8. Do not implement Phase 8 chrome/telemetry/auto-update kills (D-SCOPE). Do not rename project layout (D-PLUGIN).
  </action>
  <verify>
    <automated>cargo build -p xai-grok-pager-bin --bin bum && cargo check -p xai-grok-shell-base -p xai-grok-voice -p xai-grok-shell -p xai-grok-mcp -p xai-grok-hooks -p xai-grok-workspace && cargo test -p xai-grok-config --lib paths -- --nocapture && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture && ! rg -n 'var(_os)?\("GROK_HOME"\)|env::var(_os)?\("GROK_HOME"\)|std::env::var(_os)?\("GROK_HOME"\)' crates/codegen/xai-grok-config/src crates/codegen/xai-fast-worktree/src crates/codegen/xai-grok-shell-base/src crates/codegen/xai-grok-voice/src crates/codegen/xai-grok-shell/src --glob '!**/*test*' --glob '!**/tests/**' && ! rg -n 'join\("\.grok"\)' crates/codegen/xai-grok-shell/src/bundle.rs crates/codegen/xai-grok-config/src/paths.rs crates/codegen/xai-fast-worktree/src/db/mod.rs && ! rg -n '\$GROK_HOME|~/\.grok' crates/codegen/xai-grok-workspace/src/hub_auth.rs crates/codegen/xai-grok-mcp/src/credentials.rs crates/codegen/xai-grok-hooks/src/trust.rs crates/codegen/xai-grok-shell/src/mcp_doctor.rs && rg -n 'BUM_HOME|~/\.bum' crates/codegen/xai-grok-workspace/src/hub_auth.rs crates/codegen/xai-grok-mcp/src/credentials.rs crates/codegen/xai-grok-hooks/src/trust.rs crates/codegen/xai-grok-shell/src/mcp_doctor.rs | head -n 20</automated>
  </verify>
  <done>
    Production product-root readers honor BUM_HOME / grok_home only; operational product-home labels (hub_auth, mcp credentials, hooks trust, mcp_doctor) teach BUM_HOME / ~/.bum; classified shell-inclusive gate clean on executable reads and guidance files; bundle/SoT free of stock product-home join; bum builds; isolation + path tests green. Phase 1 ready for verify-work. No Phase-8 chrome/telemetry scope creep.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bum process → filesystem | Product state must not land in stock CLI homes |
| Discovery reads → agent/role defs | Stock ~/.grok agents/roles/personas must not become bum identity |
| Bundle sync → cache root | Authenticated extension sync must not write $HOME/.grok/bundled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | agent discovery legacy | high | mitigate | Remove stock HOME/.grok user-agent scan |
| T-01-09 | Tampering / Disclosure | product writers | high | mitigate | Hermetic isolation test + recursive trap asserts; Windows USERPROFILE/platform home vars or cfg-gated assert |
| T-01-10 | Tampering | accidental migration | medium | mitigate | No import code; clean re-login only (D-MIGRATE) |
| T-01-11 | Information Disclosure | production GROK_HOME bypass | high | mitigate | Cut changelog/voice/auth diagnostic; shell-inclusive rg gate |
| T-01-13 | Tampering | bundle.rs bundled_root | high | mitigate | Root via grok_home(); never dirs::home_dir()/.grok |
| T-01-14 | Information Disclosure | roles/personas under stock home | high | mitigate | User discovery under product home; keep project cwd/.grok only |
| T-01-15 | Information Disclosure | operational product-home guidance | medium | mitigate | hub_auth / mcp credentials / hooks trust / mcp_doctor path labels teach BUM_HOME / ~/.bum; classified file-scoped rg gate |
| T-01-SC | Tampering | package installs | low | accept | No new untrusted packages; tempfile from workspace if needed |
</threat_model>

<verification>
- `cargo test -p xai-grok-agent discovery`
- `cargo test -p xai-grok-shell bundle` / config
- `cargo test -p xai-grok-pager-bin --test home_isolation`
- `cargo build -p xai-grok-pager-bin --bin bum`
- Shell-inclusive production legacy product-home env-read grep gate
- File-scoped operational product-home guidance gate (hub_auth, mcp credentials, hooks trust, mcp_doctor)
</verification>

<success_criteria>
- Legacy stock agent scan gone
- Roles/personas and bundle use bum product home
- Extension bundle tests OnceLock-safe under BUM_HOME / injected root
- Hermetic recursive isolation green (Windows-aware)
- Production product-root readers no longer honor GROK_HOME
- Operational product-home path labels teach BUM_HOME / ~/.bum (not stock home) without Phase-8 chrome rebrand
- bum binary builds and is the documented local run target
- Phase success criteria 1–3 from ROADMAP satisfied
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-05-SUMMARY.md` when done
</output>
