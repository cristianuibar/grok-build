---
phase: 01-product-identity-isolated-home
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - crates/codegen/xai-grok-test-support/src/env.rs
  - crates/codegen/xai-grok-test-support/src/leader.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/content.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/flows.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/leader.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/scripted.rs
  - crates/codegen/xai-grok-pager/src/app/leader_cluster/mod.rs
  - crates/codegen/xai-grok-pager/src/app/session_startup.rs
  - crates/codegen/xai-grok-pager/src/app/dispatch/tests/session/lifecycle.rs
  - crates/codegen/xai-grok-update/tests/common/mod.rs
  - crates/codegen/xai-grok-shell/src/leader/mod.rs
  - crates/codegen/xai-grok-shell/benches/session_list.rs
  - crates/codegen/xai-grok-shell/tests
  - crates/codegen/xai-grok-workspace/src/folder_trust.rs
  - crates/codegen/xai-grok-workspace/src/permission/resolution.rs
  - crates/codegen/xai-grok-workspace/src/trust.rs
  - crates/codegen/xai-grok-workspace/src/worktree/mod.rs
autonomous: true
requirements:
  - ID-03
must_haves:
  truths:
    - "Shared test sandboxes set BUM_HOME (not GROK_HOME) as product home override"
    - "Convention: HOME=tmp root and BUM_HOME=home.join(\".bum\") so isolation can assert no HOME/.grok product tree"
    - "PTY flows, leader, scripted, and test-support leader lock/log paths derive product tree from .bum / BUM_HOME"
    - "Pager-local leader_cluster / session_startup / lifecycle fixtures set BUM_HOME and write under .bum (not ignored GROK_HOME)"
    - "Shell integration tests and product-home-relevant benches (session_list) use BUM_HOME so they remain sandboxed after production ignores GROK_HOME"
    - "Workspace crate product-home tests (folder_trust, permission/resolution, trust, worktree, and any other workspace product-home setters) sandbox via BUM_HOME with OnceLock-safe discipline and pass behavioral cargo test"
  artifacts:
    - path: crates/codegen/xai-grok-test-support/src/env.rs
      provides: "test_env_cmd_tokio BUM_HOME sandbox"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-test-support/src/leader.rs
      provides: "leader harness BUM_HOME + lock/log under .bum"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-pager-pty-harness/src/content.rs
      provides: "env_for_pager BUM_HOME + .bum"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-pager-pty-harness/src/flows.rs
      provides: "OAuth/fixture product home under .bum"
      contains: ".bum"
    - path: crates/codegen/xai-grok-pager-pty-harness/src/leader.rs
      provides: "cluster socket/sessions under .bum"
      contains: ".bum"
    - path: crates/codegen/xai-grok-pager-pty-harness/src/scripted.rs
      provides: "generated config under .bum product home"
      contains: ".bum"
    - path: crates/codegen/xai-grok-pager/src/app/leader_cluster/mod.rs
      provides: "in-process leader cluster BUM_HOME product sandbox"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-shell/benches/session_list.rs
      provides: "bench product-home setter uses BUM_HOME when product-home relevant"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-workspace/src/folder_trust.rs
      provides: "workspace folder_trust product-home tests use BUM_HOME"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-workspace/src/trust.rs
      provides: "workspace trust product-home test helpers use BUM_HOME"
      contains: BUM_HOME
  key_links:
    - from: test sandbox env
      to: child process product home
      via: "BUM_HOME + HOME"
      pattern: "BUM_HOME"
    - from: pager unit fixtures
      to: grok_home OnceLock / product writers
      via: "BUM_HOME before first resolve"
      pattern: "BUM_HOME"
    - from: workspace product-home tests
      to: grok_home OnceLock / trust store writers
      via: "BUM_HOME before first resolve; LockedTestEnv or one scenario per process"
      pattern: "BUM_HOME"
---

<objective>
Cut **all** product-home test fixtures over to `BUM_HOME` / `.bum` — including PTY flows/leader/scripted, test-support leader lock/log, **pager-local** leader_cluster/session/lifecycle fixtures, update test_home, shell tests, product-home-relevant shell benches, and **workspace crate** product-home isolation tests (folder_trust, permission/resolution, trust, worktree, plus any other workspace product-home setters found by inventory) — so automated suites exercise the same isolation contract as production (per D-HOME, D-VERIFY). Addresses review concerns: incomplete PTY/leader fixture cutover; pager-local fixtures omitted; shell tests still sandboxed only via GROK_HOME; workspace product-home tests left on GROK_HOME; cargo check alone insufficient; verify masking via semicolon.

Purpose: Leaving tests on GROK_HOME re-validates the old coupling and, after production ignores GROK_HOME, can write to the developer real `.bum` or miss regressions.
Output: Complete harness + pager + shell + workspace fixture inventory cutover with behavioral cargo test verifies and fail-fast `&&` gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@.planning/phases/01-product-identity-isolated-home/01-REVIEWS.md
@crates/codegen/xai-grok-test-support/src/env.rs
@crates/codegen/xai-grok-test-support/src/leader.rs
@crates/codegen/xai-grok-pager-pty-harness/src/content.rs
@crates/codegen/xai-grok-pager-pty-harness/src/flows.rs
@crates/codegen/xai-grok-pager-pty-harness/src/leader.rs
@crates/codegen/xai-grok-pager-pty-harness/src/scripted.rs
@crates/codegen/xai-grok-pager/src/app/leader_cluster/mod.rs
@crates/codegen/xai-grok-pager/src/app/session_startup.rs
@crates/codegen/xai-grok-pager/src/app/dispatch/tests/session/lifecycle.rs
@crates/codegen/xai-grok-update/tests/common/mod.rs
@crates/codegen/xai-grok-shell/benches/session_list.rs
@crates/codegen/xai-grok-workspace/src/folder_trust.rs
@crates/codegen/xai-grok-workspace/src/permission/resolution.rs
@crates/codegen/xai-grok-workspace/src/trust.rs
@crates/codegen/xai-grok-workspace/src/worktree/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test-support env + leader harness (incl. lock/log paths) to BUM_HOME/.bum</name>
  <files>crates/codegen/xai-grok-test-support/src/env.rs, crates/codegen/xai-grok-test-support/src/leader.rs</files>
  <behavior>
    - test_env_cmd_tokio sets BUM_HOME=home.join(".bum") and HOME=home; does not set GROK_HOME as product home
    - Leader child env injects BUM_HOME and socket under home/.bum
    - leader.lock and leader.log helpers path under home/.bum (not home/.grok)
  </behavior>
  <action>
    Per D-HOME and D-VERIFY (Addresses review concern: leader lock/log still under .grok independently of env):
    1. In `test_env_cmd_tokio` (`env.rs`), set `BUM_HOME` to `home.join(".bum")` while still setting `HOME` to `home`. Remove setting product home via `GROK_HOME`.
    2. Rewrite the Windows sandbox comment to mention BUM_HOME and `.bum` (why HOME alone is insufficient).
    3. In `leader.rs`: switch product-home sandbox setters from `GROK_HOME` + `home.join(".grok")` to `BUM_HOME` + `home.join(".bum")` (including `GROK_LEADER_SOCKET` default under `.bum` if currently under `.grok`).
    4. Update **direct path helpers** for leader lock and log (currently `home.join(".grok").join("leader.lock")` / `leader.log`) to `home.join(".bum")` (or derive from the same product root constant used by the harness).
    5. Update adjacent comments that document the old env key.
    6. Leave other GROK_* knobs (telemetry, proxy base URL, disable autoupdater) unchanged this phase (D-SCOPE). Keep EnvGuard API unchanged.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-test-support && ! rg -n 'join\("\.grok"\)' crates/codegen/xai-grok-test-support/src/leader.rs crates/codegen/xai-grok-test-support/src/env.rs</automated>
  </verify>
  <done>
    test-support env and leader helpers (env, socket, lock, log) use BUM_HOME / `.bum` only for product home.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full PTY product-home cutover (content, flows, leader, scripted)</name>
  <files>crates/codegen/xai-grok-pager-pty-harness/src/content.rs, crates/codegen/xai-grok-pager-pty-harness/src/flows.rs, crates/codegen/xai-grok-pager-pty-harness/src/leader.rs, crates/codegen/xai-grok-pager-pty-harness/src/scripted.rs</files>
  <behavior>
    - env_for_pager includes BUM_HOME at home/.bum; no GROK_HOME product override
    - flows OAuth/fixture writes under home/.bum
    - PTY leader cluster socket + sessions root under home/.bum
    - scripted generated config/product tree under home/.bum
    - Unit assertions for env shape updated
  </behavior>
  <action>
    Per D-HOME / D-VERIFY (Addresses review concern: PTY setup split between .bum and .grok after partial cutover):
    1. `content.rs` `env_for_pager`: product path segment `.bum`; env key `BUM_HOME`; update unit tests for key and path suffix.
    2. `flows.rs`: any fixture that joins `content.home()` with `.grok` for auth/config must use `.bum` (and BUM_HOME semantics).
    3. `leader.rs`: shared GROK_HOME / `.grok` socket and sessions roots → product home `.bum` (comments and path construction).
    4. `scripted.rs`: generated config under isolated product home → `.bum` (not `.grok`); comments that name product home env should say BUM_HOME.
    5. Grep the pty-harness crate for remaining product-home `join(".grok")` and `GROK_HOME` product overrides in `src/` (leave project-layout or scenario content that intentionally models project `.grok` only if clearly project-local; product isolation roots must be `.bum`).
    6. Prefer deriving a single helper for product home under content.home() if that reduces drift.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-pager-pty-harness env_for_pager -- --nocapture && ! rg -n 'join\("\.grok"\)|GROK_HOME' crates/codegen/xai-grok-pager-pty-harness/src/content.rs crates/codegen/xai-grok-pager-pty-harness/src/flows.rs crates/codegen/xai-grok-pager-pty-harness/src/leader.rs crates/codegen/xai-grok-pager-pty-harness/src/scripted.rs</automated>
  </verify>
  <done>
    PTY harness product-home surfaces (content, flows, leader, scripted) use BUM_HOME / `.bum` with tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update + shell + pager-local + bench + workspace product-home inventory with behavioral tests</name>
  <files>crates/codegen/xai-grok-update/tests/common/mod.rs, crates/codegen/xai-grok-shell/src/leader/mod.rs, crates/codegen/xai-grok-shell/tests, crates/codegen/xai-grok-shell/benches/session_list.rs, crates/codegen/xai-grok-pager/src/app/leader_cluster/mod.rs, crates/codegen/xai-grok-pager/src/app/session_startup.rs, crates/codegen/xai-grok-pager/src/app/dispatch/tests/session/lifecycle.rs, crates/codegen/xai-grok-workspace/src/folder_trust.rs, crates/codegen/xai-grok-workspace/src/permission/resolution.rs, crates/codegen/xai-grok-workspace/src/trust.rs, crates/codegen/xai-grok-workspace/src/worktree/mod.rs</files>
  <behavior>
    - update test_home sets BUM_HOME before first product path resolve
    - Every shell test/fixture that set GROK_HOME as product-home sandbox uses BUM_HOME instead
    - Pager-local leader_cluster setters use BUM_HOME; session_startup / lifecycle tests that write product home after env setup use BUM_HOME (not only GROK_HOME)
    - Shell bench session_list product-home setter uses BUM_HOME when it sandboxes product home
    - Workspace product-home isolation tests (folder_trust, permission/resolution, trust helpers, worktree helpers, plus any other workspace product-home setters found by inventory) set BUM_HOME and do not rely on ignored GROK_HOME
    - Representative shell + pager + workspace tests run green under the new sandbox
    - Tests must not rely on ignored GROK_HOME or a previously pinned OnceLock for sandboxing
  </behavior>
  <action>
    Per D-HOME, D-VERIFY, OnceLock discipline (Addresses review concerns: incomplete fixture cutover — pager-local fixtures omitted; shell tests stop being sandboxed once production ignores GROK_HOME; workspace product-home tests left on GROK_HOME; session_list bench setter; cargo check insufficient; verify masking):
    1. In `xai-grok-update/tests/common/mod.rs` `test_home()`: set `BUM_HOME` instead of `GROK_HOME` before any product path resolve; update comments. Leave non-home GROK_* clearances as-is if present.
    2. **Inventory shell** executable product-home setters: `rg -n 'GROK_HOME' crates/codegen/xai-grok-shell --glob '*.rs'` and switch every production-test sandbox that intends to isolate product home to `BUM_HOME` (examples: `tests/signed_managed_config/common.rs`, `tests/test_built_binary_e2e.rs`, `tests/test_debug_logging.rs`, `tests/test_mcp_permission_persistence.rs`, leader unit tests in `src/leader/mod.rs`). Prefer path roots under tmp `.bum`.
    3. **Inventory pager crate** (mandatory — not only shell/PTY/test-support): `rg -n 'GROK_HOME|join\("\.grok"\)' crates/codegen/xai-grok-pager --glob '*.rs'`. Cut over at minimum:
       - `src/app/leader_cluster/mod.rs` product-home setters for in-process cluster sandbox → `BUM_HOME` + `.bum` product root
       - Tests/helpers that write product home after setting only the old product-home env key, including `src/app/session_startup.rs` and `src/app/dispatch/tests/session/lifecycle.rs` (and any sibling trust/effects/session fixtures found by inventory that sandbox product home)
       Set `BUM_HOME` **before** first `grok_home()` / product-home resolve in that process; do not rely on an ignored legacy product-home env key or a previously pinned OnceLock value.
    4. **Shell bench:** `crates/codegen/xai-grok-shell/benches/session_list.rs` — if it sets the old product-home env key as product-home sandbox, switch to `BUM_HOME` + `.bum` so bench paths stay isolated after production ignores the legacy key. Leave non-product-home GROK_* knobs alone.
    5. **Inventory workspace crate** (mandatory HIGH residual — product-home isolation tests): `rg -n 'GROK_HOME' crates/codegen/xai-grok-workspace --glob '*.rs'`. Cut over every test/helper that sets the old product-home env key to isolate product state, at minimum:
       - `src/folder_trust.rs` test modules using EnvVarGuard product-home isolation
       - `src/permission/resolution.rs` test modules isolating product home for permission/claude-import markers
       - `src/trust.rs` LockedTestEnv / product-home helper that points product home at a tempdir
       - `src/worktree/mod.rs` LockedTestEnv product-home helper used by worktree registry tests
       - Any other workspace product-home setters the inventory finds (e.g. comments/docs in helpers that name the product-home env key)
       Strategy: set `BUM_HOME` (prefer temp path as the product root, consistent with crate helpers) **before** first `grok_home()` / product-home resolve. Keep `LockedTestEnv` / `EnvVarGuard` serialization. If a single process mutates product home across scenarios after OnceLock pin, use one-env-scenario-per-process (or the existing process lock) — do not assume re-setting env re-resolves OnceLock. Update helper rustdocs that still document the old product-home env key as the isolation knob.
    6. Do not introduce dual-read of the legacy product-home env key in production for test convenience.
    7. Optional guard: if a shared spawn helper exists, reject setting the legacy product-home env key as product home and require BUM_HOME before spawn — only if low-cost in test-support; not required if inventory is complete.
    8. Run representative **behavioral** suites (not only check): shell tests that touch auth/config/home; pager leader_cluster / session-related unit tests when available; **workspace** tests covering folder_trust / trust / permission resolution product-home paths (lib tests filtered to those modules or full `-p xai-grok-workspace --lib` if filters are awkward).
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell --test test_debug_logging -- --nocapture && cargo test -p xai-grok-shell leader -- --nocapture && cargo test -p xai-grok-workspace --lib folder_trust -- --nocapture && cargo test -p xai-grok-workspace --lib trust -- --nocapture && cargo check -p xai-grok-update -p xai-grok-pager && ! rg -n '\.env\("GROK_HOME"|set_var\("GROK_HOME"|var\("GROK_HOME"\)|EnvVarGuard::set\("GROK_HOME"|\.set\("GROK_HOME"' crates/codegen/xai-grok-shell/tests crates/codegen/xai-grok-update/tests/common/mod.rs crates/codegen/xai-grok-pager/src/app/leader_cluster crates/codegen/xai-grok-shell/benches/session_list.rs crates/codegen/xai-grok-workspace/src/folder_trust.rs crates/codegen/xai-grok-workspace/src/permission/resolution.rs crates/codegen/xai-grok-workspace/src/trust.rs crates/codegen/xai-grok-workspace/src/worktree/mod.rs 2>/dev/null && rg -n 'BUM_HOME' crates/codegen/xai-grok-update/tests/common/mod.rs crates/codegen/xai-grok-pager/src/app/leader_cluster/mod.rs crates/codegen/xai-grok-workspace/src/folder_trust.rs crates/codegen/xai-grok-workspace/src/trust.rs | head -n 20</automated>
  </verify>
  <done>
    Update test_home, inventoried shell + pager fixtures, product-home-relevant session_list bench, and workspace product-home isolation tests (folder_trust, permission/resolution, trust, worktree, plus inventory extras) use BUM_HOME with OnceLock-safe discipline; representative shell + workspace behavioral tests pass; no intentional legacy product-home env override left in those helpers; verify chain uses fail-fast `&&`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test env → production path code | Wrong sandbox env reintroduces stock-home coupling or writes developer `.bum` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege / Disclosure | test sandboxes | medium | mitigate | Tests set BUM_HOME only; complete fixture inventory including PTY/shell/pager/workspace/benches; OnceLock-safe env strategy |
| T-01-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo check -p xai-grok-test-support`
- `cargo test -p xai-grok-pager-pty-harness env_for_pager`
- `cargo test -p xai-grok-shell leader` and representative shell tests
- `cargo test -p xai-grok-workspace --lib folder_trust` and `trust` (behavioral, not check-only)
- Negative greps for product-home legacy env setters in listed harness + pager leader_cluster + session_list bench + workspace product-home files (fail-fast `&&`)
</verification>

<success_criteria>
- Shared harnesses sandbox via BUM_HOME + `.bum`
- PTY flows/leader/scripted and leader lock/log complete
- Pager-local leader_cluster / session_startup / lifecycle fixtures cut over
- Shell tests + product-home-relevant session_list bench inventoried and cut over
- Workspace product-home isolation tests (folder_trust, permission/resolution, trust, worktree, inventory extras) use BUM_HOME with OnceLock-safe discipline and pass behavioral tests
- Behavioral tests green (not check-only); verify gates do not mask failures with `;`
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-04-SUMMARY.md` when done
</output>
