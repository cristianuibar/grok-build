---
phase: 08-quiet-fork-rebrand-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
  - crates/codegen/xai-grok-shell/src/agent/app.rs
  - crates/codegen/xai-grok-shell/src/auth/error.rs
  - crates/codegen/xai-grok-shell/src/auth/device_code.rs
  - crates/codegen/xai-grok-shell/src/mcp_doctor.rs
  - crates/codegen/xai-grok-shell/src/plugin.rs
  - crates/codegen/xai-grok-pager-minimal/src/welcome.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
autonomous: true
requirements:
  - ID-02
must_haves:
  truths:
    - "OAuth browser return text says return to bum (D-01, UI-SPEC)"
    - "User-facing Open Grok Build browser prompt rebranded to Open bum (D-01 residual)"
    - "Auth/device-code/mcp-doctor/plugin user-facing errors instruct bum login / bum mcp / bum plugin not stock grok (D-01; C1-H1)"
    - "pager-minimal welcome product name is bum (D-01, D-14)"
    - "Composition-root version banner is bum (pager) - v{version} (UI-SPEC)"
    - "pager-bin residual messages rebranded: bum agent server starting, bum crashed during your last session (C1-H1)"
    - "No stock product name Grok Build / stock grok CLI instructions in these user-facing chrome sites (model labels out of scope)"
    - "Green p8_oauth_return, p8_shell_runtime_cli, p8_minimal_welcome, and p8_bin_banner unit tests pass"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
      provides: "OAuth callback return-to-bum copy"
      contains: "return to bum"
    - path: crates/codegen/xai-grok-shell/src/agent/app.rs
      provides: "Open bum browser prompt (was Open Grok Build)"
      contains: "Open bum"
    - path: crates/codegen/xai-grok-shell/src/auth/error.rs
      provides: "Auth errors instruct bum login"
      contains: "bum login"
    - path: crates/codegen/xai-grok-pager-minimal/src/welcome.rs
      provides: "minimal welcome product span bum"
      contains: "bum"
    - path: crates/codegen/xai-grok-pager-bin/src/main.rs
      provides: "version banner bum (pager) + residual crash/server copy"
      contains: "bum (pager)"
  key_links:
    - from: OIDC callback HTML
      to: browser post-login UX
      via: "callback_response message string only"
      pattern: "return to bum"
    - from: shell agent app browser open prompt
      to: TUI/headless open-in-browser UX
      via: "format string only"
      pattern: "Open bum"
    - from: shell auth/mcp/plugin error strings
      to: user CLI recovery instructions
      via: "product CLI name bum"
      pattern: "bum login|bum mcp|bum plugin"
    - from: pager-bin banner + residual eprintln
      to: user-visible process identity
      via: "println/format/eprintln identity lines"
      pattern: "bum \\(pager\\)|bum agent server|bum crashed"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** use a product that presents as bum and never phones home or auto-updates as stock Grok Build, **so that** my fork identity and privacy are intact.

<objective>
Complete ID-02 **non-pager-core chrome + shell/bin residual runtime strings** (C1-H1): OAuth browser return text, Open bum prompt, auth/device-code/mcp-doctor/plugin user-facing CLI instructions, pager-minimal welcome product name, composition-root version banner + crash/server residual messages (per D-01, D-14; UI-SPEC). Parallel-safe with Plan 02 (no shared files with Plan 02).

Purpose: No residual stock product identity on OAuth return, shell recovery copy, minimal mode, or bin banner/crash/server lines.
Output: Shell + minimal + bin chrome rebranded + green p8_ unit proofs including p8_shell_runtime_cli.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-quiet-fork-rebrand-polish/08-CONTEXT.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-UI-SPEC.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-PATTERNS.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-01-SUMMARY.md
@crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
@crates/codegen/xai-grok-shell/src/agent/app.rs
@crates/codegen/xai-grok-shell/src/auth/error.rs
@crates/codegen/xai-grok-shell/src/auth/device_code.rs
@crates/codegen/xai-grok-shell/src/mcp_doctor.rs
@crates/codegen/xai-grok-shell/src/plugin.rs
@crates/codegen/xai-grok-pager-minimal/src/welcome.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-01 | Product chrome → bum (OAuth, shell residual CLI, minimal, bin banner) |
| D-03 | No crate renames |
| D-13 | Leave GROK_* internal |
| D-14 | Welcome/product badge path includes minimal |

## Scope boundaries

- Do **not** change should_check_for_updates, run_update_command, min-version, Sentry init, or OTEL init behavior (Plans 04–05). User-visible identity strings in pager-bin only for ID-02.
- Do **not** rebrand IdP/provider issuer branding in OAuth HTML.
- Do **not** touch pager TUI / headless / plugin_cmd files owned by Plan 02.
- Explicit exclusions (document in SUMMARY; residual gate Plan 06): model catalog `Grok Build (xAI)` / id `grok-build`, SuperGrok commercial names, internal crate names (`xai-grok-*`), type names (`GrokAuth`), HTTP headers (`x-grok-client-*`), agent system prompts (deferred), managed source label `grok.com` host identity.

**File ownership note:** This plan owns user-facing identity strings in pager-bin main.rs (banner + crash/server residual). Plan 04 owns update-gate functions in the same file afterward (depends_on 08-03). Plan 05 owns Sentry/OTLP init regions.

## Review traceability

- Addresses review concern: **C1-H1** incomplete ID-02 string inventory (shell + pager-bin residual runtime copy).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OAuth + Open bum + shell runtime CLI residual inventory (D-01, C1-H1)</name>
  <files>crates/codegen/xai-grok-shell/src/auth/oidc/login.rs, crates/codegen/xai-grok-shell/src/agent/app.rs, crates/codegen/xai-grok-shell/src/auth/error.rs, crates/codegen/xai-grok-shell/src/auth/device_code.rs, crates/codegen/xai-grok-shell/src/mcp_doctor.rs, crates/codegen/xai-grok-shell/src/plugin.rs</files>
  <read_first>
    - 08-UI-SPEC.md OAuth browser return
    - 08-PATTERNS.md login.rs callback_response
    - crates/codegen/xai-grok-shell/src/auth/oidc/login.rs (callback_response ~159–169)
    - crates/codegen/xai-grok-shell/src/agent/app.rs (~556 Open Grok Build)
    - crates/codegen/xai-grok-shell/src/auth/error.rs (Run `grok login` thiserror + messages)
    - crates/codegen/xai-grok-shell/src/auth/device_code.rs (Try `grok login` / device-code expiry messages)
    - crates/codegen/xai-grok-shell/src/mcp_doctor.rs (run `grok login` / `grok mcp add` / `grok mcp doctor`)
    - crates/codegen/xai-grok-shell/src/plugin.rs (Run `grok plugin list` user-facing errors)
  </read_first>
  <behavior>
    - p8_oauth_return_message_is_return_to_bum: success CallbackResult message is You can close this window and return to bum.
    - Access-denied path unchanged (Close this window and try again.)
    - p8_shell_runtime_cli_auth_error_instructs_bum_login: auth/error Display strings use bum login
    - p8_shell_runtime_cli_device_code_instructs_bum_login: device_code user bail/help uses bum login
    - p8_shell_runtime_cli_mcp_doctor_instructs_bum: mcp_doctor user println/errors use bum mcp / bum login
    - p8_shell_runtime_cli_plugin_instructs_bum_plugin: plugin.rs user-facing errors use bum plugin list
  </behavior>
  <action>
    Per D-01, UI-SPEC, and review C1-H1 (shell residual inventory):
    1. Change success OAuth callback browser message to You can close this window and return to bum. Leave error title/message and provider issuer branding alone. Add unit test p8_oauth_return_* against callback_response or extracted pure message helper.
    2. Rebrand agent/app.rs browser open prompt from Open Grok Build to Open bum (UI-SPEC-aligned). Leave non-product URLs alone.
    3. auth/error.rs: every user-facing thiserror/Display string that says Run `grok login` (and variants with --device-auth) → bum login. Do not rename Auth types or crate paths.
    4. auth/device_code.rs: user-facing Try `grok login` / Run `grok login --device-auth` bail messages → bum. Leave `x-grok-client-*` headers, referrer grok-build, and GrokAuth type names unchanged.
    5. mcp_doctor.rs: user-facing recovery strings (auth expired — run `grok login`, Run `grok mcp add --help`, Run `grok mcp doctor --json`) → bum CLI. Leave managed source label grok.com host identity and internal GrokComConfig alone.
    6. plugin.rs: user-facing Run `grok plugin list` error Display strings → bum plugin list. Leave marketplace protocol types alone.
    7. Add green p8_shell_runtime_cli_* tests asserting key Display/message constants contain bum login / bum mcp / bum plugin and do not instruct stock grok CLI for those recovery paths.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-shell --lib p8_oauth_return -- --nocapture && cargo test -p xai-grok-shell --lib p8_shell_runtime_cli -- --nocapture && rg -n "Open bum" crates/codegen/xai-grok-shell/src/agent/app.rs && ! rg -n "Open Grok Build" crates/codegen/xai-grok-shell/src/agent/app.rs</automated>
  </verify>
  <done>
    OAuth return + Open bum + shell auth/mcp/plugin residual CLI copy use bum; p8_oauth_return + p8_shell_runtime_cli green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pager-minimal welcome product name → bum (D-01, D-14)</name>
  <files>crates/codegen/xai-grok-pager-minimal/src/welcome.rs</files>
  <read_first>
    - 08-UI-SPEC.md pager-minimal welcome product
    - crates/codegen/xai-grok-pager-minimal/src/welcome.rs (~75 product Span)
  </read_first>
  <behavior>
    - p8_minimal_welcome_product_is_bum: welcome product span/text is bum not stock Grok Build product word
  </behavior>
  <action>
    Per D-01/D-14: replace minimal welcome product display string with bum. Keep layout/style. Add p8_minimal_welcome unit test if crate supports --lib tests; otherwise a small cfg(test) module in welcome.rs.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-minimal --lib p8_minimal_welcome -- --nocapture</automated>
  </verify>
  <done>
    Minimal welcome shows bum; p8_minimal_welcome green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pager-bin banner + residual server/crash identity → bum (D-01, C1-H1)</name>
  <files>crates/codegen/xai-grok-pager-bin/src/main.rs</files>
  <read_first>
    - 08-UI-SPEC.md Version / banner locked copy
    - crates/codegen/xai-grok-pager-bin/src/main.rs (Grok Build (pager) - v{} ~881; Grok agent server starting ~91; Grok crashed ~1494)
  </read_first>
  <behavior>
    - p8_bin_banner_format_is_bum_pager: pure format helper or const prefix is bum (pager) - v
    - p8_bin_agent_server_startup_says_bum: print_serve_startup_info / related copy says bum agent server (not Grok agent server)
    - p8_bin_crash_message_says_bum: last-session crash eprintln says bum crashed (not Grok crashed)
    - Do not assert update gates or OTEL/Sentry behavior yet (Plans 04–05)
  </behavior>
  <action>
    Per D-01, UI-SPEC, and review C1-H1 (pager-bin residual):
    1. Change composition-root version banner format from stock Grok Build (pager) prefix to bum (pager) - v{version}. Extract a tiny pure format function if needed so unit test can assert without running full async_main.
    2. Rebrand serve startup eprintln Grok agent server starting… → bum agent server starting… (product name only).
    3. Rebrand crash recovery eprintln Grok crashed during your last session. → bum crashed during your last session.
    4. Grep main.rs for other user-facing stock product identity strings (Grok Build / bare product Grok in eprintln/println paths); rebrand product chrome only. Leave client_name resource attributes that Plans 05 may retarget if still internal OTEL labels — do not enable OTEL.
    5. Touch ONLY identity strings — do not modify should_check_for_updates, run_update_command, finish_update_on_exit, enforce_minimum_version_or_exit, Sentry init, or otel_layer wiring (Plans 04–05).

    Add p8_bin_banner_* / residual p8_bin_* unit tests in main.rs cfg(test) module asserting the pure helpers/constants.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-bin --bin bum p8_bin_ -- --nocapture</automated>
  </verify>
  <done>
    Banner + agent-server + crash residual identity are bum; p8_bin_* green; update/OTEL/Sentry gates untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth callback HTML → browser | Untrusted client display only; no tokens in message |
| Bin banner stdout | Local process identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | OAuth HTML | medium | mitigate | Keep success message free of tokens/codes; only product rename |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
p8_oauth_return, p8_shell_runtime_cli, p8_minimal_welcome, p8_bin_ green.
</verification>

<success_criteria>
- OAuth return, shell residual CLI recovery copy, minimal welcome, bin banner/crash/server present as bum (C1-H1)
- No OPS-01/02 behavior changes in this plan
</success_criteria>

<output>
Create `.planning/phases/08-quiet-fork-rebrand-polish/08-03-SUMMARY.md` when done
</output>
