---
phase: 08-quiet-fork-rebrand-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - crates/codegen/xai-grok-pager-bin/src/main.rs
  - crates/codegen/xai-grok-update/src/auto_update.rs
  - crates/codegen/xai-grok-update/src/minimum_version.rs
  - crates/codegen/xai-grok-pager/src/settings/defs.rs
  - crates/codegen/xai-grok-pager/src/settings/registry.rs
  - crates/codegen/xai-grok-pager/tests/settings_e2e.rs
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "should_check_for_updates always returns false in release and debug (D-04, D-07) — no stock channel probe from composition root"
    - "cli.auto_update effective default is false; None does not mean on; first-run does not persist true (D-07)"
    - "Explicit bum update / run_update_command is no-op exit 0 with UI-SPEC locked disabled message (D-05)"
    - "enforce_minimum_version_or_exit never installs stock binary / never force-exits for stock floor (D-06)"
    - "Settings auto_update default false; description does not promise stock channel installs (UI-SPEC)"
    - "Local version display strings use bum product prefix if still printed (ID-02 adjacent printers)"
    - "Hermetic no-network proof: startup, explicit update, min-version, leader, and finish_update_on_exit/Ctrl+U paths never invoke stock update helpers (C1-M2)"
    - "Green p8_no_auto_update, p8_auto_update_default, p8_min_version, p8_update_cmd, p8_update_no_network tests pass"
  artifacts:
    - path: crates/codegen/xai-grok-pager-bin/src/main.rs
      provides: "hard-off should_check_for_updates + no-op run_update_command + finish_update_on_exit short-circuit"
      contains: "Stock auto-update is disabled in bum"
    - path: crates/codegen/xai-grok-update/src/auto_update.rs
      provides: "unwrap_or(false) + no first-run true persist"
      contains: "unwrap_or(false)"
    - path: crates/codegen/xai-grok-update/src/minimum_version.rs
      provides: "min-version hard no-op entry"
      contains: "enforce_minimum_version_or_exit"
  key_links:
    - from: should_check_for_updates
      to: startup / leader LeaderAutoUpdateConfig spawn
      via: "false gate → skip run_update_if_available and leader checker"
      pattern: "should_check_for_updates"
    - from: run_update_command / finish_update_on_exit
      to: stock x.ai/cli channel
      via: "early return before check_update_status / run_update / run_update_if_available"
      pattern: "Stock auto-update is disabled"
    - from: settings default false
      to: auto_update.rs unwrap_or(false)
      via: "registry + defs aligned with update crate"
      pattern: "auto_update"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** use a product that presents as bum and never phones home or auto-updates as stock Grok Build, **so that** my fork identity and privacy are intact.

<objective>
Hard-off stock xAI auto-update for OPS-01: composition-root gate, update crate defaults, explicit CLI no-op + locked message, min-version ignore, settings default/description (per D-04, D-05, D-06, D-07; UI-SPEC update copy).

Purpose: bum is never overwritten by official Grok Build updates from the stock channel.
Output: Gates + defaults + CLI no-op + hermetic no-network proofs (incl. Ctrl+U finish_update_on_exit) + green p8_* update proofs; settings_e2e expectations flipped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-quiet-fork-rebrand-polish/08-CONTEXT.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-RESEARCH.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-PATTERNS.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-UI-SPEC.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-03-SUMMARY.md
@crates/codegen/xai-grok-pager-bin/src/main.rs
@crates/codegen/xai-grok-update/src/auto_update.rs
@crates/codegen/xai-grok-update/src/minimum_version.rs
@crates/codegen/xai-grok-pager/src/settings/defs.rs
@crates/codegen/xai-grok-pager/src/settings/registry.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-04 | Hard-off stock auto-update |
| D-05 | Explicit update no-op + UI-SPEC message |
| D-06 | Min-version ignore / never force stock upgrade |
| D-07 | Code defaults + tests (not env-only) |

UI-SPEC locked primary message: Stock auto-update is disabled in bum. Install or build locally to upgrade.  
Optional second CLI line: This fork does not download updates from the stock x.ai channel.  
Settings description preference: Stock update channel (disabled in bum).

## Scope boundaries

- Do **not** delete the update crate; keep local version display capability.
- Do **not** implement a new public signed install channel (deferred).
- Do **not** change telemetry/feedback (Plan 05).
- Preserve Plan 03 bum (pager) banner + residual identity strings while editing main.rs gates.

## Review traceability

- Addresses review concern: **C1-M2** stronger hermetic no-network update proofs including finish_update_on_exit / Ctrl+U.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Composition-root hard-off + CLI no-op + hermetic no-network proofs (D-04, D-05, C1-M2)</name>
  <files>crates/codegen/xai-grok-pager-bin/src/main.rs</files>
  <read_first>
    - 08-PATTERNS.md should_check_for_updates + run_update_command targets
    - crates/codegen/xai-grok-pager-bin/src/main.rs (should_check_for_updates ~2000; run_update_command ~2037; finish_update_on_exit ~1934; call sites ~887, ~963, ~1184, ~1914)
  </read_first>
  <behavior>
    - p8_no_auto_update_should_check_always_false: should_check_for_updates(false) and should_check_for_updates(true) both false
    - p8_update_cmd_message_contains_disabled_copy: pure message helper or captured stdout path contains Stock auto-update is disabled in bum and Install or build locally to upgrade
    - p8_update_no_network_run_update_command: run_update_command never invokes stock helpers (injected call counter / test double stays 0)
    - p8_update_no_network_finish_update_on_exit: finish_update_on_exit never calls run_update_if_available (injected counter 0; returns without stock channel)
    - p8_update_no_network_startup_and_leader_gated: with should_check always false, startup and leader branches cannot reach run_update_if_available (unit or structural gate + counter on any extractable helper)
  </behavior>
  <action>
    Per D-04, D-05, D-07, UI-SPEC, and review C1-M2:
    1. Change should_check_for_updates to always return false (ignore debug flag, no_auto_update flag, and GROK_DISABLE_AUTOUPDATER — all paths hard-off). Document quiet-fork rationale in a short comment. Call sites for startup and LeaderAutoUpdateConfig already skip when false — no scatter changes required beyond the gate.
    2. Change run_update_command to early-return Ok(()) after printing the locked disabled message (and optional second line). Do not call check_update_status, run_update, or any stock channel fetch. Prefer exit success (Ok) per research A3.
    3. Short-circuit finish_update_on_exit (Ctrl+U / quit-for-update) so it never falls through to auto_update::run_update_if_available. Print the same locked disabled message (or return false/Ok without network) and do not await/spawn stock update children for install. Addresses C1-M2 explicitly — structural message-only proofs are insufficient.
    4. Hermetic no-network proofs (required, not optional): introduce a thin injectable seam (function pointer, cfg(test) static AtomicUsize counter, or trait/callback held by test-only wrapper) around stock helpers (run_update_if_available and/or check_update_status / run_update). Unit tests must prove counter stays 0 for: (a) run_update_command, (b) finish_update_on_exit with both Some(handle-failure paths) and None adopted waiter, (c) any extractable startup/leader helper that would call run_update_if_available when gate true. Prefer pure unit tests over live network. Do not rely solely on source-order comments.
    5. Add p8_no_auto_update_*, p8_update_cmd_*, and p8_update_no_network_* unit tests in main.rs cfg(test). Make should_check_for_updates and message constant/helper testable.

    Do not alter Sentry/OTLP init here. Keep bum identity strings from Plan 03.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-bin --bin bum p8_no_auto_update -- --nocapture && cargo test -p xai-grok-pager-bin --bin bum p8_update_cmd -- --nocapture && cargo test -p xai-grok-pager-bin --bin bum p8_update_no_network -- --nocapture</automated>
  </verify>
  <done>
    Gate always false; update command + finish_update_on_exit no-op with UI-SPEC message; hermetic counters prove zero stock helper calls; p8_no_auto_update + p8_update_cmd + p8_update_no_network green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update crate defaults false + min-version hard no-op + product printer strings (D-06, D-07)</name>
  <files>crates/codegen/xai-grok-update/src/auto_update.rs, crates/codegen/xai-grok-update/src/minimum_version.rs</files>
  <read_first>
    - 08-RESEARCH.md OPS-01 traps (unwrap_or(true), first-run persist, min-version)
    - crates/codegen/xai-grok-update/src/auto_update.rs (~435, ~481–494, ~2301 print_update_status strings ~60–99)
    - crates/codegen/xai-grok-update/src/minimum_version.rs (enforce_minimum_version_or_exit ~267)
  </read_first>
  <behavior>
    - p8_auto_update_default_none_is_off: effective auto_update with None is false (no stock fetch path taken)
    - p8_auto_update_first_run_does_not_persist_true: when None, config update does not write Some(true)
    - p8_min_version_or_exit_is_noop: enforce_minimum_version_or_exit returns without network/install/exit
    - Printer product prefix uses bum when local status lines remain
  </behavior>
  <action>
    Per D-06, D-07 and UI-SPEC:
    1. Invert auto_update unwrap_or(true) sites used for effective enablement to unwrap_or(false) in auto_update.rs (including ~435, ~487, ~2301 and any other auto_update-specific true defaults for this flag).
    2. Remove or invert first-run persistence that writes st.cli.auto_update = Some(true) when None — do not opt users into stock channel. Prefer leave None or write Some(false) only if a write is still required; default must not enable network update.
    3. Make enforce_minimum_version_or_exit a hard no-op (immediate return) so managed/remote minimum_version never force-installs stock Grok Build. Prefer entry no-op over rewriting all callers.
    4. Rebrand user-facing print_update_status product prefix Grok Build - v to bum - v for any remaining local status path. Stock new-version-available messaging must not be user-reachable after gates; still rebrand or gate printers so residual paths do not say stock product name.
    5. Add p8_auto_update_default_* and p8_min_version_* unit tests in the update crate. Prefer pure unit tests over live network. Update any in-crate unit tests that asserted default true for auto_update in the same plan.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-update --lib p8_auto_update -- --nocapture && cargo test -p xai-grok-update --lib p8_min_version -- --nocapture</automated>
  </verify>
  <done>
    Default off; no first-run true; min-version no-op; p8_auto_update + p8_min_version green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settings registry default false + description + e2e flip (D-07)</name>
  <files>crates/codegen/xai-grok-pager/src/settings/defs.rs, crates/codegen/xai-grok-pager/src/settings/registry.rs, crates/codegen/xai-grok-pager/tests/settings_e2e.rs</files>
  <read_first>
    - 08-UI-SPEC.md Settings auto_update copy
    - crates/codegen/xai-grok-pager/src/settings/defs.rs (auto_update SettingMeta ~1220)
    - crates/codegen/xai-grok-pager/src/settings/registry.rs (unwrap_or(true) ~669; assert default true ~878–882)
    - crates/codegen/xai-grok-pager/tests/settings_e2e.rs (auto_update default true cases ~1722, ~5998)
  </read_first>
  <behavior>
    - Registry meta default for auto_update is false
    - Effective value when pager.auto_update is None is false
    - settings_e2e expectations for default auto_update use false
    - Description matches disabled stock channel wording (UI-SPEC)
  </behavior>
  <action>
    Per D-07 and UI-SPEC:
    1. SettingKind::Bool { default: false } for auto_update in defs.rs.
    2. Description → Stock update channel (disabled in bum). (or equivalent UI-SPEC preference; must not promise automatic stock installs).
    3. registry effective unwrap_or(false); flip registry unit assert that required default true to require false; update doc comments that say None → default true for auto_update only.
    4. Flip settings_e2e expected defaults for auto_update from true to false (lines that assert default true). Keep toggle interaction tests but start from off default.
    5. Optional p8_settings_auto_update_default_false unit in registry tests module if not already covered by registry assert — name with p8_ for phase discovery.

    Do not change show_tips or unrelated settings defaults.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager --lib settings::registry -- --nocapture && cargo test -p xai-grok-pager --test settings_e2e auto_update -- --nocapture</automated>
  </verify>
  <done>
    Settings default off + description quiet-fork accurate; registry + settings_e2e green for auto_update.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process → stock https://x.ai/cli | Untrusted remote binary channel — must not be contacted by default |
| Managed min-version policy → install script | Forced upgrade path must not run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | auto-update channel | critical | mitigate | should_check_for_updates hard false; CLI no-op; unwrap_or(false); no first-run true |
| T-08-06 | Tampering | minimum_version force install | critical | mitigate | enforce_minimum_version_or_exit hard no-op |
| T-08-07 | Denial of Service | update CLI | low | accept | Explicit update exits 0 with message (not a hang); user upgrades via local build |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
pager-bin p8_no_auto_update + p8_update_cmd + p8_update_no_network green; update crate p8_auto_update + p8_min_version green; settings registry + settings_e2e auto_update green.
</verification>

<success_criteria>
- Stock auto-update channel cannot overwrite bum on default paths
- Explicit update and finish_update_on_exit are no-op with hermetic zero stock-helper calls (C1-M2)
- Explicit update is no-op with UI-SPEC copy
- Min-version never force-installs stock
- Settings default off
</success_criteria>

<output>
Create `.planning/phases/08-quiet-fork-rebrand-polish/08-04-SUMMARY.md` when done
</output>
