---
phase: 10-codex-responses-wire-parity
plan: 05
type: execute
wave: 5
depends_on:
  - 10-02
  - 10-03
  - 10-04
  - 10-06
  - 10-07
files_modified:
  - .planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
autonomous: false
requirements:
  - OPS-04
  - OPS-05
must_haves:
  truths:
    - "Focused automated coverage proves trusted request shaping, header isolation, delta-only completion, cross-provider encrypted sanitation, and encrypted-error matching."
    - "OPS-04 is recorded as PASS only after a real redacted rebuilt-bum GPT-5.6 turn and tool/read/edit flow completes once."
    - "OPS-05 is recorded as PASS only after a real redacted Grok-to-GPT-5.6 switch has no encrypted-content 400 and ordinary context remains useful."
    - "No fixture result, absent credential, provider outage, or unredacted secret is represented as live PASS evidence."
  artifacts:
    - path: .planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
      provides: "Final automated evidence and redacted live-validation record"
      contains: "Validation Sign-Off"
  key_links:
    - from: "Wave 1-4 regression suites"
      to: "Human dual-login checkpoint"
      via: "Consolidated focused checks before rebuilt-bum OPS-04/OPS-05 rerun"
      pattern: "OPS-0"
---

## Plan user story

**As a** bum daily-driver user, **I want to** complete GPT-5.6 turns and Grok↔GPT-5.6 switches reliably, **so that** I can keep coding in one CLI without a restart.

<objective>
Record final Phase 10 automated evidence and obtain the required redacted human proof for OPS-04 and OPS-05 without claiming a live pass before it exists.

Purpose: turn the wire/reliability changes into honest daily-driver validation rather than fixture-only confidence.
Output: updated validation evidence, consolidated focused checks, and a blocking human checkpoint with precise success/failure criteria.
</objective>

<context>
@.planning/phases/10-codex-responses-wire-parity/10-CONTEXT.md
@.planning/phases/10-codex-responses-wire-parity/10-RESEARCH.md
@.planning/phases/10-codex-responses-wire-parity/10-PATTERNS.md
@.planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
@.planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-02-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-03-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-04-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-06-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-07-PLAN.md
</context>

## Locked decisions honored

- Automated proof precedes live verification but cannot replace it. Phase 9 and Phase 10 remain non-green until actual redacted OPS-04/OPS-05 results are recorded.
- The human uses bum's own session identity and real dual OAuth; optional Codex headers remain request metadata and no credentials, auth documents, or raw token-bearing transcripts are committed.
- Phase 11 effort/catalog policy and Phase 12 WebSocket/tool/attribution work remain deferred. This plan does not broaden validation to all OPS-03..OPS-06.

<tasks>

<task type="auto">
  <name>Task 1: Run the consolidated focused regression suite and record only actual automated evidence</name>
  <files>.planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
    - .planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-02-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-03-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-04-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-06-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-07-PLAN.md
  </read_first>
  <action>
    After Waves 1 through 4 complete, run every exact focused command in this task's `<automated>` chain as the consolidated source of truth: sampler profile serialization and literal compilation, sampling-types system/instructions and encrypted-error predicates, stream delta fallback including multi-output/incomplete controls, actor fake-SSE counter controls, actor-owned provider/epoch sanitation, deterministic held-response capture, outbound trusted-header isolation including same-session declassification, end-to-end encrypted-400 no-compaction/no-resubmit, and shell production/test-fixture literal compilation. Update `10-VALIDATION.md` with command names, date, pass/fail output summary, and links to the responsible plan/task; record a failure as a failure and return to the owning plan rather than converting it into a live UAT claim. Do not modify product code, STATE, ROADMAP, or prior UAT artifacts in this task.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-sampler --lib client::tests::responses_wire_profile_defaults_and_propagates_to_client_defaults -- --exact && cargo test -p xai-grok-sampler --lib client::tests::trusted_codex_responses_profile_on_off_serializes_exactly -- --exact && cargo test -p xai-grok-sampler --lib --no-run && cargo test -p xai-grok-sampler --test test_actor --no-run && cargo test -p xai-grok-sampling-types --lib conversation::tests::responses_api_never_emits_system_role_in_input -- --exact && cargo test -p xai-grok-sampling-types --lib error::tests::encrypted_content_error_accepts_spaced_case_insensitive_400_only -- --exact && cargo test -p xai-grok-sampler --lib stream::responses::tests::text_delta_then_completed_uses_fallback_when_terminal_empty -- --exact && cargo test -p xai-grok-sampler --lib stream::responses::tests::text_delta_then_completed_terminal_text_wins -- --exact && cargo test -p xai-grok-sampler --lib stream::responses::tests::text_delta_then_completed_multi_output_index_preserves_arrival_order -- --exact && cargo test -p xai-grok-sampler --lib stream::responses::tests::text_delta_then_incomplete_preserves_length_without_fallback -- --exact && cargo test -p xai-grok-sampler --test test_actor delta_only_terminal_empty_completes_once_without_retry -- --exact && cargo test -p xai-grok-sampler --test test_actor genuinely_empty_completed_response_retries_boundedly -- --exact && cargo test -p xai-grok-shell --lib session::acp_session::model_switch::tests::cross_provider_transition_sanitizes_existing_and_late_reasoning -- --exact && cargo test -p xai-grok-shell --lib session::acp_session::model_switch::tests::same_provider_codex_transition_preserves_encrypted_reasoning -- --exact && cargo test -p xai-grok-shell --lib session::acp_session::model_switch::tests::unknown_prior_provider_sanitizes_encrypted_reasoning -- --exact && cargo test -p xai-grok-shell --test model_switch_gate held_grok_response_after_codex_switch_is_sanitized -- --exact && cargo test -p xai-grok-shell --test model_switch_gate codex_to_codex_transition_retains_encrypted_reasoning -- --exact && cargo test -p xai-grok-shell --lib session::acp_session::codex_reconstruct_refresh_tests::trusted_codex_reconstruct_enables_profile_and_metadata -- --exact && cargo test -p xai-grok-shell --test model_switch_gate trusted_codex_wire_headers_are_sent_and_stable -- --exact && cargo test -p xai-grok-shell --test model_switch_gate codex_wire_headers_do_not_leak_to_xai_byok_or_custom -- --exact && cargo test -p xai-grok-shell --test model_switch_gate trusted_to_untrusted_switch_strips_codex_identity_headers -- --exact && cargo test -p xai-grok-shell --lib session::acp_session::auth_error_no_retry_tests::encrypted_content_400_is_classified_terminal -- --exact && cargo test -p xai-grok-shell --test model_switch_gate encrypted_content_400_is_terminal_before_compaction_or_resubmit -- --exact && cargo check -p xai-grok-shell --lib && cargo test -p xai-grok-shell --lib --no-run && cargo test -p xai-grok-shell --test provider_routing --no-run && cargo fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    `10-VALIDATION.md` names every focused command and its real result, maps each result to an OPS-04 or OPS-05 behavior layer, and contains no invented live PASS. A failing command leaves a visible non-pass result and directs remediation to its owning plan.
  </acceptance_criteria>
  <done>The validation artifact contains traceable automated evidence for every Phase 10 reliability and wire contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human redacted rebuilt-bum OPS-04 and OPS-05 verification</name>
  <files>.planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
    - .planning/phases/09-daily-driver-end-to-end-validation/09-SESSION-HANDOFF.md
    - .planning/phases/10-codex-responses-wire-parity/10-CONTEXT.md
  </read_first>
  <what-built>Trusted Codex wire shaping, delta-only retry recovery, cross-provider encrypted-history sanitation, and trusted-header/error-classification regressions have passed their focused automated checks.</what-built>
  <action>
    Perform the real dual-login verification only after the automated suite is green. Build the current bum binary, work in a disposable workspace, and record only redacted observations in `10-VALIDATION.md`. Do not paste tokens, auth files, account IDs, or raw secret-bearing transcripts. If an outcome is not a successful live retry, record it as a non-pass with its observed class; do not mark either OPS item PASS based on fixtures or assumptions.
  </action>
  <how-to-verify>
    1. Build `bum` with the established `cargo build -p xai-grok-pager-bin` command and launch the rebuilt `target/debug/bum` from a disposable workspace after confirming both OAuth sessions are usable.
    2. **OPS-04:** select `gpt-5.6-sol`; send a small prompt; confirm the turn completes once with no post-success retry; then perform a normal read/edit/tool task. Record model, redacted outcome, and whether completion happened once.
    3. **OPS-05:** in the same bum process, make a Grok turn, switch to `gpt-5.6-sol`, and send a follow-up that depends on ordinary prior context. Confirm there is no encrypted-content/decryption 400 and that context remains useful. Record only redacted outcome and relevant model IDs.
    4. Mark PASS only when the stated live behavior occurs. For authentication, account, outage, or product failures, record the non-pass class and concise redacted symptom; do not infer a green result.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md && rg -n "OPS-04|OPS-05|Automated|Validation Sign-Off|PASS|non-pass" .planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md</automated>
    <human-check>OPS-04 shows one completed GPT-5.6 turn followed by a read/edit/tool action; OPS-05 shows a same-process Grok-to-GPT-5.6 follow-up without encrypted-content 400 and with useful ordinary context; both observations are redacted.</human-check>
  </verify>
  <acceptance_criteria>
    A PASS label appears only beside real redacted evidence for its exact OPS behavior. If either live check cannot run or fails, the artifact clearly says non-pass and does not close Phase 9 or Phase 10 as green.
  </acceptance_criteria>
  <done>Live OPS-04/OPS-05 evidence is honestly recorded as PASS or an explicit non-pass, with no secrets and no fixture-only waiver.</done>
  <resume-signal>Type "approved" only after both redacted live checks meet their stated PASS criteria; otherwise provide the observed non-pass class and a redacted symptom.</resume-signal>
</task>

</tasks>

<threat_model>
## L1 threat model

| Threat ID | Boundary | Risk | Mitigation and proof |
|---|---|---|---|
| T-10-11 | Automated fixtures -> phase claim | Fixture-only tests are represented as real daily-driver success. | Record commands/results separately; human checkpoint alone may add a live PASS label. |
| T-10-12 | Human OAuth session -> planning artifact | Tokens, account data, or raw transcripts leak into committed documentation. | Redacted observations only; explicit no-secret instruction and artifact inspection. |
| T-10-13 | Live service failure -> product status | Account or outage failure is misclassified as PASS or silently waived. | Record exact non-pass category and preserve Phase 9/10 non-green status until rerun evidence exists. |
</threat_model>

## Spec-less probe assumptions

No Phase 10 SPEC supplies Edge Coverage or Prohibitions. OPS-04 and OPS-05 are the testable truths: deterministic Rust regressions cover the repeatable wire/retry/switch behavior, while real dual OAuth remains a mandatory human backstop. Numerical precision is inapplicable; evidence is categorical completion/error behavior rather than a measured tolerance.

## Wave graph

| Wave | Plans | Dependency rationale |
|---|---|---|
| 1 | 10-01, 10-03 | Independent sampler-profile core and encrypted-history sanitizer work. |
| 2 | 10-02, 10-06 | Stream actor work waits for sampler fixture compatibility; shell production literals wait for the profile field. |
| 3 | 10-07 | Shell test/fixture literal compilation waits for all production-literal compatibility work. |
| 4 | 10-04 | Trusted profile/header behavior waits for sampler core plus every shell literal update. |
| 5 | 10-05 | Consolidated evidence and human validation waits for all implementation and compatibility plans. |

## Source audit coverage

| Source artifact | Locked item | Plan coverage |
|---|---|---|
| `10-CONTEXT.md` | Typed trusted profile, explicit body defaults, bum request metadata | 10-01, 10-04 |
| `10-PATTERNS.md` | Sampler-core literal fallout and default-preserving configuration construction | 10-01 |
| `10-PATTERNS.md` | Delta-only retry reconstruction and actor fake-SSE regression | 10-02 |
| `10-PATTERNS.md` | Narrow local encrypted-history sanitizer | 10-03 |
| `10-RESEARCH.md` | Trusted first-party header ownership and one shared 400 matcher | 10-04 |
| `10-PATTERNS.md` | Shell production/config literal fallout after the profile field lands | 10-06 |
| `10-PATTERNS.md` | Shell test/fixture literal fallout after the profile field lands | 10-07 |
| `10-VALIDATION.md` | Focused Rust layers plus manual-only OPS-04/OPS-05 proof | 10-05 |

## Artifacts this phase produces

- Updated `10-VALIDATION.md` with actual focused-test evidence and redacted live-check outcomes.
- A human-verification record that distinguishes automated proof from live OPS-04/OPS-05 evidence.
- `10-05-SUMMARY.md` documenting whether the live checkpoint passed or remains explicitly non-green.

<verification>
Run the consolidated focused checks before the human gate. Inspect `10-VALIDATION.md` after the checkpoint to confirm it has no secret-bearing material and no unsupported PASS label.
</verification>

<success_criteria>
- Every Phase 10 automated behavior layer has recorded focused evidence.
- OPS-04 and OPS-05 live status is honest, redacted, and evidence-backed.
- No plan or validation artifact claims Phase 9/10 green without actual live dual-login success.
</success_criteria>

<output>
Create `.planning/phases/10-codex-responses-wire-parity/10-05-SUMMARY.md` when execution completes.
</output>
