---
phase: 02-multi-slot-credentials-xai-oauth
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/manager_tests.rs
  - crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs
  - crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "AuthManager::new loads xAI credentials from a nested multi-slot auth.json under the given grok_home"
    - "AuthManager::update persists into providers.xai without clobbering a seeded providers.codex"
    - "get_valid_token / current_or_expired returns the xAI key from multi-slot disk"
    - "ShellAuthCredentialProvider resolves Bearer-capable credentials from multi-slot-backed AuthManager"
    - "Agent-turn seam is proven: nested auth.json → AuthManager → sampling_config.api_key (or equivalent turn config seed) → Authorization Bearer construction path"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/manager_tests.rs
      provides: "multi-slot AuthManager load/update/token tests"
      contains: providers
    - path: crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
      provides: "ShellAuthCredentialProvider multi-slot disk → token proof (mandatory)"
      contains: ShellAuthCredentialProvider
    - path: crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs
      provides: "optional helper coverage; not sole agent-turn proof"
      contains: Bearer
  key_links:
    - from: AuthManager::new / update
      to: storage read_auth_json / locked write
      via: "xAI slot adapter"
      pattern: "read_auth_json|write_auth_json|mutate_auth_document"
    - from: nested auth.json
      to: sampling_config.api_key / ShellAuthCredentialProvider Bearer
      via: "AuthManager token → agent turn / HTTP auth seam"
      pattern: "api_key|Bearer|ShellAuthCredentialProvider"
---

## Phase Goal

**As a** bum user, **I want to** complete an xAI login-like credential write and have agent HTTP auth pick up that token from the multi-slot store, **so that** coding turns authenticate on the xAI path after login (AUTH-01 success criterion 3).

<objective>
Prove the agent-turn auth path against multi-slot storage with automated fixtures (per D-06, D-08, D-12, D-14): AuthManager load/update/get_valid_token, **mandatory ShellAuthCredentialProvider**, and the **primary turn seam** nested file → AuthManager → `sampling_config.api_key` / Bearer construction. Prefer storage-only adapter — no dual AuthManager (D-12). No live IdP.

Addresses review concerns: helper-only Bearer proof overstates agent-turn; ShellAuthCredentialProvider must be mandatory; clarify AuthManager update lock contract after Plan 01.

Purpose: CI gate for “after xAI login, agent turn authenticates on xAI path” without browser OAuth in CI.
Output: Extended manager_tests + credential_provider (+ optional grok_auth_credentials) tests green; existing ~manager suite remains green with nested on-disk format.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-CONTEXT.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-RESEARCH.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-PATTERNS.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-REVIEWS.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-01-PLAN.md
@crates/codegen/xai-grok-shell/src/auth/manager.rs
@crates/codegen/xai-grok-shell/src/auth/manager_tests.rs
@crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
@crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs
@crates/codegen/xai-grok-shell/src/auth/storage.rs
@crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs
@crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
</context>

## Artifacts this phase produces (this plan)

| Symbol / test | Location | Kind |
|---------------|----------|------|
| Multi-slot AuthManager tests | `auth/manager_tests.rs` | nested load, update no-clobber, get_valid_token |
| ShellAuthCredentialProvider multi-slot proof | `auth/credential_provider.rs` | **mandatory** Bearer / snapshot from multi-slot manager |
| Agent-turn seam test | manager_tests / credential_provider / focused helper test | nested → AuthManager → sampling_config.api_key / Bearer |
| Optional GrokAuthCredentials coverage | `util/grok_auth_credentials.rs` | helper surface; not sole proof |

## Locked decisions covered

D-06 (OAuth paths remain valid via AuthManager.update used by flows), D-08 (path under passed home / bum), D-12 (single manager), D-14 (automated proof), D-15 (no Codex tokens required — fixture-only reserved slot).

Note: Parallel with Plan 02 (wave 2). Avoid editing `manager.rs` write_scope_removal / try_devbox_recovery here — owned by Plan 02. If a test needs logout+codex, depend on Plan 02 or keep that assertion in Plan 02 only.

**Lock contract after Plan 01:** AuthManager::update production path must use locked full-document mutation (Plan 01 API). New multi-slot integrity tests should acquire the same lock path production uses (or call update which owns locking) — do not normalize unlocked RMW as the contract (Addresses review concern: tests calling update without lock invariant).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthManager multi-slot load/update/token tests</name>
  <files>crates/codegen/xai-grok-shell/src/auth/manager_tests.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/manager_tests.rs (fixture patterns: tempfile, AuthManager::new, write_auth_json, make_auth)
    - crates/codegen/xai-grok-shell/src/auth/manager.rs (`new`, `update`, `get_valid_token`, `auth`, `read_disk_auth`)
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (`write_auth_json`, `mutate_auth_document`, `write_fixture_auth_document`)
    - crates/codegen/xai-grok-shell/src/auth/config.rs (`GrokComConfig::default`, `auth_scope`)
  </read_first>
  <behavior>
    - Nested document with providers.xai entry for default auth_scope → AuthManager::new finds token / current auth matches fixture key
    - Seed providers.codex + call mgr.update(xai_auth).await → raw file still has codex payload; xAI scope updated
    - After multi-slot load, get_valid_token (or auth().await.map key) returns the xAI access key without network when token not expired
    - Path is dir.path().join("auth.json") under tempfile — never asserts stock home paths
  </behavior>
  <action>
    Extend `manager_tests.rs` with multi-slot cases (D-08, D-11, D-12, D-14; MVP vertical slice: agent credential supply):

    1. Build nested fixture using `write_fixture_auth_document` or locked `write_auth_json` after Plan 01 (prefer fixture helper when seeding codex). Scope key must match `GrokComConfig::default().auth_scope()` so `lookup_auth` succeeds.
    2. Use non-expired `expires_at` / `GrokAuth` fields so `get_valid_token` does not attempt IdP refresh (avoid network). Prefer API-key mode or long-lived OIDC fixture already used in manager_tests.
    3. Assert update merge: distinctive codex `GrokAuth` key remains in raw JSON after `update`. Prefer calling the production `update` path so locking matches production (Addresses review concern: do not normalize unlocked RMW).
    4. Do not invent dual AuthManager or provider parameter on login (D-12, D-15).
    5. If existing tests write flat JSON via raw helpers, they must still pass via Plan 01 migration; only change fixtures if something breaks under nested writes from `write_auth_json`.
    6. Production manager.rs changes only if a test reveals a real bug beyond storage adapter — prefer zero manager logic change for load/update (scope removal/devbox owned by Plan 02).
  </action>
  <acceptance_criteria>
    - At least one test loads nested multi-slot auth and resolves xAI credentials via AuthManager
    - At least one test proves update does not clobber seeded codex (raw JSON assertion)
    - At least one test proves get_valid_token or auth() returns the xAI key from multi-slot disk without network
    - `cargo test -p xai-grok-shell --lib auth::manager -- --nocapture` green (or `auth::manager_tests` filter as appropriate for module path)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib auth::manager -- --nocapture</automated>
  </verify>
  <done>
    AuthManager multi-slot load/update/token path is automated and green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ShellAuthCredentialProvider + agent-turn Bearer seam (mandatory)</name>
  <files>crates/codegen/xai-grok-shell/src/auth/credential_provider.rs, crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs, crates/codegen/xai-grok-shell/src/auth/manager_tests.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/credential_provider.rs (ShellAuthCredentialProvider apply / snapshot)
    - crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs (apply Bearer + X-XAI-Token-Auth — helper only, not sole proof)
    - crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs (seeds sampling_config.api_key from AuthManager ~2344+)
    - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs (per-turn sampling config ~310)
    - crates/codegen/xai-grok-sampler/src/client.rs (Bearer construction from api_key ~419) — read only for assertion target shape
    - Task 1 manager multi-slot fixture patterns
  </read_first>
  <behavior>
    - **Mandatory:** Disk-backed nested xAI auth under temp dir → AuthManager::new → ShellAuthCredentialProvider → resolve/snapshot user_token equals fixture key; apply() sets Authorization Bearer for user/OAuth path
    - **Agent-turn seam (Addresses review concern: primary turn path):** nested auth.json → AuthManager → extract token the same way agent_ops seeds `sampling_config.api_key` (AuthManager valid key) → assert the value that would become the sampler Bearer is the fixture token. Prefer a unit test that constructs SamplingConfig (or a minimal struct/fields) with api_key from AuthManager after multi-slot load, and asserts Authorization Bearer header construction using the same helper/sampler code path the product uses when only the key string is available. Do not rely solely on GrokAuthCredentials::with_auth_manager as the only proof.
    - Optional: additional GrokAuthCredentials disk-backed test is fine as secondary coverage, not a substitute
    - No deployment_key path required for this proof
  </behavior>
  <action>
    Add disk-backed multi-slot credential + agent-turn seam proof (AUTH-01 agent-turn criterion; D-14; review MEDIUM):

    1. **ShellAuthCredentialProvider is mandatory** (Addresses review concern: optional coverage insufficient). Extend `credential_provider` tests: tempfile nested multi-slot → AuthManager::new → ShellAuthCredentialProvider::new → snapshot/apply yields fixture token / Bearer header.
    2. **Agent-turn seam test (Addresses review concern: sampler Bearer path):** implement one focused test documenting the production chain nested auth.json → AuthManager → sampling_config.api_key → Authorization Bearer. Prefer extracting or calling the same header construction used when sampling_config.api_key is set (shell agent_ops seed + sampler client pattern). Acceptable shapes:
       - unit test in shell that loads multi-slot manager, sets `sampling_config.api_key = Some(auth.key)`, then builds the request headers the sampler would attach from that key; or
       - a small pure function test if header build is already unit-testable from the api_key string obtained via AuthManager after nested load.
       Goal: prove the **turn** credential seed path, not only a side credential helper.
    3. Do not change Bearer header contract or introduce Codex headers this phase.
    4. Do not log tokens in new test println!/tracing of full secrets — assert equality in tests is fine.
    5. If credential modules need no production code changes, tests-only is success.
    6. GrokAuthCredentials-only tests are optional extras; they must not be the only agent-turn evidence.
  </action>
  <acceptance_criteria>
    - Automated test proves multi-slot disk → AuthManager → ShellAuthCredentialProvider yields expected token/Bearer
    - Automated test proves multi-slot disk → AuthManager → sampling_config.api_key (or equivalent turn seed) → Authorization Bearer path with fixture token
    - Existing credential unit tests remain green
    - Verify commands cover both modules (filters below)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib auth::credential_provider:: -- --nocapture && cargo test -p xai-grok-shell --lib util::grok_auth_credentials:: -- --nocapture</automated>
  </verify>
  <done>
    Agent-turn credential attachment is proven against multi-slot auth store via ShellAuthCredentialProvider and the sampling_config.api_key Bearer seam — not helper-only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AuthManager cache ↔ disk multi-slot file | Token load for agent turns |
| Credential provider ↔ HTTP client | Bearer injection into outbound requests |
| Agent turn sampling_config ↔ sampler | Primary inference auth seed path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Information Disclosure | credential apply / tests | high | mitigate | Never log full tokens in production paths; tests may assert equality only; Debug redaction retained |
| T-02-11 | Tampering | AuthManager update vs codex slot | high | mitigate | Isolation test after update; relies on Plan 01 lock-scoped merge-safe write |
| T-02-12 | Spoofing | wrong home path | high | mitigate | Tests use tempfile home only; construction via AuthManager::new(temp, …) (D-08) |
| T-02-19 | Spoofing | incomplete agent-turn proof | medium | mitigate | Mandatory ShellAuthCredentialProvider + sampling_config.api_key → Bearer seam test |
| T-02-04 | Information Disclosure | token logging | high | mitigate | No new tracing of auth document bodies |
| T-02-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo test -p xai-grok-shell --lib auth::manager`
- `cargo test -p xai-grok-shell --lib auth::credential_provider::`
- `cargo test -p xai-grok-shell --lib util::grok_auth_credentials::` (if extended)
- Spot-check: no live network in new tests
</verification>

<success_criteria>
- AuthManager supplies xAI credentials from multi-slot store
- Update does not clobber seeded codex
- ShellAuthCredentialProvider works from multi-slot disk
- Agent-turn sampling_config.api_key → Bearer seam proven from multi-slot store
</success_criteria>

<!-- Review feedback incorporated (cycle 1) -->
<!-- MEDIUM: agent-turn seam; mandatory ShellAuthCredentialProvider -->
<!-- MEDIUM: phase-gate filters for util/credential_provider handled in Plan 04 -->

<output>
Create `.planning/phases/02-multi-slot-credentials-xai-oauth/02-03-SUMMARY.md` when done
</output>
