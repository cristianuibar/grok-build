---
phase: 12.1-close-gap-id-02-fix-bum-version-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-pager-bin/src/main.rs
autonomous: true
requirements:
  - ID-02
must_haves:
  truths:
    - "Command::Version non-JSON human text product token is bum (not stock grok) per D-01 D-02 D-05"
    - "Version/hash/channel still come from display_version_with_commit + channel_label (D-03)"
    - "JSON version payload keys remain currentVersion + channel only (D-04)"
    - "Unit test p8_bin_version_cmd_product_is_bum proves format_version_cmd_line shape bum {version} (D-07)"
  artifacts:
    - path: crates/codegen/xai-grok-pager-bin/src/main.rs
      provides: "VERSION_CMD_PRODUCT_NAME + format_version_cmd_line + wired Command::Version text path"
      contains: "VERSION_CMD_PRODUCT_NAME"
    - path: crates/codegen/xai-grok-pager-bin/src/main.rs
      provides: "p8_bin_version_cmd_product_is_bum unit gate"
      contains: "p8_bin_version_cmd_product_is_bum"
  key_links:
    - from: format_version_cmd_line
      to: Command::Version non-JSON println
      via: "println!(\"{}\", format_version_cmd_line(&display_version_with_commit(...)))"
      pattern: "format_version_cmd_line"
    - from: VERSION_CMD_PRODUCT_NAME
      to: format_version_cmd_line body
      via: "local product const = bum"
      pattern: "VERSION_CMD_PRODUCT_NAME"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** see `bum version` identify the product as bum with version/hash still present, **so that** the release-blocking ID-02 branding gap is closed.

<objective>
Close ID-02 on the composition-root version subcommand: replace the stock product-token prefix on `Command::Version` human-readable output with a Phase-8-style local product const + pure formatter, keep `display_version_with_commit` / channel plumbing, leave JSON unchanged, and lock the formatter with a `p8_bin_*` unit test (D-01..D-07).

Purpose: Live residual is only the non-JSON version branch hard-coded product token in pager-bin `main.rs`.
Output: Wired `bum {version}` text path + green unit test in `xai-grok-pager-bin`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-CONTEXT.md
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-RESEARCH.md
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-PATTERNS.md
@.planning/REQUIREMENTS.md
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Locked decisions (this plan)

| ID | Decision |
|----|----------|
| D-01 | Product label is **bum** only (not long marketing name / "bum (CLI)") |
| D-02 | Text shape is **`bum {version}`** — same layout as stock, only product token changes |
| D-03 | Keep **`display_version_with_commit`** + `channel_label()` plumbing |
| D-04 | JSON `--json` stays **`currentVersion` + `channel` only** — no product field |
| D-05 | Primary fix is **`Command::Version` human-readable path** only |
| D-06 | No crate renames; no full residual rebrand outside this path |
| D-07 | Unit gate on pure formatter (hermetic probe + static gate in plan 02) |

## Scope boundaries

- Do **not** edit clap `--version` path in `xai-grok-pager` `cli.rs` (already `name=bum`).
- Do **not** change `xai-grok-version` or reimplement version/hash composition.
- Do **not** add a product field to JSON.
- Do **not** rename crates (`xai-grok-*`) or sweep non-version residual product strings (D-06).
- Do **not** run dual-login UAT (D-08 — plan 02 documents; not this plan).
- Do **not** use workspace-wide `cargo fmt --all` or full-workspace tests.

## Discretion (resolved)

- Local `const VERSION_CMD_PRODUCT_NAME: &str = "bum"` next to banner helpers (Phase 8 pattern).
- Pure `format_version_cmd_line` for unit testing without process spawn.
- Test name `p8_bin_version_cmd_product_is_bum` so existing `p8_bin_` discovery still finds it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VERSION_CMD_PRODUCT_NAME + format_version_cmd_line + p8_bin unit test (D-01, D-02, D-07)</name>
  <files>crates/codegen/xai-grok-pager-bin/src/main.rs</files>
  <read_first>
    - .planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-PATTERNS.md (helper + p8_bin_* pattern)
    - crates/codegen/xai-grok-pager-bin/src/main.rs lines ~88–101 (format_pager_banner neighbors)
    - crates/codegen/xai-grok-pager-bin/src/main.rs lines ~2218–2255 (existing p8_bin_* tests)
  </read_first>
  <behavior>
    - p8_bin_version_cmd_product_is_bum: format_version_cmd_line("0.1.220-alpha.4 (f3eccc6)") equals "bum 0.1.220-alpha.4 (f3eccc6)"
    - Line starts with "bum " and does not start with stock product token "grok "
    - VERSION_CMD_PRODUCT_NAME equals "bum"
  </behavior>
  <action>
    Per D-01, D-02, D-07 and 12.1-PATTERNS.md: near `format_pager_banner` (~lines 88–101), add a product-token const `VERSION_CMD_PRODUCT_NAME` set to bum and a pure `fn format_version_cmd_line(version_with_channel: &str) -> String` that returns `{product} {version_with_channel}` using that const (single space; no extra punctuation; not "bum - v…").

    In the existing `#[cfg(test)] mod tests` block next to other `p8_bin_*` tests (~2223+), add `p8_bin_version_cmd_product_is_bum` asserting exact equality for sample body `0.1.220-alpha.4 (f3eccc6)`, starts_with bum plus trailing space, does not start_with stock product token plus trailing space, and const equals bum.

    Do not wire `Command::Version` yet (Task 2). Do not touch JSON. Do not edit other crates.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture</automated>
  </verify>
  <done>
    Pure helper + const exist; p8_bin_version_cmd_product_is_bum is green; format shape is bum {version} per D-01 D-02.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Command::Version non-JSON branch through format_version_cmd_line (D-03, D-04, D-05)</name>
  <files>crates/codegen/xai-grok-pager-bin/src/main.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager-bin/src/main.rs Command::Version arm (~1625–1641)
    - Task 1 helpers (VERSION_CMD_PRODUCT_NAME, format_version_cmd_line)
    - 12.1-RESEARCH.md recommended fix for version composition keep
  </read_first>
  <behavior>
    - Non-JSON Version branch calls format_version_cmd_line around display_version_with_commit(env VERSION_WITH_COMMIT, channel_label())
    - JSON branch still emits only currentVersion and channel keys (no product field)
    - No remaining hard-coded stock product-token format string on the Version text path
  </behavior>
  <action>
    Per D-03, D-04, D-05: in the `Command::Version { json }` match arm non-JSON else branch only, replace the hard-coded stock product-token format + separate display args with a single println of `format_version_cmd_line(&xai_grok_version::display_version_with_commit(env!("VERSION_WITH_COMMIT"), xai_grok_update::channel_label()))`.

    Keep calling `display_version_with_commit` with the same two arguments (D-03). Leave the JSON branch byte-for-byte equivalent: serde_json payload with only `currentVersion` and `channel` (D-04). Do not change early `return Ok(())`.

    After wire-up, re-run unit filter and confirm the stock product-token format string is no longer present under this file via scoped search (exact pattern documented in plan 02 static gate: double-quoted grok-brace-brace product format under pager-bin main.rs). Plan 02 owns formal static gate acceptance; this task only ensures that residual is gone after wire-up.

    Scoped rustfmt check only (no workspace fmt):
    `cargo fmt -- --check crates/codegen/xai-grok-pager-bin/src/main.rs`
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture && cargo test -p xai-grok-pager-bin --bin bum p8_bin_banner -- --nocapture && ! rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs && cargo fmt -- --check crates/codegen/xai-grok-pager-bin/src/main.rs</automated>
  </verify>
  <done>
    Version text path prints via format_version_cmd_line; version/hash/channel plumbing intact; JSON unchanged; stock product-token format string absent from main.rs; p8_bin_version + p8_bin_banner green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → composition-root println | Local untrusted user invokes `bum version`; output is local stdout only |
| Test process → hermetic HOME | Not this plan (plan 02); unit tests are pure string, no FS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.1-01 | Information Disclosure | Command::Version JSON/text | low | accept | Version path does not touch credentials; no secrets in product token |
| T-12.1-02 | Tampering | main.rs product string | low | mitigate | Pure const + unit test prevent accidental stock token reintroduction at formatter layer |
| T-12.1-SC | Tampering | package installs | low | accept | Zero new packages this plan — no install surface |
</threat_model>

<verification>
- `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` green
- Stock product-token format string absent from `crates/codegen/xai-grok-pager-bin/src/main.rs`
- JSON branch untouched (manual read of match arm)
</verification>

<success_criteria>
1. `format_version_cmd_line` returns `bum {version_with_channel}` using `VERSION_CMD_PRODUCT_NAME` (D-01, D-02)
2. `Command::Version` non-JSON uses that helper with `display_version_with_commit` (D-03, D-05)
3. JSON keys remain only `currentVersion` + `channel` (D-04)
4. `p8_bin_version_cmd_product_is_bum` green (D-07 unit half)
</success_criteria>

<output>
Create `.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-01-SUMMARY.md` when done
</output>
