---
phase: 12.1-close-gap-id-02-fix-bum-version-branding
plan: 01
subsystem: cli
tags: [version, branding, id-02, pager-bin, product-token]

requires:
  - phase: 08-quiet-fork-rebrand-polish
    provides: Phase-8 local PRODUCT_NAME + p8_bin_* pure-helper unit pattern
provides:
  - VERSION_CMD_PRODUCT_NAME product token const (bum)
  - format_version_cmd_line pure formatter (bum {version})
  - Command::Version non-JSON wired through formatter
  - p8_bin_version_cmd_product_is_bum unit regression gate
affects:
  - 12.1-02 hermetic binary probe + static gate acceptance

tech-stack:
  added: []
  patterns:
    - "Local VERSION_CMD_PRODUCT_NAME const + pure format_version_cmd_line (Phase 8 PRODUCT_NAME style)"
    - "Unit-test product token without process spawn; plan 02 owns hermetic spawn"

key-files:
  created: []
  modified:
    - crates/codegen/xai-grok-pager-bin/src/main.rs

key-decisions:
  - "Product label is bum only (D-01); text shape bum {version} (D-02)"
  - "Keep display_version_with_commit + channel_label (D-03); JSON unchanged (D-04)"
  - "Pure helper + unit test first, then wire Command::Version (D-05, D-07)"

patterns-established:
  - "format_version_cmd_line(version_with_channel) is the only composition-root version text prefix path"
  - "p8_bin_version_* filter discovers version product unit gates on --bin bum"

requirements-completed: [ID-02]

coverage:
  - id: D1
    description: "Command::Version non-JSON human text product token is bum (not stock grok)"
    requirement: ID-02
    verification:
      - kind: unit
        ref: crates/codegen/xai-grok-pager-bin/src/main.rs#p8_bin_version_cmd_product_is_bum
        status: pass
      - kind: other
        ref: "live: target/debug/bum version → bum 0.1.220-alpha.4 (…)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Version/hash/channel still come from display_version_with_commit + channel_label"
    requirement: ID-02
    verification:
      - kind: other
        ref: "Command::Version arm calls display_version_with_commit(env!(VERSION_WITH_COMMIT), channel_label())"
        status: pass
    human_judgment: false
  - id: D3
    description: "JSON version payload keys remain currentVersion + channel only"
    requirement: ID-02
    verification:
      - kind: other
        ref: "live: bum version --json keys {currentVersion, channel}"
        status: pass
    human_judgment: false
  - id: D4
    description: "Unit test p8_bin_version_cmd_product_is_bum proves format_version_cmd_line shape bum {version}"
    requirement: ID-02
    verification:
      - kind: unit
        ref: "cargo test -p xai-grok-pager-bin --bin bum p8_bin_version"
        status: pass
    human_judgment: false

duration: 4min
completed: 2026-07-22
status: complete
---

# Phase 12.1 Plan 01: Version cmd product formatter Summary

**`bum version` now prints `bum {version}` via `VERSION_CMD_PRODUCT_NAME` + pure `format_version_cmd_line`, with unit gate and live binary smoke green; JSON unchanged.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-22T05:56:32Z
- **Completed:** 2026-07-22T06:00:51Z
- **Tasks:** 2/2
- **Files modified:** 1

## Accomplishments

- Added `VERSION_CMD_PRODUCT_NAME = "bum"` and pure `format_version_cmd_line` next to banner helpers
- Locked formatter with `p8_bin_version_cmd_product_is_bum` (exact equality + starts_with `bum ` / not `grok `)
- Wired `Command::Version` non-JSON path through the formatter; stock `"grok {}"` residual removed
- Live rebuild smoke: `bum version` → `bum 0.1.220-alpha.4 (2d76d21)`; JSON keys only `currentVersion` + `channel`

## Task Commits

Each task was committed atomically:

1. **Task 1: VERSION_CMD_PRODUCT_NAME + format_version_cmd_line + p8_bin unit test** - `90c631e`
2. **Task 2: Wire Command::Version non-JSON through format_version_cmd_line** - `2d76d21`

**Plan metadata:** (pending docs commit)

## Files Created/Modified

- `crates/codegen/xai-grok-pager-bin/src/main.rs` — product const, pure formatter, unit test, Version arm wire-up

## Decisions Made

- Local const + pure helper (Phase 8 pattern) over shared branding crate or inline-only change — enables unit test without spawn
- Do not touch JSON product field, clap `--version`, crate names, or residual non-version `grok` strings (D-04, D-06)

## Deviations from Plan

### Auto-fixed Issues

None for correctness.

### Process notes

**1. [Rule 3 - Blocking] Avoided workspace-wide cargo fmt spill**
- **Found during:** Task 2 verification
- **Issue:** Plan verify used `cargo fmt -- --check crates/codegen/xai-grok-pager-bin/src/main.rs`, which walked the workspace (edition-2024 let-chain noise + multi-crate diffs)
- **Fix:** Skipped workspace-spilling `cargo fmt`; applied intentional Version-arm formatting matching rustfmt style for that snippet only; kept diff to the Version wire-up only
- **Files modified:** none beyond intentional Task 2 edit
- **Verification:** unit tests green; `rg '"grok \{\}"'` clean; live `bum version` smoke pass

**Total deviations:** 1 process (fmt scope); no Rule 1/2 code auto-fixes
**Impact on plan:** No behavior change vs plan success criteria; hermetic spawn + formal static gate remain plan 02

## Issues Encountered

- Stale `target/debug/bum` still printed `grok …` until rebuild after Task 2; rebuild confirmed live path.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 can add hermetic `home_isolation` product-token probe + formal static gate acceptance
- No blockers for 12.1-02

## Known Stubs

None.

## Threat Flags

None — no new network/auth/file surface; product token string only (plan T-12.1-01 accept, T-12.1-02 mitigated by const + unit test).

## Self-Check: PASSED

- FOUND: `crates/codegen/xai-grok-pager-bin/src/main.rs` contains `VERSION_CMD_PRODUCT_NAME`, `format_version_cmd_line`, `p8_bin_version_cmd_product_is_bum`
- FOUND: commit `90c631e`, commit `2d76d21`
- FOUND: stock `"grok {}"` absent from pager-bin main.rs
- FOUND: unit filter `p8_bin_version` green; live `bum version` starts with `bum `

---
*Phase: 12.1-close-gap-id-02-fix-bum-version-branding*
*Completed: 2026-07-22*
