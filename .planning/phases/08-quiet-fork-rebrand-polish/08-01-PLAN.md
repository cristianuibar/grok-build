---
phase: 08-quiet-fork-rebrand-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
  - .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md
autonomous: true
requirements:
  - ID-02
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Wave 1 compile-safe harness: p8_ discovery lists ≥1 green tests in shell (telemetry) and pager (smoke) before product waves"
    - "All Plan 01 p8_ tests compile and stay GREEN (no intentional-red under the p8_ filter)"
    - "Telemetry default Disabled is locked green under p8_telemetry (OPS-02 already-true path)"
    - "Model brand regression anchor remains green: catalog/display still allows Grok Build (xAI) for model id grok-build (D-02)"
    - "home_isolation Phase 1 proof still green (isolation baseline for quiet fork)"
    - "08-VALIDATION.md maps ID-02 / OPS-01 / OPS-02 to planned p8_ filters without claiming product green yet"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "p8_telemetry green unit(s) for resolve_telemetry_mode default Disabled"
      contains: p8_
    - path: crates/codegen/xai-grok-pager/src/app/cli.rs
      provides: "p8_wave1 harness smoke unit (or adjacent cfg(test) module) discoverable under p8_"
      contains: p8_
    - path: .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md
      provides: "Nyquist requirement → planned test map for Phase 8"
      contains: p8_
  key_links:
    - from: Wave 1 p8_ green scaffold
      to: Plans 02–05 GREEN product proofs
      via: "unique p8_ prefixes become phase-gate discovery targets; later plans ADD green tests with product code"
      pattern: "p8_"
    - from: resolve_telemetry_mode
      to: OPS-02 quiet default
      via: "default Resolved TelemetryMode::Disabled when unset"
      pattern: "TelemetryMode::Disabled"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** use a product that presents as bum and never phones home or auto-updates as stock Grok Build, **so that** my fork identity and privacy are intact.

<objective>
Create wave 1 **compile-safe green** automated harness (`p8_` prefix) and VALIDATION inventory for ID-02, OPS-01, OPS-02 before product waves (per D-08 telemetry already-Disabled lock; RESEARCH Validation Architecture; Phase 7 green-only protocol).

Purpose: Scaffold filters and baseline proofs without poisoning later `cargo test … p8_` runs.
Output: Green p8_ discovery in shell + pager; 08-VALIDATION.md map; home_isolation still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-CONTEXT.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-RESEARCH.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-PATTERNS.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-UI-SPEC.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
@crates/codegen/xai-grok-pager/tests/dynamic_enum_model_names.rs
</context>

## Locked decisions (traceability)

CONTEXT has section locks; stable IDs for plan coverage:

| ID | Decision |
|----|----------|
| D-01 | All product chrome → bum (welcome, clap, picker, OAuth, feedback/update/billing product copy) |
| D-02 | Keep model brands (Grok / GPT catalog names; model id grok-build) |
| D-03 | Leave internal crates `xai-grok-*` unchanged |
| D-04 | Hard-off stock xAI auto-update channel |
| D-05 | Explicit update path no-op + clear UI-SPEC message |
| D-06 | Ignore stock min-version / forced upgrade |
| D-07 | Enforce OPS-01 via code defaults + tests (not env-only) |
| D-08 | Default TelemetryMode::Disabled |
| D-09 | Mixpanel / product analytics off by default |
| D-10 | Sentry / crash phone-home off by default |
| D-11 | Local tracing/logs under ~/.bum remain OK |
| D-12 | Clap name=bum, about=bum TUI |
| D-13 | Leave broader GROK_* env family internal; BUM_HOME stays user-facing home |
| D-14 | Hero/welcome badge rebrand to bum; drop Beta marketing chrome |
| D-15 | /feedback quiet: disabled message, no phone-home |

**This plan covers:** green scaffold + D-02/D-08 baseline locks + VALIDATION inventory for D-01..D-15. Product green is Plans 02–05.

## Expected-green protocol (HARD)

1. **No intentional-red under `p8_`.** Every test named `p8_*` that lands here must compile and **pass** on current mainline APIs.
2. **Do not assert future product strings** (clap about already bum, hero already Thanks for using bum, etc.) until the owning product plan lands those strings and tests in the same wave.
3. **No `#[ignore]` expected-red placeholders** under `p8_`.
4. Later plans add green `p8_*` tests **with** the product behavior. Phase gate is all-green.

## Scope boundaries

- Do **not** rebrand chrome, flip auto_update defaults, short-circuit feedback, or force Sentry disabled here.
- Do **not** edit `default_models.json` or rename model Grok Build labels (D-02).
- Do **not** mass-rename GROK_* env vars (D-13).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN scaffold — p8_telemetry default Disabled + VALIDATION inventory</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-quiet-fork-rebrand-polish/08-RESEARCH.md (Validation Architecture, TelemetryMode default)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (resolve_telemetry_mode ~2079; existing telemetry unit tests ~9094+)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (resolve_feedback defaults_to_true_when_unset ~8819 — inventory only; Plan 05 flips)
  </read_first>
  <behavior>
    - p8_telemetry_resolve_mode_defaults_to_disabled: unset features/remote/env → TelemetryMode::Disabled, ConfigSource::Default (GREEN today per D-08)
    - Optional p8_telemetry_disabled_implies_no_easy_enabled_without_explicit_config: document current precedence in SUMMARY; do not change resolve precedence here
  </behavior>
  <action>
    1. Add compiling unit test(s) with p8_telemetry prefix near existing resolve_telemetry_mode tests in config.rs. Assert default path returns TelemetryMode::Disabled (per D-08, OPS-02 baseline already true).
    2. Create 08-VALIDATION.md with requirement → planned automated filter map for ID-02 / OPS-01 / OPS-02 (p8_cli_brand, p8_welcome, p8_feedback, p8_auto_update, p8_min_version, p8_update_cmd, p8_telemetry, model catalog regression, home_isolation). Mark Plan 01 rows green; product rows planned for Plans 02–05.
    3. In SUMMARY inventory list Plan 02–05 green names without landing failing tests.
    4. Do not flip resolve_feedback default, remote telemetry pin, or product chrome.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --lib p8_telemetry -- --list | grep -c 'p8_' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --lib p8_telemetry -- --nocapture; test -f .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md</automated>
  </verify>
  <done>
    Shell lists ≥1 p8_telemetry tests all green; 08-VALIDATION.md exists with ID-02/OPS-01/OPS-02 map; no intentional-red.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN scaffold — pager p8_ wave1 smoke + model brand + home_isolation baseline</name>
  <files>crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/app/cli.rs (PagerArgs clap attrs; existing test modules if any)
    - crates/codegen/xai-grok-pager/tests/dynamic_enum_model_names.rs (Grok Build (xAI) model label lock)
    - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs (Phase 1 hermetic pattern)
  </read_first>
  <behavior>
    - p8_wave1_harness_smoke_compiles: trivial green unit under pager --lib p8_ (infrastructure discoverability)
    - Existing model catalog tests still pass: dynamic_enum_model_names / format_cli_model_row keep Grok Build (xAI) for grok-build (D-02) — re-run, do not change expectations
    - home_isolation tests still pass (BUM_HOME isolation baseline)
  </behavior>
  <action>
    1. Add a minimal green p8_wave1_harness_smoke_* unit test under pager (cfg(test) in cli.rs or small adjacent test module) so `cargo test -p xai-grok-pager --lib p8_` discovers ≥1 test. Do not assert clap already says bum (stock still name=grok today — Plan 02 owns that green flip).
    2. Re-run existing model brand regression filters without editing default_models.json or model display names (per D-02).
    3. Re-run home_isolation; only add a p8_ named alias/wrapper if a thin discoverable mark is needed without changing isolation semantics. Prefer keep existing test names in VALIDATION map.
    4. Do not implement ID-02 product string changes or OPS-01 gates here.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-pager --lib p8_ -- --list | grep -c 'p8_' || true); test "$n" -ge 1; cargo test -p xai-grok-pager --lib p8_ -- --nocapture; cargo test -p xai-grok-pager --test dynamic_enum_model_names -- --nocapture; cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Pager p8_ discovery ≥1 and green; model Grok Build (xAI) regression still green; home_isolation still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 1 green harness → CI | Must not land failing p8_ tests that poison later waves |
| Test env → product home | Hermetic BUM_HOME only; no ambient stock homes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | harness / logs | low | mitigate | No real OAuth/tokens; fixture-only; no Sentry enable in tests |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
Shell p8_telemetry green; pager p8_ green; model brand + home_isolation green; VALIDATION inventory present.
</verification>

<success_criteria>
- Wave 1 compile-safe green harness exists for Phase 8 filter discovery
- Unique p8_ prefixes; zero intentional-red under p8_
- No product rebrand/OPS hard-off scope creep
</success_criteria>

<output>
Create `.planning/phases/08-quiet-fork-rebrand-polish/08-01-SUMMARY.md` when done
</output>
