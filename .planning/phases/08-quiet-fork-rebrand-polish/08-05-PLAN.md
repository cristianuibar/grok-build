---
phase: 08-quiet-fork-rebrand-polish
plan: 05
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-04
files_modified:
  - crates/codegen/xai-grok-pager/src/app/dispatch/notes.rs
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/extensions/feedback.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
autonomous: true
requirements:
  - OPS-02
must_haves:
  truths:
    - "Default TelemetryMode remains Disabled; product analytics/Mixpanel not active on default path (D-08, D-09)"
    - "Sentry crash phone-home off by default for quiet fork (D-10)"
    - "resolve_feedback defaults to false when unset (D-15 adjacent)"
    - "/feedback enter/send never emits Effect::SendFeedback; shows Feedback is disabled in bum (no phone-home). (D-15, UI-SPEC)"
    - "No stock team thank-you phone-home framing on feedback path"
    - "Local logs under ~/.bum remain allowed (D-11) — no change that forces log disable"
    - "Green p8_feedback and p8_telemetry (extended) tests pass"
  artifacts:
    - path: crates/codegen/xai-grok-pager/src/app/dispatch/notes.rs
      provides: "feedback short-circuit system message, no SendFeedback"
      contains: "Feedback is disabled in bum"
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "resolve_feedback default false + telemetry default Disabled lock"
      contains: "default(false)"
    - path: crates/codegen/xai-grok-pager-bin/src/main.rs
      provides: "Sentry init hard-disabled for quiet fork (or equivalent always-off default)"
      contains: "sentry::init"
  key_links:
    - from: dispatch_enter_feedback_mode
      to: Effect pipeline
      via: "return empty vec; no SendFeedback"
      pattern: "SendFeedback"
    - from: resolve_feedback
      to: shell handle_feedback gate
      via: "default false + is_feedback_enabled"
      pattern: "resolve_feedback"
    - from: is_error_reporting_disabled_sync / sentry Config.disabled
      to: Sentry network
      via: "disabled true on default quiet path"
      pattern: "disabled"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** use a product that presents as bum and never phones home or auto-updates as stock Grok Build, **so that** my fork identity and privacy are intact.

<objective>
Deliver OPS-02 quiet telemetry/feedback: feedback short-circuit at pager dispatch, resolve_feedback default false, telemetry Disabled verified/hardened, Sentry off by default (per D-08, D-09, D-10, D-11, D-15; UI-SPEC feedback copy).

Purpose: Product does not phone home to xAI analytics/feedback by default.
Output: No SendFeedback on default path; feedback default false; Sentry hard-off; green p8_feedback + p8_telemetry tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-quiet-fork-rebrand-polish/08-CONTEXT.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-RESEARCH.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-PATTERNS.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-UI-SPEC.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-01-SUMMARY.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-02-SUMMARY.md
@crates/codegen/xai-grok-pager/src/app/dispatch/notes.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/extensions/feedback.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
@crates/codegen/xai-grok-telemetry/src/config.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-08 | TelemetryMode::Disabled default |
| D-09 | Mixpanel / product analytics off by default |
| D-10 | Sentry off by default |
| D-11 | Local logs under ~/.bum OK |
| D-15 | /feedback quiet disabled message; no phone-home |

UI-SPEC primary feedback copy: Feedback is disabled in bum (no phone-home).  
Optional second line: Local logs under ~/.bum remain available for debugging.

## Discretion (document in SUMMARY)

1. **Remote telemetry re-enable:** Prefer hard pin — resolve_telemetry_mode ignores remote enable for product events unless explicit env GROK_TELEMETRY_ENABLED (or features.telemetry explicit) is set. Aligns with CONTEXT no easy stock phone-home. Implement pin if remote can flip Enabled after default Disabled; if already env/config-only without remote win on unset default, verify with test and document.
2. **Sentry:** Force Config.disabled = true at pager-bin init for quiet fork hard privacy (still leave crate linked). Do not advertise opt-in UX.
3. **Feedback UX:** Discoverable command stays; short-circuit at enter; do not open collect mode; no Effect::SendFeedback.

## Scope boundaries

- Do **not** rename GROK_* env vars (D-13).
- Do **not** disable local tracing/file logs (D-11).
- Do **not** change auto-update gates (Plan 04).
- Do **not** remove /feedback slash registration — disabled message only.

**File ownership:** notes.rs exclusive to this plan. pager-bin main.rs Sentry region only (Plan 04 owns update gates — sequential waves prevent conflict; if Plan 04 already shipped, edit only sentry::init block).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pager /feedback short-circuit — no SendFeedback (D-15)</name>
  <files>crates/codegen/xai-grok-pager/src/app/dispatch/notes.rs</files>
  <read_first>
    - 08-UI-SPEC.md Feedback quiet fork
    - 08-PATTERNS.md notes.rs enter/send targets
    - crates/codegen/xai-grok-pager/src/app/dispatch/notes.rs (dispatch_enter_feedback_mode, dispatch_send_feedback)
  </read_first>
  <behavior>
    - p8_feedback_enter_shows_disabled_message: enter path pushes system block with Feedback is disabled in bum (no phone-home).
    - p8_feedback_enter_returns_no_effects: returns empty Effect vec
    - p8_feedback_enter_stays_normal_mode: does not leave PromptInputMode::Feedback
    - p8_feedback_send_returns_no_send_effect: send path never returns Effect::SendFeedback; same disabled message (or no-op if mode unreachable)
    - Stock thank-you team message must not appear
  </behavior>
  <action>
    Per D-15 and UI-SPEC:
    1. dispatch_enter_feedback_mode: push RenderBlock::system with Feedback is disabled in bum (no phone-home). Optionally second system/hint line for local logs under ~/.bum. Do NOT set PromptInputMode::Feedback. Return empty vec.
    2. dispatch_send_feedback: same disabled message; return empty vec; never Effect::SendFeedback; never stock team thank-you string.
    3. Keep router Action wiring; only handler bodies change.
    4. Add p8_feedback_* unit tests using existing AppView/agent test harness patterns in dispatch tests (create notes test module or extend dispatch tests). Assert effects is empty and scrollback/system text contains disabled copy.

    Do not delete slash command registration.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager --lib p8_feedback -- --nocapture</automated>
  </verify>
  <done>
    Feedback enter/send quiet; no SendFeedback; p8_feedback green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolve_feedback default false + shell feedback gate message (D-15)</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/extensions/feedback.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (resolve_feedback ~2167; resolve_feedback_defaults_to_true_when_unset ~8819)
    - crates/codegen/xai-grok-shell/src/extensions/feedback.rs (handle_feedback is_feedback_enabled)
    - 08-UI-SPEC.md feedback copy (no easy opt-in as primary UX)
  </read_first>
  <behavior>
    - p8_feedback_resolve_defaults_to_false: unset → false, ConfigSource::Default
    - Existing env override tests still allow explicit GROK_FEEDBACK_ENABLED for developers without advertising in TUI
    - Shell handle_feedback when disabled uses quiet-fork-aligned message (not primarily set GROK_FEEDBACK_ENABLED=true as user-facing CTA)
  </behavior>
  <action>
    Per D-15:
    1. Change resolve_feedback BoolFlag chain .default(true) → .default(false).
    2. Rename/flip unit test resolve_feedback_defaults_to_true_when_unset to assert false (keep or add p8_feedback_resolve_defaults_to_false alias for phase discovery).
    3. Update remote/config override tests only if they assumed default true as baseline unexpectedly.
    4. Align shell extensions/feedback.rs disabled-path user message with quiet-fork wording (disabled in bum / no phone-home). Do not make env opt-in the primary displayed instruction.

    Telemetry: extend p8_telemetry tests from Plan 01 if implementing remote pin (discretion): when remote would enable but env/config unset, stay Disabled. Document decision in SUMMARY.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-shell --lib p8_feedback -- --nocapture; cargo test -p xai-grok-shell --lib p8_telemetry -- --nocapture; cargo test -p xai-grok-shell --lib resolve_feedback -- --nocapture</automated>
  </verify>
  <done>
    Feedback default false; p8_feedback + p8_telemetry shell green; handle_feedback quiet copy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sentry hard-off at composition root + telemetry default verify (D-08, D-09, D-10)</name>
  <files>crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-shell/src/agent/config.rs</files>
  <read_first>
    - 08-PATTERNS.md Sentry init block
    - crates/codegen/xai-grok-pager-bin/src/main.rs (sentry::init ~1483)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (is_error_reporting_disabled_sync, resolve_telemetry_mode)
    - crates/codegen/xai-grok-telemetry Mixpanel client mode gates (read-only confirm)
  </read_first>
  <behavior>
    - Default process: Sentry disabled true for quiet fork
    - resolve_telemetry_mode default Disabled remains green under p8_telemetry
    - Mixpanel emit path remains inactive when mode Disabled (document + optional pure unit if public API exists)
  </behavior>
  <action>
    Per D-08, D-09, D-10:
    1. At pager-bin sentry::init, force disabled: true for quiet fork (discretion hard privacy). Keep client/version fields. Do not remove sentry dependency.
    2. Confirm is_error_reporting_disabled_sync still true when telemetry disabled (inheritance). Optional additional p8_ unit on sync helper if testable without full filesystem config.
    3. Do not add UX to enable Sentry/Mixpanel. Do not touch local tracing init beyond ensuring it still writes under product home (D-11).
    4. If Plan 04 already edited main.rs, only change the sentry Config block — leave update gates intact.
    5. Add p8_sentry_disabled_by_default pure test if a helper can express the fork policy; otherwise document in SUMMARY that disabled is a compile-time constant true at init and covered by code review + manual phase check.

    Prefer a tiny pub(crate) or cfg(test)-visible function fork_sentry_disabled() -> bool { true } for unit proof if that keeps the plan automated-strong.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-shell --lib p8_telemetry -- --nocapture; cargo test -p xai-grok-pager-bin --bin bum p8_ -- --nocapture</automated>
  </verify>
  <done>
    Sentry off by default; telemetry Disabled verified; Mixpanel not on default path; D-11 local logs untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI/ACP → x.ai feedback HTTP | Untrusted phone-home — must not fire on default path |
| Process → Mixpanel / Sentry | Analytics/crash exfiltration — off by default |
| Local disk ~/.bum logs | Allowed local observability (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08 | Information Disclosure | /feedback SendFeedback | high | mitigate | Pager short-circuit + resolve_feedback default false; no Effect |
| T-08-09 | Information Disclosure | Mixpanel / product events | high | mitigate | TelemetryMode::Disabled default; optional remote pin |
| T-08-10 | Information Disclosure | Sentry | high | mitigate | sentry Config.disabled true at init |
| T-08-11 | Information Disclosure | residual env opt-in | medium | accept | Dev env knobs remain internal (D-13); not advertised in UX |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
pager p8_feedback green; shell p8_feedback + p8_telemetry + resolve_feedback green; pager-bin p8_ green for sentry policy if present.
</verification>

<success_criteria>
- No feedback phone-home on default quiet path
- Telemetry Disabled; Sentry off; Mixpanel inactive
- Local logs still OK
</success_criteria>

<output>
Create `.planning/phases/08-quiet-fork-rebrand-polish/08-05-SUMMARY.md` when done
</output>
