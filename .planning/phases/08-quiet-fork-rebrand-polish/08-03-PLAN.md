---
phase: 08-quiet-fork-rebrand-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
  - crates/codegen/xai-grok-pager-minimal/src/welcome.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
autonomous: true
requirements:
  - ID-02
must_haves:
  truths:
    - "OAuth browser return text says return to bum (D-01, UI-SPEC)"
    - "pager-minimal welcome product name is bum (D-01, D-14)"
    - "Composition-root version banner is bum (pager) - v{version} (UI-SPEC)"
    - "No stock product name Grok Build in these user-facing chrome sites (model labels out of scope)"
    - "Green p8_oauth_return and p8_minimal_welcome and/or p8_bin_banner unit tests pass"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
      provides: "OAuth callback return-to-bum copy"
      contains: "return to bum"
    - path: crates/codegen/xai-grok-pager-minimal/src/welcome.rs
      provides: "minimal welcome product span bum"
      contains: "bum"
    - path: crates/codegen/xai-grok-pager-bin/src/main.rs
      provides: "version banner bum (pager)"
      contains: "bum (pager)"
  key_links:
    - from: OIDC callback HTML
      to: browser post-login UX
      via: "callback_response message string only"
      pattern: "return to bum"
    - from: pager-bin banner
      to: user-visible process identity
      via: "println/format version line"
      pattern: "bum \\(pager\\)"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** use a product that presents as bum and never phones home or auto-updates as stock Grok Build, **so that** my fork identity and privacy are intact.

<objective>
Complete ID-02 **non-pager-core chrome**: OAuth browser return text, pager-minimal welcome product name, composition-root version banner (per D-01, D-14; UI-SPEC). Parallel-safe with Plan 02 (no shared files with Plan 02).

Purpose: No residual stock product identity on OAuth return, minimal mode, or bin banner.
Output: Three chrome sites rebranded + green p8_ unit proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-quiet-fork-rebrand-polish/08-CONTEXT.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-UI-SPEC.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-PATTERNS.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-01-SUMMARY.md
@crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
@crates/codegen/xai-grok-pager-minimal/src/welcome.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-01 | Product chrome → bum (OAuth, minimal, bin banner) |
| D-03 | No crate renames |
| D-13 | Leave GROK_* internal |
| D-14 | Welcome/product badge path includes minimal |

## Scope boundaries

- Do **not** change should_check_for_updates, run_update_command, min-version, or Sentry init (Plans 04–05). Only the user-visible version banner format string in pager-bin for ID-02.
- Do **not** rebrand IdP/provider issuer branding in OAuth HTML.
- Do **not** touch pager TUI files owned by Plan 02.

**File ownership note:** This plan owns the version-banner string edit in pager-bin main.rs only. Plan 04 owns update-gate functions in the same file afterward (depends_on 08-03).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OAuth browser return → bum (D-01)</name>
  <files>crates/codegen/xai-grok-shell/src/auth/oidc/login.rs</files>
  <read_first>
    - 08-UI-SPEC.md OAuth browser return
    - 08-PATTERNS.md login.rs callback_response
    - crates/codegen/xai-grok-shell/src/auth/oidc/login.rs (callback_response ~159–169)
  </read_first>
  <behavior>
    - p8_oauth_return_message_is_return_to_bum: success CallbackResult message is You can close this window and return to bum.
    - Access-denied path unchanged (Close this window and try again.)
  </behavior>
  <action>
    Per D-01 and UI-SPEC: change success OAuth callback browser message to You can close this window and return to bum. Leave error title/message and provider issuer branding alone. Add unit test p8_oauth_return_* against callback_response or extracted pure message helper if needed for testability without full axum server.

    Prefer testing pure string selection (title/message tuple) over full HTTP if the function is private — make a thin testable helper only if required; do not redesign OAuth flow.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-shell --lib p8_oauth_return -- --nocapture</automated>
  </verify>
  <done>
    Success OAuth return message matches UI-SPEC; p8_oauth_return green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pager-minimal welcome product name → bum (D-01, D-14)</name>
  <files>crates/codegen/xai-grok-pager-minimal/src/welcome.rs</files>
  <read_first>
    - 08-UI-SPEC.md pager-minimal welcome product
    - crates/codegen/xai-grok-pager-minimal/src/welcome.rs (~75 product Span)
  </read_first>
  <behavior>
    - p8_minimal_welcome_product_is_bum: welcome product span/text is bum not stock Grok Build product word
  </behavior>
  <action>
    Per D-01/D-14: replace minimal welcome product display string with bum. Keep layout/style. Add p8_minimal_welcome unit test if crate supports --lib tests; otherwise a small cfg(test) module in welcome.rs.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-minimal --lib p8_minimal_welcome -- --nocapture</automated>
  </verify>
  <done>
    Minimal welcome shows bum; p8_minimal_welcome green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pager-bin version banner → bum (pager) (D-01)</name>
  <files>crates/codegen/xai-grok-pager-bin/src/main.rs</files>
  <read_first>
    - 08-UI-SPEC.md Version / banner locked copy
    - crates/codegen/xai-grok-pager-bin/src/main.rs (Grok Build (pager) - v{} ~881)
  </read_first>
  <behavior>
    - p8_bin_banner_format_is_bum_pager: pure format helper or const prefix is bum (pager) - v
    - Do not assert update gates yet (Plan 04)
  </behavior>
  <action>
    Per D-01 and UI-SPEC: change composition-root version banner format from stock Grok Build (pager) prefix to bum (pager) - v{version}. Extract a tiny pure format function if needed so unit test can assert without running full async_main.

    Touch ONLY the banner identity string in this task — do not modify should_check_for_updates, run_update_command, enforce_minimum_version_or_exit, or Sentry init (Plan 04/05 file regions).

    Add p8_bin_banner_* unit test in main.rs cfg(test) module (or lib helper if bin structure requires — prefer cfg(test) in main.rs matching existing patterns).
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-bin --bin bum p8_bin_banner -- --nocapture</automated>
  </verify>
  <done>
    Banner is bum (pager); p8_bin_banner green; update gates untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth callback HTML → browser | Untrusted client display only; no tokens in message |
| Bin banner stdout | Local process identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | OAuth HTML | medium | mitigate | Keep success message free of tokens/codes; only product rename |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
p8_oauth_return, p8_minimal_welcome, p8_bin_banner green.
</verification>

<success_criteria>
- OAuth return, minimal welcome, bin banner present as bum
- No OPS-01/02 behavior changes in this plan
</success_criteria>

<output>
Create `.planning/phases/08-quiet-fork-rebrand-polish/08-03-SUMMARY.md` when done
</output>
