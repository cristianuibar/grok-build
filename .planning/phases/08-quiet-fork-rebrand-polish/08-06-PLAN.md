---
phase: 08-quiet-fork-rebrand-polish
plan: 06
type: execute
wave: 5
depends_on:
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md
  - .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md
autonomous: true
requirements:
  - ID-02
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "All phase success criteria proven by green automated filters (ROADMAP 1–3)"
    - "ID-02: clap/welcome/OAuth/minimal/banner p8_ subgroups green; model Grok Build (xAI) regression still green (D-02)"
    - "OPS-01: p8_no_auto_update, p8_auto_update_default, p8_min_version, p8_update_cmd green"
    - "OPS-02: p8_feedback, p8_telemetry green; feedback default false"
    - "home_isolation still green (BUM_HOME isolation)"
    - "08-VALIDATION.md maps every requirement to concrete green commands"
    - "08-PHASE-GATE.md runs per-subgroup discover ≥1 then execute — green only, no intentional-red"
    - "Deferred ideas not implemented (public x.ai channel, crate rename, mass GROK_* rename, Phase 9 live E2E)"
  artifacts:
    - path: .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md
      provides: "Final Nyquist map ID-02/OPS-01/OPS-02 → green filters"
      contains: "p8_"
    - path: .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md
      provides: "Runnable phase gate script/steps"
      contains: "discover"
  key_links:
    - from: ROADMAP Phase 8 success criteria
      to: cargo p8_ + model + home_isolation filters
      via: "VALIDATION.md + PHASE-GATE.md"
      pattern: "ID-02|OPS-01|OPS-02|p8_"
    - from: D-02 model brand lock
      to: dynamic_enum_model_names / format_cli_model_row
      via: "must remain green after rebrand"
      pattern: "Grok Build \\(xAI\\)"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** use a product that presents as bum and never phones home or auto-updates as stock Grok Build, **so that** my fork identity and privacy are intact.

<objective>
Close Phase 8: update 08-VALIDATION.md with greened filter names from Plans 01–05, write 08-PHASE-GATE.md that discovers and executes every required green subgroup for ID-02, OPS-01, OPS-02 (per D-01..D-15 coverage; green-only protocol).

Purpose: Prove roadmap success criteria before Phase 9 without empty filters or intentional-red exceptions.
Output: Updated VALIDATION + PHASE-GATE + aggregate green cargo sequence documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-CONTEXT.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-RESEARCH.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-01-SUMMARY.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-02-SUMMARY.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-03-SUMMARY.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-04-SUMMARY.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-05-SUMMARY.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md
</context>

## Locked decisions (gate coverage)

All D-01..D-15 must be evidenced by green tests or explicit VALIDATION rows (D-03/D-13 are non-change proofs: crates still xai-grok-*; GROK_* names unchanged — document as static inventory checks).

## Green-only gate protocol

1. Plan 01 shipped compile-safe green scaffolds only.
2. Plans 02–05 added product behavior and green p8_* tests together.
3. This plan claims **all required subgroups green** — never green-except-red.
4. Per-subgroup discovery ≥1 before execute (avoid vacuous empty filters).

## Scope boundaries

- Do **not** implement deferred ideas (public signed x.ai install channel, crate rename, mass env rename, live Phase 9 E2E).
- Prefer documentation + running existing tests; add only missing thin discovery wrappers if a required subgroup has zero tests (should not happen if Plans 02–05 complete).

<tasks>

<task type="auto">
  <name>Task 1: Finalize 08-VALIDATION.md requirement → green filter map</name>
  <files>.planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md</files>
  <read_first>
    - 08-01 through 08-05 SUMMARY.md (actual greened test names)
    - 08-RESEARCH.md Validation Architecture table
    - 07-VALIDATION.md structure as format analog if useful
  </read_first>
  <action>
    Update 08-VALIDATION.md so every ID-02, OPS-01, OPS-02 behavior row lists a concrete green cargo command and file that exists. Include:
    - ID-02: p8_cli_brand, p8_welcome, p8_oauth_return, p8_minimal_welcome, p8_bin_banner; billing string tests; model brand regression (dynamic_enum_model_names / format_cli_model_row)
    - OPS-01: p8_no_auto_update, p8_update_cmd, p8_auto_update, p8_min_version; settings auto_update default
    - OPS-02: p8_feedback (pager+shell), p8_telemetry; resolve_feedback default false
    - Isolation: home_isolation
    - Explicit exclusions: deferred ideas, D-02 do-not-rebrand note, D-13 GROK_* leave-internal note
    Mark each row green only when SUMMARY/prove commands pass. Cite D-01..D-15 coverage.
  </action>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md; grep -E 'ID-02|OPS-01|OPS-02|p8_' .planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md | head -20</automated>
  </verify>
  <done>
    VALIDATION maps all three requirements to real green filters with D-NN coverage notes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write and run 08-PHASE-GATE.md (per-subgroup discover + execute)</name>
  <files>.planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md</files>
  <read_first>
    - 07-PHASE-GATE.md (discover ≥1 then execute pattern)
    - 08-VALIDATION.md (final map)
  </read_first>
  <action>
    Create 08-PHASE-GATE.md with a runnable shell sequence (set -euo pipefail) that for each required subgroup:
    1. cargo test … -- --list | count filter matches ≥ 1
    2. cargo test … filter -- --nocapture

    Required subgroups (minimum):
    - pager lib: p8_cli_brand, p8_welcome, p8_feedback (and aggregate p8_ if preferred after per-subgroup)
    - shell lib: p8_telemetry, p8_oauth_return, p8_feedback / resolve_feedback
    - pager-minimal: p8_minimal_welcome
    - pager-bin: p8_no_auto_update, p8_update_cmd, p8_bin_banner (and p8_sentry if present)
    - update: p8_auto_update, p8_min_version
    - model regression: dynamic_enum_model_names
    - home_isolation: pager-bin --test home_isolation

    Document PASS criteria = all commands exit 0. No intentional-red exceptions. No live network to x.ai required.

    Execute the gate once during this plan and capture pass in SUMMARY (or fix missing tests by adding only discovery-safe wrappers if a Plan 02–05 name drifted — prefer update VALIDATION names to match reality over inventing product behavior).
  </action>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md; rg -n "p8_|home_isolation|dynamic_enum" .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md | head -30; test "$(rg -c 'p8_' .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md)" -ge 1</automated>
  </verify>
  <done>
    PHASE-GATE exists with per-subgroup discover+execute; gate run recorded green in SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Static inventory — model brands intact + deferred exclusions documented</name>
  <files>.planning/phases/08-quiet-fork-rebrand-polish/08-VALIDATION.md, .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md</files>
  <read_first>
    - crates/codegen/xai-grok-pager/tests/dynamic_enum_model_names.rs
    - default_models.json or catalog path for grok-build display name (do not edit)
    - 08-CONTEXT.md Deferred Ideas
  </read_first>
  <action>
    1. Run model brand regression and confirm catalog still exposes Grok Build (xAI) for grok-build (D-02).
    2. In VALIDATION or PHASE-GATE appendix, document static checks: no requirement to rename xai-grok-* crates (D-03); GROK_* env family left internal (D-13); deferred public install channel / crate rename / Phase 9 E2E out of scope.
    3. Optional grep inventory (document results in SUMMARY): product chrome files from UI-SPEC checklist no longer user-present stock product TUI name; exclude model fixtures from that inventory.
    4. Do not implement Phase 9 live E2E.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager --test dynamic_enum_model_names -- --nocapture; cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Model brand + isolation green; exclusions documented; phase gate ready for orchestrator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase gate CI → network | Gate must not require live x.ai; proves absence of phone-home via unit gates |
| Docs → executors | VALIDATION must not claim green without filters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | false-green empty filters | medium | mitigate | Per-subgroup discover ≥1 before execute |
| T-08-13 | Information Disclosure | residual phone-home | high | mitigate | Gate includes OPS-01/02 p8_ proofs from Plans 04–05 |
| T-08-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
Full PHASE-GATE sequence green; VALIDATION complete; model + isolation green.
</verification>

<success_criteria>
- ROADMAP Phase 8 success criteria 1–3 proven automated
- ID-02, OPS-01, OPS-02 all green under p8_ (+ named regressions)
- Deferred scope explicitly excluded
</success_criteria>

<output>
Create `.planning/phases/08-quiet-fork-rebrand-polish/08-06-SUMMARY.md` when done
</output>
