---
phase: 02-multi-slot-credentials-xai-oauth
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/manager_tests.rs
  - crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs
  - crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "AuthManager::new loads xAI credentials from a nested multi-slot auth.json under the given grok_home"
    - "AuthManager::update persists into providers.xai without clobbering a seeded providers.codex"
    - "get_valid_token / current_or_expired returns the xAI key from multi-slot disk"
    - "GrokAuthCredentials / ShellAuthCredentialProvider can apply Authorization Bearer from multi-slot-backed manager"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/manager_tests.rs
      provides: "multi-slot AuthManager load/update/token tests"
      contains: providers
    - path: crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs
      provides: "disk-backed multi-slot credential resolve test (or sibling test module)"
      contains: Bearer
  key_links:
    - from: AuthManager::new / update
      to: storage read_auth_json / write_auth_json
      via: "xAI slot adapter"
      pattern: "read_auth_json|write_auth_json"
    - from: GrokAuthCredentials::apply
      to: Authorization header
      via: "AuthManager token"
      pattern: "Bearer"
---

## Phase Goal

**As a** bum user, **I want to** complete an xAI login-like credential write and have agent HTTP auth pick up that token from the multi-slot store, **so that** coding turns authenticate on the xAI path after login (AUTH-01 success criterion 3).

<objective>
Prove the agent-turn auth path against multi-slot storage with automated fixtures (per D-06, D-08, D-12, D-14): AuthManager load/update/get_valid_token and credential Bearer apply. Prefer storage-only adapter — no dual AuthManager (D-12). No live IdP.

Purpose: CI gate for “after xAI login, agent turn authenticates on xAI path” without browser OAuth in CI.
Output: Extended manager_tests + credential tests green; existing ~manager suite remains green with nested on-disk format.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-CONTEXT.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-RESEARCH.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-PATTERNS.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-01-PLAN.md
@crates/codegen/xai-grok-shell/src/auth/manager.rs
@crates/codegen/xai-grok-shell/src/auth/manager_tests.rs
@crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
@crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs
@crates/codegen/xai-grok-shell/src/auth/storage.rs
</context>

## Artifacts this phase produces (this plan)

| Symbol / test | Location | Kind |
|---------------|----------|------|
| Multi-slot AuthManager tests | `auth/manager_tests.rs` | nested load, update no-clobber, get_valid_token |
| Disk-backed credential test | `util/grok_auth_credentials.rs` and/or `credential_provider.rs` | Bearer / snapshot from multi-slot manager |

## Locked decisions covered

D-06 (OAuth paths remain valid via AuthManager.update used by flows), D-08 (path under passed home / bum), D-12 (single manager), D-14 (automated proof), D-15 (no Codex tokens required — fixture-only reserved slot).

Note: Parallel with Plan 02 (wave 2). Avoid editing `manager.rs` write_scope_removal here — owned by Plan 02. If a test needs logout+codex, depend on Plan 02 or keep that assertion in Plan 02 only.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthManager multi-slot load/update/token tests</name>
  <files>crates/codegen/xai-grok-shell/src/auth/manager_tests.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/manager_tests.rs (fixture patterns: tempfile, AuthManager::new, write_auth_json, make_auth)
    - crates/codegen/xai-grok-shell/src/auth/manager.rs (`new`, `update`, `get_valid_token`, `auth`, `read_disk_auth`)
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (`write_auth_json`, `write_fixture_auth_document` if present)
    - crates/codegen/xai-grok-shell/src/auth/config.rs (`GrokComConfig::default`, `auth_scope`)
  </read_first>
  <behavior>
    - Nested document with providers.xai entry for default auth_scope → AuthManager::new finds token / current auth matches fixture key
    - Seed providers.codex + call mgr.update(xai_auth).await → raw file still has codex payload; xAI scope updated
    - After multi-slot load, get_valid_token (or auth().await.map key) returns the xAI access key without network when token not expired
    - Path is dir.path().join("auth.json") under tempfile — never asserts stock home paths
  </behavior>
  <action>
    Extend `manager_tests.rs` with multi-slot cases (D-08, D-11, D-12, D-14; MVP vertical slice: agent credential supply):

    1. Build nested fixture using `write_fixture_auth_document` or `write_auth_json` after Plan 01 (prefer fixture helper when seeding codex). Scope key must match `GrokComConfig::default().auth_scope()` so `lookup_auth` succeeds.
    2. Use non-expired `expires_at` / `GrokAuth` fields so `get_valid_token` does not attempt IdP refresh (avoid network). Prefer API-key mode or long-lived OIDC fixture already used in manager_tests.
    3. Assert update merge: distinctive codex `GrokAuth` key remains in raw JSON after `update`.
    4. Do not invent dual AuthManager or provider parameter on login (D-12, D-15).
    5. If existing tests write flat JSON via raw helpers, they must still pass via Plan 01 migration; only change fixtures if something breaks under nested writes from `write_auth_json`.
    6. Production manager.rs changes only if a test reveals a real bug beyond storage adapter — prefer zero manager logic change for load/update.
  </action>
  <acceptance_criteria>
    - At least one test loads nested multi-slot auth and resolves xAI credentials via AuthManager
    - At least one test proves update does not clobber seeded codex (raw JSON assertion)
    - At least one test proves get_valid_token or auth() returns the xAI key from multi-slot disk without network
    - `cargo test -p xai-grok-shell --lib auth::manager -- --nocapture` green (or `auth::manager_tests` filter as appropriate for module path)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib auth::manager -- --nocapture</automated>
  </verify>
  <done>
    AuthManager multi-slot load/update/token path is automated and green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Credential provider / GrokAuthCredentials multi-slot agent path</name>
  <files>crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs, crates/codegen/xai-grok-shell/src/auth/credential_provider.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/util/grok_auth_credentials.rs (apply Bearer + X-XAI-Token-Auth, existing hot_swap tests)
    - crates/codegen/xai-grok-shell/src/auth/credential_provider.rs (ShellAuthCredentialProvider apply / snapshot)
    - Task 1 manager multi-slot fixture patterns
  </read_first>
  <behavior>
    - Disk-backed: write nested xAI auth under temp dir → AuthManager::new → GrokAuthCredentials.with_auth_manager (or ShellAuthCredentialProvider) → resolve/snapshot user_token equals fixture key
    - apply() sets Authorization Bearer &lt;token&gt; for user/OAuth path (existing header contract)
    - No deployment_key path required for this proof
  </behavior>
  <action>
    Add disk-backed multi-slot credential proof (AUTH-01 agent-turn criterion; D-14):

    1. Prefer extending existing test module in `grok_auth_credentials.rs` that already constructs AuthManager + applies credentials — replace hot_swap-only with (or add) load-from-disk multi-slot fixture.
    2. Optionally assert `ShellAuthCredentialProvider::snapshot` token field if that API is the production agent path; one green path is enough if both share AuthManager.
    3. Do not change Bearer header contract or introduce Codex headers this phase.
    4. Do not log tokens in new test println!/tracing of full secrets — assert equality in tests is fine.
    5. If credential modules need no production code changes, tests-only is success.
  </action>
  <acceptance_criteria>
    - Automated test proves multi-slot disk → AuthManager → credential surface yields the expected Bearer token value
    - Existing credential unit tests remain green
    - `cargo test -p xai-grok-shell --lib grok_auth_credentials -- --nocapture` and/or credential_provider filters green; if filters miss, `cargo test -p xai-grok-shell --lib auth::credential_provider -- --nocapture`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib -- grok_auth_credentials credential_provider --nocapture</automated>
  </verify>
  <done>
    Agent-turn credential attachment is proven against multi-slot auth store with fixtures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AuthManager cache ↔ disk multi-slot file | Token load for agent turns |
| Credential provider ↔ HTTP client | Bearer injection into outbound requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Information Disclosure | credential apply / tests | high | mitigate | Never log full tokens in production paths; tests may assert equality only; Debug redaction retained |
| T-02-11 | Tampering | AuthManager update vs codex slot | high | mitigate | Isolation test after update; relies on Plan 01 merge-safe write |
| T-02-12 | Spoofing | wrong home path | high | mitigate | Tests use tempfile home only; construction via AuthManager::new(temp, …) (D-08) |
| T-02-04 | Information Disclosure | token logging | high | mitigate | No new tracing of auth document bodies |
| T-02-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo test -p xai-grok-shell --lib auth::manager`
- Credential / grok_auth_credentials tests for multi-slot disk path
- Spot-check: no live network in new tests
</verification>

<success_criteria>
- AuthManager supplies xAI credentials from multi-slot store
- Update does not clobber seeded codex
- Agent credential Bearer path works from disk-backed multi-slot manager
</success_criteria>

<output>
Create `.planning/phases/02-multi-slot-credentials-xai-oauth/02-03-SUMMARY.md` when done
</output>
