---
phase: 02-multi-slot-credentials-xai-oauth
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/device_code.rs
  - crates/codegen/xai-grok-shell/src/auth/flow.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "bum login still defaults to xAI with --oauth / --device-auth flags (no required --provider)"
    - "Device-code and browser login code paths still compile and pass existing unit coverage"
    - "Path-wrong product-home docs that send users to stock grok auth path are corrected for auth persistence"
    - "Full auth lib suite green including multi-slot isolation and credential supply tests"
    - "AuthManager default path remains {grok_home}/auth.json (bum home after Phase 1)"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/device_code.rs
      provides: "product-home-correct persistence docs for device-code success"
      contains: auth.json
    - path: crates/codegen/xai-grok-pager/src/app/cli.rs
      provides: "Login flags without provider argument (verified)"
      contains: device_auth
  key_links:
    - from: pager-bin Command::Login
      to: run_cli_login
      via: "oauth / device_auth flags"
      pattern: "run_cli_login"
    - from: AuthManager::new
      to: "{grok_home}/auth.json"
      via: "Phase 1 grok_home → BUM_HOME / ~/.bum"
      pattern: "auth.json"
---

## Phase Goal

**As a** bum user, **I want to** run `bum login` (browser or device-code) knowing credentials land only under the bum auth store, **so that** xAI OAuth works end-to-end under bum without stock-home coupling (AUTH-01).

<objective>
Close AUTH-01 CLI/path surface: verify login defaults to xAI (D-07, D-06), fix path-wrong product-home documentation that still points at stock grok auth paths (D-08, D-16 exception for wrong paths only), re-verify flow/device-code unit coverage, and run the full auth lib suite as the phase gate (D-14). No Codex OAuth (D-15). No full chrome rebrand (Phase 8).

Purpose: Ship confidence that multi-slot storage is wired through real login entrypoints and that CI auth suite is green.
Output: Doc/path fixes if needed; CLI flag verification; full `auth::` suite green; optional note if raw flat test helpers still rely on migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-CONTEXT.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-RESEARCH.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-VALIDATION.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-01-PLAN.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-02-PLAN.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-03-PLAN.md
@crates/codegen/xai-grok-shell/src/auth/flow.rs
@crates/codegen/xai-grok-shell/src/auth/device_code.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Artifacts this phase produces (this plan)

| Symbol / change | Location | Kind |
|-----------------|----------|------|
| Device-code rustdoc path fix | `auth/device_code.rs` | docs: product auth store / `$BUM_HOME/auth.json` wording |
| Login CLI verification | `cli.rs` + `pager-bin` | no `--provider`; flags retained (likely no code change) |
| Phase gate suite | auth lib tests | all green |

## Locked decisions covered

D-06 (browser + device-code), D-07 (login defaults xAI), D-08 (bum home only), D-13 (no import), D-14 (automated gate), D-15 (no Codex OAuth), D-16 (chrome rebrand deferred — only path-wrong fixes).

<tasks>

<task type="auto">
  <name>Task 1: Login CLI + device-code path docs + compile-time surface</name>
  <files>crates/codegen/xai-grok-shell/src/auth/device_code.rs, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-shell/src/auth/flow.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/device_code.rs (rustdoc around persistence path ~line 207)
    - crates/codegen/xai-grok-pager/src/app/cli.rs (`Command::Login` oauth / device_auth flags)
    - crates/codegen/xai-grok-pager-bin/src/main.rs (`Command::Login` → `run_cli_login`)
    - crates/codegen/xai-grok-shell/src/auth/flow.rs (`run_cli_login` signature and default transport selection)
    - crates/codegen/xai-grok-shell/src/auth/manager.rs (`AuthManager::new` path priority comments)
  </read_first>
  <action>
    Seal AUTH-01 user-facing entrypoints without implementing Codex login (D-06, D-07, D-08, D-15):

    1. Fix device-code success rustdoc that still claims credentials persist under the stock grok product-home auth path. Replace with product auth store wording: `$BUM_HOME/auth.json` or “product home `auth.json`” (Phase 1 SoT). Do not rebrand unrelated “Grok Build” chrome (Phase 8).
    2. Verify `Command::Login` still exposes `--oauth` (alias oidc) and `--device-auth` (device-code alias) with mutual conflicts, and does **not** add a required `--provider` flag this phase. Code change only if flags regressed.
    3. Verify pager-bin routes Login to `xai_grok_shell::auth::run_cli_login(&config, oauth, device_auth, devbox)` with no provider argument. No dual-login CLI.
    4. Confirm `run_cli_login` / device and loopback branches still construct `AuthManager::new(&grok_home, …)` so writes go through multi-slot storage under product home. Minimal/no logic change expected if Plan 01 adapter is correct.
    5. Scan auth module rustdocs for other product-home path mistakes that would send operators to stock grok auth storage for bum credentials; fix only wrong home paths (D-08 exception). Leave generic `grok login` CLI name strings for Phase 8 unless they encode a wrong filesystem path.
    6. Do not implement AUTH-02–05, model routing, or import from stock homes (D-13, D-15).
  </action>
  <acceptance_criteria>
    - device_code persistence docs do not claim stock grok home auth path for bum credentials
    - Login CLI still has oauth + device-auth flags and no required provider flag (grep/read confirms)
    - pager-bin still calls `run_cli_login` with the four existing args
    - `cargo check -p xai-grok-shell -p xai-grok-pager-bin` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p xai-grok-shell -p xai-grok-pager-bin</automated>
  </verify>
  <done>
    Login entrypoints and path docs align with bum multi-slot auth store; CLI still defaults xAI.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full auth suite gate + bypass-helper audit</name>
  <files>crates/codegen/xai-grok-shell/src/auth/storage.rs, crates/codegen/xai-grok-shell/src/agent/relay.rs, crates/codegen/xai-grok-shell/src/auth/refresh/oidc_refresher_tests.rs</files>
  <read_first>
    - .planning/phases/02-multi-slot-credentials-xai-oauth/02-VALIDATION.md (phase gate)
    - crates/codegen/xai-grok-shell/src/agent/relay.rs (`write_test_auth_to_disk` test helper)
    - crates/codegen/xai-grok-shell/src/auth/refresh/oidc_refresher_tests.rs (`write_auth_to_disk` if present)
    - Multi-slot tests from Plans 01–03
  </read_first>
  <behavior>
    - cargo test -p xai-grok-shell --lib auth:: fully green
    - Flat raw fixtures written by test helpers still load via read migration OR helpers switched to write_auth_json / write_fixture_auth_document
    - Multi-slot isolation + credential supply tests included in green suite
  </behavior>
  <action>
    Phase gate and hermetic fixture hygiene (D-14, RESEARCH Pitfall 5):

    1. Run `cargo test -p xai-grok-shell --lib auth::` and fix any residual failures from multi-slot on-disk shape (manager_tests, storage, device_code, flow, refresh). Prefer fixing production adapters over mass-rewriting tests; update tests only when they assert raw flat root shape incorrectly.
    2. Audit `write_test_auth_to_disk` / `write_auth_to_disk` helpers: either leave flat writes (migration accepts them) and add a comment that production writes are nested, or switch helpers to `write_auth_json` for consistency. Do not break refresh regression tests.
    3. Confirm no new test opens a real user `~/.grok` or stock Codex credentials path. Temp dirs only.
    4. Optionally run broader `cargo test -p xai-grok-shell --lib` if time allows; auth:: is the AUTH-01 gate per VALIDATION.md.
    5. Do not run live browser OAuth against auth.x.ai in CI. Manual smoke remains optional outside this plan.
  </action>
  <acceptance_criteria>
    - `cargo test -p xai-grok-shell --lib auth::` exits 0
    - Multi-slot storage isolation tests and AuthManager credential tests are part of the green set
    - Test helpers either migrate-compatible or use storage API; no dependency on live IdP for the gate
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib auth:: -- --nocapture</automated>
  </verify>
  <done>
    AUTH-01 phase gate green: multi-slot store + xAI credential path + login surface verified by automated suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI login → AuthManager → auth.json | User-triggered credential write under product home |
| Docs / operator guidance → filesystem | Wrong path docs can leak ops to stock credential stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Information Disclosure | device_code / auth docs | medium | mitigate | Correct product-home auth path in rustdocs so operators do not open stock credential files for bum |
| T-02-05 | Elevation of Privilege | stock home import | high | mitigate | No import path; tests temp-only; Phase 1 BUM_HOME SoT retained |
| T-02-14 | Spoofing | login defaults | medium | mitigate | Keep xAI-only default login; no provider flag that could confuse slots this phase |
| T-02-03 | Information Disclosure | auth.json perms | high | mitigate | Unchanged secure write path from Plans 01–02 |
| T-02-04 | Information Disclosure | token logging | high | mitigate | No new token logging in flow/device paths |
| T-02-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo check -p xai-grok-shell -p xai-grok-pager-bin`
- `cargo test -p xai-grok-shell --lib auth::` (phase gate)
</verification>

<success_criteria>
- bum login defaults xAI; oauth/device flags preserved
- Path-wrong stock-home auth docs fixed for device-code
- Full auth lib suite green including multi-slot isolation
- AUTH-01 automated success criteria covered across Plans 01–04
</success_criteria>

<output>
Create `.planning/phases/02-multi-slot-credentials-xai-oauth/02-04-SUMMARY.md` when done
</output>
