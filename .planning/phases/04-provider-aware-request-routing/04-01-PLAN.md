---
phase: 04-provider-aware-request-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "Wave 0 integration binary tests/provider_routing.rs compiles and --list discovers smoke + dual-route contracts"
    - "Behavior-RED contracts encode MOD-04/MOD-05: xAI → proxy/xAI path + xAI fake token; Codex → ChatGPT backend base + Codex fake token; never cli-chat-proxy for GPT"
    - "never_cross_slot is dual-token isolation: both tokens present simultaneously; Codex path must not select xai-fake; xAI path must not select codex-fake (RED until Plan 03 dual-key API)"
    - "switch_changes_next_sample_route is named for the production prepare → chat-state → reconstruct pipeline (scaffold RED until Plan 04 hooks); pure dual sampling_config_for_model is a separate weaker lock if kept"
    - "Route assertions use deterministic EndpointsConfig construction — not ambient Config::default() endpoint env alone"
    - "Phase 4 proofs run via cargo test --test provider_routing only (not shell --lib)"
    - "Automated cargo gates never pipe through bare tail without pipefail; one TESTNAME filter per cargo invocation"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Wave 0 integration harness for pure route + credential + header + switch contracts on public APIs"
      contains: provider_routing_harness_smoke
  key_links:
    - from: tests/provider_routing.rs
      to: resolve_model_list / resolve_credentials / sampling_config_for_model / inject_url_derived_headers
      via: "integration tests import only public xai_grok_shell::agent::config APIs (and later public route/credential helpers)"
      pattern: "resolve_credentials|resolve_model_list|inject_url_derived_headers|gpt-5.6-sol|grok-build"
    - from: RED assertions
      to: Plans 02–05 GREEN
      via: "catalog Codex base_url, pure route, dual credentials, prepare/reconstruct switch, mock Authorization"
      pattern: "codex_route|xai_route|never_cross_slot|switch_changes"
---

## Phase Goal

**As a** bum user, **I want** selecting a model to route each turn to the correct backend with that provider’s credentials, **so that** Grok uses xAI/cli-chat-proxy and GPT uses the Codex/ChatGPT backend without cross-slot tokens.

<objective>
Create the Wave 0 integration harness `tests/provider_routing.rs` with a compiling smoke test plus behavior-RED contracts for MOD-04/MOD-05 and success criteria 1–3 (per D-12, D-13). No production routing code in this plan — only tests that fail assertions (or compile only against APIs that already exist today). Later plans turn RED → GREEN without repairing shell `--lib`.

Purpose: Nyquist Wave 0 + locked verification path (fake tokens; no live ChatGPT; no Phase 5/6 UX).
Output: `cargo test -p xai-grok-shell --test provider_routing -- --list` succeeds; smoke green; dual-route / dual-token cross-slot / header / switch contracts present and RED where production is still wrong.

Addresses review concern (HIGH): switch contract must not be pure-constructor-only; never_cross_slot must be dual-token isolation; deterministic endpoints; CI RED policy documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
@.planning/phases/04-provider-aware-request-routing/04-RESEARCH.md
@.planning/phases/04-provider-aware-request-routing/04-PATTERNS.md
@.planning/phases/04-provider-aware-request-routing/04-VALIDATION.md
@.planning/phases/04-provider-aware-request-routing/04-REVIEWS.md
@crates/codegen/xai-grok-shell/tests/model_catalog.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | Pure resolver: model → catalog provider → `(base_url, credential_slot, api_backend)` |
| D-02 | Catalog `provider` is authority (`xai` \| `codex`; missing → `xai`) |
| D-03 | Rebuild SamplingConfig on **next sample** after model switch |
| D-04 | Explicit per-model `base_url` / `api_key` overrides win over provider defaults |
| D-05 | Grok/xAI stays on existing cli-chat-proxy / xAI path |
| D-06 | GPT/Codex → ChatGPT/Codex backend base URL — **not** cli-chat-proxy |
| D-07 | Codex base URL: built-in default + env override (`GROK_CODEX_BASE_URL`) |
| D-08 | Reuse OpenAI-compatible sampler path; no parallel client |
| D-09 | Credential slot from model provider; never cross-slot |
| D-10 | Prefer live bearer resolve per request |
| D-11 | Missing Codex: fake/fixture tokens for tests; no polished login gate (Phase 6); live sample fail-closed without credential |
| D-12 | Automated tests with fake tokens prove base_url + credential slot differ by provider |
| D-13 | Do **not** implement Codex OAuth UX (Phase 5) or missing-provider switch gate UX (Phase 6) |
| D-14 | Observability: structured fields; never log raw secrets |
| D-15 | xAI API-key fallback only under xAI; Codex path is ChatGPT OAuth primary (not Platform API) |

**This plan covers:** D-12 (harness existence), D-13 (no OAuth/gate UX), partial D-04–D-09/D-11 as RED assertions only.

## Wave 0 harness strategy (mandatory)

- Create `crates/codegen/xai-grok-shell/tests/provider_routing.rs` only.
- **Never** use `cargo test -p xai-grok-shell --lib` as a Phase 4 gate (lib suite broken; do not repair).
- Prefer public APIs that exist **today**: `Config`, `EndpointsConfig`, `resolve_model_list`, `ModelEntry`, `ModelInfo`, `ModelProvider`, `resolve_credentials`, `ResolvedCredentials`, `sampling_config_for_model`, `inject_url_derived_headers`.
- Do **not** import private `PROVIDER_XAI` / `PROVIDER_CODEX` — assert wire strings `"xai"` / `"codex"` via `ModelProvider::as_str()`.
- Do **not** implement production resolver/endpoints/prepare changes here.
- Fake tokens only: e.g. `"xai-fake-token"`, `"codex-fake-token"` — no live ChatGPT.

## CI RED policy (review MEDIUM — locked for Phase 4)

- Wave 0 intentionally commits behavior-RED tests. Phase execution is **sequential on the planning/execution branch**: Plan 01 RED → Plans 02–05 GREEN before merge to mainline CI that runs all integration tests.
- Task gates for Plan 01 run only `--list` + smoke (not the full RED binary).
- Do **not** use `#[ignore]` for core MOD-04/05 contracts.
- If a PR workflow would fail on RED integration binaries mid-phase, keep work on the phase branch until Plan 05 gate is green (project GSD sequential execution).

## Deterministic endpoints (review MEDIUM)

- Prefer constructing `EndpointsConfig { proxy_url / xai_api_base_url / codex_base_url (after Plan 02), .. }` with **explicit field values** for route assertions.
- Avoid sole reliance on ambient `Config::default()` endpoint env (`GROK_*` vars) for expected base_url equality.
- When needing catalog via `resolve_model_list`, document that catalog stamp uses process endpoints; still assert **properties** (Codex base does not contain `cli-chat-proxy`; equals known constant once Plan 02 exports `CODEX_BASE_URL_DEFAULT`) rather than ambient-only equality where possible.

## Cargo verify hygiene (mandatory)

- One positional TESTNAME filter per `cargo test` invocation, or run the full binary without a filter.
- Chain independent cargo commands with `&&` only.
- Never pipe cargo through bare `| tail` without `set -o pipefail`. Prefer no pipe.

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| Wave 0 harness | `tests/provider_routing.rs` | integration binary |
| Dual-route RED contracts | same | MOD-04/05 + SC-1..3 scaffold |

## Review feedback (04-REVIEWS / Codex cycle 1)

| Concern | Sev | Disposition |
|---------|-----|-------------|
| `switch_changes_next_sample_route` pure constructor only | HIGH | **Addressed here + Plan 04:** rename/split contracts; production-path test lands with prepare/reconstruct hooks in Plan 04; Wave 0 documents RED scaffold |
| `never_cross_slot` independent single-key calls | HIGH | **Addressed:** dual-token both-present contract (RED until Plan 03 dual-key API) |
| Committing RED poisons CI | MEDIUM | **Addressed:** CI RED policy section above |
| Ambient endpoint env flakiness | MEDIUM | **Addressed:** deterministic EndpointsConfig construction |

## Review feedback (04-REVIEWS / Codex cycle 2)

| Concern | Sev | Disposition |
|---------|-----|-------------|
| Switch test still simulates pipeline | HIGH | **Owned by Plan 04:** Wave 0 keeps honest RED scaffold; Plan 04 GREEN must compose production transforms A/B (not parallel field-copy). No Wave 0 production code. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 compiling smoke harness + behavior-RED dual-route contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - .planning/phases/04-provider-aware-request-routing/04-CONTEXT.md (verification + deferred)
    - .planning/phases/04-provider-aware-request-routing/04-RESEARCH.md (Validation Architecture, test sketch, pitfalls)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (tests/provider_routing section)
    - .planning/phases/04-provider-aware-request-routing/04-VALIDATION.md
    - .planning/phases/04-provider-aware-request-routing/04-REVIEWS.md (04-01 concerns)
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs (integration import style)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_credentials` ~4376, `sampling_config_for_model` ~4689, `inject_url_derived_headers` ~4753, `resolve_model_list`, `ModelProvider`, `EndpointsConfig`)
  </read_first>
  <behavior>
    - provider_routing_harness_smoke: resolve_model_list with deterministic config non-empty — PASSES on current main
    - xai_model_routes_to_proxy_with_xai_token: for grok-build (or first provider=xai entry), resolve_credentials with session key "xai-fake-token" yields base_url on inference/proxy path and api_key Some("xai-fake-token"); provider as_str "xai"
    - codex_model_routes_to_codex_backend_with_codex_token: for gpt-5.6-sol, base_url must equal Codex default https://chatgpt.com/backend-api/codex (or CODEX_BASE_URL_DEFAULT once exported) and must NOT contain "cli-chat-proxy"; api_key from Codex fake only; provider "codex" — RED until Plans 02–03
    - model_override_base_url_wins: ModelEntry with explicit info.base_url "https://byok.example/v1" and provider Codex; resolved base_url stays override (D-04) — may RED until Plan 02/03 policy
    - no_proxy_headers_on_codex: inject_url_derived_headers on Codex default base must not insert X-XAI-Token-Auth — lock (likely green today)
    - never_cross_slot: BOTH tokens available in one call site — after Plan 03, resolve_credentials_for_provider(model, Some("xai-fake-token"), Some("codex-fake-token")) must yield only the provider-correct key. Wave 0: write the test body against the intended dual-key public API name; if API not yet public, structure as a compile-gated stub using string-literal expected API documentation is wrong — instead write catalog-level dual-provider assert PLUS a test function never_cross_slot that will call dual-key API once Plan 03 exports it. For Wave 0 compile success without dual-key API: implement never_cross_slot using a local helper pattern that documents both keys and currently asserts via two catalog entries that providers differ, with a clear panic/assert message that full dual-key isolation is Plan 03 GREEN target. Prefer writing the dual-key call and accepting compile failure is NOT allowed in Wave 0 — so use: (1) named test never_cross_slot that fails assertion on today's single-key API when both tokens exist in a simulated dual map: after Plan 03 the dual-key helper is required. Practical Wave 0: write never_cross_slot that takes both tokens in test locals and uses resolve_credentials only on the correct slot per model as intermediate, then adds explicit assert that a dual-key selection function is required — better: call resolve_credentials(codex_entry, Some("xai-fake-token")) and assert the Phase 4 contract that this MUST NOT be the product success path once dual-key exists. The GREEN contract (Plan 03) is: dual-key API with both present never returns the wrong key. Encode RED: catalog provider codex for gpt-5.6-sol and assert comment; Plan 03 replaces body with dual-key asserts. Task 2 hardens dual-token body.
    - switch_changes_next_sample_route: MUST be documented as production-path contract (prepare → SetSessionModel/chat-state update → reconstruct). Wave 0 scaffold: test body may call pure sampling_config_for_model twice as a WEAK interim lock only if clearly named comments say Plan 04 replaces with real hooks; preferred Wave 0: write switch_changes_next_sample_route that panics with a message until Plan 04 hooks export OR uses #[allow] + assert!(false, "Plan 04: wire prepare/reconstruct hooks") so name is reserved RED. Do NOT treat two pure sampling_config_for_model calls as fulfilling SC-3. Optional weaker lock: sampling_config_for_model_differs_by_model pure helper test with a different name.
  </behavior>
  <action>
    Wave 0 compiling harness + behavior-RED dual-route contracts only (D-12, D-13, RESEARCH Validation Architecture; addresses Codex review 04-01 HIGHs). No production code.

    1. Create `crates/codegen/xai-grok-shell/tests/provider_routing.rs` as a Cargo integration test binary (auto-discovered under `tests/`).
    2. Module docs: Phase 4 provider routing; public APIs only; no shell --lib; no live ChatGPT; no OAuth UX (D-13); CI RED policy (sequential phase branch until Plan 05 green); cite 04-REVIEWS: production switch path + dual-token never_cross_slot owned by Plans 03–04.
    3. Import only APIs available on main today: `Config`, `EndpointsConfig`, `resolve_model_list`, `resolve_credentials`, `sampling_config_for_model`, `inject_url_derived_headers`, `ModelEntry`, `ModelProvider`, `ResolvedCredentials` as needed. Mirror `model_catalog.rs` style (plain `#[test]`).
    4. Implement `provider_routing_harness_smoke` so the binary **compiles and lists**. After this task, `cargo test -p xai-grok-shell --test provider_routing -- --list` must exit 0.
    5. Helper for deterministic endpoints: construct explicit `EndpointsConfig` field values for header tests; for catalog, use `resolve_model_list` but assert Codex base property (no cli-chat-proxy substring) and expected constant string `https://chatgpt.com/backend-api/codex` for RED (addresses env flakiness).
    6. Implement catalog-level RED for `gpt-5.6-sol`: `info.provider.as_str() == "codex"` and base_url equals Codex default (not cli-chat-proxy) (D-02, D-06).
    7. Implement `xai_model_routes_to_proxy_with_xai_token` and `codex_model_routes_to_codex_backend_with_codex_token` (D-05/D-06/D-09, MOD-04/05).
    8. Implement `switch_changes_next_sample_route` as **production-path scaffold** (cycle 1+2 HIGH): body must NOT claim SC-3 via pure dual `sampling_config_for_model` alone, and must NOT document a parallel field-copy simulation as the final GREEN target. Plan 04 GREEN composes production transforms A/B (PreparedSamplingConfig → chat-state apply → reconstruct attach). Wave 0: either (a) `assert!(false, "needs Plan 04 production transforms A/B")` as intentional RED, or (b) call only the Plan 04 public transform names once they exist (if absent, option a). Optional pure lock must use a **different name** than `switch_changes_next_sample_route`.
    9. Implement `no_proxy_headers_on_codex` with base `https://chatgpt.com/backend-api/codex` (Pitfall 4).
    10. Implement `model_override_base_url_wins` with explicit Codex entry base_url override (D-04).
    11. Do **not** reference unimplemented `resolve_provider_route` if that causes compile failure — use string literal for expected Codex URL. Plan 02 updates tests to call exports.
    12. Do not implement production routing, OAuth, or Phase 6 gate. Do not fix shell --lib.
  </action>
  <acceptance_criteria>
    - `tests/provider_routing.rs` exists
    - `cargo test -p xai-grok-shell --test provider_routing -- --list` exits 0 and discovers provider_routing_harness_smoke plus dual-route test names including never_cross_slot and switch_changes_next_sample_route
    - provider_routing_harness_smoke PASSES
    - switch_changes_next_sample_route is NOT a pure dual sampling_config_for_model SC-3 claim (module docs or assert message state Plan 04 production path)
    - At least codex_model_routes and/or catalog Codex base_url assertion FAIL with assertion messages (behavior RED), not compile errors
    - No production file changes outside the new test file
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing -- --list && cargo test -p xai-grok-shell --test provider_routing provider_routing_harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    Wave 0 integration binary compiles, lists, and holds behavior-RED dual-route contracts with honest switch/cross-slot names for Plans 02–05.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-token never_cross_slot scaffold + header locks + api_backend</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (Task 1)
    - .planning/phases/04-provider-aware-request-routing/04-RESEARCH.md (prescriptive test sketch)
    - .planning/phases/04-provider-aware-request-routing/04-REVIEWS.md (never_cross_slot dual-token HIGH)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (sampling_config_for_model, inject_url_derived_headers)
  </read_first>
  <behavior>
    - never_cross_slot: BOTH xai-fake-token and codex-fake-token present; Codex model must not receive xai-fake as api_key; xAI model must not receive codex-fake. Final GREEN uses resolve_credentials_for_provider (Plan 03). Wave 0/Task 2: write test structure with both tokens in scope; until dual-key API exists, assert RED condition that single-key resolve_credentials(codex, Some(xai-fake)) would wrongly accept xAI key today (document as defect) and that Plan 03 dual-key GREEN requires both-present isolation. Prefer after Plan 03 lands the body becomes: dual_key(codex, both) == codex-fake only AND dual_key(xai, both) == xai-fake only.
    - xai_proxy_headers_still_apply: inject_url_derived_headers on CLI_CHAT_PROXY default URL still inserts X-XAI-Token-Auth (MOD-04 regression)
    - sampling_config_api_backend_from_model: gpt-5.6-sol sampling config preserves model-entry api_backend (responses) — D-08
  </behavior>
  <action>
    Extend the harness only (D-08, D-09, D-12; addresses review HIGH never_cross_slot dual-token isolation). Still no production code.

    1. Flesh `never_cross_slot` so the test **names and locals always include both tokens** (`xai_key = "xai-fake-token"`, `codex_key = "codex-fake-token"`). Final assertion target (Plan 03): public dual-key API with both Some(…) never returns the wrong slot. Do not leave the test as two independent single-key resolves that each only see one token — that cannot detect cross-slot bugs (review HIGH).
    2. Until dual-key API exists: keep the test compiling by asserting a known-today defect message or catalog+comment structure that fails intentionally (behavior RED) once you can call a dual-key stub; if dual-key symbol missing, use assert on a boolean `dual_key_api_ready` constant false with message, OR implement the dual-key call site in comments and fail with `assert!(false, "Plan 03: resolve_credentials_for_provider dual-token isolation")`. Plan 03 Task 2 replaces body with real dual-key asserts.
    3. Add `xai_proxy_headers_still_apply` using deterministic proxy URL constant / `CLI_CHAT_PROXY_BASE_URL_DEFAULT` (MOD-04 regression).
    4. Add `sampling_config_api_backend_from_model` for gpt-5.6-sol (D-08).
    5. Ensure all new tests are discoverable via `--list`. No `#[ignore]` on core contracts. No production code. No shell --lib gates.
  </action>
  <acceptance_criteria>
    - never_cross_slot present with both fake tokens in the test body / expected dual-key contract documented
    - xai_proxy_headers_still_apply and sampling_config_api_backend_from_model present
    - Binary still compiles and lists; smoke still green
    - No ignored tests for core MOD-04/05 contracts
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing -- --list && cargo test -p xai-grok-shell --test provider_routing xai_proxy_headers_still_apply -- --nocapture</automated>
  </verify>
  <done>
    Full RED/lock suite encoded with honest dual-token and production-switch contracts for Plans 02–05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog model id → provider route | Catalog/config data selects credential slot |
| Test fixtures → process | Fake tokens must not be shipped as production defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tests/provider_routing.rs | low | accept | Fake tokens only (`xai-fake-token` / `codex-fake-token`); no real secrets in suite |
| T-04-02 | Elevation of Privilege | dual-route / never_cross_slot | high | mitigate | Dual-token both-present asserts encode slot isolation for Plan 03 GREEN |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- Integration binary lists and smoke-passes
- Codex dual-route contracts exist and are RED until later plans
- switch/cross-slot contracts named honestly per 04-REVIEWS
- No shell --lib usage
</verification>

<success_criteria>
- Wave 0 harness complete per VALIDATION.md
- D-12 test surface started; D-13 boundary respected (no OAuth/gate code)
</success_criteria>

<output>
Create `.planning/phases/04-provider-aware-request-routing/04-01-SUMMARY.md` when done
</output>
