---
phase: 04-provider-aware-request-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "Wave 0 integration binary tests/provider_routing.rs compiles and --list discovers smoke + dual-route contracts"
    - "Behavior-RED contracts encode MOD-04/MOD-05: xAI → proxy/xAI path + xAI fake token; Codex → ChatGPT backend base + Codex fake token; never cli-chat-proxy for GPT"
    - "Contracts assert model override base_url wins, no X-XAI proxy headers on Codex base, never cross-slot tokens, switch changes next sample route"
    - "Phase 4 proofs run via cargo test --test provider_routing only (not shell --lib)"
    - "Automated cargo gates never pipe through bare tail without pipefail; one TESTNAME filter per cargo invocation"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Wave 0 integration harness for pure route + credential + header + switch contracts on public APIs"
      contains: provider_routing_harness_smoke
  key_links:
    - from: tests/provider_routing.rs
      to: resolve_model_list / resolve_credentials / sampling_config_for_model / inject_url_derived_headers
      via: "integration tests import only public xai_grok_shell::agent::config APIs (and later pub resolve_provider_route)"
      pattern: "resolve_credentials|resolve_model_list|inject_url_derived_headers|gpt-5.6-sol|grok-build"
    - from: RED assertions
      to: Plans 02–04 GREEN
      via: "catalog Codex base_url, pure route, dual credentials, switch sample config"
      pattern: "codex_route|xai_route|never_cross_slot|switch_changes"
---

## Phase Goal

**As a** bum user, **I want** selecting a model to route each turn to the correct backend with that provider’s credentials, **so that** Grok uses xAI/cli-chat-proxy and GPT uses the Codex/ChatGPT backend without cross-slot tokens.

<objective>
Create the Wave 0 integration harness `tests/provider_routing.rs` with a compiling smoke test plus behavior-RED contracts for MOD-04/MOD-05 and success criteria 1–3 (per D-12, D-13). No production routing code in this plan — only tests that fail assertions (or compile only against APIs that already exist today). Later plans turn RED → GREEN without repairing shell `--lib`.

Purpose: Nyquist Wave 0 + locked verification path (fake tokens; no live ChatGPT; no Phase 5/6 UX).
Output: `cargo test -p xai-grok-shell --test provider_routing -- --list` succeeds; smoke green; dual-route / cross-slot / header / switch contracts present and RED where production still stamps Codex onto cli-chat-proxy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
@.planning/phases/04-provider-aware-request-routing/04-RESEARCH.md
@.planning/phases/04-provider-aware-request-routing/04-PATTERNS.md
@.planning/phases/04-provider-aware-request-routing/04-VALIDATION.md
@crates/codegen/xai-grok-shell/tests/model_catalog.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | Pure resolver: model → catalog provider → `(base_url, credential_slot, api_backend)` |
| D-02 | Catalog `provider` is authority (`xai` \| `codex`; missing → `xai`) |
| D-03 | Rebuild SamplingConfig on **next sample** after model switch |
| D-04 | Explicit per-model `base_url` / `api_key` overrides win over provider defaults |
| D-05 | Grok/xAI stays on existing cli-chat-proxy / xAI path |
| D-06 | GPT/Codex → ChatGPT/Codex backend base URL — **not** cli-chat-proxy |
| D-07 | Codex base URL: built-in default + env override (`GROK_CODEX_BASE_URL`) |
| D-08 | Reuse OpenAI-compatible sampler path; no parallel client |
| D-09 | Credential slot from model provider; never cross-slot |
| D-10 | Prefer live bearer resolve per request |
| D-11 | Missing Codex: fake/fixture tokens for tests; no polished login gate (Phase 6) |
| D-12 | Automated tests with fake tokens prove base_url + credential slot differ by provider |
| D-13 | Do **not** implement Codex OAuth UX (Phase 5) or missing-provider switch gate UX (Phase 6) |
| D-14 | Observability: structured fields; never log raw secrets |
| D-15 | xAI API-key fallback only under xAI; Codex path is ChatGPT OAuth primary (not Platform API) |

**This plan covers:** D-12 (harness existence), D-13 (no OAuth/gate UX), partial D-04–D-09/D-11 as RED assertions only.

## Wave 0 harness strategy (mandatory)

- Create `crates/codegen/xai-grok-shell/tests/provider_routing.rs` only.
- **Never** use `cargo test -p xai-grok-shell --lib` as a Phase 4 gate (lib suite broken; do not repair).
- Prefer public APIs that exist **today**: `Config`, `EndpointsConfig`, `resolve_model_list`, `ModelEntry`, `ModelInfo`, `ModelProvider`, `resolve_credentials`, `ResolvedCredentials`, `sampling_config_for_model`, `inject_url_derived_headers`.
- Do **not** import private `PROVIDER_XAI` / `PROVIDER_CODEX` — assert wire strings `"xai"` / `"codex"` via `ModelProvider::as_str()`.
- Do **not** implement production resolver/endpoints/prepare changes here.
- Fake tokens only: e.g. `"xai-fake-token"`, `"codex-fake-token"` — no live ChatGPT.

## Cargo verify hygiene (mandatory)

- One positional TESTNAME filter per `cargo test` invocation, or run the full binary without a filter.
- Chain independent cargo commands with `&&` only.
- Never pipe cargo through bare `| tail` without `set -o pipefail`. Prefer no pipe.

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| Wave 0 harness | `tests/provider_routing.rs` | integration binary |
| Dual-route RED contracts | same | MOD-04/05 + SC-1..3 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 compiling smoke harness + behavior-RED dual-route contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - .planning/phases/04-provider-aware-request-routing/04-CONTEXT.md (verification + deferred)
    - .planning/phases/04-provider-aware-request-routing/04-RESEARCH.md (Validation Architecture, test sketch, pitfalls)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (tests/provider_routing section)
    - .planning/phases/04-provider-aware-request-routing/04-VALIDATION.md
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs (integration import style)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_credentials` ~4376, `sampling_config_for_model` ~4689, `inject_url_derived_headers` ~4753, `resolve_model_list`, `ModelProvider`)
  </read_first>
  <behavior>
    - provider_routing_harness_smoke: resolve_model_list(Config::default(), None) non-empty — PASSES on current main (proves --list)
    - xai_model_routes_to_proxy_with_xai_token: for grok-build (or first provider=xai entry), resolve_credentials with session key "xai-fake-token" yields base_url equal to endpoints proxy / resolve_inference_base_url and api_key Some("xai-fake-token"); provider as_str "xai" — may PASS today for xAI rows
    - codex_model_routes_to_codex_backend_with_codex_token: for gpt-5.6-sol, resolved base_url must equal EndpointsConfig default Codex base (https://chatgpt.com/backend-api/codex after Plan 02) and must NOT contain "cli-chat-proxy"; api_key from Codex fake only; provider "codex" — behavior RED until Plans 02–03 (today codex rows still stamped with proxy)
    - model_override_base_url_wins: ModelEntry with explicit info.base_url "https://byok.example/v1" and provider Codex; resolve path keeps that base_url when own/session path uses override — RED until resolve_provider_route / credentials honor override (D-04)
    - no_proxy_headers_on_codex: inject_url_derived_headers on Codex default base must not insert X-XAI-Token-Auth — should PASS today if Codex host is not classified as cli-chat-proxy (encode as lock)
    - never_cross_slot: given both fake tokens available, Codex model must not receive xai-fake-token as session key and xAI model must not receive codex-fake-token — encode via public resolve helper once Plan 03 lands; until then assert with dual-key helper signature documented as TODO only if the helper is not public yet — prefer writing the test against resolve_credentials(model, Some(slot_key)) with the correct slot key only and a negative: Codex entry with xAI session key still would be wrong product behavior after Plan 03 should not apply xAI key when a provider-aware API is used. Minimal RED for this task: document never_cross_slot that will call public resolve_credentials_for_provider (or resolve_provider_route + resolve_credentials) once exported — if compile would fail, keep a pure catalog assertion that gpt-5.6-sol has provider codex and a comment that full never_cross_slot lands in Plan 03 Task 1 extension. Prefer: write never_cross_slot using only resolve_credentials with the WRONG key and assert current broken behavior is NOT the final contract — actually for RED, after Plan 03, passing xai key to a codex-only resolver must not set api_key to xai. For Task 1, implement never_cross_slot as: sampling_config_for_model / resolve_credentials for gpt-5.6-sol with session "xai-fake-token" must NOT be accepted as the Phase 4 success path — assert that after full phase, a dedicated dual-key API rejects cross application. Practical Task 1 approach matching Phase 3: write tests that compile today on public APIs:
      1) catalog codex entry base_url must not contain cli-chat-proxy (RED)
      2) catalog xai entry base_url equals proxy path (likely green)
      3) inject headers on chatgpt.com base has no X-XAI-Token-Auth (green lock)
      4) resolve_credentials(codex_entry, Some("codex-fake")) api_key is codex-fake and base_url will match catalog base (RED until stamp)
    - switch_changes_next_sample_route: build two SamplerConfigs via sampling_config_for_model for grok-build with xai-fake vs gpt-5.6-sol with codex-fake; assert base_url differs and api_key differs — RED until codex base stamped (D-03/D-12)
  </behavior>
  <action>
    Wave 0 compiling harness + behavior-RED dual-route contracts only (D-12, D-13, RESEARCH Validation Architecture). No production code.

    1. Create `crates/codegen/xai-grok-shell/tests/provider_routing.rs` as a Cargo integration test binary (auto-discovered under `tests/`).
    2. Module docs: Phase 4 provider routing; public APIs only; no shell --lib; no live ChatGPT; no OAuth UX (D-13).
    3. Import only APIs available on main today: `Config`, `EndpointsConfig`, `resolve_model_list`, `resolve_credentials`, `sampling_config_for_model`, `inject_url_derived_headers`, `ModelEntry`, `ModelProvider`, `ResolvedCredentials` as needed. Mirror `model_catalog.rs` style (plain `#[test]`).
    4. Implement `provider_routing_harness_smoke` so the binary **compiles and lists**. After this task, `cargo test -p xai-grok-shell --test provider_routing -- --list` must exit 0.
    5. Implement catalog-level RED: load `resolve_model_list(&Config::default(), None)`; for `gpt-5.6-sol` assert `info.provider.as_str() == "codex"` and `info.base_url` equals the locked Codex default `https://chatgpt.com/backend-api/codex` (or does not contain `cli-chat-proxy`). This is behavior-RED until Plan 03 stamps defaults (D-02, D-06).
    6. Implement `xai_model_routes_to_proxy_with_xai_token`: take `grok-build` entry; `resolve_credentials(entry, Some("xai-fake-token"))`; assert key and base_url match xAI/proxy inference path (D-05, MOD-04).
    7. Implement `codex_model_routes_to_codex_backend_with_codex_token`: take `gpt-5.6-sol`; credentials with `"codex-fake-token"`; assert base is Codex backend not proxy; key is codex fake (D-06, D-09, MOD-05) — RED until stamping.
    8. Implement `switch_changes_next_sample_route`: `sampling_config_for_model` for xAI vs Codex entries with respective fake keys; assert `base_url` and `api_key` differ between the two configs (D-03, SC-3) — RED until Codex base correct.
    9. Implement `no_proxy_headers_on_codex`: call `inject_url_derived_headers` with base `https://chatgpt.com/backend-api/codex`; assert map has no `X-XAI-Token-Auth` key (Pitfall 4).
    10. Implement `model_override_base_url_wins`: clone/build a Codex entry via public fields with `info.base_url = "https://byok.example/v1"`; resolve_credentials with a session key still keeps that base_url (D-04). Use `ModelEntry::fallback` + field mutation if needed.
    11. Do **not** reference unimplemented `resolve_provider_route` / `CODEX_BASE_URL_DEFAULT` if that would cause compile failure — use string literal for expected Codex URL matching RESEARCH discretion. When Plan 02 exports symbols, Plan 02 Task 2 will update tests to call them.
    12. Do not implement production routing, OAuth, or Phase 6 gate. Do not fix shell --lib.
  </action>
  <acceptance_criteria>
    - `tests/provider_routing.rs` exists
    - `cargo test -p xai-grok-shell --test provider_routing -- --list` exits 0 and discovers provider_routing_harness_smoke plus dual-route test names
    - provider_routing_harness_smoke PASSES
    - At least codex_model_routes_to_codex_backend_with_codex_token and/or catalog Codex base_url assertion FAIL with assertion messages (behavior RED), not compile errors
    - No production file changes outside the new test file
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing -- --list && cargo test -p xai-grok-shell --test provider_routing provider_routing_harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    Wave 0 integration binary compiles, lists, and holds behavior-RED dual-route contracts for Plans 02–04 to turn green. Observing RED on Codex base_url before Plan 03 is preferred; automated gate for this task is --list + smoke.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand RED suite for override, cross-slot intent, and dual sampling_config switch</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (Task 1)
    - .planning/phases/04-provider-aware-request-routing/04-RESEARCH.md (prescriptive test sketch)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (sampling_config_for_model, inject_url_derived_headers)
  </read_first>
  <behavior>
    - never_cross_slot_tokens_intent: after dual-key public API exists (Plan 03), Codex model + both keys yields only codex key; xAI model + both keys yields only xAI key. If dual-key API not public yet, encode as two-step: (a) resolve_credentials(codex, Some("codex-fake")) key is codex-fake; (b) resolve_credentials(xai, Some("xai-fake")) key is xai-fake; plus catalog provider authority assert. Add a named test `never_cross_slot` that documents the full dual-key contract and will be completed in Plan 03 — for this task ensure the test body either is #[ignore] forbidden OR uses only existing APIs to assert providers differ and keys are independent when resolved separately (D-09)
    - xai_proxy_headers_still_apply: inject_url_derived_headers on CLI_CHAT_PROXY default URL still inserts X-XAI-Token-Auth (regression lock for MOD-04)
    - sampling_config_api_backend_from_model: gpt-5.6-sol sampling config preserves model-entry api_backend (responses) — D-08 model-entry driven backend
  </behavior>
  <action>
    Extend the harness only (D-08, D-09, D-12). Still no production code.

    1. Add `xai_proxy_headers_still_apply` using `CLI_CHAT_PROXY_BASE_URL_DEFAULT` or `Config::default().endpoints.proxy_url()` so xAI path keeps proxy headers (MOD-04 regression).
    2. Add `sampling_config_api_backend_from_model` for gpt-5.6-sol: after `sampling_config_for_model` with empty/fake credentials, `api_backend` matches catalog entry (Responses) — D-08 no parallel client / no inventing backend table.
    3. Flesh `never_cross_slot` with independent per-provider resolve_credentials calls and provider as_str asserts; leave a clear assert comment that Plan 03 dual-key helper must keep this green without applying the wrong slot.
    4. Ensure all new tests are discoverable via `--list`. Prefer behavior-RED only where product is still wrong; green locks for already-correct header classification.
    5. Do not implement production code. Do not add shell --lib gates.
  </action>
  <acceptance_criteria>
    - Additional tests present: never_cross_slot (or never_cross_slot_tokens_intent), xai_proxy_headers_still_apply, sampling_config_api_backend_from_model
    - Binary still compiles and lists; smoke still green
    - No ignored tests for core MOD-04/05 contracts
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing -- --list && cargo test -p xai-grok-shell --test provider_routing xai_proxy_headers_still_apply -- --nocapture</automated>
  </verify>
  <done>
    Full RED/lock suite for Phase 4 routing is encoded in provider_routing.rs and ready for Plans 02–04 implementation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog model id → provider route | Untrusted only in the sense of config/catalog data selects credential slot |
| Test fixtures → process | Fake tokens must not be shipped as production defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tests/provider_routing.rs | low | accept | Fake tokens only (`xai-fake-token` / `codex-fake-token`); no real secrets in suite |
| T-04-02 | Elevation of Privilege | dual-route assertions | high | mitigate | never_cross_slot + dual base_url asserts encode slot isolation for later GREEN |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- Integration binary lists and smoke-passes
- Codex dual-route contracts exist and are RED until later plans
- No shell --lib usage
</verification>

<success_criteria>
- Wave 0 harness complete per VALIDATION.md
- D-12 test surface started; D-13 boundary respected (no OAuth/gate code)
</success_criteria>

<output>
Create `.planning/phases/04-provider-aware-request-routing/04-01-SUMMARY.md` when done
</output>
