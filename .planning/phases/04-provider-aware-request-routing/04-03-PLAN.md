---
phase: 04-provider-aware-request-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "default_models() stamps provider=xai rows with resolve_inference_base_url + xai_api_base_url; provider=codex rows with resolve_codex_base_url and api_base_url None"
    - "resolve_model_list no-prefetch gpt-5.6-* entries have base_url = Codex backend (not cli-chat-proxy)"
    - "Provider-aware credential resolve: session key is slot-selected; XAI_API_KEY fallback never applies to ModelProvider::Codex"
    - "Explicit model own_credential / base_url override still wins over provider defaults"
    - "Dual-route MOD-04/MOD-05 tests green under --test provider_routing (catalog + resolve_credentials + sampling_config_for_model)"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "provider-aware default_models stamping + resolve_credentials_for_provider (or extended resolve_credentials)"
      contains: resolve_codex_base_url
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Green codex_model_routes + xai_model_routes + override + never_cross_slot"
      contains: codex_model_routes_to_codex_backend
  key_links:
    - from: default_models()
      to: ModelEntry.info.base_url
      via: "branch on m.provider when building ModelEntryConfig"
      pattern: "ModelProvider::Codex|resolve_codex_base_url|api_base_url"
    - from: resolve_credentials*
      to: ProviderRoute / slot keys
      via: "session arm uses provider-selected key; Codex skips XAI_API_KEY"
      pattern: "resolve_credentials|ModelProvider::Codex|session_key"
---

## Phase Goal

**As a** bum user looking at the default catalog, **I want** GPT models to already carry the Codex backend base URL and accept only Codex credentials, **so that** sampling config built from catalog entries is provider-correct before session wiring.

<objective>
Stamp bundled catalog URLs by `ModelProvider` and make credential resolution provider-aware (per D-02, D-04, D-05, D-06, D-09, D-11, D-15). Turn Plan 01 RED catalog/credential tests GREEN via public `resolve_model_list` + `resolve_credentials` / sibling. Update any dual-endpoint expectations that assumed every row has `api_base_url` → proxy. Do **not** yet change `prepare_sampling_config_for_model`, ModelsManager, or reconstruct BearerResolver (Plan 04).

Purpose: Catalog + credential pure path delivers MOD-04/MOD-05 observable truths for next-sample config builders.
Output: Green dual-route tests; Codex rows no longer point at cli-chat-proxy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
@.planning/phases/04-provider-aware-request-routing/04-RESEARCH.md
@.planning/phases/04-provider-aware-request-routing/04-PATTERNS.md
@.planning/phases/04-provider-aware-request-routing/04-02-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
</context>

## Locked decisions covered

D-02, D-04, D-05, D-06, D-09, D-11, D-12, D-15. Explicitly **not** D-03 session reconstruct (Plan 04), not D-13 OAuth/gate.

**Pitfall 1 (RESEARCH):** `default_models()` currently sets every entry to `resolve_inference_base_url()` + `api_base_url: Some(xai_api_base_url)` — must branch on provider.

**Lib-test note:** `default_models_dual_endpoint_routing` in `config.rs` `#[cfg(test)]` is **not** a Phase 4 gate (shell --lib broken). Port provider-aware dual-endpoint assertions into `tests/provider_routing.rs`. Optionally update the colocated unit test for future lib repair — not required for phase green.

## Cargo verify hygiene (mandatory)

- One TESTNAME filter per cargo or full binary. No bare `| tail`. No shell `--lib`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider-aware default_models URL stamping</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`default_models` ~3432–3495, ModelProvider, resolve_codex_base_url from Plan 02)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (default_models stamping)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (RED catalog Codex base asserts)
  </read_first>
  <behavior>
    - catalog_codex_rows_use_codex_base: resolve_model_list default → gpt-5.6-sol/terra/luna info.base_url == endpoints.resolve_codex_base_url() and does not contain cli-chat-proxy
    - catalog_xai_rows_use_proxy_or_inference: grok-build base_url == resolve_inference_base_url() / proxy path
    - catalog_codex_api_base_url_none: Codex entries have api_base_url None (no xAI Platform fallback for product path D-15)
    - catalog_xai_keeps_api_base_url: xAI entries still have api_base_url Some(xai_api_base_url) for API-key dual endpoint
  </behavior>
  <action>
    Fix Pitfall 1 / D-02 / D-05 / D-06 stamping only.

    1. In `default_models()`, when building `ModelEntryConfig`, replace unconditional proxy/`xai_api_base_url` with:
       - `ModelProvider::Xai` → `base_url: endpoints.resolve_inference_base_url()`, `api_base_url: Some(endpoints.xai_api_base_url.clone())`
       - `ModelProvider::Codex` → `base_url: endpoints.resolve_codex_base_url()`, `api_base_url: None`
    2. Preserve all other fields (provider, api_backend, agent_type, etc.) unchanged.
    3. If JSON ever embeds an explicit base_url field later, that is out of scope — today JSON has no base_url; code stamps all.
    4. Update `tests/provider_routing.rs` catalog asserts to GREEN. Add dual-endpoint regression test names above.
    5. If colocated `default_models_dual_endpoint_routing` still loops all entries with `api_base_url.is_some()`, optionally gate that unit test by provider so a future --lib repair is not wrong — **do not** rely on --lib for verify.
    6. Do not change resolve_credentials signature behavior for xAI-only callers yet if Task 2 owns that — but stamping alone should flip catalog base_url RED → GREEN for Codex rows when using session key with resolve_credentials (base_url comes from info.base_url).
  </action>
  <acceptance_criteria>
    - default_models stamps Codex vs xAI differently as specified
    - cargo test -p xai-grok-shell --test provider_routing catalog_codex (or codex_model_routes) green for base_url
    - Grok rows still on inference/proxy path
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing codex_model_routes -- --nocapture && cargo test -p xai-grok-shell --test provider_routing xai_model_routes -- --nocapture</automated>
  </verify>
  <done>
    Bundled catalog entries carry provider-correct base URLs; GPT no longer inherits cli-chat-proxy by default.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider-aware resolve_credentials + never cross-slot</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_credentials` ~4372–4421, `ResolvedCredentials`, `own_credential`, Plan 02 `resolve_provider_route`)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (resolve_credentials provider-aware)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
  </read_first>
  <behavior>
    - never_cross_slot: public dual-key API with both xai-fake and codex-fake → Codex model api_key is codex-fake only; xAI model api_key is xai-fake only
    - codex_skips_xai_api_key_env_fallback: with no session and no own credential, Codex model must not pick up XAI_API_KEY env (D-15) — use serial env guard or document that Codex falls through to empty key + codex base_url
    - model_override_base_url_wins: explicit entry base_url preserved (D-04)
    - own_credential_wins: model api_key/env_key still beats session for both providers
    - empty_codex_key_allows_route_construction: resolve returns base_url = Codex route with api_key None (D-11 fixture inject at higher layer; no panic)
  </behavior>
  <action>
    Make credentials provider-aware (D-04, D-09, D-11, D-15, RESEARCH Pattern 2).

    1. Prefer adding a public sibling (keeps existing call sites compiling):
       `pub fn resolve_credentials_for_provider(model: &ModelEntry, xai_session_key: Option<&str>, codex_session_key: Option<&str>) -> ResolvedCredentials`
       Priority:
       a. `model.own_credential()` → key + `info.base_url` (BYOK) + ApiKey auth_type
       b. Else provider-selected session key: if `info.provider == Codex` use codex_session_key; if Xai use xai_session_key. When Some, SessionToken + `info.base_url` (already provider-stamped)
       c. Else if provider is **Xai only**: existing `XAI_API_KEY` / `api_base_url` fallback path
       d. Else (Codex, no key): `api_key: None`, `base_url: info.base_url` (or `resolve_provider_route` if base empty)
    2. Keep `pub fn resolve_credentials(model, session_key)` as a thin wrapper that passes `session_key` as the xAI slot only (backward compatible for current xAI-only callers) **or** document that it treats session_key as the key for `model.info.provider` only (single key for that model’s slot). **Preferred for Plan 04 simplicity:** wrapper maps `session_key` into the correct slot arg based on `model.info.provider` and None for the other — so existing `resolve_credentials(model, Some(xai))` on a Codex model would incorrectly pass xAI key into Codex slot if called that way. **Correct product behavior:** `resolve_credentials(model, session_key)` must mean “session key for **this model’s provider**” only — callers that always pass xAI key must be fixed in Plan 04 prepare path to pass None for Codex models. Implement:
       - `resolve_credentials(model, session_key)` = session_key applies **only if** it is intended for this model’s provider (document: callers supply slot-correct key). Do **not** apply XAI_API_KEY fallback when provider is Codex.
    3. Add `resolve_credentials_for_provider` for tests and prepare path that has both keys available.
    4. Extend debug log fields: `provider = %info.provider.as_str()`, `base_url = %base_url` — never log full api_key (D-14).
    5. Ensure `enforce_disable_api_key_auth` still only swaps first-party **xAI** URLs (verify ChatGPT Codex base is not `is_first_party_xai_url`).
    6. Update `tests/provider_routing.rs`: never_cross_slot GREEN using `resolve_credentials_for_provider`; codex env fallback; override wins; empty key route construction.
    7. Do not implement prepare_sampling or BearerResolver here (Plan 04). No OAuth UX (D-13).
  </action>
  <acceptance_criteria>
    - resolve_credentials_for_provider (or equivalent public dual-key API) exists
    - Codex never receives XAI_API_KEY fallback
    - never_cross_slot and dual-route credential tests green
    - Existing xAI BYOK/session priority preserved for provider=xai
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing never_cross_slot -- --nocapture && cargo test -p xai-grok-shell --test provider_routing model_override_base_url_wins -- --nocapture && cargo check -p xai-grok-shell</automated>
  </verify>
  <done>
    Credential resolve is provider-slot-correct with catalog bases; MOD-04/05 pure path is green without session reconstruct yet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session key input → Authorization material | Caller must supply slot-correct key; dual-key API prevents accidental mix when both present |
| XAI_API_KEY env → Codex models | Must not auto-apply (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Elevation of Privilege | resolve_credentials* | high | mitigate | never_cross_slot tests; Codex skips XAI_API_KEY; dual-key selector by ModelProvider |
| T-04-06 | Information Disclosure | resolve_credentials debug log | medium | mitigate | Log provider + base_url + auth_type only; never raw bearer (D-14) |
| T-04-07 | Spoofing | empty Codex key | low | mitigate | Allow route construction for tests (D-11); live fail-closed deferred to Plan 05 / existing sample errors |
| T-04-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Full provider_routing dual-route + cross-slot suite green for catalog/credentials; prepare path still Plan 04.
</verification>

<success_criteria>
MOD-04/MOD-05 true for resolve_model_list + resolve_credentials + sampling_config_for_model with fake tokens.
</success_criteria>

<output>
Create `.planning/phases/04-provider-aware-request-routing/04-03-SUMMARY.md` when done
</output>
