---
phase: 04-provider-aware-request-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "default_models() stamps provider-default base_url via resolve_provider_route (not a parallel if/else table)"
    - "provider=xai rows get inference/proxy base + xai api_base_url; provider=codex rows get resolve_codex_base_url and api_base_url None"
    - "ConfigModelOverride that changes provider without explicit base_url re-normalizes base_url via resolve_provider_route"
    - "resolve_credentials_for_provider dual-key API is the safe production credential path; never_cross_slot GREEN with both tokens present"
    - "Session OAuth keys applied only when ProviderRoute.session_oauth_allowed (or equivalent); custom host requires own credential"
    - "XAI_API_KEY fallback never applies to ModelProvider::Codex"
    - "Every resolve_credentials call site inventoried: converted or explicitly deferred with rationale"
    - "Dual-route MOD-04/MOD-05 tests green under --test provider_routing"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "provider-aware default_models stamping via resolve_provider_route + resolve_credentials_for_provider + override rebind"
      contains: resolve_credentials_for_provider
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Green codex_model_routes + xai_model_routes + override + never_cross_slot dual-token + rebind"
      contains: never_cross_slot
  key_links:
    - from: default_models()
      to: resolve_provider_route
      via: "stamp base_url from ProviderRoute for each bundled provider"
      pattern: "resolve_provider_route|ModelProvider::Codex|resolve_codex_base_url"
    - from: ConfigModelOverride::apply
      to: resolve_provider_route
      via: "provider rebind without base_url override re-normalizes base"
      pattern: "provider|resolve_provider_route|base_url"
    - from: resolve_credentials_for_provider
      to: session_oauth_allowed + dual keys
      via: "slot select by provider; skip session oauth on custom host"
      pattern: "session_oauth_allowed|xai_session_key|codex_session_key"
---

## Phase Goal

**As a** bum user looking at the default catalog, **I want** GPT models to already carry the Codex backend base URL and accept only Codex credentials, **so that** sampling config built from catalog entries is provider-correct before session wiring.

<objective>
Stamp bundled catalog URLs by `ModelProvider` **via resolve_provider_route** and make credential resolution provider-aware with dual-key safety (per D-02, D-04, D-05, D-06, D-09, D-11, D-15). Turn Plan 01 RED catalog/credential tests GREEN. Inventory all `resolve_credentials` callers. Do **not** yet change `prepare_sampling_config_for_model` / ModelsManager / reconstruct (Plan 04) except as forced by public API signature if needed.

Purpose: Catalog + credential pure path delivers MOD-04/MOD-05 truths and eliminates dual-implementation drift.
Output: Green dual-route + dual-token never_cross_slot; Codex rows no longer point at cli-chat-proxy; caller inventory recorded in SUMMARY.

Addresses review concern (HIGH): single-key API unsafe; provider rebind must re-normalize base; production must call resolve_provider_route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
@.planning/phases/04-provider-aware-request-routing/04-RESEARCH.md
@.planning/phases/04-provider-aware-request-routing/04-PATTERNS.md
@.planning/phases/04-provider-aware-request-routing/04-REVIEWS.md
@.planning/phases/04-provider-aware-request-routing/04-02-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/agent/models.rs
@crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
</context>

## Locked decisions covered

D-02, D-04, D-05, D-06, D-09, D-11, D-12, D-15. Explicitly **not** D-03 session reconstruct (Plan 04), not D-13 OAuth/gate.

**Pitfall 1 (RESEARCH):** `default_models()` currently sets every entry to `resolve_inference_base_url()` + `api_base_url: Some(xai_api_base_url)` — must branch via `resolve_provider_route`.

## resolve_credentials call-site inventory (review HIGH — mandatory)

Executor MUST inventory and record disposition in SUMMARY for every production (non-test) call site of `resolve_credentials` / siblings. Known sites at plan time:

| Call site | File (approx) | Phase 4 disposition |
|-----------|---------------|---------------------|
| `ModelsManager::sampling_config` | `agent/models.rs` ~951 | **Convert in Plan 04** to dual-key / provider-aware |
| `prepare_sampling_config_for_model` | `mvp_agent/agent_ops.rs` ~1096 | **Convert in Plan 04** |
| `try_resolve_model_credentials` | `config.rs` ~4476 | **Convert this plan** to dual-key or provider-slot-correct single key |
| `resolve_model_to_sampling_config` | `config.rs` ~4782 | **Convert this plan** |
| Internal helpers near resolve_credentials | `config.rs` ~4455, ~7322 | **Convert this plan** with public dual-key |
| Subagent spawn path | `agent/subagent/mod.rs` ~1019 | **Defer to Phase 7** with explicit comment + leave xAI-safe behavior if only xAI keys available; do not cross-apply xAI key to Codex models if subagent model is Codex (minimal fail-safe: use provider-aware resolve with codex key None) — **minimum convert this plan**: call provider-aware resolve so Codex models do not get XAI_API_KEY fallback |
| Tests / web_search e2e | various | Update if they break; not phase gate |

Single-key `resolve_credentials(model, session_key)` semantics after this plan:

- Means: `session_key` is the key for **this model’s provider slot only** (not “always xAI”).
- Does **not** type-check provenance; production dual-slot builders **must** call `resolve_credentials_for_provider`.
- Prefer `#[deprecated]` rustdoc on single-key for dual-slot contexts: “prefer resolve_credentials_for_provider when both slots available”.
- Codex path never applies `XAI_API_KEY` env fallback.

## Cargo verify hygiene (mandatory)

- One TESTNAME filter per cargo or full binary. No bare `| tail`. No shell `--lib`.
- XAI_API_KEY env tests: `serial_test::serial` + RAII restore (review MEDIUM).

## Review feedback (04-REVIEWS / Codex cycle 1)

| Concern | Sev | Disposition |
|---------|-----|-------------|
| Single-key wrapper unsafe | HIGH | **Addressed:** dual-key public API; single-key documented; callers inventoried convert/defer |
| Provider rebind leaves old base_url | HIGH | **Addressed:** ConfigModelOverride apply re-normalizes via resolve_provider_route |
| Call sites outside named builders | MEDIUM | **Addressed:** inventory table + convert/defer this plan |
| XAI_API_KEY env test isolation | MEDIUM | **Addressed:** serial + EnvGuard required |
| Colocated unit test api_base_url assumption | MEDIUM | **Addressed:** update dual-endpoint unit test by provider if present (not --lib gate) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider-aware default_models stamping + override rebind via resolve_provider_route</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`default_models` ~3432–3495, `ConfigModelOverride::apply` ~3680–3720, ModelProvider, resolve_provider_route from Plan 02)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (default_models stamping)
    - .planning/phases/04-provider-aware-request-routing/04-REVIEWS.md (production authority + rebind)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (RED catalog Codex base asserts)
  </read_first>
  <behavior>
    - catalog_codex_rows_use_codex_base: resolve_model_list default → gpt-5.6-sol/terra/luna info.base_url == endpoints.resolve_codex_base_url() and does not contain cli-chat-proxy
    - catalog_xai_rows_use_proxy_or_inference: grok-build base_url == resolve_inference_base_url() / proxy path
    - catalog_codex_api_base_url_none: Codex entries have api_base_url None (D-15)
    - catalog_xai_keeps_api_base_url: xAI entries still have api_base_url Some(xai_api_base_url)
    - provider_override_rebinds_base_url: ConfigModelOverride setting provider=codex without base_url on an xAI-stamped entry yields Codex default base after apply (addresses review HIGH rebind)
    - provider_override_explicit_base_preserved: override with provider + explicit base_url keeps explicit base
  </behavior>
  <action>
    Fix Pitfall 1 / D-02 / D-05 / D-06 using **resolve_provider_route** as production authority (review HIGH).

    1. In `default_models()`, when building `ModelEntryConfig` / `ModelEntry`:
       - Call `resolve_provider_route(m.provider, endpoints, None)` for provider-default base_url.
       - `ModelProvider::Xai` → `api_base_url: Some(endpoints.xai_api_base_url.clone())`
       - `ModelProvider::Codex` → `api_base_url: None`
       - Do **not** hardcode parallel base strings outside the resolver.
    2. Preserve all other fields (provider, api_backend, agent_type, etc.) unchanged.
    3. In `ConfigModelOverride::apply` (or immediately after provider is applied in the model merge path):
       - If override sets `provider` and does **not** set an explicit `base_url`, re-normalize `entry.info.base_url` (and Codex `api_base_url: None` / xAI api_base_url restore as appropriate) via `resolve_provider_route(new_provider, endpoints, None)`.
       - If override sets explicit `base_url`, keep it (D-04); session_oauth_allowed will gate OAuth later.
    4. Update `tests/provider_routing.rs` catalog asserts to GREEN. Add rebind tests above.
    5. Optionally fix colocated `default_models_dual_endpoint_routing` unit test expectations by provider — not a --lib gate but prevent future wrongness (review MEDIUM).
    6. Do not change dual-key credentials yet if Task 2 owns that — stamping alone should flip catalog base_url RED → GREEN.
  </action>
  <acceptance_criteria>
    - default_models stamps via resolve_provider_route
    - provider-only override re-normalizes base_url
    - cargo test provider_routing codex_model_routes and xai_model_routes green for base_url
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing codex_model_routes -- --nocapture && cargo test -p xai-grok-shell --test provider_routing xai_model_routes -- --nocapture</automated>
  </verify>
  <done>
    Bundled catalog + provider rebind carry provider-correct base URLs via the pure resolver.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-key resolve_credentials_for_provider + never_cross_slot GREEN + caller inventory</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_credentials` ~4372–4421, `ResolvedCredentials`, `own_credential`, `try_resolve_model_credentials`, `resolve_model_to_sampling_config`, Plan 02 `resolve_provider_route`)
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs (~1019 resolve_credentials)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (resolve_credentials provider-aware)
    - .planning/phases/04-provider-aware-request-routing/04-REVIEWS.md (single-key unsafe + inventory)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
  </read_first>
  <behavior>
    - never_cross_slot: resolve_credentials_for_provider with BOTH xai-fake and codex-fake → Codex model api_key is only codex-fake; xAI model api_key is only xai-fake
    - custom_host_skips_session_oauth: Codex model with base_url https://byok.example/v1 and dual session keys → api_key None unless own_credential set (session_oauth_allowed false)
    - own_credential_on_custom_host_wins: model own api_key on custom host used
    - codex_skips_xai_api_key_env_fallback: serial env XAI_API_KEY set; Codex model no session/own → api_key None, base still Codex (D-15)
    - model_override_base_url_wins: explicit entry base_url preserved (D-04)
    - empty_codex_key_allows_route_construction: resolve returns base_url = Codex route with api_key None (D-11 construction OK; live fail-closed is Plan 05)
  </behavior>
  <action>
    Make credentials provider-aware (D-04, D-09, D-11, D-15; review HIGH dual-key + custom host).

    1. Add public:
       `pub fn resolve_credentials_for_provider(model: &ModelEntry, xai_session_key: Option<&str>, codex_session_key: Option<&str>) -> ResolvedCredentials`
       Priority:
       a. `model.own_credential()` → key + `info.base_url` + ApiKey auth_type (BYOK / model-owned always allowed)
       b. Else compute route = `resolve_provider_route(model.info.provider, endpoints_from_entry_or_default, Some(model.info.base_url) if distinguishing override — pass override when base is non-default or always pass Some(base) carefully so session_oauth_allowed is correct)`. Practical rule: call `resolve_provider_route(provider, endpoints, model_base_override_opt)` where override_opt is Some when the entry’s base_url was user/override-set; if stamping always sets base, compare to provider default or track session_oauth via `resolve_provider_route(provider, endpoints, Some(base.as_str()))` and treat first-party equality as allowed.
       c. If `session_oauth_allowed` and provider Codex and codex_session_key Some → SessionToken + base
       d. If `session_oauth_allowed` and provider Xai and xai_session_key Some → SessionToken + base
       e. Else if provider Xai only: existing `XAI_API_KEY` / `api_base_url` fallback
       f. Else: api_key None, base_url from entry/route

    2. Single-key `resolve_credentials(model, session_key)`:
       - Map session_key into the correct slot arg for `model.info.provider` and None for the other.
       - Document: caller must supply slot-correct key; dual-slot builders use dual-key API.
       - Never apply XAI_API_KEY when provider is Codex.

    3. Convert in-crate config helpers (`try_resolve_model_credentials`, `resolve_model_to_sampling_config`, internal helpers) to provider-aware path this plan.

    4. Subagent `resolve_credentials` site: minimum convert to provider-aware single-key / dual with None for missing Codex so Codex models cannot pick XAI_API_KEY (Phase 7 owns full dual-slot subagent isolation — note in SUMMARY).

    5. Extend debug log fields: `provider`, `base_url`, `has_api_key` — never full api_key (D-14).

    6. Ensure `enforce_disable_api_key_auth` still only swaps first-party **xAI** URLs.

    7. Update `tests/provider_routing.rs`: never_cross_slot GREEN with both tokens present; custom host OAuth skip; codex env fallback with serial guard; override wins; empty key construction.

    8. Record full call-site inventory dispositions in SUMMARY.md when done.

    9. Do not implement prepare_sampling or BearerResolver here (Plan 04). No OAuth UX (D-13).
  </action>
  <acceptance_criteria>
    - resolve_credentials_for_provider exists and never_cross_slot dual-token GREEN
    - custom host does not receive session OAuth keys
    - Codex never receives XAI_API_KEY fallback
    - Call-site inventory complete in SUMMARY (convert/defer each)
    - Existing xAI BYOK/session priority preserved for provider=xai on first-party bases
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing never_cross_slot -- --nocapture && cargo test -p xai-grok-shell --test provider_routing model_override_base_url_wins -- --nocapture && cargo check -p xai-grok-shell</automated>
  </verify>
  <done>
    Credential resolve is dual-key safe with catalog bases and host OAuth policy; MOD-04/05 pure path green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dual session keys → Authorization material | Dual-key API prevents mix when both present; single-key still requires correct caller |
| XAI_API_KEY env → Codex models | Must not auto-apply (D-15) |
| Session OAuth → custom base_url | Must not attach (session_oauth_allowed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Elevation of Privilege | resolve_credentials* | high | mitigate | dual-key never_cross_slot; Codex skips XAI_API_KEY; inventory converts unsafe callers |
| T-04-06 | Information Disclosure | resolve_credentials debug log | medium | mitigate | Log provider + base_url + has_api_key only; never raw bearer (D-14) |
| T-04-07 | Spoofing | empty Codex key construction | low | mitigate | Allow construction for tests (D-11); live fail-closed Plan 05 |
| T-04-11 | Information Disclosure | custom host + session oauth | high | mitigate | Honor session_oauth_allowed from resolve_provider_route |
| T-04-12 | Tampering | provider rebind without base update | high | mitigate | ConfigModelOverride re-normalizes via resolve_provider_route |
| T-04-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Full provider_routing dual-route + dual-token + rebind suite green for catalog/credentials; prepare path still Plan 04.
</verification>

<success_criteria>
MOD-04/MOD-05 true for resolve_model_list + resolve_credentials_for_provider + sampling_config_for_model with fake tokens; production stamp uses resolve_provider_route.
</success_criteria>

<output>
Create `.planning/phases/04-provider-aware-request-routing/04-03-SUMMARY.md` when done
</output>
