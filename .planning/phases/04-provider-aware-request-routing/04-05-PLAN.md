---
phase: 04-provider-aware-request-routing
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs
  - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "Debug/structured logs for routing include provider and/or credential_slot and base_url without raw secrets"
    - "Live sample path fail-closed when neither real nor fixture credential exists for the active provider (existing sampling/auth error — no new Phase 6 login modal)"
    - "Full provider_routing suite green; model_catalog regression still green; cargo check shell + sampler"
    - "No Codex OAuth UX and no missing-provider switch gate UX shipped (D-13)"
    - "Grok path still receives proxy-derived headers; Codex path never receives X-XAI-Token-Auth"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Phase gate dual-route + observability + fail-closed contracts"
      contains: provider_routing
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "routing debug fields without secrets"
      contains: provider
  key_links:
    - from: resolve_credentials / prepare
      to: tracing debug fields
      via: "provider, base_url, has_api_key — never full bearer"
      pattern: "tracing::debug|provider|base_url"
    - from: empty Codex credentials
      to: sample error path
      via: "fail closed without Phase 6 modal"
      pattern: "api_key|None|SessionToken"
---

## Phase Goal

**As a** bum user (and as an engineer debugging dual-provider routing), **I want** safe observability and fail-closed behavior when credentials are missing, **so that** routing is operable without leaking secrets and without pretending Phase 6 UX exists.

<objective>
Close Phase 4: observability without secrets (D-14), fail-closed live sample when no credential for active provider (D-11 partial — no polished gate), full automated suite green for MOD-04/MOD-05 and SC-1..3 (D-12). Explicitly verify Phase 5/6 work is **not** present (D-13).

Purpose: Phase gate + safety polish after wiring.
Output: Green full `provider_routing` + `model_catalog` regression; documented fail-closed; no secret logging.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
@.planning/phases/04-provider-aware-request-routing/04-RESEARCH.md
@.planning/phases/04-provider-aware-request-routing/04-VALIDATION.md
@.planning/phases/04-provider-aware-request-routing/04-04-PLAN.md
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-sampler/src/client.rs
</context>

## Locked decisions covered

D-11 (fail closed without polished gate), D-12 (full automated proof), D-13 (no OAuth/gate UX), D-14 (observability). Regression locks for D-05/D-06 header behavior.

## Cargo verify hygiene (mandatory)

- Full binary runs OK. One TESTNAME when filtering. No bare `| tail`. No shell `--lib`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Observability fields + fail-closed without Phase 6 UX</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/agent/mvp_agent/agent_ops.rs, crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (resolve_credentials debug log ~4413–4415)
    - crates/codegen/xai-grok-sampler/src/client.rs (client_new / post logs ~507–519, 571–588 — auth prefix only)
    - .planning/phases/04-provider-aware-request-routing/04-CONTEXT.md (observability + missing Codex)
  </read_first>
  <behavior>
    - resolve_credentials_debug_includes_provider: logging path includes provider string (code review + optional test that ResolvedCredentials construction sets expected fields — do not assert on tracing subscriber unless already patterned)
    - empty_codex_credentials_constructs_route_without_panic: resolve with None keys for Codex returns api_key None and codex base_url (D-11)
    - no_phase6_login_gate_symbols: grep/docs — plan action forbids adding missing-provider modal/prompt strings; verify no new UX modules for login gate
    - secret_not_in_debug_format: ensure no format! of full api_key in new code paths (code review acceptance)
  </behavior>
  <action>
    Observability + fail-closed (D-11, D-14, D-13).

    1. Ensure `resolve_credentials` / `resolve_credentials_for_provider` / prepare path debug logs include `provider = %…as_str()`, `base_url = %…`, and has_key boolean or auth_type — **never** the full bearer string (D-14). Match existing sampler prefix style if logging a key at all.

    2. Fail-closed for live sample: do **not** invent a new TUI modal. When Codex sample has no api_key and no bearer_resolver, existing sampler/client will fail the HTTP/auth path — acceptable. Optionally surface a clear tracing warn already present in prepare_sampling when no session key — extend message with provider for Codex without adding Phase 6 copy that prompts login UI.

    3. Add/keep test `empty_codex_credentials_constructs_route_without_panic` for pure resolve.

    4. Explicitly **do not** add: Codex OAuth browser flow, logout/status UX, mid-session missing-provider block, login prompt modal (D-13). If any such code was accidentally introduced in earlier plans, remove it.

    5. Double-check inject_url_derived_headers regression tests still green (xAI proxy headers on; Codex off).

    6. Do not add new external packages.
  </action>
  <acceptance_criteria>
    - Routing logs include provider/base_url without raw secrets
    - Empty Codex key does not panic on route construction
    - No Phase 5/6 UX modules added
    - Header regression tests green
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing empty_codex -- --nocapture && cargo test -p xai-grok-shell --test provider_routing no_proxy_headers_on_codex -- --nocapture && cargo test -p xai-grok-shell --test provider_routing xai_proxy_headers_still_apply -- --nocapture</automated>
  </verify>
  <done>
    Safe observability and fail-closed route construction complete without Phase 6 UX.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full provider_routing + model_catalog regression + cargo check</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - .planning/phases/04-provider-aware-request-routing/04-VALIDATION.md
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
    - .planning/ROADMAP.md Phase 4 success criteria
  </read_first>
  <action>
    Phase gate verification only (D-12, MOD-04, MOD-05, SC-1..3). Fix any remaining RED tests in provider_routing.rs that are still legitimate Phase 4 contracts. Do not expand scope into OAuth or gate UX.

    1. Run full suite commands below; fix failures that are in Phase 4 scope.
    2. Confirm model_catalog still green (no regression from default_models stamping).
    3. Confirm cargo check shell + sampler.
    4. Update test module docs to list contracts covered: xAI route, Codex route, override, switch, never_cross_slot, headers, bearer attach policy, empty key.
    5. Do not use shell --lib. Do not pipe cargo through bare tail.
  </action>
  <acceptance_criteria>
    - Full provider_routing binary green
    - model_catalog binary green
    - cargo check -p xai-grok-shell -p xai-grok-sampler ok
    - ROADMAP success criteria 1–3 satisfied by automated tests with fake tokens
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing -- --nocapture && cargo test -p xai-grok-shell --test model_catalog -- --nocapture && cargo check -p xai-grok-shell -p xai-grok-sampler</automated>
  </verify>
  <done>
    Phase 4 automated gate is green: dual-provider routing proven with fake tokens; catalog regressions clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logs / telemetry | Must not emit raw OAuth or fixture tokens |
| Missing credential → user | Fail closed via existing errors, not a new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information Disclosure | tracing / unified_log | high | mitigate | provider + base_url + has_key only; never raw bearer (D-14) |
| T-04-07 | Denial of Service | empty credentials sample | low | accept | Fail closed is intentional; Phase 6 owns UX recovery |
| T-04-10 | Elevation of Privilege | accidental Phase 5/6 UX skip | medium | mitigate | Explicit D-13 non-implementation check in Task 1 |
| T-04-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Full provider_routing + model_catalog + cargo check green.
</verification>

<success_criteria>
Phase 4 complete: MOD-04, MOD-05, SC-1..3 true under automated fake-token tests; observability safe; no Phase 5/6 scope leak.
</success_criteria>

<output>
Create `.planning/phases/04-provider-aware-request-routing/04-05-SUMMARY.md` when done
</output>
