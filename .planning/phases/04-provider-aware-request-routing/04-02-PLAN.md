---
phase: 04-provider-aware-request-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-04
  - MOD-05
must_haves:
  truths:
    - "CODEX_BASE_URL_DEFAULT is https://chatgpt.com/backend-api/codex and EndpointsConfig exposes codex_base_url + resolve_codex_base_url()"
    - "GROK_CODEX_BASE_URL env overrides the Codex base (mirror GROK_XAI_API_BASE_URL / GROK_CLI_CHAT_PROXY_BASE_URL style)"
    - "Pure resolve_provider_route maps ModelProvider + endpoints + optional model base_url override → base_url + credential_slot + provider"
    - "Catalog provider authority: xai → inference/proxy base + slot xai; codex → resolve_codex_base_url + slot codex; override URL wins for base_url but slot still follows provider"
    - "Pure route tests green under --test provider_routing; no shell --lib"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "CODEX_BASE_URL_DEFAULT, EndpointsConfig.codex_base_url, resolve_codex_base_url, ProviderRoute, resolve_provider_route"
      contains: resolve_provider_route
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Green pure-route + env override tests"
      contains: resolve_provider_route
  key_links:
    - from: ModelProvider
      to: resolve_provider_route
      via: "match provider; never agent_type"
      pattern: "ModelProvider::Codex|resolve_codex_base_url|PROVIDER_"
    - from: EndpointsConfig::default
      to: GROK_CODEX_BASE_URL
      via: "env var fill like xai_api_base_url"
      pattern: "GROK_CODEX_BASE_URL|codex_base_url"
---

## Phase Goal

**As a** bum user, **I want** the product to know the correct backend base URL and credential slot for each model’s catalog provider, **so that** later sampling build paths can stamp GPT onto ChatGPT/Codex and Grok onto xAI without guessing.

<objective>
Implement the pure routing core (per D-01, D-02, D-05, D-06, D-07): `CODEX_BASE_URL_DEFAULT`, `EndpointsConfig` Codex field + `resolve_codex_base_url()`, and public `resolve_provider_route` / `ProviderRoute`. Prove with `--test provider_routing` unit-style tests (env override + pure map). Do **not** yet change `default_models()` stamping, `resolve_credentials`, prepare, or BearerResolver (Plans 03–04).

Purpose: Pure, testable authority for all later wiring; zero new crates; no parallel sampler.
Output: Public pure APIs; pure-route tests green; catalog Codex base_url may still be RED until Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
@.planning/phases/04-provider-aware-request-routing/04-RESEARCH.md
@.planning/phases/04-provider-aware-request-routing/04-PATTERNS.md
@.planning/phases/04-provider-aware-request-routing/04-01-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/auth/model.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
</context>

## Locked decisions covered

D-01 (pure resolver), D-02 (provider authority), D-04 (override wins for base_url), D-05/D-06 (dual bases), D-07 (default + env), D-08 (no parallel client — pure types only), D-13 (no OAuth/gate).

**Discretion (research defaults):** place symbols in `agent/config.rs` next to `resolve_credentials`; default URL `https://chatgpt.com/backend-api/codex`; env `GROK_CODEX_BASE_URL`; credential_slot strings match wire `"xai"` / `"codex"` (use `crate::auth::model::PROVIDER_XAI` / `PROVIDER_CODEX` inside library; tests assert public wire strings). Do **not** require `xai-grok-env` changes.

## Cargo verify hygiene (mandatory)

- One TESTNAME filter per cargo invocation or full binary. No bare `| tail`. No shell `--lib`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codex endpoint default + env override on EndpointsConfig</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (constants ~46–48, EndpointsConfig ~143–249, resolvers ~302–314, Default ~540–575)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (EndpointsConfig + Codex default URL)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
  </read_first>
  <behavior>
    - codex_base_url_default_constant: CODEX_BASE_URL_DEFAULT == "https://chatgpt.com/backend-api/codex"
    - resolve_codex_base_url_default: EndpointsConfig with default/empty codex field returns CODEX_BASE_URL_DEFAULT
    - resolve_codex_base_url_env_override: with GROK_CODEX_BASE_URL set (serial env guard), Default or resolve returns the override URL — use existing serial_test / EnvGuard patterns from config or test-support; if integration test mutates env, use serial_test::serial and restore
  </behavior>
  <action>
    Add Codex endpoint surface mirroring xAI (D-07, D-06, RESEARCH Pattern discretion).

    1. After `XAI_API_BASE_URL_DEFAULT`, add public `CODEX_BASE_URL_DEFAULT: &str = "https://chatgpt.com/backend-api/codex"` with doc that this is ChatGPT/Codex backend **not** Platform `api.openai.com` (D-15/D-06).
    2. On `EndpointsConfig`, add `pub codex_base_url: String` (same non-Option style as `xai_api_base_url`) with doc Env: `GROK_CODEX_BASE_URL`.
    3. In `Default`, set `codex_base_url: std::env::var("GROK_CODEX_BASE_URL").unwrap_or_else(|_| CODEX_BASE_URL_DEFAULT.to_owned())`.
    4. Implement `pub fn resolve_codex_base_url(&self) -> String` — if `codex_base_url` is blank/whitespace after trim, fall back to `CODEX_BASE_URL_DEFAULT`; else return the configured string (mirror blank_as_unset style used for other resolvers).
    5. Ensure serde Default / `from_config_value` still works — field is plain String with struct-level `#[serde(default)]` on EndpointsConfig so missing TOML keys get Default.
    6. Update `tests/provider_routing.rs` with green tests for default constant + resolve_codex_base_url. For env override, use `serial_test::serial` + RAII env guard if available in the crate/dev-deps (xai-grok-test-support EnvGuard); if env mutation is risky in integration binary, set `EndpointsConfig { codex_base_url: "https://codex.test/base".into(), ..Default::default() }` field override without env for one test and document env path covered by constructing Default only when safe — prefer field override for reliability in parallel CI, plus one serial env test if EnvGuard exists.
    7. Do **not** change `default_models()` stamping yet (Plan 03). Do not change resolve_credentials. Do not touch sampler crate.
  </action>
  <acceptance_criteria>
    - CODEX_BASE_URL_DEFAULT and EndpointsConfig.codex_base_url + resolve_codex_base_url exist and are public
    - provider_routing tests for default resolve pass
    - cargo check -p xai-grok-shell succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p xai-grok-shell && cargo test -p xai-grok-shell --test provider_routing resolve_codex_base_url -- --nocapture</automated>
  </verify>
  <done>
    Codex base URL is a first-class endpoint with default + override, testable without AuthManager.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public pure resolve_provider_route + ProviderRoute</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (ModelProvider ~3366–3393, resolve_credentials ~4376, new endpoint helpers from Task 1)
    - crates/codegen/xai-grok-shell/src/auth/model.rs (PROVIDER_XAI, PROVIDER_CODEX)
    - .planning/phases/04-provider-aware-request-routing/04-PATTERNS.md (resolve_provider_route pure shape)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
  </read_first>
  <behavior>
    - resolve_provider_route_xai_default: provider Xai, no override → base_url == endpoints.resolve_inference_base_url(), credential_slot == "xai", provider Xai
    - resolve_provider_route_codex_default: provider Codex, no override → base_url == endpoints.resolve_codex_base_url(), credential_slot == "codex", provider Codex
    - resolve_provider_route_override_wins: any provider with non-empty model_base_url_override → base_url is override; credential_slot still matches provider (D-04 + D-09)
    - resolve_provider_route_blank_override_ignored: whitespace-only override falls through to provider default
  </behavior>
  <action>
    Implement pure resolver per D-01, D-02, D-04, D-05, D-06, D-09. Placement: `agent/config.rs` near `resolve_credentials` (RESEARCH discretion).

    1. Add public struct `ProviderRoute { pub base_url: String, pub credential_slot: &'static str, pub provider: ModelProvider }` with Debug/Clone/PartialEq/Eq.
    2. Add `pub fn resolve_provider_route(provider: ModelProvider, endpoints: &EndpointsConfig, model_base_url_override: Option<&str>) -> ProviderRoute`.
    3. credential_slot: `ModelProvider::Xai` → `crate::auth::model::PROVIDER_XAI`; `Codex` → `PROVIDER_CODEX` (same wire strings as as_str).
    4. If override is Some non-empty after trim, base_url = override owned string; else match provider: Xai → `endpoints.resolve_inference_base_url()`, Codex → `endpoints.resolve_codex_base_url()`.
    5. api_backend is **not** selected here — model-entry driven (D-08 discretion). Do not invent a per-provider backend table.
    6. Export symbols so integration tests import `resolve_provider_route`, `ProviderRoute`, `CODEX_BASE_URL_DEFAULT` from `xai_grok_shell::agent::config`.
    7. Update `tests/provider_routing.rs` with the four green pure-route tests above. Assert credential_slot with string literals `"xai"` / `"codex"`.
    8. Do not yet wire into default_models or prepare_sampling (Plans 03–04). Do not implement OAuth or gate UX (D-13).
  </action>
  <acceptance_criteria>
    - resolve_provider_route and ProviderRoute are public
    - Pure route tests green under --test provider_routing
    - No change to default_models stamping or BearerResolver yet
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing resolve_provider_route -- --nocapture && cargo check -p xai-grok-shell</automated>
  </verify>
  <done>
    Pure provider route is the single authority function for base_url + credential_slot selection.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env / TOML endpoints → base_url | Operator-controlled URL selection for Codex/xAI |
| Model override base_url | BYOK custom URL wins; slot still from provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Spoofing | resolve_provider_route override | medium | mitigate | Override changes URL only; credential_slot always from ModelProvider (D-04/D-09) |
| T-04-04 | Tampering | GROK_CODEX_BASE_URL | medium | accept | Same trust model as GROK_XAI_API_BASE_URL / proxy URL env (operator local config) |
| T-04-05 | Information Disclosure | default URL constants | low | accept | Public non-secret product endpoints |
| T-04-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Pure route + Codex endpoint tests green; catalog stamping still deferred to Plan 03.
</verification>

<success_criteria>
D-01/D-07 pure core shipped; integration tests green for resolve_provider_route_* and resolve_codex_base_url_*.
</success_criteria>

<output>
Create `.planning/phases/04-provider-aware-request-routing/04-02-SUMMARY.md` when done
</output>
