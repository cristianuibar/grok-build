Reviewed Phase 1 implementation commits `66aad62^..2e25e9f` at HEAD `9d058cd`. No files were modified.

## HIGH

- **A new unit test deletes and overwrites real user data.** [config/tests.rs:2266](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/config/tests.rs:2266) resolves the process-global product home—normally the developer’s real `~/.bum`—then [removes fixed role/persona files before the test](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/config/tests.rs:2282), overwrites them, and [deletes them again](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/config/tests.rs:2347). `#[serial]` prevents test races but provides no filesystem isolation. Running this test can destroy real `roles/reviewer.toml`, `personas/reviewer.toml`, and bundled equivalents.

- **Supported installation and packaging paths remain stock Grok and violate isolation.** The npm package still exposes only `grok` ([package.json:2](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/npm/grok/package.json:2)); its postinstall hard-codes `~/.grok/bin` and writes `~/.grok/config.toml` ([postinstall.js:19](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/npm/grok/bin/postinstall.js:19), [postinstall.js:174](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/npm/grok/bin/postinstall.js:174)). The shipped bootstrap installer reads stock auth and writes binaries/config/policy under `~/.grok` ([install.sh:110](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/scripts/install.sh:110), [install.sh:156](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/scripts/install.sh:156), [install.sh:264](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/scripts/install.sh:264)). Additionally, the platform-package assembler still looks for the removed `target/.../xai-grok-pager` artifact ([assemble-platform-packages.js:83](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager/npm/grok/scripts/assemble-platform-packages.js:83)). Distribution either fails or installs stock Grok while mutating its home.

- **The updater can silently replace the fork with an upstream Grok binary.** Release checks are enabled unless explicitly suppressed ([main.rs:1958](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager-bin/src/main.rs:1958)). Update sources remain `@xai-official/grok`, `xai-org-shared/grok-build`, `x.ai/cli`, and the stock GCS bucket ([version.rs:12](/home/cristian/bum/grok-build/crates/codegen/xai-grok-update/src/version.rs:12)). For a recognized installer, it downloads a `grok-*` artifact ([auto_update.rs:1184](/home/cristian/bum/grok-build/crates/codegen/xai-grok-update/src/auto_update.rs:1184)) and activates it as `~/.bum/bin/bum` ([auto_update.rs:1224](/home/cristian/bum/grok-build/crates/codegen/xai-grok-update/src/auto_update.rs:1224), [auto_update.rs:1341](/home/cristian/bum/grok-build/crates/codegen/xai-grok-update/src/auto_update.rs:1341)). This is both a product-integrity and privacy release blocker.

## MEDIUM

- **`GROK_AUTH_PATH` remains a write-capable escape from isolated auth storage.** [auth/manager.rs:295](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/auth/manager.rs:295) accepts an arbitrary inherited path instead of requiring `$BUM_HOME/auth.json`; startup can rewrite it ([manager.rs:303](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/auth/manager.rs:303)), and logout can rewrite or delete it ([manager.rs:454](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/auth/manager.rs:454)). If a shell exports `GROK_AUTH_PATH=~/.grok/auth.json`, `bum` shares and mutates stock credentials. Converted child-process fixtures set `BUM_HOME` but do not clear this or `GROK_WORKSPACE_HOME` ([test-support/env.rs:153](/home/cristian/bum/grok-build/crates/codegen/xai-grok-test-support/src/env.rs:153)), leaving tests exposed to host paths.

- **Empty `BUM_HOME` defeats the no-CWD-fallback safety contract.** The resolver correctly treats an empty value as absent ([paths.rs:26](/home/cristian/bum/grok-build/crates/codegen/xai-grok-config/src/paths.rs:26)), but `user_grok_home()` treats mere presence as resolvable ([paths.rs:71](/home/cristian/bum/grok-build/crates/codegen/xai-grok-config/src/paths.rs:71)). With `BUM_HOME=""` and no OS home, global config, plugins, trust, and MCP credentials can be treated as living under the repository-relative `.bum`.

- **The twin home resolvers are not actually lockstep when no home exists.** Config falls back to `<cwd>/.bum` ([paths.rs:31](/home/cristian/bum/grok-build/crates/codegen/xai-grok-config/src/paths.rs:31)); fast-worktree returns an error ([db/mod.rs:355](/home/cristian/bum/grok-build/crates/codegen/xai-fast-worktree/src/db/mod.rs:355)); workspace then falls back to shared `/tmp/.bum` ([worktree/mod.rs:621](/home/cristian/bum/grok-build/crates/codegen/xai-grok-workspace/src/worktree/mod.rs:621)). Worktrees and their database can split from the rest of product state, with possible cross-user/container collisions.

- **Product-home rules are classified as workspace rules.** Global instruction discovery correctly starts at the resolved product home ([agents_md.rs:88](/home/cristian/bum/grok-build/crates/codegen/xai-grok-agent/src/prompt/agents_md.rs:88)), but scope partitioning recognizes only `$HOME/.grok` and `$HOME/.claude` ([prompt_build.rs:42](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/session/acp_session_impl/prompt_build.rs:42)). Consequently, `~/.bum/AGENTS.md` and `$BUM_HOME/rules/*.md` are rendered as workspace-controlled rules rather than user rules.

- **Runtime recovery guidance still directs users and the model to stock paths.** Examples include deployment-key setup ([pager-bin/main.rs:167](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager-bin/src/main.rs:167)), auth recovery ([auth_method.rs:393](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/agent/auth_method.rs:393)), and the system prompt’s `~/.grok/docs/user-guide` instruction ([prompt.md:43](/home/cristian/bum/grok-build/crates/codegen/xai-grok-agent/templates/prompt.md:43)). Users may place secrets/config in stock state, while the model may read stale stock documentation.

- **The updater safety suite was not fully cut over.** [test_blitz_cancel.rs:47](/home/cristian/bum/grok-build/crates/codegen/xai-grok-update/tests/test_blitz_cancel.rs:47) still seeds and verifies `grok` plus `agent`, while production now swaps `bum` plus `agent`. Its non-ignored happy path should fail or continue protecting the obsolete link, so the critical atomic-update invariant is not covered for the shipped command.

- **The isolation proof misses the most dangerous flows.** It only runs `bum version` and explicitly disables auto-update ([home_isolation.rs:97](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs:97)). It does not exercise auth/login/logout, sessions, MCP credentials, config mutations, installers, or update activation—precisely where the remaining leaks exist.

- **`bum` still consumes stock system policy from `/etc/grok`.** [paths.rs:83](/home/cristian/bum/grok-build/crates/codegen/xai-grok-config/src/paths.rs:83) retains the stock system directory, which the config loader consumes. A host managed for stock Grok can silently impose endpoints, configuration, or fail-closed requirements on `bum`.

## LOW

- **The isolation assertions allow directory-only and non-regular writes.** [home_isolation.rs:18](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs:18) snapshots regular file contents only, while [lines 61–67](/home/cristian/bum/grok-build/crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs:61) permit a previously absent stock tree to be created if it contains no regular files. Empty directories, sockets, FIFOs, and broken symlinks can escape detection.

- **`voice_probe` implements different `BUM_HOME` semantics.** [voice_probe.rs:125](/home/cristian/bum/grok-build/crates/codegen/xai-grok-voice/src/bin/voice_probe.rs:125) uses UTF-8 `var`, accepts an empty override as `./config.toml`, and ignores non-UTF-8 paths, unlike the product resolver.

- **Custom-home diagnostics can report the wrong path.** MCP doctor checks the resolved home but always displays `~/.bum/config.toml` ([mcp_doctor.rs:150](/home/cristian/bum/grok-build/crates/codegen/xai-grok-shell/src/mcp_doctor.rs:150)), even when `BUM_HOME` points elsewhere.

## Overall risk and ship readiness

**Overall risk: HIGH. Ship readiness: NOT READY.**

The normal Rust product-home path is mostly sound: I found no remaining production Rust reader that honors `GROK_HOME` as the product root, and primary session/auth writers generally use `grok_home()`. However, distribution still writes stock state, the updater can replace the fork with upstream Grok, and a new test can delete real `~/.bum` files. Those three issues should block shipping Phase 1.