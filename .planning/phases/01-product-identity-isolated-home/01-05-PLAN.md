---
phase: 01-product-identity-isolated-home
plan: 05
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - crates/codegen/xai-grok-agent/src/discovery.rs
  - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
  - crates/codegen/xai-grok-pager-bin/Cargo.toml
autonomous: true
requirements:
  - ID-01
  - ID-03
must_haves:
  truths:
    - "Agent discovery does not treat stock ~/.grok user agents as first-class bum user state when product home is bum"
    - "Temp-home run of bum leaves product state only under BUM_HOME; no product writes under .grok or .codex"
    - "User can build/run primary CLI as bum with documented cargo invocation"
  artifacts:
    - path: crates/codegen/xai-grok-agent/src/discovery.rs
      provides: "no stock-home legacy agent scan for product isolation"
    - path: crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
      provides: "temp-home isolation integration proof"
      contains: BUM_HOME
  key_links:
    - from: bum process under temp HOME + BUM_HOME
      to: filesystem product sinks
      via: "main startup path (version/docs extract/home create)"
      pattern: "BUM_HOME|\\.bum"
---

## Phase Goal

**As a** bum user, **I want to** run `bum` with a temporary home and know stock Grok/Codex dirs stay untouched, **so that** isolation is proven and identity is safe for later multi-provider auth phases.

<objective>
Close Phase 1 with legacy stock-home read gating, automated isolation proof (zero product writes under `.grok` / `.codex` under temp home), and a green build/run path for `bum` (per D-VERIFY, D-MIGRATE, D-PLUGIN leave project layouts, D-BIN).

Purpose: Isolation without automated proof regresses (PITFALLS half-rebrand / incomplete isolation).
Output: discovery fix + integration test + grep/docs confirmation for local `cargo run -p xai-grok-pager-bin --bin bum`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@crates/codegen/xai-grok-agent/src/discovery.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
@crates/codegen/xai-grok-pager-bin/Cargo.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disable stock ~/.grok legacy agent discovery reads</name>
  <files>crates/codegen/xai-grok-agent/src/discovery.rs</files>
  <behavior>
    - user_agent_dirs no longer appends literal HOME/.grok agents/bundled agents as legacy product state when product home differs
    - Project-local cwd/.grok agent discovery remains (D-PLUGIN)
    - Tests: former includes_legacy_when_differs now expects exclusion (or renamed isolation assertion); dedup/project-local cases still correct
  </behavior>
  <action>
    Per RESEARCH recommendation and D-MIGRATE / isolation identity (no pull of stock Grok user agents as bum's):
    1. In `user_agent_dirs`, remove or permanently gate off the legacy branch that adds `home.join(".grok")` agent directories when `grok_home` differs from that path.
    2. Keep scanning the actual product home path passed as `grok_home` (bum home after SoT).
    3. Do not rename or remove project-layout discovery under cwd `.grok` elsewhere in this module (D-PLUGIN).
    4. Update unit tests:
       - `user_agent_dirs_includes_legacy_grok_when_grok_home_differs` → assert legacy HOME/.grok is NOT included when product home is e.g. HOME/.bum
       - Adjust any test that only passed because of legacy scan; prefer fixtures that place agents under the product home path under test
    5. Leave credential import / migration unimplemented (D-MIGRATE).
  </action>
  <verify>
    <automated>cargo test -p xai-grok-agent discovery -- --nocapture</automated>
  </verify>
  <done>
    Stock `~/.grok` is not treated as bum user-agent product state; project `.grok` discovery preserved; tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Temp-home isolation integration test</name>
  <files>crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs, crates/codegen/xai-grok-pager-bin/Cargo.toml</files>
  <behavior>
    - Spawns built `bum` (CARGO_BIN_EXE_bum or cargo-built path) with HOME=temp and BUM_HOME=temp/.bum (or BUM_HOME=temp product root — if using temp as BUM_HOME root, still assert no sibling .grok/.codex product sinks under HOME)
    - Invokes a short non-interactive path that exercises product-home creation (prefer `--version` / `version` if main creates home/docs; if version avoids home, call a minimal path that triggers `grok_home()` / docs extract as main does on startup — mirror main.rs startup)
    - Asserts product root under BUM_HOME exists or received expected writes
    - Asserts no product-state filenames appear under HOME/.grok or HOME/.codex (and/or those dirs are absent)
    - Does not set GROK_HOME
    - Does not require network; disable telemetry/autoupdate env knobs like test_env_cmd_tokio
  </behavior>
  <action>
    Per D-VERIFY:
    1. Add integration test file `crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs` (ensure package has `[[test]]` or default tests/ discovery; add dev-deps tempfile if missing on the bin package — prefer reusing existing workspace deps).
    2. Resolve binary via `CARGO_BIN_EXE_bum` (set by cargo test for the package bin) with fallback only if needed.
    3. Use TempDir for HOME; set BUM_HOME to `home.join(".bum")` matching harness convention from 01-04.
    4. Run short CLI; if full agent is too heavy, choose the lightest main entry that still calls product home init (inspect `main.rs` for docs extract / version path). Isolation proof may create empty home via `grok_home()` create_dir_all — that is sufficient if combined with unit tests from 01-01, but prefer an entry that matches real startup.
    5. Assert zero product writes under `.grok` and `.codex` relative to the temp HOME (D-VERIFY). Known product filenames to watch if dirs exist: auth.json, config.toml, sessions, leader.sock, marketplace-cache, mcp_credentials.json.
    6. Wire package features/dev-deps only as needed; do not add new third-party crates if tempfile already workspace-available.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Automated isolation test proves temp-home bum run does not write product state under `.grok` or `.codex`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Production grep gate + local bum invocation confirmation</name>
  <files>crates/codegen/xai-grok-config/src/paths.rs, crates/codegen/xai-fast-worktree/src/db/mod.rs, crates/codegen/xai-grok-pager-bin/Cargo.toml</files>
  <action>
    Per D-WRITERS, D-HOME, D-BIN, D-PLUGIN:
    1. Grep production resolvers for remaining product-home defaults: confirm `paths.rs` and `resolve_grok_home` do not read GROK_HOME or default join `.grok` for product home. Fix any remaining production bypasses found (not project-layout `cwd.join(".grok")`).
    2. Confirm `managed_grok_bin_name` / `grok_application` leaf is `bum`.
    3. Confirm package `[[bin]]` is `bum` and document in Cargo.toml comment if missing: `cargo run -p xai-grok-pager-bin --bin bum`.
    4. Do not rename project `.grok-plugin` or project `.grok` trees (D-PLUGIN).
    5. Do not implement Phase 8 chrome/telemetry/auto-update kills (D-SCOPE).
  </action>
  <verify>
    <automated>cargo build -p xai-grok-pager-bin --bin bum && cargo test -p xai-grok-config --lib paths -- --nocapture && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Grep gate clean for production product-home SoT/twin; bum builds; isolation + path unit tests green. Phase 1 goal ready for verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bum process → filesystem | Product state must not land in stock CLI homes |
| Discovery reads → agent defs | Stock ~/.grok agents must not become bum identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | agent discovery legacy | high | mitigate | Remove stock HOME/.grok user-agent scan |
| T-01-09 | Tampering / Disclosure | product writers | high | mitigate | Temp-home isolation test asserts zero writes under .grok/.codex |
| T-01-10 | Tampering | accidental migration | medium | mitigate | No import code; clean re-login only (D-MIGRATE) |
| T-01-SC | Tampering | package installs | low | accept | No new untrusted packages; tempfile from workspace if needed |
</threat_model>

<verification>
- `cargo test -p xai-grok-agent discovery`
- `cargo test -p xai-grok-pager-bin --test home_isolation`
- `cargo build -p xai-grok-pager-bin --bin bum`
</verification>

<success_criteria>
- Legacy stock agent scan gone
- Isolation integration green
- bum binary builds and is the documented local run target
- Phase success criteria 1–3 from ROADMAP satisfied
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-05-SUMMARY.md` when done
</output>
