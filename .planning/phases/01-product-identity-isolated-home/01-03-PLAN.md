---
phase: 01-product-identity-isolated-home
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-pager-bin/Cargo.toml
  - crates/codegen/xai-grok-test-support/src/env.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/env.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/bin/pty_scenario.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/bin/scroll_matrix.rs
autonomous: true
requirements:
  - ID-01
must_haves:
  truths:
    - "cargo build -p xai-grok-pager-bin --bin bum produces an invocable bum artifact"
    - "Test harnesses resolve CARGO_BIN_EXE_bum / target/debug/bum via pager-bin package"
    - "Crate package name remains xai-grok-pager-bin (no crate rename)"
  artifacts:
    - path: crates/codegen/xai-grok-pager-bin/Cargo.toml
      provides: "[[bin]] name and default-run bum"
      contains: "name = \"bum\""
    - path: crates/codegen/xai-grok-test-support/src/env.rs
      provides: "grok_binary resolves bum artifact"
      contains: CARGO_BIN_EXE_bum
  key_links:
    - from: Cargo [[bin]] bum
      to: CARGO_BIN_EXE_bum / local debug path
      via: "test-support + pty-harness resolvers"
      pattern: "bum"
---

## Phase Goal

**As a** developer or daily-driver user, **I want to** invoke the product as `bum`, **so that** the shipped command name matches the bum product identity (ID-01).

<objective>
Rename the composition-root shipped binary to `bum` and update harness binary resolution so local build/install and e2e helpers find the new artifact (per D-BIN). Keep crate/package names `xai-grok-*`.

Purpose: ID-01 is the user-facing ship name; harness drift leaves CI red after rename.
Output: `[[bin]]`/`default-run` = bum; test-support + pty-harness resolve `bum`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@crates/codegen/xai-grok-pager-bin/Cargo.toml
@crates/codegen/xai-grok-test-support/src/env.rs
@crates/codegen/xai-grok-pager-pty-harness/src/env.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship [[bin]] as bum</name>
  <files>crates/codegen/xai-grok-pager-bin/Cargo.toml</files>
  <action>
    Per D-BIN:
    1. In `crates/codegen/xai-grok-pager-bin/Cargo.toml` only (root workspace Cargo.toml is generated — do not hand-edit it): set `default-run = "bum"` and `[[bin]] name = "bum"` with existing `path = "src/main.rs"`.
    2. Keep package `name = "xai-grok-pager-bin"`.
    3. Update the comment block that says the artifact is still named `xai-grok-pager` to state the user-facing binary is `bum`; note local dev: `cargo run -p xai-grok-pager-bin --bin bum`.
    4. Prefer single bin target `bum` — no dual `grok`/`xai-grok-pager` compatibility alias in v1 (D-BIN).
    5. Do not rename crates, npm packages, or install-channel docs (D-SCOPE / Phase 8).
  </action>
  <verify>
    <automated>cargo build -p xai-grok-pager-bin --bin bum</automated>
  </verify>
  <done>
    `cargo build -p xai-grok-pager-bin --bin bum` succeeds and produces the bum artifact under target.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test-support binary resolution for bum</name>
  <files>crates/codegen/xai-grok-test-support/src/env.rs</files>
  <action>
    Per D-BIN, update binary resolution helpers only in this task (product home env key cutover for sandboxes is plan 01-04 — if you must touch adjacent lines, leave GROK_HOME sandbox strings for 01-04 unless a compile break forces a one-line BUM_HOME fix; prefer not mixing home env mass-update here):
    1. `local_grok_binary_path`: debug artifact name `bum` + EXE_SUFFIX (not `xai-grok-pager`).
    2. `ensure_local_grok_binary`: `cargo build -p xai-grok-pager-bin --bin bum` (fix wrong `-p xai-grok-pager` for the composition-root binary).
    3. `grok_binary()`: read `CARGO_BIN_EXE_bum` after optional `GROK_BINARY` override (keep env name `GROK_BINARY` this phase — D-SCOPE non-home knobs).
    4. Update panic/assert messages that say `xai-grok-pager` to `bum`.
    5. Function name `grok_binary` may remain (discretion); behavior returns the bum binary path.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-test-support</automated>
  </verify>
  <done>
    Test-support builds and resolves the bum artifact via GROK_BINARY, CARGO_BIN_EXE_bum, or local cargo build of pager-bin.
  </done>
</task>

<task type="auto">
  <name>Task 3: PTY harness binary resolution for bum</name>
  <files>crates/codegen/xai-grok-pager-pty-harness/src/env.rs, crates/codegen/xai-grok-pager-pty-harness/src/bin/pty_scenario.rs, crates/codegen/xai-grok-pager-pty-harness/src/bin/scroll_matrix.rs</files>
  <action>
    Per D-BIN:
    1. In `pty-harness/src/env.rs`: local path leaf `bum`; cargo args already use `-p xai-grok-pager-bin` — switch `--bin` to `bum`; `CARGO_BIN_EXE_xai-grok-pager` → `CARGO_BIN_EXE_bum`; update comments/docs on resolution order.
    2. In `pty_scenario.rs` and `scroll_matrix.rs` clap about/help strings that document default binary env `CARGO_BIN_EXE_xai-grok-pager`, update to `CARGO_BIN_EXE_bum` (path-label accuracy for invocation; not full chrome rebrand).
    3. Grep in-repo for remaining `CARGO_BIN_EXE_xai-grok-pager` and `--bin xai-grok-pager` under harnesses/tests and fix call sites that would break after single-bin rename (same change set). Do not expand into UI chrome string rewrites (D-SCOPE).
  </action>
  <verify>
    <automated>cargo check -p xai-grok-pager-pty-harness && (rg -n 'CARGO_BIN_EXE_xai-grok-pager|--bin xai-grok-pager' crates/codegen/xai-grok-test-support crates/codegen/xai-grok-pager-pty-harness crates/codegen/xai-grok-pager-bin --glob '!target/**' || true)</automated>
  </verify>
  <done>
    PTY harness resolves `bum`; no remaining CARGO_BIN_EXE / --bin references to the old pager artifact in test-support, pty-harness, or pager-bin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build artifact name | User/CI expects invocable `bum`; wrong name confuses install and tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Spoofing | Binary identity | medium | mitigate | Single ship name `bum`; no grok alias that blurs stock CLI (D-BIN) |
| T-01-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo build -p xai-grok-pager-bin --bin bum`
- `cargo check -p xai-grok-test-support -p xai-grok-pager-pty-harness`
</verification>

<success_criteria>
- Primary command artifact is `bum`
- Harnesses find `bum` without dual-bin alias
- Package/crate names unchanged
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-03-SUMMARY.md` when done
</output>
