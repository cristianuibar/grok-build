---
phase: 01-product-identity-isolated-home
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - crates/codegen/xai-grok-test-support/src/env.rs
  - crates/codegen/xai-grok-pager-pty-harness/src/content.rs
  - crates/codegen/xai-grok-update/tests/common/mod.rs
  - crates/codegen/xai-grok-shell/src/leader/mod.rs
autonomous: true
requirements:
  - ID-03
must_haves:
  truths:
    - "Shared test sandboxes set BUM_HOME (not GROK_HOME) as product home override"
    - "Convention: HOME=tmp root and BUM_HOME=home.join(\".bum\") so isolation can assert no HOME/.grok product tree"
    - "Update-suite test_home and leader test helpers use BUM_HOME before first grok_home() resolve"
  artifacts:
    - path: crates/codegen/xai-grok-test-support/src/env.rs
      provides: "test_env_cmd_tokio BUM_HOME sandbox"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-pager-pty-harness/src/content.rs
      provides: "env_for_pager BUM_HOME + .bum"
      contains: BUM_HOME
  key_links:
    - from: test sandbox env
      to: child process product home
      via: "BUM_HOME + HOME"
      pattern: "BUM_HOME"
---

<objective>
Cut test harness product-home env sandboxes over to `BUM_HOME` / `.bum` so automated suites exercise the same isolation contract as production (per D-HOME, D-VERIFY) without dual-honoring `GROK_HOME`.

Purpose: Leaving tests on GROK_HOME re-validates the old coupling and misses regressions.
Output: test-support, pty env_for_pager, update test_home, and critical shell leader helpers set BUM_HOME.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@crates/codegen/xai-grok-test-support/src/env.rs
@crates/codegen/xai-grok-pager-pty-harness/src/content.rs
@crates/codegen/xai-grok-update/tests/common/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_env_cmd_tokio + comments to BUM_HOME</name>
  <files>crates/codegen/xai-grok-test-support/src/env.rs</files>
  <action>
    Per D-HOME and D-VERIFY (convention from RESEARCH/PATTERNS):
    1. In `test_env_cmd_tokio`, set `BUM_HOME` to `home.join(".bum")` while still setting `HOME` to `home`. Remove setting product home via `GROK_HOME`.
    2. Rewrite the Windows sandbox comment to mention BUM_HOME and `.bum` (why HOME alone is insufficient).
    3. Leave other GROK_* knobs (telemetry, proxy base URL, disable autoupdater) unchanged this phase (D-SCOPE).
    4. Keep EnvGuard API unchanged; consumers may set BUM_HOME with EnvGuard in unit tests.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-test-support</automated>
  </verify>
  <done>
    Child-process sandbox uses BUM_HOME under tmp/.bum; no GROK_HOME product override in test_env_cmd_tokio.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PTY env_for_pager product home</name>
  <files>crates/codegen/xai-grok-pager-pty-harness/src/content.rs</files>
  <behavior>
    - env_for_pager includes BUM_HOME pointing at home/.bum
    - Does not inject GROK_HOME as product home
    - env_for_pager_shape (or equivalent) unit assertions updated for key and path segment
  </behavior>
  <action>
    Per D-HOME / D-VERIFY:
    1. Change product home path segment from `.grok` to `.bum` and env key from GROK_HOME to BUM_HOME in `env_for_pager`.
    2. Update unit tests asserting env shape (key name and path suffix).
    3. Mirror test-support convention; do not set GROK_HOME for isolation.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-pager-pty-harness env_for_pager -- --nocapture</automated>
  </verify>
  <done>
    PTY harness sandboxes product home under BUM_HOME/.bum with tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update suite test_home + scoped leader/e2e GROK_HOME fixtures</name>
  <files>crates/codegen/xai-grok-update/tests/common/mod.rs, crates/codegen/xai-grok-shell/src/leader/mod.rs</files>
  <action>
    Per D-HOME, D-VERIFY, OnceLock discipline:
    1. In `xai-grok-update/tests/common/mod.rs` `test_home()`: set `BUM_HOME` instead of `GROK_HOME` before any product path resolve; update module comments that describe GROK_HOME isolation to BUM_HOME. Leave non-home GROK_* clearances as-is if present.
    2. Grep `crates/codegen/xai-grok-shell` (and leader tests in `leader/mod.rs`) for process env product-home sandboxes that set `GROK_HOME` for test isolation; switch those product-home overrides to `BUM_HOME`. Prefer scoped fixes that unblock shell tests after SoT cutover — do not mass-rename every comment in the monorepo.
    3. Grep for other high-impact fixtures that set product home via GROK_HOME in crates this phase already depends on (shell, update, pager tests) and fix enough that targeted `cargo test -p` for those crates can run without relying on GROK_HOME. Workspace-wide every-file comment polish is out of scope.
    4. Do not introduce dual-read of GROK_HOME in production for "test convenience."
  </action>
  <verify>
    <automated>cargo check -p xai-grok-update -p xai-grok-shell</automated>
  </verify>
  <done>
    Critical test fixtures that initialize product home use BUM_HOME; update/shell packages compile against the new contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test env → production path code | Wrong sandbox env reintroduces stock-home coupling in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege / Disclosure | test sandboxes | medium | mitigate | Tests set BUM_HOME only; never teach dual-honor via fixtures |
| T-01-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo check -p xai-grok-test-support -p xai-grok-update -p xai-grok-shell`
- `cargo test -p xai-grok-pager-pty-harness env_for_pager`
</verification>

<success_criteria>
- Shared harnesses sandbox via BUM_HOME + `.bum`
- Update test_home and scoped shell fixtures match
- No intentional GROK_HOME product override left in those helpers
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-04-SUMMARY.md` when done
</output>
