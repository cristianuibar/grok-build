---
phase: 01-product-identity-isolated-home
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - crates/codegen/xai-fast-worktree/src/db/mod.rs
  - crates/codegen/xai-grok-workspace/src/worktree/mod.rs
  - crates/codegen/xai-grok-shell/src/leader/mod.rs
  - crates/codegen/xai-grok-sandbox/src/paths.rs
autonomous: true
requirements:
  - ID-03
must_haves:
  truths:
    - "fast-worktree resolve_grok_home uses BUM_HOME and defaults to ~/.bum in lockstep with config SoT"
    - "Workspace worktree fallback default is .bum when twin resolve fails"
    - "Managed leader binary leaf under product home is bum / bum.exe"
  artifacts:
    - path: crates/codegen/xai-fast-worktree/src/db/mod.rs
      provides: "twin resolve_grok_home + GrokHomeFixture on BUM_HOME"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-shell/src/leader/mod.rs
      provides: "managed_grok_bin_name returns bum"
      contains: "\"bum\""
  key_links:
    - from: resolve_grok_home
      to: worktrees.db / workspace worktree paths
      via: "open_default / workspace grok_home fallback"
      pattern: "BUM_HOME|\\.bum"
---

<objective>
Keep the deliberate home-resolution twin and managed-install leaf names aligned with the config SoT so worktrees, leader managed bin, and sandbox writable root cannot drift back to `~/.grok` (per D-HOME, D-WRITERS).

Purpose: Incomplete twin/managed cutover is the classic half-isolation failure mode for ID-03.
Output: Updated fast-worktree resolver/fixture, workspace fallback, managed bin name, sandbox docs if needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@crates/codegen/xai-grok-config/src/paths.rs
@crates/codegen/xai-fast-worktree/src/db/mod.rs
@crates/codegen/xai-grok-workspace/src/worktree/mod.rs
@crates/codegen/xai-grok-shell/src/leader/mod.rs
@crates/codegen/xai-grok-sandbox/src/paths.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lockstep twin resolve_grok_home + GrokHomeFixture</name>
  <files>crates/codegen/xai-fast-worktree/src/db/mod.rs</files>
  <behavior>
    - resolve_grok_home reads BUM_HOME first; else $HOME/.bum via dunce canonicalize (sync with default_grok_home)
    - Error context mentions BUM_HOME / HOME (not GROK_HOME as product override)
    - GrokHomeFixture sets/restores BUM_HOME under mutex; optional dir name bum-home
    - Existing fixture-based unit/integration tests still pass under cargo test
  </behavior>
  <action>
    Per D-HOME and D-WRITERS, edit `xai-fast-worktree` twin only (do not re-open config SoT from plan 01-01):
    1. `resolve_grok_home()`: `var("BUM_HOME")` instead of `GROK_HOME`; default join `.bum` instead of `.grok`; update comments that reference product home and lockstep with `xai_grok_config::default_grok_home`.
    2. `GrokHomeFixture`: set/restore `BUM_HOME` (including Drop); update comments on GROK_HOME_ENV_LOCK — lock static may keep old name; env key must be BUM_HOME.
    3. Run fixture-backed tests; fix only string/env mismatches required for green suite.
    4. Do not add migration from stock `.grok` worktree DB (D-MIGRATE).
  </action>
  <verify>
    <automated>cargo test -p xai-fast-worktree --lib -- --nocapture</automated>
  </verify>
  <done>
    Twin resolver and fixture use BUM_HOME + `.bum`; fast-worktree lib tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Workspace fallback + managed bin leaf + sandbox notes</name>
  <files>crates/codegen/xai-grok-workspace/src/worktree/mod.rs, crates/codegen/xai-grok-shell/src/leader/mod.rs, crates/codegen/xai-grok-sandbox/src/paths.rs</files>
  <action>
    Per D-HOME, D-WRITERS, D-BIN (managed leaf under home):
    1. In `xai-grok-workspace/src/worktree/mod.rs` local `grok_home()` fallback when `resolve_grok_home` fails: join `.bum` not `.grok`. Update comments that say worktrees live under `.grok` product tree to `.bum` / product home.
    2. In `xai-grok-shell/src/leader/mod.rs`, change `managed_grok_bin_name()` to return `bum` / `bum.exe`. Update the function rustdoc. Leader unit tests that write `bin_dir.join(managed_grok_bin_name())` should stay green via the helper (no hard-coded `grok` leaf left for managed path).
    3. In `xai-grok-sandbox/src/paths.rs`, update comments that claim writable product dir is `~/.grok` if present; keep re-export of `xai_grok_config::grok_home()` as the implementation (no second resolver).
    4. Leave `GROK_LEADER_SOCKET` env name alone (D-SCOPE non-home knobs). Leave project `.grok` layout paths alone (D-PLUGIN).
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell leader -- --nocapture; cargo check -p xai-grok-workspace -p xai-grok-sandbox</automated>
  </verify>
  <done>
    Managed bin is `bum`, workspace fallback is `.bum`, sandbox docs match SoT; no second product-home env introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Twin resolver → worktree DB | Alternate code path that could write worktrees outside bum home |
| Managed bin under home | Leader may prefer `$HOME_PRODUCT/bin/bum` when current exe is under product home |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `resolve_grok_home` twin | high | mitigate | Same env+default as config SoT; no GROK_HOME honor |
| T-01-05 | Information Disclosure | workspace fallback `.grok` | medium | mitigate | Fallback join `.bum` so resolve failure cannot create stock tree |
| T-01-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo test -p xai-fast-worktree --lib`
- `cargo check -p xai-grok-workspace -p xai-grok-sandbox`
- Leader tests referencing managed bin remain green
</verification>

<success_criteria>
- Twin + fixture + workspace fallback + managed leaf all target bum product home
- No production twin path still defaults to `.grok` or reads `GROK_HOME` as product home
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-02-SUMMARY.md` when done
</output>
