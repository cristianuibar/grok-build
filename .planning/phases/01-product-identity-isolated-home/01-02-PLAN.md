---
phase: 01-product-identity-isolated-home
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - crates/codegen/xai-fast-worktree/src/db/mod.rs
  - crates/codegen/xai-grok-workspace/src/worktree/mod.rs
  - crates/codegen/xai-grok-shell/src/leader/mod.rs
  - crates/codegen/xai-grok-sandbox/src/paths.rs
  - crates/codegen/xai-grok-update/src/auto_update.rs
  - crates/codegen/xai-grok-update/tests/test_install_internal.rs
  - crates/codegen/xai-grok-update/tests/test_concurrent_convergence.rs
  - crates/codegen/xai-grok-update/tests/common/mod.rs
autonomous: true
requirements:
  - ID-01
  - ID-03
must_haves:
  truths:
    - "fast-worktree resolve_grok_home uses BUM_HOME and defaults to ~/.bum in lockstep with config SoT"
    - "Workspace worktree fallback default is .bum when twin resolve fails"
    - "Managed leader binary leaf under product home is bum / bum.exe"
    - "Updater swap_managed_bin_links installs bin/bum (not bin/grok); no grok compatibility alias under managed home"
    - "Leader unit tests that hard-code managed bin path expect bin/bum"
  artifacts:
    - path: crates/codegen/xai-fast-worktree/src/db/mod.rs
      provides: "twin resolve_grok_home + GrokHomeFixture on BUM_HOME"
      contains: BUM_HOME
    - path: crates/codegen/xai-grok-shell/src/leader/mod.rs
      provides: "managed_grok_bin_name returns bum; tests use bin/bum"
      contains: "\"bum\""
    - path: crates/codegen/xai-grok-update/src/auto_update.rs
      provides: "swap_managed_bin_links leaf bum"
      contains: "\"bum\""
  key_links:
    - from: resolve_grok_home
      to: worktrees.db / workspace worktree paths
      via: "open_default / workspace grok_home fallback"
      pattern: "BUM_HOME|\\.bum"
    - from: swap_managed_bin_links
      to: leader managed_grok_bin_name / grok_application
      via: "product home bin/bum"
      pattern: "bum"
---

<objective>
Keep the deliberate home-resolution twin, managed-install leaf names, and live updater install path aligned with the config SoT so worktrees, leader managed bin, and auto-update cannot drift back to `~/.grok` / `bin/grok` (per D-HOME, D-WRITERS, D-BIN). Addresses review concerns: updater installs bin/grok; leader hard-coded tests; twin platform home semantics; verify masking.

Purpose: Incomplete twin/managed/updater cutover is the classic half-isolation / split-brain managed install failure mode for ID-01 and ID-03.
Output: Updated fast-worktree resolver/fixture, workspace fallback, managed bin name + leader tests, sandbox docs, updater swap_managed_bin_links + install tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-identity-isolated-home/01-CONTEXT.md
@.planning/phases/01-product-identity-isolated-home/01-RESEARCH.md
@.planning/phases/01-product-identity-isolated-home/01-PATTERNS.md
@.planning/phases/01-product-identity-isolated-home/01-REVIEWS.md
@crates/codegen/xai-grok-config/src/paths.rs
@crates/codegen/xai-fast-worktree/src/db/mod.rs
@crates/codegen/xai-grok-workspace/src/worktree/mod.rs
@crates/codegen/xai-grok-shell/src/leader/mod.rs
@crates/codegen/xai-grok-sandbox/src/paths.rs
@crates/codegen/xai-grok-update/src/auto_update.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lockstep twin resolve_grok_home + GrokHomeFixture + platform note</name>
  <files>crates/codegen/xai-fast-worktree/src/db/mod.rs</files>
  <behavior>
    - resolve_grok_home reads BUM_HOME first; else default join .bum under resolved user home
    - Error context mentions BUM_HOME (not GROK_HOME as product override)
    - GrokHomeFixture sets/restores BUM_HOME under mutex; optional dir name bum-home
    - Comments document platform home input: prefer aligning with config SoT (std::env::home_dir) OR explicitly document remaining HOME-env vs home_dir difference and why twin cannot share crate dep this phase
    - Existing fixture-based unit/integration tests still pass
  </behavior>
  <action>
    Per D-HOME and D-WRITERS (Addresses review concern: twin platform mismatch with config home_dir vs HOME env):
    1. `resolve_grok_home()`: `var("BUM_HOME")` instead of `GROK_HOME`; default join `.bum` instead of `.grok`; update comments for lockstep with `xai_grok_config::default_grok_home`.
    2. Prefer aligning user-home resolution with config SoT: use `std::env::home_dir()` (same deprecated allow pattern as config) when BUM_HOME unset, falling back to explicit HOME only if needed for error messaging — goal is same default path on Linux/macOS/Windows as config. If a hard crate-boundary constraint forces keeping HOME env only, document that constraint in a short comment and ensure tests set both HOME and any platform vars the twin reads.
    3. `GrokHomeFixture`: set/restore `BUM_HOME` (including Drop); lock static may keep old name; env key must be BUM_HOME.
    4. Run fixture-backed tests; fix only string/env mismatches required for green suite.
    5. Do not add migration from stock `.grok` worktree DB (D-MIGRATE).
  </action>
  <verify>
    <automated>cargo test -p xai-fast-worktree --lib -- --nocapture</automated>
  </verify>
  <done>
    Twin resolver and fixture use BUM_HOME + `.bum`; platform home input aligned or documented; fast-worktree lib tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workspace fallback + managed bin leaf + leader hard-coded tests + sandbox</name>
  <files>crates/codegen/xai-grok-workspace/src/worktree/mod.rs, crates/codegen/xai-grok-shell/src/leader/mod.rs, crates/codegen/xai-grok-sandbox/src/paths.rs</files>
  <behavior>
    - Workspace fallback joins .bum not .grok
    - managed_grok_bin_name returns bum / bum.exe
    - Leader tests that hard-code bin/grok for managed install preference are updated to bin/bum (or managed_grok_bin_name())
    - Sandbox docs claim writable product dir is bum home via grok_home()
  </behavior>
  <action>
    Per D-HOME, D-WRITERS, D-BIN (Addresses review concern: leader tests hard-code bin/grok; helper-only claim was inaccurate):
    1. In `xai-grok-workspace/src/worktree/mod.rs` local `grok_home()` fallback when `resolve_grok_home` fails: join `.bum` not `.grok`. Update comments that say worktrees live under `.grok` product tree to `.bum` / product home.
    2. In `xai-grok-shell/src/leader/mod.rs`, change `managed_grok_bin_name()` to return `bum` / `bum.exe`. Update function rustdoc.
    3. Explicitly update **all** hard-coded managed-bin path assertions in leader tests (including managed-install preference cases around bin_dir.join paths) to `bum` / `bum.exe` or to call `managed_grok_bin_name()` — do not leave literal stock leaf strings as expected managed product paths.
    4. In `xai-grok-sandbox/src/paths.rs`, update comments that claim writable product dir is `~/.grok` if present; keep re-export of `xai_grok_config::grok_home()` as the implementation (no second resolver).
    5. Leave `GROK_LEADER_SOCKET` env name alone (D-SCOPE). Leave project `.grok` layout paths alone (D-PLUGIN).
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell leader -- --nocapture && cargo check -p xai-grok-workspace -p xai-grok-sandbox</automated>
  </verify>
  <done>
    Managed bin is `bum`, leader hard-coded tests match, workspace fallback is `.bum`, sandbox docs match SoT.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Updater managed bin leaf bin/bum + install tests</name>
  <files>crates/codegen/xai-grok-update/src/auto_update.rs, crates/codegen/xai-grok-update/tests/test_install_internal.rs, crates/codegen/xai-grok-update/tests/test_concurrent_convergence.rs, crates/codegen/xai-grok-update/tests/common/mod.rs</files>
  <behavior>
    - swap_managed_bin_links creates/updates managed leaf bum / bum.exe under product home bin/ (not grok)
    - Install/convergence tests assert product-home bin/bum (no managed bin/grok alias requirement)
    - Activation path that calls swap_managed_bin_links remains under product home (grok_home/BUM_HOME)
  </behavior>
  <action>
    Per D-BIN no-alias and D-WRITERS (Addresses review concern: updater independently installs bin/grok; conflicts with leader bin/bum and no-alias decision):
    1. In `swap_managed_bin_links` (`xai-grok-update/src/auto_update.rs`), change the primary managed executable leaf from `grok`/`grok.exe` to `bum`/`bum.exe`. Keep co-managed `agent` leaf behavior if it already exists unless tests require only bum.
    2. Update rustdocs/comments on managed install paths that describe `bin/grok` as the product managed binary under home to `bin/bum`. Do not introduce a compatibility symlink from grok to bum under product home (D-BIN).
    3. Update install tests that assert managed link layout: `tests/test_install_internal.rs`, `tests/test_concurrent_convergence.rs`, and helpers in `tests/common/mod.rs` (and any sibling update test that asserts `bin/grok` as the managed product leaf). Expected managed path becomes `bin/bum`.
    4. If `test_home()` still sets GROK_HOME, switch product-home env to BUM_HOME here if not already done by plan 01-04 (safe either way; product-home key must be BUM_HOME before resolve). Prefer not leaving update tests on GROK_HOME after this task.
    5. Download artifact filename prefixes that are internal (e.g. versioned download stems) may keep historical naming if not user-facing ship name — but the **installed managed command under product home** must be `bum`. System-wide `/usr/local/bin/grok` install-channel polish is Phase 8 unless the same function blindly recreates only stock names under product home.
    6. Do not implement Phase 8 auto-update kill (D-SCOPE); only rename managed leaf for identity consistency.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-update --test test_install_internal -- --nocapture && cargo test -p xai-grok-update --test test_concurrent_convergence -- --nocapture</automated>
  </verify>
  <done>
    Updater provisions `bin/bum` under product home; install tests green; no managed `bin/grok` alias required for activation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Twin resolver → worktree DB | Alternate code path that could write worktrees outside bum home |
| Managed bin under home | Leader/updater may prefer `$HOME_PRODUCT/bin/bum` when current exe is under product home |
| Updater activation → bin links | Wrong leaf installs stock-named binary beside bum identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | `resolve_grok_home` twin | high | mitigate | Same env+default as config SoT; no GROK_HOME honor; platform home input aligned/documented |
| T-01-05 | Information Disclosure | workspace fallback `.grok` | medium | mitigate | Fallback join `.bum` so resolve failure cannot create stock tree |
| T-01-12 | Spoofing | updater `swap_managed_bin_links` | high | mitigate | Managed leaf `bum` only; no grok alias under product home (D-BIN) |
| T-01-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- `cargo test -p xai-fast-worktree --lib`
- `cargo test -p xai-grok-shell leader && cargo check -p xai-grok-workspace -p xai-grok-sandbox`
- `cargo test -p xai-grok-update --test test_install_internal && cargo test -p xai-grok-update --test test_concurrent_convergence`
</verification>

<success_criteria>
- Twin + fixture + workspace fallback + managed leaf + updater all target bum product home / bin/bum
- No production twin path still defaults to `.grok` or reads GROK_HOME as product home
- No live updater path still installs managed bin/grok as the product command
</success_criteria>

<output>
Create `.planning/phases/01-product-identity-isolated-home/01-02-SUMMARY.md` when done
</output>
