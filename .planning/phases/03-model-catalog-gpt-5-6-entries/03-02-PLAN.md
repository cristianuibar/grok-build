---
phase: 03-model-catalog-gpt-5-6-entries
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/agent/models.rs
  - crates/codegen/xai-grok-shell/tests/model_catalog.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "After remote prefetch replaces the catalog, bundled provider=codex defaults reappear when not on a custom models endpoint"
    - "Prefetch still prunes bundled xAI defaults not present in the remote map (enterprise/xAI list intent preserved)"
    - "Custom models endpoint (has_custom_endpoint) does not inject bundled GPT defaults"
    - "Empty Some(prefetched) still receives Codex bundled defaults when !has_custom_endpoint"
    - "GPT rows remain visible for both xAI session auth and API-key auth (supported_in_api); never filtered by Codex credential presence"
    - "Logged-in mixed list still contains Grok remote rows plus GPT family in one flat list"
    - "Prefetch/visibility proofs run via --test model_catalog (not shell --lib)"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "resolve_model_list Codex union-append after prefetch replace"
      contains: ModelProvider::Codex
    - path: crates/codegen/xai-grok-shell/tests/model_catalog.rs
      provides: "prefetch survival + dual-auth visibility integration tests"
      contains: codex_defaults_survive_prefetch
  key_links:
    - from: resolve_model_list prefetched branch
      to: default_model_entries Codex rows
      via: "after resolved = prefetched, if !has_custom_endpoint insert missing provider=codex bundled keys"
      pattern: "has_custom_endpoint|ModelProvider::Codex|prefetched"
    - from: available_models
      to: visible_for_auth
      via: "xAI session vs API-key only — no providers.codex gate"
      pattern: "visible_for_auth|supported_in_api|available_models"
---

## Phase Goal

**As a** bum user logged into xAI (remote model prefetch active), **I want to** still see GPT-5.6 Sol/Terra/Luna next to remote Grok models in one list, **so that** the mixed catalog is real on the normal daily path—not only offline defaults.

<objective>
Change `resolve_model_list` so remote prefetch no longer erases bundled Codex defaults: after `resolved = prefetched`, re-append bundled entries with `provider == Codex` when `!endpoints.has_custom_endpoint()` and the key is missing (per D-03, D-09, D-11, D-12, D-15 and RESEARCH critical merge risk). Extend Plan 01 `tests/model_catalog.rs` integration tests for prune/visibility; prove GPT visibility is independent of Codex login. Empty `Some({})` still injects Codex when `!has_custom_endpoint` (RESEARCH Q1 RESOLVED).

Purpose: Plan 01 data alone disappears on the normal logged-in prefetch path today; this plan is the MOD-01/02 production correctness fix.
Output: Prefetch-survival + enterprise-skip + auth-visibility filters green under `--test model_catalog`; no routing/OAuth; no shell `--lib` repair.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-CONTEXT.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-01-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/agent/models.rs
@crates/codegen/xai-grok-shell/tests/model_catalog.rs
</context>

## Locked decisions covered

D-03 (same SoT defaults must survive merge), D-09 (flat mixed list), D-11 (order: remote/xAI first then append Sol→Terra→Luna), D-12 (no Codex-login filter), D-14 (catalog only), D-15 (automated proofs).

**Research-critical:** Do not full-union all bundled defaults after prefetch — only `provider=codex` rows. Preserve enterprise `has_custom_endpoint()` skip of built-ins.

**Q1 RESOLVED:** Empty `Some(IndexMap::new())` still gets Codex re-insert when `!has_custom_endpoint`. Encode as `empty_prefetch_still_gets_codex_defaults` in `model_catalog.rs` (do not keep empty-base expectation as the phase gate).

**Harness:** All verifies use `cargo test -p xai-grok-shell --test model_catalog …`. Do not use shell `--lib`. Do not rewrite remote xAI names (Q2 RESOLVED — Plan 01 leaves remote names as returned).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED prefetch-survival + enterprise + visibility tests in model_catalog</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs (Plan 01 Wave 0 harness)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_model_list` ~3107–3226, `has_custom_endpoint`, public `ModelEntry` / `ModelInfo` / `Config`)
    - crates/codegen/xai-grok-shell/src/agent/models.rs (`pub fn available_models`)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md (Pitfall 1, Pattern 2, Q1 RESOLVED)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-01-SUMMARY.md (if present — provider field and GPT keys)
  </read_first>
  <behavior>
    - codex_defaults_survive_prefetch: prefetched map with only a remote xAI model (build via ModelEntry::fallback + public field mutation; key e.g. secret-xyz or remote-only slug) still yields gpt-5.6-sol/terra/luna with provider Codex after resolve_model_list; grok-build absent if not in prefetched — D-03/D-09
    - prefetch_still_prunes_bundled_xai: prefetched-only remote entry does not resurrect grok-build when it was only a bundled xAI default — preserve prune intent for xAI
    - custom_endpoint_skips_codex_inject: Config with endpoints.models_base_url or models_list_url set (has_custom_endpoint true) and empty/prefetched remote does not insert gpt-5.6-* from bundles
    - empty_prefetch_still_gets_codex_defaults: resolve_model_list(cfg, Some(empty IndexMap)) with default endpoints contains the three GPT keys (Q1 RESOLVED)
    - gpt_visible_session_and_api_key: for resolved catalog, available_models(..., is_session_auth=true) and false both include gpt-5.6-sol (supported_in_api true); do not consult Codex auth slot — D-12
    - mixed_list_with_prefetch: prefetched grok-build-like entry + Codex append → flat list contains both remote key and GPT keys; GPT keys appear after prefetched entries (append order)
  </behavior>
  <action>
    Extend the Plan 01 integration binary only (D-15, RESEARCH Q3). Do **not** add Phase 3 gates on config.rs/models.rs `#[cfg(test)]` modules or `cargo test --lib`.

    1. Add the behavior-named tests to `tests/model_catalog.rs`. Build prefetched entries with public APIs: `ModelEntry::fallback(slug, &cfg.endpoints)` then set `entry.info.name` / `entry.info.provider` (ModelProvider::Xai) / supported_in_api as needed. No private `prefetch_model_entry`.
    2. For custom endpoint: `let mut cfg = Config::default(); cfg.endpoints.models_base_url = Some("https://enterprise.example/v1".into());` then resolve with empty or remote-only prefetched map.
    3. For empty prefetch (Q1): assert GPT keys present when `!has_custom_endpoint`.
    4. For visibility: call `xai_grok_shell::agent::models::available_models(&catalog, true/false)` and assert gpt-5.6-sol present both ways; assert grok-build (supported_in_api false) remains session-only if present in catalog.
    5. Explicitly assert no AuthManager / providers.codex involvement (pure catalog functions only).
    6. Do not implement union-append in this task if practicing strict RED; survival tests must fail until Task 2. No `#[ignore]`.
  </action>
  <acceptance_criteria>
    - New tests in model_catalog.rs encode prefetch survival, xAI prune, custom endpoint skip, empty prefetch Codex inject, dual auth visibility
    - Survival tests RED until Task 2 implements merge
    - No ignored tests for these contracts; no shell --lib verify
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog codex_defaults_survive_prefetch -- --nocapture 2>&1 | tail -40</automated>
  </verify>
  <done>
    Prefetch-survival and visibility contracts are automated in the integration harness and RED pending merge implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Union-append Codex bundled defaults after prefetch replace</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_model_list` full function, `default_model_entries`, `has_custom_endpoint`)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md (Pattern: Codex union-append)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md (merge sketch, Pitfall 1/6/7)
  </read_first>
  <behavior>
    - After prefetched replace and before config_models loop: if !has_custom_endpoint, for each default_model_entries entry with provider Codex and key missing from resolved, insert entry
    - Append preserves IndexMap order: prefetched keys first, then Sol→Terra→Luna JSON order among missing Codex keys
    - When prefetched is None, existing defaults path unchanged (already includes GPT from Plan 01)
    - When has_custom_endpoint, never inject built-ins (existing skip at top remains authoritative; re-append also gated)
    - Task 1 tests GREEN; Plan 01 no-prefetch tests stay GREEN
  </behavior>
  <action>
    Implement the RESEARCH Pattern 2 merge layer only (D-03, D-09, D-11).

    1. In `resolve_model_list`, immediately after the block that assigns `resolved = prefetched` (and still inside/after the `if let Some(prefetched)` handling so it runs whenever a prefetched map was applied), and **before** the `config_models` override loop, add: if `!cfg.endpoints.has_custom_endpoint()`, iterate `default_model_entries(&cfg.endpoints)` and `insert` each entry where `entry.info.provider == ModelProvider::Codex` and `!resolved.contains_key(&key)`.
    2. Do **not** re-insert provider=Xai bundled defaults after prefetch (preserves remote prune of grok-build etc.).
    3. Keep the initial `has_custom_endpoint` skip of loading defaults at the top of the function; double-gate re-append with the same condition so enterprise custom endpoints never receive bundled GPT.
    4. Prefer matching on `ModelProvider::Codex` (explicit field), never `agent_type == "codex"` (D-05).
    5. Green all Task 1 tests and re-run Plan 01 filters under the integration binary (`catalog_includes_gpt56`, `mixed_catalog_order`). Do not run shell `--lib`.
    6. No CLI, no ACP meta, no routing, no Codex OAuth, no picker chrome (D-14). Do not rewrite remote-prefetched Grok display names (Q2 RESOLVED).
  </action>
  <acceptance_criteria>
    - codex_defaults_survive_prefetch and related model_catalog tests GREEN
    - grok-build still pruned when not in prefetched
    - custom endpoint tests prove no GPT inject
    - empty prefetch yields GPT rows when !custom_endpoint (Q1)
    - cargo test -p xai-grok-shell --test model_catalog filters for survival + catalog_includes pass
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog resolve_model_list codex_defaults_survive_prefetch catalog_includes_gpt56 empty_prefetch -- --nocapture 2>&1 | tail -60</automated>
  </verify>
  <done>
    Prefetch path preserves mixed catalog; enterprise custom endpoint remains clean; automated proofs green via integration tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: available_models dual-auth visibility proof for GPT</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs, crates/codegen/xai-grok-shell/src/agent/models.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/models.rs (`pub fn available_models`, `visible_for_auth` usage)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`ModelInfo::visible_for_auth`)
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs
  </read_first>
  <behavior>
    - Building catalog via resolve_model_list(None) then available_models for is_session_auth true and false includes all three GPT ids
    - grok-build (supported_in_api false) remains session-only as today
    - No code path in available_models checks Codex credentials / providers.codex
  </behavior>
  <action>
    Finish D-12 / D-15 visibility proofs in the integration harness. If Task 1 already added green tests, only fix failures. Otherwise add `gpt_visible_*` tests that resolve the real default catalog (or a minimal IndexMap with grok supported_in_api false + GPT true) and assert GPT keys present for both auth flags while grok is session-only.

    1. Do **not** add any Codex-login gate to `available_models` or `resolve_model_catalog` (Phase 6).
    2. Grep `available_models` body to confirm no auth-slot provider checks were introduced.
    3. Gate: `cargo test -p xai-grok-shell --test model_catalog gpt_visible -- --nocapture` only — not shell `--lib`.
  </action>
  <acceptance_criteria>
    - GPT visible for session and API-key auth paths
    - No Codex credential filtering in list path
    - model_catalog gpt_visible filters green
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog available_models gpt_visible -- --nocapture 2>&1 | tail -50</automated>
  </verify>
  <done>
    Catalog visibility matches Phase 3 rules: GPT always listable regardless of Codex login; xAI oauth-only flags still apply to Grok-style rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote prefetch map → merged catalog | Network/cache untrusted relative to bundled Codex binding |
| Enterprise custom models endpoint → catalog | Org-controlled list; must not force-feed client bundles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | resolve_model_list re-append | high | mitigate | Only re-insert bundled defaults with provider=Codex from embedded JSON; never re-label remote rows as codex |
| T-03-06 | Elevation of privilege | has_custom_endpoint path | medium | mitigate | Gate re-append on !has_custom_endpoint so enterprise-only catalogs stay enterprise-only |
| T-03-07 | Information disclosure | available_models | low | accept | Listing models without Codex login is intentional Phase 3 product behavior (D-12); gate deferred to Phase 6 |
| T-03-SC | Tampering | package installs | low | accept | No new dependencies |
</threat_model>

<verification>
- Prefetch survival tests green under `--test model_catalog`
- Prune of bundled xAI defaults still green
- Custom endpoint skip green
- Dual-auth GPT visibility green
- Plan 01 no-prefetch filters still green on same binary
- No shell --lib gate
</verification>

<success_criteria>
- Normal prefetch path shows mixed Grok+GPT catalog
- Enterprise custom endpoint does not receive forced GPT bundles
- Empty Some({}) still injects Codex when !has_custom_endpoint (Q1)
- GPT list visibility independent of Codex OAuth
- No routing or OAuth implementation; no remote name rewrite (Q2); no lib-suite repair (Q3)
</success_criteria>

<output>
Create `.planning/phases/03-model-catalog-gpt-5-6-entries/03-02-SUMMARY.md` when done
</output>
