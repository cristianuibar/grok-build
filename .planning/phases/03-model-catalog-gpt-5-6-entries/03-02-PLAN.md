---
phase: 03-model-catalog-gpt-5-6-entries
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/tests/model_catalog.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "After remote prefetch replaces the catalog, bundled provider=codex defaults reappear when not on a custom models endpoint"
    - "When prefetched already contains a canonical Codex bundled id (gpt-5.6-sol/terra/luna), bundled provider=codex and UI-SPEC name win over remote rebinding to xai"
    - "Prefetch still prunes bundled xAI defaults not present in the remote map (enterprise/xAI list intent preserved)"
    - "Custom models endpoint (has_custom_endpoint) does not inject bundled GPT defaults"
    - "Empty Some(prefetched) still receives Codex bundled defaults when !has_custom_endpoint"
    - "GPT rows remain visible for both xAI session auth and API-key auth (supported_in_api); never filtered by Codex credential presence"
    - "Logged-in mixed list still contains Grok remote rows plus GPT family in one flat list"
    - "Prefetch/visibility proofs run via --test model_catalog (not shell --lib)"
    - "Automated cargo gates never pipe through bare tail; one TESTNAME filter per cargo invocation"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "resolve_model_list Codex union-append + collision preference for bundled Codex ids"
      contains: ModelProvider::Codex
    - path: crates/codegen/xai-grok-shell/tests/model_catalog.rs
      provides: "prefetch survival + collision + dual-auth visibility integration tests"
      contains: prefetched_collision_cannot_rebind_codex_default_to_xai
  key_links:
    - from: resolve_model_list prefetched branch
      to: default_model_entries Codex rows
      via: "after resolved = prefetched, if !has_custom_endpoint insert or replace missing/colliding provider=codex bundled keys with bundled entry authoritative"
      pattern: "has_custom_endpoint|ModelProvider::Codex|prefetched"
    - from: available_models
      to: visible_for_auth
      via: "xAI session vs API-key only — no providers.codex gate"
      pattern: "visible_for_auth|supported_in_api|available_models"
---

## Phase Goal

**As a** bum user logged into xAI (remote model prefetch active), **I want to** still see GPT-5.6 Sol/Terra/Luna next to remote Grok models in one list, **so that** the mixed catalog is real on the normal daily path—not only offline defaults.

<objective>
Change `resolve_model_list` so remote prefetch no longer erases bundled Codex defaults: after `resolved = prefetched`, re-append (or **replace on collision**) bundled entries with `provider == Codex` when `!endpoints.has_custom_endpoint()` (per D-03, D-09, D-11, D-12, D-15 and RESEARCH critical merge risk). **Collision policy (review MEDIUM):** when prefetched already has key `gpt-5.6-sol` / `gpt-5.6-terra` / `gpt-5.6-luna`, the **bundled** entry is authoritative for `provider=codex` and UI-SPEC name/description — remote cannot rebind that default to xai. Extend Plan 01 `tests/model_catalog.rs` for prune/visibility/collision; prove GPT visibility is independent of Codex login. Empty `Some({})` still injects Codex when `!has_custom_endpoint` (RESEARCH Q1 RESOLVED).

Addresses review concern: union-append collision rebinds; add `prefetched_collision_cannot_rebind_codex_default_to_xai`. Addresses review concern: false-green multi-filter / `| tail` cargo gates.

Purpose: Plan 01 data alone disappears on the normal logged-in prefetch path today; this plan is the MOD-01/02 production correctness fix.
Output: Prefetch-survival + collision + enterprise-skip + auth-visibility filters green under `--test model_catalog`; no routing/OAuth; no shell `--lib` repair.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-CONTEXT.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-01-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/agent/models.rs
@crates/codegen/xai-grok-shell/tests/model_catalog.rs
</context>

## Locked decisions covered

D-03 (same SoT defaults must survive merge), D-09 (flat mixed list), D-11 (order: remote/xAI first then append Sol→Terra→Luna when missing), D-12 (no Codex-login filter), D-14 (catalog only), D-15 (automated proofs).

**Research-critical:** Do not full-union all bundled defaults after prefetch — only `provider=codex` rows. Preserve enterprise `has_custom_endpoint()` skip of built-ins.

**Collision policy (review):** Bundled Codex defaults are authoritative for their three wire ids. After prefetch replace, for each bundled entry with `provider == Codex`: if key missing → insert; if key present → **replace** with the bundled entry (provider codex + UI-SPEC name/description). Do not leave a remote row that forced `provider=Xai` on `gpt-5.6-sol` (etc.) as the winner.

**Q1 RESOLVED:** Empty `Some(IndexMap::new())` still gets Codex re-insert when `!has_custom_endpoint`. Encode as `empty_prefetch_still_gets_codex_defaults`.

**Harness:** All verifies use `cargo test -p xai-grok-shell --test model_catalog …`. One TESTNAME filter per cargo invocation or full binary. No bare `| tail`. Do not use shell `--lib`. Do not rewrite remote xAI names for non-Codex keys (Q2 RESOLVED — Plan 01 leaves remote names as returned for ordinary Grok remotes).

**File ownership note:** Only `config.rs` (merge logic) and `tests/model_catalog.rs` are modified. `models.rs` is read-only inspection for `available_models` unless a bug forces a one-line fix (prefer zero change).

## Cargo verify hygiene (mandatory — review HIGH)

- One TESTNAME filter per `cargo test`, or run the full binary without a filter.
- Chain with `&&` only. Never bare `| tail` without pipefail.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED prefetch-survival + collision + enterprise + visibility tests in model_catalog</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs (Plan 01 Wave 0 harness)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_model_list` ~3107–3226, `has_custom_endpoint`, public `ModelEntry` / `ModelInfo` / `Config` / `ModelProvider`)
    - crates/codegen/xai-grok-shell/src/agent/models.rs (`pub fn available_models`) — inspection only
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md (Pitfall 1, Pattern 2, Q1 RESOLVED)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md (collision policy)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-01-SUMMARY.md (if present — provider field and GPT keys)
  </read_first>
  <behavior>
    - codex_defaults_survive_prefetch: prefetched map with only a remote xAI model (build via ModelEntry::fallback + public field mutation; key e.g. secret-xyz) still yields gpt-5.6-sol/terra/luna with provider as_str "codex" after resolve_model_list; grok-build absent if not in prefetched — D-03/D-09
    - prefetch_still_prunes_bundled_xai: prefetched-only remote entry does not resurrect grok-build when it was only a bundled xAI default — preserve prune intent for xAI
    - custom_endpoint_skips_codex_inject: Config with endpoints.models_base_url or models_list_url set (has_custom_endpoint true) and empty/prefetched remote does not insert gpt-5.6-* from bundles
    - empty_prefetch_still_gets_codex_defaults: resolve_model_list(cfg, Some(empty IndexMap)) with default endpoints contains the three GPT keys (Q1 RESOLVED)
    - prefetched_collision_cannot_rebind_codex_default_to_xai: prefetched map contains gpt-5.6-sol entry with provider Xai and a non-UI-SPEC name (e.g. "Hijacked Sol"); after resolve, that key has as_str "codex" and UI-SPEC name GPT-5.6 Sol (Codex) — bundled authoritative
    - gpt_visible_session_and_api_key: for resolved catalog, available_models(..., is_session_auth=true) and false both include gpt-5.6-sol (supported_in_api true); do not consult Codex auth slot — D-12
    - mixed_list_with_prefetch: prefetched grok-build-like entry + Codex append → flat list contains both remote key and GPT keys; GPT keys appear after prefetched entries when they were missing (append order)
  </behavior>
  <action>
    Extend the Plan 01 integration binary only (D-15, RESEARCH Q3). Do **not** add Phase 3 gates on config.rs/models.rs `#[cfg(test)]` modules or `cargo test --lib`. Addresses review concern: collision test.

    1. Add the behavior-named tests to `tests/model_catalog.rs`. Build prefetched entries with public APIs: `ModelEntry::fallback(slug, &cfg.endpoints)` then set `entry.info.name` / `entry.info.provider` (ModelProvider::Xai) / supported_in_api as needed. No private `prefetch_model_entry`. Assert providers with string literals `"xai"` / `"codex"` via `as_str()`.
    2. For collision: insert prefetched `gpt-5.6-sol` with `ModelProvider::Xai` and a hijacked name; assert after resolve provider is codex and name matches UI-SPEC.
    3. For custom endpoint: `let mut cfg = Config::default(); cfg.endpoints.models_base_url = Some("https://enterprise.example/v1".into());` then resolve with empty or remote-only prefetched map.
    4. For empty prefetch (Q1): assert GPT keys present when `!has_custom_endpoint`.
    5. For visibility: call `xai_grok_shell::agent::models::available_models(&catalog, true/false)` and assert gpt-5.6-sol present both ways.
    6. Explicitly assert no AuthManager / providers.codex involvement (pure catalog functions only).
    7. Do not implement union-append/collision in this task if practicing strict RED; survival + collision tests must fail until Task 2. No `#[ignore]`.
  </action>
  <acceptance_criteria>
    - New tests in model_catalog.rs encode prefetch survival, collision authority, xAI prune, custom endpoint skip, empty prefetch Codex inject, dual auth visibility
    - Survival and collision tests RED until Task 2 implements merge
    - No ignored tests for these contracts; no shell --lib verify
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog codex_defaults_survive_prefetch -- --nocapture</automated>
  </verify>
  <done>
    Prefetch-survival, collision, and visibility contracts are automated in the integration harness and RED pending merge implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Union-append Codex bundled defaults with collision authority after prefetch replace</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`resolve_model_list` full function, `default_model_entries`, `has_custom_endpoint`)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md (Pattern: Codex union-append)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md (merge sketch, Pitfall 1/6/7)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md (collision policy)
  </read_first>
  <behavior>
    - After prefetched replace and before config_models loop: if !has_custom_endpoint, for each default_model_entries entry with provider Codex: if key missing insert; if key present replace with bundled entry (bundled authoritative for provider + name)
    - Append preserves IndexMap order for newly inserted keys: prefetched keys first, then Sol→Terra→Luna JSON order among missing Codex keys; replaced keys keep their existing map position
    - When prefetched is None, existing defaults path unchanged (already includes GPT from Plan 01)
    - When has_custom_endpoint, never inject or replace with built-ins (existing skip at top remains authoritative; re-append also gated)
    - Task 1 tests GREEN including prefetched_collision_cannot_rebind_codex_default_to_xai; Plan 01 no-prefetch tests stay GREEN
  </behavior>
  <action>
    Implement the RESEARCH Pattern 2 merge layer with collision authority (D-03, D-09, D-11). Addresses review concern: prefetched collision cannot rebind Codex defaults to xai.

    1. In `resolve_model_list`, immediately after the block that assigns `resolved = prefetched` (and still inside/after the `if let Some(prefetched)` handling so it runs whenever a prefetched map was applied), and **before** the `config_models` override loop, add: if `!cfg.endpoints.has_custom_endpoint()`, iterate `default_model_entries(&cfg.endpoints)` for each entry where `entry.info.provider == ModelProvider::Codex`:
       - if `!resolved.contains_key(&key)` → `insert` bundled entry
       - if key present → `insert` (IndexMap replace) bundled entry so provider, name, description come from ship SoT
    2. Do **not** re-insert or force provider=Xai bundled defaults after prefetch (preserves remote prune of grok-build etc.).
    3. Keep the initial `has_custom_endpoint` skip of loading defaults at the top of the function; double-gate re-append with the same condition so enterprise custom endpoints never receive bundled GPT.
    4. Prefer matching on `ModelProvider::Codex` (explicit field), never `agent_type == "codex"` (D-05).
    5. Green all Task 1 tests. Prefer full binary: `cargo test -p xai-grok-shell --test model_catalog -- --nocapture`. Do not run shell `--lib`.
    6. No CLI, no ACP meta, no routing, no Codex OAuth, no picker chrome (D-14). Do not rewrite remote-prefetched Grok display names for non-Codex keys (Q2 RESOLVED).
  </action>
  <acceptance_criteria>
    - codex_defaults_survive_prefetch and prefetched_collision_cannot_rebind_codex_default_to_xai GREEN
    - grok-build still pruned when not in prefetched
    - custom endpoint tests prove no GPT inject
    - empty prefetch yields GPT rows when !custom_endpoint (Q1)
    - Full model_catalog binary green for survival + Plan 01 no-prefetch tests
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog -- --nocapture</automated>
  </verify>
  <done>
    Prefetch path preserves mixed catalog with bundled Codex authority on collisions; enterprise custom endpoint remains clean; automated proofs green via integration tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: available_models dual-auth visibility proof for GPT</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/models.rs (`pub fn available_models`, `visible_for_auth` usage) — inspection only
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`ModelInfo::visible_for_auth`)
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs
  </read_first>
  <behavior>
    - Building catalog via resolve_model_list(None) then available_models for is_session_auth true and false includes all three GPT ids
    - grok-build (supported_in_api false) remains session-only as today
    - No code path in available_models checks Codex credentials / providers.codex
  </behavior>
  <action>
    Finish D-12 / D-15 visibility proofs in the integration harness. If Task 1 already added green tests, only fix failures. Otherwise add `gpt_visible_*` tests that resolve the real default catalog (or a minimal IndexMap with grok supported_in_api false + GPT true) and assert GPT keys present for both auth flags while grok is session-only.

    1. Do **not** add any Codex-login gate to `available_models` or `resolve_model_catalog` (Phase 6).
    2. Grep `available_models` body to confirm no auth-slot provider checks were introduced (inspection only — do not edit models.rs unless a regression was introduced; prefer zero production change).
    3. Gate: single filter or full binary — `cargo test -p xai-grok-shell --test model_catalog gpt_visible -- --nocapture` only — not shell `--lib`.
  </action>
  <acceptance_criteria>
    - GPT visible for session and API-key auth paths
    - No Codex credential filtering in list path
    - model_catalog gpt_visible filters green
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog gpt_visible -- --nocapture</automated>
  </verify>
  <done>
    Catalog visibility matches Phase 3 rules: GPT always listable regardless of Codex login; xAI oauth-only flags still apply to Grok-style rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote prefetch map → merged catalog | Network/cache untrusted relative to bundled Codex binding |
| Enterprise custom models endpoint → catalog | Org-controlled list; must not force-feed client bundles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | resolve_model_list re-append | high | mitigate | Only re-insert/replace bundled defaults with provider=Codex from embedded JSON; never re-label arbitrary remote non-Codex keys as codex |
| T-03-05b | Spoofing | remote collision on gpt-5.6-* ids | high | mitigate | Bundled Codex entry wins on key collision for the three ship ids (provider+name authoritative) |
| T-03-06 | Elevation of privilege | has_custom_endpoint path | medium | mitigate | Gate re-append on !has_custom_endpoint so enterprise-only catalogs stay enterprise-only |
| T-03-07 | Information disclosure | available_models | low | accept | Listing models without Codex login is intentional Phase 3 product behavior (D-12); gate deferred to Phase 6 |
| T-03-SC | Tampering | package installs | low | accept | No new dependencies |
</threat_model>

<verification>
- Prefetch survival + collision tests green under `--test model_catalog`
- Prune of bundled xAI defaults still green
- Custom endpoint skip green
- Dual-auth GPT visibility green
- Plan 01 no-prefetch filters still green on same binary
- No shell --lib gate; no bare `| tail`
</verification>

<success_criteria>
- Normal prefetch path shows mixed Grok+GPT catalog
- Prefetch collision on gpt-5.6-* cannot rebind to xai
- Enterprise custom endpoint does not receive forced GPT bundles
- Empty Some({}) still injects Codex when !has_custom_endpoint (Q1)
- GPT list visibility independent of Codex OAuth
- No routing or OAuth implementation; no remote name rewrite for ordinary Grok keys (Q2); no lib-suite repair (Q3)
</success_criteria>

<output>
Create `.planning/phases/03-model-catalog-gpt-5-6-entries/03-02-SUMMARY.md` when done
</output>
