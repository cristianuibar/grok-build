---
phase: 03-model-catalog-gpt-5-6-entries
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-pager/src/models.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "ACP ModelInfo projection includes catalog name with provider suffix for every entry"
    - "ACP meta includes provider key with wire value xai or codex alongside existing agentType"
    - "bum models CLI prints Default model header plus rows as star/dash id (name) with provider-bearing name"
    - "CLI does not hide GPT rows based on Codex login status"
    - "Slash /model and settings DynamicEnum inherit provider-labeled names without structural picker changes"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "to_acp_model_info meta.provider"
      contains: "\"provider\""
    - path: crates/codegen/xai-grok-pager/src/models.rs
      provides: "CLI list format id (name) per UI-SPEC"
      contains: available_models
  key_links:
    - from: ModelInfo.provider
      to: acp::ModelInfo.meta
      via: "to_acp_model_info inserts meta provider string from as_str()"
      pattern: "to_acp_model_info|provider|agentType"
    - from: list_models ACP state
      to: bum models stdout
      via: "pager models.rs prints model_id and name"
      pattern: "list_available_models|model_id|name"
---

## Phase Goal

**As a** bum user, **I want to** see provider labels in every model list surface (TUI name, CLI list, ACP meta), **so that** I can tell which backend owns each row before Phase 4 routing exists.

<objective>
Project provider-aware labels to consumers: ACP `to_acp_model_info` keeps UI-SPEC names and adds `meta.provider`; `bum models` CLI prints `*|{id} ({name})` / `-{id} ({name})` per UI-SPEC (D-10, D-15). Confirm slash/settings inherit names with no structural picker change.

Purpose: Plan 01–02 make catalog correct; this plan makes labels observable on all Phase 3 surfaces.
Output: ACP meta + CLI format tests green; inherit-only surfaces documented/verified by unit or compile-level checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-01-PLAN.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-02-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-pager/src/models.rs
@crates/codegen/xai-grok-pager/src/slash/commands/model.rs
@crates/codegen/xai-grok-shell/src/cli_models.rs
</context>

## Locked decisions covered

D-10 (name suffix as visible provider label), D-12 (no Codex-login hide), D-15 (tests), UI-SPEC CLI copy table.

**Discretion:** Optional pure format helper in pager `models.rs` (or small free function) for unit testing without spawning shell; ACP meta key is string `"provider"` with values from `ModelProvider::as_str()`; UI must not depend on meta alone — name carries the label.

**Inherit-only (no structural change):** `slash/commands/model.rs` `build_model_items` already uses `info.name`; settings DynamicEnum uses active catalog names; `views/picker.rs` unchanged.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ACP to_acp_model_info meta.provider + name pass-through tests</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`to_acp_model_info` ~4788–4837, any existing agentType meta tests)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (ACP ModelInfo section)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md (to_acp_model_info meta.provider pattern)
  </read_first>
  <behavior>
    - to_acp_model_info_sets_meta_provider: for a catalog entry with provider Codex and name GPT-5.6 Sol (Codex), ACP ModelInfo name equals that string and meta map contains provider = "codex"
    - to_acp_model_info_provider_xai: xAI entry meta provider = "xai" and agentType meta still present (distinct fields)
    - Name still falls back to model id when name is None
  </behavior>
  <action>
    1. Add unit tests around `to_acp_model_info` that build a small IndexMap of ModelEntry with known ModelInfo provider/name (use fallback or test helpers; set provider explicitly).
    2. Implement: in `to_acp_model_info`, after inserting agentType, insert meta key provider with `serde_json::Value::String(info.provider.as_str().to_owned())` (D-10 machine surface). Keep name projection as `info.name.clone().unwrap_or_else(|| info.model.clone())` — do not synthesize suffixes in ACP if name already set in catalog.
    3. Do not remove or rename agentType; provider is additional.
    4. No routing based on meta; no picker changes.
  </action>
  <acceptance_criteria>
    - meta.provider present with xai/codex wire values
    - agentType still present
    - name carries UI-SPEC suffix when catalog provides it
    - cargo test filter for to_acp / meta provider green
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib to_acp_model_info -- --nocapture 2>&1 | tail -40</automated>
  </verify>
  <done>
    ACP projection exposes provider binding in meta and provider-labeled names for TUI consumers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bum models CLI prints id (name) per UI-SPEC</name>
  <files>crates/codegen/xai-grok-pager/src/models.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/models.rs (full file)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (CLI rows: Default model, Available models, star/dash id (name))
    - crates/codegen/xai-grok-shell/src/cli_models.rs (list_models return shape / available model fields)
    - agent-client-protocol ModelInfo name field usage in list state
  </read_first>
  <behavior>
    - format_model_list_line (or equivalent pure helper): current model → two-space star space id space (name); non-current → two-space dash space id space (name); if name empty/None use id only as name fallback inside parens or skip empty parens — prefer always show (name) with fallback to id string so scripting still sees id first
    - Headers unchanged: Default model: {id} and Available models:
    - Auth preamble lines unchanged; no filter of models by Codex login
    - No extra (default) text suffix on the star line (UI-SPEC uses star marker only; name already carries provider)
  </behavior>
  <action>
    Update CLI list formatting to match UI-SPEC (D-10, MOD-01/02 surface).

    1. Extract a small pure function (e.g. `format_cli_model_row(is_current: bool, id: &str, name: &str) -> String`) in `models.rs` so unit tests do not need to spawn the agent. When name is empty, fall back to id for the parenthetical so rows stay `* {id} ({id})` or simply use name-or-id once — prefer UI-SPEC: always `* {id} ({name})` with name = ACP name or id if blank.
    2. In the print loop over `state.available_models`, use ACP `ModelInfo` name (field path as currently exposed on the list row type — inspect `list_models` / acp ModelInfo). Replace the current id-only / `(default)` suffix pattern.
    3. Keep Default model header as id-only per UI-SPEC.
    4. Add unit tests for the pure formatter covering current vs non-current and name-with-Codex-suffix.
    5. Do not hide GPT rows; do not call Codex auth APIs for filtering (D-12).
    6. Do not restructure slash picker or settings registry — inherit-only.
  </action>
  <acceptance_criteria>
    - CLI formatter tests green for `* id (name)` / `- id (name)`
    - models.rs uses formatter in list_available_models
    - No Codex-login filtering
    - cargo test -p xai-grok-pager --lib models (or relevant filter) passes; if pager lib tests cannot see the helper, `#[cfg(test)]` module in models.rs is enough
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager --lib format_cli_model -- --nocapture 2>&1 | tail -40; cargo check -p xai-grok-pager 2>&1 | tail -20</automated>
  </verify>
  <done>
    bum models CLI shows provider-bearing names next to ids per UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 3: Inherit-surface sanity + full Phase 3 test sweep</name>
  <files>crates/codegen/xai-grok-pager/src/slash/commands/model.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/slash/commands/model.rs (`build_model_items` display = info.name / current suffix)
    - crates/codegen/xai-grok-pager/src/settings/registry.rs (DynamicEnumSource::ActiveModelCatalog if present)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (surfaces table)
  </read_first>
  <action>
    Confirm inherit-only surfaces need no structural change for MOD-01/02 (D-09, D-10).

    1. Read `build_model_items`: display must remain based on `info.name` so UI-SPEC names like Grok Build (xAI) and GPT-5.6 Sol (Codex) appear automatically; current row may append (current) → e.g. Grok Build (xAI) (current). Do not add a separate badge column or provider filter mode.
    2. Only edit slash/settings if a bug prevents name display; prefer zero code change with a short code comment only if necessary — avoid drive-by refactors.
    3. Run the Phase 3 automated sweep:
       - cargo test -p xai-grok-models --lib
       - cargo test -p xai-grok-shell --lib catalog_includes_gpt56 mixed_catalog_order codex_defaults_survive_prefetch provider_default_xai to_acp_model_info resolve_model_list -- --nocapture
       - cargo test -p xai-grok-pager --lib format_cli_model -- --nocapture (or the filter used in Task 2)
    4. Fix any regressions introduced by this plan. Do not implement Phase 4–6 features if tests fail for unrelated harness debt — note in SUMMARY and keep Phase 3 filters green.
  </action>
  <acceptance_criteria>
    - Slash/settings remain flat mixed list via catalog names (no provider mode)
    - Phase 3 targeted test filters green across models, shell, pager
    - SUMMARY notes inherit-only verification
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-models --lib 2>&1 | tail -15; cargo test -p xai-grok-shell --lib catalog_includes_gpt56 codex_defaults_survive_prefetch to_acp_model_info -- --nocapture 2>&1 | tail -40; cargo test -p xai-grok-pager --lib format_cli_model -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    All Phase 3 surfaces expose the mixed provider-labeled catalog; automated proofs green end-to-end for catalog scope.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell catalog → ACP ModelInfo | Trusted in-process projection to TUI/CLI clients |
| CLI stdout | Local user-visible listing; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Information disclosure | CLI / ACP name list | low | accept | Model ids/names are not credentials; intentional list without Codex auth (D-12) |
| T-03-09 | Spoofing | meta.provider | medium | mitigate | meta.provider copied only from in-process ModelInfo.provider set by trusted defaults/override/remote rules — never from client input |
| T-03-SC | Tampering | package installs | low | accept | No new dependencies |
</threat_model>

<verification>
- to_acp_model_info meta.provider tests green
- CLI formatter tests green
- Full Phase 3 filter sweep green
- Grep: no Codex login gate in models.rs list path
</verification>

<success_criteria>
- Users see provider-labeled names on TUI (via name) and CLI (id + name)
- Machine consumers can read meta.provider
- MOD-01 and MOD-02 success criteria satisfied for catalog visibility surfaces
</success_criteria>

<output>
Create `.planning/phases/03-model-catalog-gpt-5-6-entries/03-03-SUMMARY.md` when done
</output>
