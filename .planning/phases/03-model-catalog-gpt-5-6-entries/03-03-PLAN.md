---
phase: 03-model-catalog-gpt-5-6-entries
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/tests/model_catalog.rs
  - crates/codegen/xai-grok-pager/src/models.rs
  - crates/codegen/xai-grok-pager/tests/format_cli_model_row.rs
  - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md
autonomous: false
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "ACP ModelInfo projection includes catalog name with provider suffix for every entry"
    - "ACP meta includes provider key with wire value xai or codex alongside existing agentType"
    - "bum models CLI prints Default model header plus rows as star/dash id (name) with provider-bearing name"
    - "CLI does not hide GPT rows based on Codex login status"
    - "Slash /model and settings DynamicEnum inherit provider-labeled names (descriptions optional/empty OK on settings)"
    - "Pure projection asserts cover ACP/list display names with (xAI)/(Codex) suffixes"
    - "Manual /model + settings checkpoint required in phase gate when interactive confirm is available"
    - "ACP/CLI proofs use --test model_catalog and --test format_cli_model_row (not shell/pager --lib)"
    - "Phase gate automated includes cargo check -p xai-grok-shell and cargo check -p xai-grok-pager"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "to_acp_model_info meta.provider"
      contains: "\"provider\""
    - path: crates/codegen/xai-grok-pager/src/models.rs
      provides: "pub format_cli_model_row + list format id (name) per UI-SPEC"
      contains: format_cli_model_row
    - path: crates/codegen/xai-grok-pager/tests/format_cli_model_row.rs
      provides: "integration tests for pure CLI row formatter"
      contains: format_cli_model_row
    - path: crates/codegen/xai-grok-shell/tests/model_catalog.rs
      provides: "to_acp_model_info meta.provider + ACP/list name suffix projection asserts"
      contains: to_acp_model_info
    - path: .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md
      provides: "settings names-only inherit; empty /model dropdown None authoritative"
      contains: names-only
  key_links:
    - from: ModelInfo.provider
      to: acp::ModelInfo.meta
      via: "to_acp_model_info inserts meta provider string from as_str()"
      pattern: "to_acp_model_info|provider|agentType"
    - from: list_models ACP state
      to: bum models stdout
      via: "pager models.rs prints model_id and name via format_cli_model_row"
      pattern: "list_available_models|format_cli_model_row|model_id|name"
---

## Phase Goal

**As a** bum user, **I want to** see provider labels in every model list surface (TUI name, CLI list, ACP meta), **so that** I can tell which backend owns each row before Phase 4 routing exists.

<objective>
Project provider-aware labels to consumers: ACP `to_acp_model_info` keeps UI-SPEC names and adds `meta.provider`; `bum models` CLI prints `*|{id} ({name})` / `-{id} ({name})` per UI-SPEC (D-10, D-15). Confirm slash/settings inherit **provider-labeled names** (settings descriptions may be empty — UI-SPEC revised names-only). Prove ACP via shell `--test model_catalog`; prove CLI via pager `--test format_cli_model_row` after making `format_cli_model_row` public (RESEARCH Q3). Pure projection asserts for ACP/list names with suffixes are required; interactive `/model` + settings is a **required** human checkpoint (review MEDIUM), not optional.

Addresses review concern: UI-SPEC settings-description vs empty DynamicEnum — revise UI-SPEC to names-only inherit. Addresses review concern: empty-state authority is no-dropdown None for empty catalog. Addresses review concern: selector coverage pure projection + required UAT. Addresses review concern: Task 3 automated includes cargo check shell+pager. Addresses review concern: false-green cargo pipes.

Purpose: Plan 01–02 make catalog correct; this plan makes labels observable on all Phase 3 surfaces.
Output: ACP meta + CLI format integration tests green; inherit surfaces covered by automated name-suffix projection asserts + required manual TUI checkpoint; UI-SPEC aligned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-01-PLAN.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-02-PLAN.md
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/tests/model_catalog.rs
@crates/codegen/xai-grok-pager/src/models.rs
@crates/codegen/xai-grok-pager/src/slash/commands/model.rs
@crates/codegen/xai-grok-shell/src/cli_models.rs
@crates/codegen/xai-grok-pager/tests/selection_model_public_api.rs
</context>

## Locked decisions covered

D-10 (name suffix as visible provider label), D-12 (no Codex-login hide), D-15 (tests), UI-SPEC CLI copy table.

**Discretion:** Pure `format_cli_model_row` must be **`pub`** in pager `models.rs` so integration tests can call it (pager `--lib` is broken — do not repair). ACP meta key is string `"provider"` with values from `ModelProvider::as_str()`; UI must not depend on meta alone — name carries the label.

**Inherit-only (no structural change):** `slash/commands/model.rs` `build_model_items` already uses `info.name`; settings DynamicEnum uses active catalog **names** (descriptions may be empty — documented in UI-SPEC). `views/picker.rs` unchanged.

**UI-SPEC reconciliation (review MEDIUM):** Prefer revise UI-SPEC so Settings inherits **provider-labeled names only** (per-row descriptions empty OK — no large pager data-flow work this phase). Empty catalog: slash `suggest_args` returning `None` (no dropdown) is authoritative; do not require a separate “No models available” empty-state render for `/model` this phase (CLI/settings empty copy remains documentation only / N/A where UI does not show that string).

**Harness (Q3):** Shell ACP tests → `--test model_catalog`. CLI format → `--test format_cli_model_row`. Never shell/pager `--lib` as Phase 3 gate.

**Inspection-only (do not list as modified unless a bug forces an edit):** `slash/commands/model.rs`, settings registry, `cli_models.rs` — read to confirm inherit; prefer zero code change.

## Cargo verify hygiene (mandatory — review HIGH)

- One TESTNAME filter per `cargo test`, or full binary without filter.
- Chain with `&&` only. Never bare `| tail`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ACP to_acp_model_info meta.provider + name pass-through + projection asserts</name>
  <files>crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`pub fn to_acp_model_info` ~4788–4837)
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (ACP ModelInfo section)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md (to_acp_model_info meta.provider pattern)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md (selector coverage / pure projection)
  </read_first>
  <behavior>
    - to_acp_model_info_sets_meta_provider: for a catalog entry with provider Codex and name GPT-5.6 Sol (Codex), ACP ModelInfo name equals that string and meta map contains provider = "codex"
    - to_acp_model_info_provider_xai: xAI entry meta provider = "xai" and agentType meta still present (distinct fields)
    - Name still falls back to model id when name is None
    - acp_list_projection_includes_provider_suffixes: resolve no-prefetch catalog, map each of grok-build and gpt-5.6-sol through to_acp_model_info; ACP names contain substrings (xAI) and (Codex) respectively — pure projection substitute for slash/settings list labels
  </behavior>
  <action>
    1. Add integration tests in `tests/model_catalog.rs` around public `to_acp_model_info`: build a small IndexMap of ModelEntry with known ModelInfo provider/name via public constructors/fields (ModelEntry::fallback + field writes, or from_config_entry). Assert meta provider with string literals `"xai"` / `"codex"`.
    2. Implement: in `to_acp_model_info`, after inserting agentType, insert meta key provider with `serde_json::Value::String(info.provider.as_str().to_owned())` (D-10 machine surface). Keep name projection as `info.name.clone().unwrap_or_else(|| info.model.clone())` — do not synthesize suffixes in ACP if name already set in catalog; do not rewrite remote names (Q2).
    3. Do not remove or rename agentType; provider is additional.
    4. Add `acp_list_projection_includes_provider_suffixes` (or equivalent) that proves the public ACP/list name feed carries UI-SPEC suffixes for default Grok + Sol — this is the required pure projection assert for selector coverage (review MEDIUM). There is no public `build_model_items` API; do not force-export private pager helpers.
    5. No routing based on meta; no picker structure changes; no shell `--lib` tests.
  </action>
  <acceptance_criteria>
    - meta.provider present with xai/codex wire values
    - agentType still present
    - name carries UI-SPEC suffix when catalog provides it
    - pure projection test for (xAI)/(Codex) on default ACP names green
    - cargo test -p xai-grok-shell --test model_catalog to_acp_model_info green (single filter or full binary)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog to_acp_model_info -- --nocapture && cargo test -p xai-grok-shell --test model_catalog acp_list_projection -- --nocapture</automated>
  </verify>
  <done>
    ACP projection exposes provider binding in meta and provider-labeled names; pure projection assert covers selector name feed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pub format_cli_model_row + bum models CLI id (name) + pager integration test</name>
  <files>crates/codegen/xai-grok-pager/src/models.rs, crates/codegen/xai-grok-pager/tests/format_cli_model_row.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/models.rs (full file)
    - crates/codegen/xai-grok-pager/src/lib.rs (`pub mod models`)
    - crates/codegen/xai-grok-pager/tests/selection_model_public_api.rs (public API integration pattern)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (CLI rows: Default model, Available models, star/dash id (name))
    - crates/codegen/xai-grok-shell/src/cli_models.rs (list_models return shape / available model fields) — inspection only
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
  </read_first>
  <behavior>
    - format_cli_model_row(is_current, id, name): current → two-space star space id space (name); non-current → two-space dash space id space (name); empty name falls back to id inside parens so rows stay `  * {id} ({name_or_id})`
    - Headers unchanged: Default model: {id} and Available models:
    - Auth preamble lines unchanged; no filter of models by Codex login
    - No extra (default) text suffix on the star line (UI-SPEC uses star marker only; name already carries provider)
  </behavior>
  <action>
    Update CLI list formatting to match UI-SPEC (D-10, MOD-01/02 surface). Use integration tests — pager `--lib` is broken (Q3). Explicit crate API: `format_cli_model_row` is public acceptance for `xai_grok_pager::models::format_cli_model_row`.

    1. Add **public** pure function `pub fn format_cli_model_row(is_current: bool, id: &str, name: &str) -> String` in `models.rs` (callable as `xai_grok_pager::models::format_cli_model_row` from integration tests). When name is empty, fall back to id for the parenthetical — UI-SPEC: always `  * {id} ({name})` / `  - {id} ({name})` with name = ACP name or id if blank.
    2. In the print loop over `state.available_models`, use ACP ModelInfo name field; call `format_cli_model_row`. Replace the current id-only / parenthetical default-marker pattern.
    3. Keep Default model header as id-only per UI-SPEC.
    4. Create `crates/codegen/xai-grok-pager/tests/format_cli_model_row.rs` with tests for current vs non-current and name-with-Codex-suffix (and empty-name fallback). Pattern after `selection_model_public_api.rs`.
    5. Do not hide GPT rows; do not call Codex auth APIs for filtering (D-12).
    6. Do not restructure slash picker or settings registry — inherit-only.
    7. Do not repair pager `--lib` compile debt; gate only `--test format_cli_model_row` + `cargo check -p xai-grok-pager`.
  </action>
  <acceptance_criteria>
    - format_cli_model_row is pub and used by list_available_models
    - cargo test -p xai-grok-pager --test format_cli_model_row green for star/dash id (name)
    - No Codex-login filtering
    - cargo check -p xai-grok-pager succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager --test format_cli_model_row -- --nocapture && cargo check -p xai-grok-pager</automated>
  </verify>
  <done>
    bum models CLI shows provider-bearing names next to ids per UI-SPEC; proven without pager --lib.
  </done>
</task>

<task type="auto">
  <name>Task 3: UI-SPEC reconcile + inherit confirm + full Phase 3 automated sweep</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs, .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/slash/commands/model.rs (`build_model_items` display = info.name / current suffix) — inspection only
    - crates/codegen/xai-grok-pager/src/settings/registry.rs (DynamicEnumSource::ActiveModelCatalog if present) — inspection only
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (surfaces table + empty-state)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md (phase gate + required manual)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md (settings descriptions + empty-state)
  </read_first>
  <action>
    Align UI-SPEC with implementable inherit surfaces, keep pure projection asserts green, run full automated phase gate including cargo checks (review MEDIUM).

    1. **UI-SPEC patch (required):** Update Settings surface contract to inherit **provider-labeled names only** (per-row descriptions may be empty / N/A on DynamicEnum — no pager description data-flow required this phase). Resolve empty-state: for empty catalog, slash returning no dropdown (`None`) is authoritative; mark contradictory “No models available” empty-state copy as **N/A for slash when suggest_args returns None** (CLI may still print a simple empty list; do not invent new empty-state UI). Document that slash `/model` row descriptions still come from catalog `description` when the arg dropdown is built (existing `build_model_items` path) — settings does not need the same descriptions.
    2. Read `build_model_items`: display must remain based on `info.name` so UI-SPEC names like Grok Build (xAI) and GPT-5.6 Sol (Codex) appear automatically; current row may append (current). Do not add a separate badge column or provider filter mode. Prefer zero production code change in pager slash/settings.
    3. Keep/ensure automated pure projection assert(s) in `model_catalog.rs` for display names including `(xAI)` and `(Codex)` on default Grok/GPT rows via resolve + to_acp (Task 1).
    4. Run the Phase 3 automated sweep (phase gate from 03-VALIDATION.md) — **must include both cargo checks**:
       - `cargo test -p xai-grok-models --lib`
       - `cargo test -p xai-grok-shell --test model_catalog -- --nocapture`
       - `cargo test -p xai-grok-pager --test format_cli_model_row -- --nocapture`
       - `cargo check -p xai-grok-shell`
       - `cargo check -p xai-grok-pager`
    5. Fix any regressions introduced by this plan. Do not implement Phase 4–6 features. Do not repair shell/pager `--lib` debt — note pre-existing harness debt in SUMMARY if encountered; keep Phase 3 integration filters green.
  </action>
  <acceptance_criteria>
    - UI-SPEC states settings names-only inherit; empty slash dropdown None authoritative
    - Slash/settings remain flat mixed list via catalog names (no provider mode); no forced description plumbing
    - model_catalog asserts display names include (xAI) and (Codex) for default Grok/GPT rows
    - Phase 3 full integration suite green including both cargo checks
    - SUMMARY notes inherit-only verification + required manual TUI checkpoint in Task 4
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-models --lib && cargo test -p xai-grok-shell --test model_catalog -- --nocapture && cargo test -p xai-grok-pager --test format_cli_model_row -- --nocapture && cargo check -p xai-grok-shell && cargo check -p xai-grok-pager</automated>
  </verify>
  <done>
    UI-SPEC aligned; all Phase 3 automated surfaces green end-to-end for catalog scope without lib-suite repair.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Required manual /model + settings selector check</name>
  <files></files>
  <what-built>
    Mixed provider-labeled catalog (Plans 01–02), ACP meta.provider, CLI id (name) rows, pure ACP/list name-suffix projection tests. Slash `/model` and Settings Default model DynamicEnum inherit catalog names with no structural picker change (settings descriptions may be empty per revised UI-SPEC).
  </what-built>
  <action>
    Pause for required human confirmation of interactive selector surfaces (review MEDIUM — not optional). Present the how-to-verify steps below; do not implement new TUI code in this checkpoint. Record approval or mismatches in the plan SUMMARY.
  </action>
  <how-to-verify>
    Required phase-gate human check:

    1. Build and launch interactive `bum` (e.g. `cargo run -p xai-grok-pager-bin --bin bum` from repo root with a temp `BUM_HOME` if desired).
    2. Open slash `/model` (or `/m`) arg dropdown. Confirm a single flat list includes at least:
       - `Grok Build (xAI)` (or with ` (current)` if already selected)
       - `GPT-5.6 Sol (Codex)`, `GPT-5.6 Terra (Codex)`, `GPT-5.6 Luna (Codex)`
    3. Confirm no separate provider-mode filter; GPT rows appear even without Codex login.
    4. Open Settings → Default model DynamicEnum (if reachable in this build). Confirm the same provider-suffixed **names** appear. Empty per-row descriptions are OK.
    5. Optional: `bum models` shows `  *`/`  -` rows with `id (name)` including Codex suffixes.

    If interactive TUI cannot be launched in this environment, document blocker in SUMMARY and rely on pure projection asserts — still report the attempt.
  </how-to-verify>
  <verify>
    <human-check>User confirms `/model` and settings Default model show provider-suffixed Grok + GPT names in one flat list without Codex-login filter.</human-check>
  </verify>
  <done>
    Human approved interactive selector names for MOD-01/02 inherit surfaces, or documented environment blocker with pure projection fallback noted in SUMMARY.
  </done>
  <resume-signal>Type "approved" with any notes, or describe mismatches (missing rows, wrong names, provider filter, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell catalog → ACP ModelInfo | Trusted in-process projection to TUI/CLI clients |
| CLI stdout | Local user-visible listing; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Information disclosure | CLI / ACP name list | low | accept | Model ids/names are not credentials; intentional list without Codex auth (D-12) |
| T-03-09 | Spoofing | meta.provider | medium | mitigate | meta.provider copied only from in-process ModelInfo.provider set by trusted defaults/override/remote rules — never from client input |
| T-03-SC | Tampering | package installs | low | accept | No new dependencies |
</threat_model>

<verification>
- to_acp_model_info meta.provider tests green under `--test model_catalog`
- pure ACP list name-suffix projection green
- CLI formatter tests green under `--test format_cli_model_row`
- Full Phase 3 automated sweep green including cargo check shell+pager
- Required human /model + settings checkpoint completed
- Grep: no Codex login gate in pager models.rs list path
- No shell/pager --lib gates; no bare `| tail`
</verification>

<success_criteria>
- Users see provider-labeled names on TUI (via name) and CLI (id + name)
- Machine consumers can read meta.provider
- Inherit surfaces proven by automated pure projection asserts plus required manual checkpoint
- UI-SPEC matches implementable settings names-only + empty dropdown authority
- MOD-01 and MOD-02 success criteria satisfied for catalog visibility surfaces
</success_criteria>

<output>
Create `.planning/phases/03-model-catalog-gpt-5-6-entries/03-03-SUMMARY.md` when done
</output>
