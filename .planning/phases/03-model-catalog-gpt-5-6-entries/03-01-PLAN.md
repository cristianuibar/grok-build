---
phase: 03-model-catalog-gpt-5-6-entries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-models/default_models.json
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/agent/models.rs
  - crates/codegen/xai-grok-shell/src/remote/client.rs
  - crates/codegen/xai-grok-shell/tests/model_catalog.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "Embedded default catalog includes grok-build plus gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna"
    - "Every shipped default entry has explicit provider binding xai or codex (not inferred from agent_type)"
    - "Grok entry is provider xai with display name Grok Build (xAI); GPT family is provider codex with UI-SPEC Codex-suffixed names"
    - "Default model id remains grok-build"
    - "Missing provider on parse defaults to xai for backward compatibility"
    - "No-prefetch resolve_model_list order is Grok then Sol then Terra then Luna"
    - "GPT agent_type remains stock default (not codex harness) this phase"
    - "Phase 3 catalog proofs run via cargo test --test model_catalog (not shell --lib)"
  artifacts:
    - path: crates/codegen/xai-grok-models/default_models.json
      provides: "mixed default catalog with provider field and GPT-5.6 family rows"
      contains: gpt-5.6-sol
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "ModelProvider enum and provider on DefaultModelJson / ModelEntryConfig / ModelInfo / ConfigModelOverride"
      contains: ModelProvider
    - path: crates/codegen/xai-grok-shell/src/remote/client.rs
      provides: "remote parse defaults provider to xai"
      contains: ModelProvider
    - path: crates/codegen/xai-grok-shell/tests/model_catalog.rs
      provides: "Wave 0 integration harness for public resolve_model_list / available_models / to_acp_model_info"
      contains: catalog_includes_gpt56
  key_links:
    - from: default_models.json
      to: default_models() / ModelEntryConfig
      via: "serde DefaultModelJson.provider mapped into ModelEntryConfig.provider then ModelInfo::from_config"
      pattern: "provider|ModelProvider|default_models"
    - from: ModelInfo.provider
      to: auth slot ids
      via: "as_str returns xai and codex matching PROVIDER_XAI / PROVIDER_CODEX"
      pattern: "as_str|PROVIDER_XAI|PROVIDER_CODEX"
    - from: tests/model_catalog.rs
      to: resolve_model_list
      via: "integration tests import xai_grok_shell::agent::config public APIs only"
      pattern: "resolve_model_list|ModelProvider|catalog_includes_gpt56"
---

## Phase Goal

**As a** bum user, **I want to** see Grok and GPT-5.6 Sol/Terra/Luna in one provider-labeled model catalog, **so that** I can pick either family from the same list before routing and Codex OAuth land.

<objective>
Deliver the Phase 3 catalog foundation: explicit `ModelProvider` (`xai` | `codex`) on the full type chain, UI-SPEC-labeled GPT-5.6 Sol/Terra/Luna rows in embedded `default_models.json`, Grok renamed with `(xAI)` suffix, constructors/helpers/remote parse updated, Wave 0 integration harness `tests/model_catalog.rs`, and automated no-prefetch proofs on **public** APIs (per D-01–D-08, D-10–D-13, D-15; RESEARCH Q3).

Purpose: Without schema + embedded SoT, later merge/CLI work has nothing to surface; Phase 4 routing needs the explicit provider field, not agent_type. Shell `--lib` is broken — proofs must not depend on repairing it.
Output: Compiling shell/models crates; default catalog resolves to mixed provider-tagged entries; `cargo test -p xai-grok-shell --test model_catalog` green for no-prefetch path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-CONTEXT.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
@crates/codegen/xai-grok-models/default_models.json
@crates/codegen/xai-grok-models/src/lib.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/agent/models.rs
@crates/codegen/xai-grok-shell/src/remote/client.rs
@crates/codegen/xai-grok-shell/src/auth/model.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-sampling-types/src/types.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | Ship Sol, Terra, and Luna as the GPT-5.6 family set |
| D-02 | Wire IDs `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna` (display names stable) |
| D-03 | Extend embedded `default_models.json` — not a separate hardcoded picker list |
| D-04 | GPT rows: id, name, description, `provider: codex`, context_window, selector-visible fields |
| D-05 | Explicit `provider` field (`xai` \| `codex`) — do **not** use `agent_type` as provider |
| D-06 | Canonical provider ids match Phase 2 auth slots (`PROVIDER_XAI` / `PROVIDER_CODEX`) |
| D-07 | All current Grok entries get `provider: "xai"` explicitly |
| D-08 | Missing `provider` defaults to `xai` for backward compatibility |
| D-09 | Single flat mixed list (no global provider filter mode) |
| D-10 | Provider visible via name suffix per UI-SPEC (` (xAI)` / ` (Codex)`) |
| D-11 | Order: Grok/xAI first, then Sol → Terra → Luna |
| D-12 | Show GPT even without Codex login (no Phase 6 gate here) |
| D-13 | Keep `grok-build` as default model |
| D-14 | Catalog + binding + visibility only — no Codex routing |
| D-15 | Automated tests: mixed list, provider binding, no Codex-auth filtering, default Grok |
| D-16 | No BYOK custom GPT requirement this phase |

**Discretion (locked by research + UI-SPEC):** `ModelProvider` enum with serde `snake_case` like `ApiBackend`; `as_str`/`display_label` helpers; GPT `agent_type` stays stock (`default_agent_type()` / grok-build-plan) — do **not** set codex harness this phase (strict harness would block switches without Phase 4 routing); GPT `context_window: 272000`; `api_backend: responses`; `supported_in_api: true`; leave GPT `base_url` to existing `endpoints.resolve_inference_base_url()` mapping (Phase 4 selects Codex endpoints); remote models force/default `provider = xai`.

**Out of scope:** prefetch union-append (Plan 02), ACP meta / CLI print format (Plan 03), routing (Phase 4), Codex OAuth (Phase 5), missing-provider gate (Phase 6).

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| `ModelProvider` | `agent/config.rs` | enum `Xai` \| `Codex`, Default=`Xai`, serde snake_case |
| `provider` field | `DefaultModelJson`, `ModelEntryConfig`, `ModelInfo`, `ConfigModelOverride` | schema chain |
| GPT rows + Grok rename | `default_models.json` | SoT data |
| No-prefetch catalog tests | `tests/model_catalog.rs` integration binary | MOD-01/02 proofs via public APIs |
| Wave 0 harness | same file | Avoids broken shell `--lib` (RESEARCH Q3) |

## Wave 0 harness strategy (mandatory)

- Create `crates/codegen/xai-grok-shell/tests/model_catalog.rs` and verify with `cargo test -p xai-grok-shell --test model_catalog …` only.
- **Never** use `cargo test -p xai-grok-shell --lib …` as a Phase 3 gate (lib suite has unrelated compile errors; do not repair them this phase).
- Call only public items: `resolve_model_list`, `Config`, `ModelEntry`, `ModelInfo`, `to_acp_model_info` (later plans), `available_models` (later plans). Pattern: Phase 2 `tests/auth_multi_slot.rs`.
- Do **not** depend on private `prefetch_model_entry` / `test_model_entry` helpers. Build fixtures with `ModelEntry::fallback` + public field writes, or `ModelEntry::from_config_entry` + `ModelEntryConfig` public fields.
- Colocated `#[cfg(test)]` edits inside `config.rs` are optional cleanup only — **not** the phase gate.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 model_catalog integration harness + RED no-prefetch tests</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md (Wave 0 + per-task map)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (locked names/descriptions table)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md (Validation Architecture + GPT wire IDs; Q3 resolved → integration tests)
    - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs (integration import style)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`pub fn resolve_model_list`, `Config`, `ModelEntry`, `ModelInfo`, `ModelEntryConfig`)
    - crates/codegen/xai-grok-models/default_models.json
    - crates/codegen/xai-grok-shell/src/auth/model.rs (`PROVIDER_XAI`, `PROVIDER_CODEX`)
  </read_first>
  <behavior>
    - harness_smoke (optional): resolve_model_list(&Config::default(), None) returns non-empty IndexMap (proves binary links)
    - catalog_includes_gpt56: resolve_model_list(Config::default(), None) contains keys gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna; each has info.provider == Codex; names equal UI-SPEC GPT-5.6 Sol (Codex) / Terra / Luna strings
    - mixed_catalog_order: no-prefetch key order is grok-build, then sol, terra, luna (Grok first, family priority Sol→Terra→Luna) — D-11
    - grok_entry_provider_xai_and_name_suffix: grok-build has provider Xai and name Grok Build (xAI) — D-07, D-10
    - default_remains_grok_build: xai_grok_models::default_model() (or shell facade) still returns grok-build — D-13
    - provider_default_xai: serde parse of ModelEntryConfig or ModelInfo JSON missing provider yields Xai — D-08
    - gpt_agent_type_is_stock: GPT rows do not set agent_type to codex harness string (equals default_agent_type()) — research Pattern 4
    - gpt_supported_in_api_true: GPT rows have supported_in_api true — D-12
  </behavior>
  <action>
    Wave 0 + RED no-prefetch contracts (D-15, RESEARCH Q3, 03-VALIDATION.md).

    1. Create `crates/codegen/xai-grok-shell/tests/model_catalog.rs` as a Cargo integration test binary (no `[[test]]` stanza required — auto-discovered under `tests/`).
    2. Import public APIs only, e.g. `use xai_grok_shell::agent::config::{resolve_model_list, Config, ModelEntryConfig, …}` and `xai_grok_models` for default_model. Mirror `auth_multi_slot.rs` style (plain `#[test]` / no shell internals).
    3. Encode MOD-01/MOD-02 no-prefetch contracts with the behavior names above so filters `catalog_includes_gpt56`, `mixed_catalog_order`, `provider_default_xai` work under `--test model_catalog`.
    4. Use pure `resolve_model_list(&Config::default(), None)` and serde of sample JSON for provider default — no network, no AuthManager, no Codex credential checks.
    5. Assert exact UI-SPEC display names/descriptions for the three GPT ids and Grok rename (03-UI-SPEC copy table). Assert `ModelProvider::as_str()` equals `PROVIDER_XAI` / `PROVIDER_CODEX` once the enum exists; until Task 2, accept compile or assert RED (no `#[ignore]`).
    6. Do **not** implement production `ModelProvider` or JSON edits here beyond what the test file needs to typecheck. Do not implement prefetch re-append, CLI, ACP meta, routing, or Codex OAuth (D-14).
    7. Do **not** add Phase 3 gates on `cargo test -p xai-grok-shell --lib`. Do not spend this phase fixing unrelated lib-test compile errors.
  </action>
  <acceptance_criteria>
    - `tests/model_catalog.rs` exists and is discovered by `cargo test -p xai-grok-shell --test model_catalog -- --list`
    - Tests cover mixed keys, providers, UI-SPEC names, order, default grok-build, missing-provider→xai, stock agent_type on GPT, supported_in_api true on GPT
    - Filters under `--test model_catalog` fail RED (assert or compile) until Task 2 — no ignored tests for these cases
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog catalog_includes_gpt56 -- --nocapture 2>&1 | tail -40</automated>
  </verify>
  <done>
    Wave 0 integration harness exists; no-prefetch MOD-01/02 contracts are RED until schema+JSON land.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelProvider schema chain + default_models.json mixed catalog</name>
  <files>crates/codegen/xai-grok-models/default_models.json, crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/agent/models.rs, crates/codegen/xai-grok-shell/src/remote/client.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (DefaultModelJson ~3318, default_models ~3349, ModelEntryConfig ~3413, ConfigModelOverride ~3548 apply ~3590, ModelInfo ~3693 fallback/from_config ~3764, test_model_entry ~5350, prefetch_model_entry ~10546, other ModelInfo/ModelEntryConfig literals)
    - crates/codegen/xai-grok-shell/src/agent/models.rs (make_entry_config / make_entry_config_with_id ~3349)
    - crates/codegen/xai-grok-shell/src/remote/client.rs (parse_remote_model_value ModelEntryConfig literal ~829–943)
    - crates/codegen/xai-grok-sampling-types/src/types.rs (ApiBackend serde pattern)
    - crates/codegen/xai-grok-shell/src/auth/model.rs (PROVIDER_XAI, PROVIDER_CODEX)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md (ModelProvider + field placement)
    - crates/codegen/xai-grok-models/default_models.json
  </read_first>
  <behavior>
    - ModelProvider serializes as xai / codex; Default is Xai; as_str matches PROVIDER_*; display_label is xAI / Codex for docs/helpers
    - default_models.json: default stays grok-build; grok-build name Grok Build (xAI), provider xai; three GPT rows per UI-SPEC with provider codex, context_window 272000, api_backend responses, supported_in_api true; no agent_type codex on GPT
    - default_models() maps provider into ModelEntryConfig; ModelInfo::from_config and fallback set provider; ConfigModelOverride optional provider applied when Some
    - parse_remote_model_value sets provider Xai (do not trust remote to invent codex)
    - All struct-literal compile sites updated (test helpers, remote, config tests)
    - Task 1 tests turn GREEN for no-prefetch path
  </behavior>
  <action>
    Implement the provider binding schema and ship mixed catalog data (D-01 through D-08, D-10, D-11, D-13).

    1. Add `ModelProvider` public enum in `agent/config.rs` near other model types: derive Debug/Clone/Copy/PartialEq/Eq/Serialize/Deserialize/Default; serde rename_all snake_case; Default = Xai; methods `as_str` → "xai"/"codex" (must equal `crate::auth::model::PROVIDER_XAI` / `PROVIDER_CODEX` — assert in unit test or use those constants in as_str body) and `display_label` → "xAI"/"Codex" (UI-SPEC human labels). Pattern after `ApiBackend` (per D-05, D-06).
    2. Add `provider: ModelProvider` with `#[serde(default)]` to `DefaultModelJson`, `ModelEntryConfig`, and `ModelInfo`. Add `provider: Option&lt;ModelProvider&gt;` to `ConfigModelOverride` and in `apply` set `entry.info.provider = v` when Some (D-08 default via serde).
    3. Map `m.provider` in `default_models()` into `ModelEntryConfig`. Set `provider` in `ModelInfo::fallback` (Default/Xai) and `ModelInfo::from_config` (from entry).
    4. Rewrite `default_models.json` per UI-SPEC / RESEARCH: keep top-level default/web_search/image_description/session_summary; update Grok name + provider xai; append Sol/Terra/Luna in that order with locked names/descriptions, provider codex, context_window 272000, api_backend responses, supported_in_api true. Do **not** set agent_type on GPT rows (stock default). Do **not** invent Codex base_url (D-03, D-04, D-14).
    5. In `parse_remote_model_value`, set `provider: ModelProvider::Xai` (or Default) on the ModelEntryConfig literal — remote catalog is xAI-sourced this phase (security: prevent remote spoofing codex provider).
    6. Fix **all** compile fan-out: `test_model_entry`, `prefetch_model_entry`, other ModelInfo literals in config.rs tests, `make_entry_config*`, any remaining ModelEntryConfig literals (config.rs ~6377+), remote literal. Use `ModelProvider::default()` or explicit Xai unless a test needs Codex.
    7. Run `cargo check -p xai-grok-shell` (and fix any other crate that constructs these types if check reveals them). Do not change `resolve_model_list` merge policy yet (Plan 02). Do not edit pager CLI (Plan 03).
    8. Green Task 1 tests via **integration** binary: `cargo test -p xai-grok-shell --test model_catalog …`. Do not run shell `--lib` as a gate. Existing colocated prefetch-count unit tests inside config.rs may still fail to compile under `--lib` (pre-existing harness debt) — ignore; Plan 02 continues in `model_catalog.rs`. Do not implement union-append here.
  </action>
  <acceptance_criteria>
    - default_models.json contains grok-build + three gpt-5.6-* rows with explicit provider fields and UI-SPEC names
    - ModelProvider exists and is public on agent::config; types carry provider; missing deserializes to xai
    - cargo check -p xai-grok-shell succeeds
    - Task 1 no-prefetch filters under `--test model_catalog` GREEN
    - GPT agent_type not codex; default_model still grok-build
    - No routing/OAuth/CLI/meta work; no shell --lib repair
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog catalog_includes_gpt56 mixed_catalog_order provider_default_xai -- --nocapture 2>&1 | tail -50 && cargo check -p xai-grok-shell 2>&1 | tail -20</automated>
  </verify>
  <done>
    Mixed embedded catalog with explicit provider binding compiles and passes no-prefetch automated proofs via integration tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Models-crate default id sanity + shell check sweep</name>
  <files>crates/codegen/xai-grok-models/default_models.json, crates/codegen/xai-grok-models/src/lib.rs</files>
  <read_first>
    - crates/codegen/xai-grok-models/src/lib.rs (LazyLock assert default ∈ models ids)
    - crates/codegen/xai-grok-models/default_models.json
  </read_first>
  <action>
    Confirm the models crate still validates after JSON expansion (D-13). The LazyLock already asserts `default` is in the models id list — run `cargo test -p xai-grok-models --lib` and `cargo check -p xai-grok-shell`. Optionally add a tiny models-crate test that default_model() == "grok-build" and models list length >= 4 if none exists; do not parse provider in the models crate (shell owns rich parse). Fix any remaining compile errors from Task 2 fan-out discovered by check. No merge/CLI/routing changes.
  </action>
  <acceptance_criteria>
    - cargo test -p xai-grok-models --lib passes
    - cargo check -p xai-grok-shell passes
    - default remains grok-build with four+ catalog models in JSON
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-models --lib 2>&1 | tail -30 && cargo check -p xai-grok-shell 2>&1 | tail -20</automated>
  </verify>
  <done>
    Models crate and shell compile cleanly with mixed defaults; default id sanity holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded default_models.json → process catalog | Ship-time trusted SoT compiled into binary |
| User config.toml [model.*] → catalog overlay | Untrusted local file; can set provider like other BYOK fields |
| Remote /v1/models JSON → prefetched entries | Untrusted network relative to Codex binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | parse_remote_model_value | high | mitigate | Always set provider=Xai on remote-parsed entries this phase; do not honor remote provider=codex |
| T-03-02 | Tampering | ConfigModelOverride.provider | medium | accept | User-controlled config already allows base_url/api_key BYOK; provider is same trust class — document for Phase 4 consumers |
| T-03-03 | Information disclosure | default_models.json | low | mitigate | No API keys/tokens in catalog JSON (existing pattern) |
| T-03-04 | Elevation of privilege | agent_type vs provider confusion | medium | mitigate | Explicit ModelProvider field; GPT agent_type stays stock; never treat agent_type as auth provider (D-05) |
| T-03-SC | Tampering | package installs | low | accept | No new npm/pip/cargo dependencies this phase |
</threat_model>

<verification>
- Task 1–2: `cargo test -p xai-grok-shell --test model_catalog` green for no-prefetch mixed list + provider binding
- cargo test -p xai-grok-models --lib green
- cargo check -p xai-grok-shell green
- Grep default_models.json for gpt-5.6-sol/terra/luna and provider fields
- No Phase 3 verify depends on shell --lib
</verification>

<success_criteria>
- Embedded SoT is a mixed, provider-labeled catalog with UI-SPEC names
- Schema exposes ModelProvider end-to-end for defaults and overrides
- Wave 0 integration harness model_catalog.rs is the automated proof vehicle
- Automated no-prefetch proofs cover MOD-01/02 core data contracts
- No routing, OAuth, or CLI work slipped in; no lib-suite repair
</success_criteria>

<output>
Create `.planning/phases/03-model-catalog-gpt-5-6-entries/03-01-SUMMARY.md` when done
</output>
