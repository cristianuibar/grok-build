---
phase: 03-model-catalog-gpt-5-6-entries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-models/default_models.json
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs
  - crates/codegen/xai-grok-shell/src/agent/models.rs
  - crates/codegen/xai-grok-shell/src/remote/client.rs
  - crates/codegen/xai-grok-shell/tests/model_catalog.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "Embedded default catalog includes grok-build plus gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna"
    - "Every shipped default entry has explicit provider binding xai or codex (not inferred from agent_type)"
    - "Grok entry is provider xai with display name Grok Build (xAI); GPT family is provider codex with UI-SPEC Codex-suffixed names"
    - "Default model id remains grok-build"
    - "Missing provider on parse defaults to xai for backward compatibility"
    - "Config [model.<id>] provider override valid/missing/invalid paths are covered; valid codex reaches ModelInfo"
    - "No-prefetch resolve_model_list order is Grok then Sol then Terra then Luna"
    - "GPT agent_type remains stock default (not codex harness) this phase"
    - "Phase 3 catalog proofs run via cargo test --test model_catalog (not shell --lib)"
    - "Automated cargo gates never pipe through bare tail without pipefail; one TESTNAME filter per cargo invocation"
  artifacts:
    - path: crates/codegen/xai-grok-models/default_models.json
      provides: "mixed default catalog with provider field and GPT-5.6 family rows"
      contains: gpt-5.6-sol
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "ModelProvider enum and provider on DefaultModelJson / ModelEntryConfig / ModelInfo / ConfigModelOverride"
      contains: ModelProvider
    - path: crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs
      provides: "fully_populated_override includes provider; invalid provider warns without dropping model"
      contains: provider
    - path: crates/codegen/xai-grok-shell/src/remote/client.rs
      provides: "remote parse defaults provider to xai"
      contains: ModelProvider
    - path: crates/codegen/xai-grok-shell/tests/model_catalog.rs
      provides: "Wave 0 integration harness for public resolve_model_list / available_models / override provider"
      contains: catalog_includes_gpt56
  key_links:
    - from: default_models.json
      to: default_models() / ModelEntryConfig
      via: "serde DefaultModelJson.provider mapped into ModelEntryConfig.provider then ModelInfo::from_config"
      pattern: "provider|ModelProvider|default_models"
    - from: ModelInfo.provider
      to: wire strings xai/codex
      via: "as_str returns public wire values xai and codex (must match auth slot ids; tests assert string literals not private PROVIDER_* imports)"
      pattern: "as_str|\"xai\"|\"codex\""
    - from: Config.config_models provider
      to: ModelInfo.provider via resolve_model_list
      via: "ConfigModelOverride.provider applied when Some"
      pattern: "ConfigModelOverride|provider|config_models"
    - from: tests/model_catalog.rs
      to: resolve_model_list
      via: "integration tests import xai_grok_shell::agent::config public APIs only"
      pattern: "resolve_model_list|ModelProvider|catalog_includes_gpt56"
---

## Phase Goal

**As a** bum user, **I want to** see Grok and GPT-5.6 Sol/Terra/Luna in one provider-labeled model catalog, **so that** I can pick either family from the same list before routing and Codex OAuth land.

<objective>
Deliver the Phase 3 catalog foundation: explicit `ModelProvider` (`xai` | `codex`) on the full type chain (including ConfigModelOverride parse path), UI-SPEC-labeled GPT-5.6 Sol/Terra/Luna rows in embedded `default_models.json`, Grok renamed with `(xAI)` suffix, constructors/helpers/remote parse updated, Wave 0 integration harness `tests/model_catalog.rs`, and automated no-prefetch proofs on **public** APIs (per D-01–D-08, D-10–D-13, D-15; RESEARCH Q3).

Addresses review concern: false-green cargo gates (no bare `| tail`; one TESTNAME filter per cargo). Addresses review concern: Task 1 must compile and `--list` before RED asserts; behavior-RED not compile-fail for missing types; wire strings `"xai"`/`"codex"` not private PROVIDER_* constants. Addresses review concern: provider override chain via `config_model_override_parse.rs` + fully_populated_override + integration coverage.

Purpose: Without schema + embedded SoT, later merge/CLI work has nothing to surface; Phase 4 routing needs the explicit provider field, not agent_type. Shell `--lib` is broken — proofs must not depend on repairing it.
Output: Compiling shell/models crates; default catalog resolves to mixed provider-tagged entries; `cargo test -p xai-grok-shell --test model_catalog` green for no-prefetch path including override provider.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-CONTEXT.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md
@.planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md
@crates/codegen/xai-grok-models/default_models.json
@crates/codegen/xai-grok-models/src/lib.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs
@crates/codegen/xai-grok-shell/src/agent/models.rs
@crates/codegen/xai-grok-shell/src/remote/client.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-sampling-types/src/types.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | Ship Sol, Terra, and Luna as the GPT-5.6 family set |
| D-02 | Wire IDs `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna` (display names stable) |
| D-03 | Extend embedded `default_models.json` — not a separate hardcoded picker list |
| D-04 | GPT rows: id, name, description, `provider: codex`, context_window, selector-visible fields |
| D-05 | Explicit `provider` field (`xai` \| `codex`) — do **not** use `agent_type` as provider |
| D-06 | Canonical provider ids match Phase 2 auth slots (`xai` / `codex` wire strings) |
| D-07 | All current Grok entries get `provider: "xai"` explicitly |
| D-08 | Missing `provider` defaults to `xai` for backward compatibility |
| D-09 | Single flat mixed list (no global provider filter mode) |
| D-10 | Provider visible via name suffix per UI-SPEC (` (xAI)` / ` (Codex)`) |
| D-11 | Order: Grok/xAI first, then Sol → Terra → Luna |
| D-12 | Show GPT even without Codex login (no Phase 6 gate here) |
| D-13 | Keep `grok-build` as default model |
| D-14 | Catalog + binding + visibility only — no Codex routing |
| D-15 | Automated tests: mixed list, provider binding, no Codex-auth filtering, default Grok |
| D-16 | No BYOK custom GPT requirement this phase |

**Discretion (locked by research + UI-SPEC):** `ModelProvider` enum with serde `snake_case` like `ApiBackend`; `as_str`/`display_label` helpers; GPT `agent_type` stays stock (`default_agent_type()` / grok-build-plan) — do **not** set codex harness this phase; GPT `context_window: 272000`; `api_backend: responses`; `supported_in_api: true`; leave GPT `base_url` to existing `endpoints.resolve_inference_base_url()` mapping; remote models force/default `provider = xai`.

**Out of scope:** prefetch union-append / collision (Plan 02), ACP meta / CLI print format (Plan 03), routing (Phase 4), Codex OAuth (Phase 5), missing-provider gate (Phase 6).

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| `ModelProvider` | `agent/config.rs` | enum `Xai` \| `Codex`, Default=`Xai`, serde snake_case |
| `provider` field | `DefaultModelJson`, `ModelEntryConfig`, `ModelInfo`, `ConfigModelOverride` | schema chain |
| GPT rows + Grok rename | `default_models.json` | SoT data |
| Override parse completeness | `config_model_override_parse.rs` `fully_populated_override` | drift guard includes provider |
| No-prefetch catalog tests | `tests/model_catalog.rs` integration binary | MOD-01/02 proofs via public APIs |
| Wave 0 harness | same file | Avoids broken shell `--lib` (RESEARCH Q3) |

## Wave 0 harness strategy (mandatory)

- Create `crates/codegen/xai-grok-shell/tests/model_catalog.rs` and verify with `cargo test -p xai-grok-shell --test model_catalog …` only.
- **Never** use `cargo test -p xai-grok-shell --lib …` as a Phase 3 gate (lib suite has unrelated compile errors; do not repair them this phase).
- Call only public items: `resolve_model_list`, `Config`, `Config::new_from_toml_cfg`, `ModelEntry`, `ModelInfo`, `ModelProvider` (after Task 2), `to_acp_model_info` (later plans), `available_models` (later plans). Pattern: Phase 2 `tests/auth_multi_slot.rs`.
- Do **not** depend on private `prefetch_model_entry` / `test_model_entry` helpers. Build fixtures with `ModelEntry::fallback` + public field writes, or `ModelEntry::from_config_entry` + `ModelEntryConfig` public fields.
- Do **not** import `crate::auth::model::PROVIDER_XAI` / `PROVIDER_CODEX` from integration tests — assert public wire string literals `"xai"` and `"codex"` (as_str / JSON / TOML).
- Colocated `#[cfg(test)]` edits inside `config.rs` are optional cleanup only — **not** the phase gate.

## Cargo verify hygiene (mandatory — review HIGH)

- **One** positional TESTNAME filter per `cargo test` invocation (Cargo accepts only one). Prefer the full binary when multiple tests matter: `cargo test -p xai-grok-shell --test model_catalog -- --nocapture`.
- For a single filter: `cargo test -p xai-grok-shell --test model_catalog catalog_includes_gpt56 -- --nocapture`.
- Chain independent cargo commands with `&&` only.
- **Never** pipe cargo output through bare `| tail` (or any pipe) without `set -o pipefail`. Prefer no pipe at all so cargo’s exit status is the shell status.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 compiling smoke harness + behavior-RED catalog key tests</name>
  <files>crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-VALIDATION.md (Wave 0 + per-task map)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-UI-SPEC.md (locked names/descriptions table)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-RESEARCH.md (Validation Architecture + GPT wire IDs; Q3 resolved → integration tests)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-REVIEWS.md (HIGH: Task 1 RED vs --list contradiction)
    - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs (integration import style)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (`pub fn resolve_model_list`, `Config`, `ModelEntry`, `ModelInfo`, `ModelEntryConfig`)
    - crates/codegen/xai-grok-models/default_models.json
  </read_first>
  <behavior>
    - harness_smoke: resolve_model_list(&Config::default(), None) returns non-empty IndexMap — PASSES on current main (proves binary links and --list works)
    - catalog_includes_gpt56: resolve_model_list(Config::default(), None) contains keys gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna — behavior RED until Task 2 JSON lands (assert failure, not compile failure)
    - mixed_catalog_order: no-prefetch key order has grok-build before the three gpt-5.6-* ids, and Sol before Terra before Luna among the GPT family — behavior RED until JSON lands
    - default_remains_grok_build: xai_grok_models::default_model() returns grok-build — D-13 (should already pass)
  </behavior>
  <action>
    Wave 0 compiling harness + behavior-RED no-prefetch key contracts only (D-15, RESEARCH Q3, 03-VALIDATION.md). Addresses review concern: no compile-fail RED; --list must succeed.

    1. Create `crates/codegen/xai-grok-shell/tests/model_catalog.rs` as a Cargo integration test binary (auto-discovered under `tests/`).
    2. Import only APIs that exist **today** on main: `resolve_model_list`, `Config`, `xai_grok_models::default_model`. Mirror `auth_multi_slot.rs` style (plain `#[test]`).
    3. Write `harness_smoke` so the binary **compiles and lists**. After this task, `cargo test -p xai-grok-shell --test model_catalog -- --list` must exit 0 and list the test names.
    4. Write `catalog_includes_gpt56`, `mixed_catalog_order`, `default_remains_grok_build` as pure key/order/default asserts on `resolve_model_list(&Config::default(), None)`. These must be **behavior-RED** (assertion failures) for missing GPT keys — **not** compile errors from missing types.
    5. **Do not** reference `ModelProvider`, `info.provider`, private auth PROVIDER_* constants, or ConfigModelOverride.provider in this task. Provider-field and UI-SPEC name asserts land in Task 2 after the schema exists (so they also stay behavior-RED or green without compile-fail).
    6. Do **not** implement production `ModelProvider`, JSON edits, or override parse here. Do not implement prefetch re-append, CLI, ACP meta, routing, or Codex OAuth (D-14).
    7. Do **not** add Phase 3 gates on `cargo test -p xai-grok-shell --lib`. Do not fix unrelated lib-test compile errors.
  </action>
  <acceptance_criteria>
    - `tests/model_catalog.rs` exists
    - `cargo test -p xai-grok-shell --test model_catalog -- --list` exits 0 and discovers harness_smoke, catalog_includes_gpt56, mixed_catalog_order, default_remains_grok_build
    - harness_smoke PASSES
    - catalog_includes_gpt56 and mixed_catalog_order FAIL with assertion messages (behavior RED), not compile errors
    - No ModelProvider / private PROVIDER_* references in the test file yet
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test model_catalog -- --list && cargo test -p xai-grok-shell --test model_catalog harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    Wave 0 integration binary compiles, lists, and holds behavior-RED key/order contracts until schema+JSON land in Task 2. Observing `catalog_includes_gpt56` fail before Task 2 is **optional** (cycle 2 LOW) — automated gate for this task is `--list` + harness_smoke; RED-before-GREEN for key tests is preferred practice, not a separate hard verify requirement.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ModelProvider schema chain + default_models.json + override parse + GREEN catalog proofs</name>
  <files>crates/codegen/xai-grok-models/default_models.json, crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs, crates/codegen/xai-grok-shell/src/agent/models.rs, crates/codegen/xai-grok-shell/src/remote/client.rs, crates/codegen/xai-grok-shell/tests/model_catalog.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (DefaultModelJson ~3318, default_models ~3349, ModelEntryConfig ~3413, ConfigModelOverride ~3548 apply ~3590, ModelInfo ~3693 fallback/from_config ~3764, test_model_entry ~5350, prefetch_model_entry ~10546, other ModelInfo/ModelEntryConfig literals)
    - crates/codegen/xai-grok-shell/src/agent/config_model_override_parse.rs (fully_populated_override ~500, parse_model_overrides, warning kinds)
    - crates/codegen/xai-grok-shell/src/agent/models.rs (make_entry_config / make_entry_config_with_id ~3349)
    - crates/codegen/xai-grok-shell/src/remote/client.rs (parse_remote_model_value ModelEntryConfig literal ~829–943)
    - crates/codegen/xai-grok-sampling-types/src/types.rs (ApiBackend serde pattern)
    - crates/codegen/xai-grok-shell/src/auth/model.rs (PROVIDER_XAI, PROVIDER_CODEX — use only inside as_str body if desired; do not export to integration tests)
    - .planning/phases/03-model-catalog-gpt-5-6-entries/03-PATTERNS.md (ModelProvider + field placement)
    - crates/codegen/xai-grok-models/default_models.json
    - crates/codegen/xai-grok-shell/tests/model_catalog.rs (Task 1 harness)
  </read_first>
  <behavior>
    - ModelProvider serializes as xai / codex; Default is Xai; as_str returns public wire strings "xai"/"codex"; display_label is xAI / Codex
    - default_models.json: default stays grok-build; grok-build name Grok Build (xAI), provider xai; three GPT rows per UI-SPEC with provider codex, context_window 272000, api_backend responses, supported_in_api true; no agent_type codex on GPT
    - default_models() maps provider into ModelEntryConfig; ModelInfo::from_config and fallback set provider; ConfigModelOverride optional provider applied when Some
    - parse_remote_model_value sets provider Xai (do not trust remote to invent codex)
    - fully_populated_override() includes provider: Some(ModelProvider::Codex) (or Xai) so struct-field drift guard stays complete
    - Integration: catalog_includes_gpt56 also asserts each GPT info.provider.as_str() == "codex" and UI-SPEC names; grok_entry_provider_xai_and_name_suffix; provider_default_xai via serde missing field → as_str "xai"; gpt_agent_type_is_stock; gpt_supported_in_api_true
    - override_provider_codex_reaches_model_info: Config with config_models entry provider Some(Codex) on a model key → resolve_model_list entry has as_str "codex"
    - override_provider_missing_defaults_xai: ModelEntryConfig/ModelInfo JSON or ConfigModelOverride without provider → xai
    - override_invalid_provider_warns_keeps_model: Config::new_from_toml_cfg with [model.foo] provider = "not-a-provider" yields model still present in config_models (field skipped) and a non-empty model_override_warnings entry about provider / InvalidValue (do not drop the model key)
  </behavior>
  <action>
    Implement the provider binding schema, ship mixed catalog data, complete override parse chain (D-01 through D-08, D-10, D-11, D-13). Addresses review concern: provider override chain + fully_populated_override.

    1. Add `ModelProvider` public enum in `agent/config.rs` near other model types: derive Debug/Clone/Copy/PartialEq/Eq/Serialize/Deserialize/Default; serde rename_all snake_case; Default = Xai; methods `as_str` → "xai"/"codex" (may equal auth PROVIDER_* constants internally) and `display_label` → "xAI"/"Codex". Pattern after `ApiBackend` (per D-05, D-06).
    2. Add `provider: ModelProvider` with `#[serde(default)]` to `DefaultModelJson`, `ModelEntryConfig`, and `ModelInfo`. Add `provider: Option&lt;ModelProvider&gt;` to `ConfigModelOverride` and in `apply` set `entry.info.provider = v` when Some (D-08 default via serde).
    3. Map `m.provider` in `default_models()` into `ModelEntryConfig`. Set `provider` in `ModelInfo::fallback` (Default/Xai) and `ModelInfo::from_config` (from entry).
    4. Rewrite `default_models.json` per UI-SPEC / RESEARCH: keep top-level default/web_search/image_description/session_summary; update Grok name + provider xai; append Sol/Terra/Luna in that order with locked names/descriptions, provider codex, context_window 272000, api_backend responses, supported_in_api true. Do **not** set agent_type on GPT rows (stock default). Do **not** invent Codex base_url (D-03, D-04, D-14).
    5. In `parse_remote_model_value`, set `provider: ModelProvider::Xai` (or Default) on the ModelEntryConfig literal — remote catalog is xAI-sourced this phase (security: prevent remote spoofing codex provider).
    6. Update `config_model_override_parse.rs` `fully_populated_override()` exhaustive literal to include `provider: Some(ModelProvider::Codex)` (or Xai). Ensure invalid provider values follow existing field-skip pattern (InvalidValue warn, model entry kept). Do not change the warn-without-drop contract of the module.
    7. Fix **all** compile fan-out: `test_model_entry`, `prefetch_model_entry`, other ModelInfo literals in config.rs tests, `make_entry_config*`, any remaining ModelEntryConfig literals, remote literal. Use `ModelProvider::default()` or explicit Xai unless a test needs Codex.
    8. Extend `tests/model_catalog.rs` with provider/name/override tests that now typecheck. Assert `info.provider.as_str() == "codex"` / `"xai"` using **string literals only** — never import private PROVIDER_* into the integration binary. Use `Config::new_from_toml_cfg` + `cfg.model_override_warnings` for invalid-provider keep-model proof; use `cfg.config_models` + `resolve_model_list` for valid override → ModelInfo.
    9. Run `cargo check -p xai-grok-shell`. Green full integration binary: `cargo test -p xai-grok-shell --test model_catalog -- --nocapture`. Do not change `resolve_model_list` merge policy yet (Plan 02). Do not edit pager CLI (Plan 03). Do not run shell `--lib` as a gate.
  </action>
  <acceptance_criteria>
    - default_models.json contains grok-build + three gpt-5.6-* rows with explicit provider fields and UI-SPEC names
    - ModelProvider exists and is public on agent::config; types carry provider; missing deserializes to xai
    - fully_populated_override includes provider field
    - cargo check -p xai-grok-shell succeeds
    - Full `cargo test -p xai-grok-shell --test model_catalog` GREEN (Task 1 key tests + provider/name/override tests)
    - Override tests: valid codex reaches ModelInfo; missing → xai; invalid warns without dropping model
    - Integration asserts use "xai"/"codex" string literals, not private PROVIDER_* imports
    - GPT agent_type not codex; default_model still grok-build
    - No routing/OAuth/CLI/meta work; no shell --lib repair
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p xai-grok-shell && cargo test -p xai-grok-shell --test model_catalog -- --nocapture</automated>
  </verify>
  <done>
    Mixed embedded catalog with explicit provider binding compiles; no-prefetch + override provider proofs green via integration tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Models-crate default id sanity + shell check sweep</name>
  <files>crates/codegen/xai-grok-models/default_models.json, crates/codegen/xai-grok-models/src/lib.rs</files>
  <read_first>
    - crates/codegen/xai-grok-models/src/lib.rs (LazyLock assert default ∈ models ids)
    - crates/codegen/xai-grok-models/default_models.json
  </read_first>
  <action>
    Confirm the models crate still validates after JSON expansion (D-13). The LazyLock already asserts `default` is in the models id list — run `cargo test -p xai-grok-models --lib` and `cargo check -p xai-grok-shell`. Optionally add a tiny models-crate test that default_model() == "grok-build" and models list length >= 4 if none exists; do not parse provider in the models crate (shell owns rich parse). Fix any remaining compile errors from Task 2 fan-out discovered by check. No merge/CLI/routing changes. Inspection-only of models crate source is OK; only modify if a test or LazyLock fix is required.
  </action>
  <acceptance_criteria>
    - cargo test -p xai-grok-models --lib passes
    - cargo check -p xai-grok-shell passes
    - default remains grok-build with four+ catalog models in JSON
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-models --lib && cargo check -p xai-grok-shell</automated>
  </verify>
  <done>
    Models crate and shell compile cleanly with mixed defaults; default id sanity holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded default_models.json → process catalog | Ship-time trusted SoT compiled into binary |
| User config.toml [model.*] → catalog overlay | Untrusted local file; can set provider like other BYOK fields |
| Remote /v1/models JSON → prefetched entries | Untrusted network relative to Codex binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | parse_remote_model_value | high | mitigate | Always set provider=Xai on remote-parsed entries this phase; do not honor remote provider=codex |
| T-03-02 | Tampering | ConfigModelOverride.provider | medium | accept | User-controlled config already allows base_url/api_key BYOK; provider is same trust class — document for Phase 4 consumers; invalid values warn+skip field |
| T-03-03 | Information disclosure | default_models.json | low | mitigate | No API keys/tokens in catalog JSON (existing pattern) |
| T-03-04 | Elevation of privilege | agent_type vs provider confusion | medium | mitigate | Explicit ModelProvider field; GPT agent_type stays stock; never treat agent_type as auth provider (D-05) |
| T-03-SC | Tampering | package installs | low | accept | No new npm/pip/cargo dependencies this phase |
</threat_model>

<verification>
- Task 1: `cargo test -p xai-grok-shell --test model_catalog -- --list` exits 0; harness_smoke green
- Task 2: full `cargo test -p xai-grok-shell --test model_catalog` green for no-prefetch mixed list + provider binding + overrides
- cargo test -p xai-grok-models --lib green
- cargo check -p xai-grok-shell green
- Grep default_models.json for gpt-5.6-sol/terra/luna and provider fields
- No Phase 3 verify depends on shell --lib
- No bare `| tail` in any automated verify
</verification>

<success_criteria>
- Embedded SoT is a mixed, provider-labeled catalog with UI-SPEC names
- Schema exposes ModelProvider end-to-end for defaults and config overrides
- Wave 0 integration harness model_catalog.rs is the automated proof vehicle and lists before RED
- Automated no-prefetch proofs cover MOD-01/02 core data contracts
- No routing, OAuth, or CLI work slipped in; no lib-suite repair
</success_criteria>

<output>
Create `.planning/phases/03-model-catalog-gpt-5-6-entries/03-01-SUMMARY.md` when done
</output>
