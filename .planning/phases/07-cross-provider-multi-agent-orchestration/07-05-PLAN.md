---
phase: 07-cross-provider-multi-agent-orchestration
plan: 05
type: execute
wave: 4
depends_on:
  - 07-03
  - 07-04
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - AGENT-02
  - AGENT-04
  - AGENT-05
  - AGENT-06
must_haves:
  truths:
    - "In-crate or public test seam exists so Authorization/spawn proofs can call pub(crate) handle_subagent_request / child sampling paths that external tests alone cannot reach (review HIGH test seam)"
    - "Grok parent → Codex child: child base_url AND outbound request Authorization match Codex fixture token — not parent xAI (D-09, D-10, D-12) — MANDATORY"
    - "Codex parent → Grok child: child base_url AND outbound request Authorization match xAI fixture token — not parent Codex (D-09, D-10, D-12) — MANDATORY both directions (no one-direction waiver)"
    - "Dual fake tokens never cross-slot on child SamplingConfig seed (D-06, D-10)"
    - "Parent model_id unchanged after successful cross-provider spawn on a REAL spawn path — not resolve-only proxy (D-11; review MEDIUM)"
    - "Missing child slot fails spawn with login-shaped error and produces no wrong-backend outbound request (D-05, D-06, D-07)"
    - "Child reasoning_effort medium applied on sampling config when Tool override set (AGENT-03/06 automated path)"
    - "Fixture tokens only — no live OAuth"
    - "Resolve-only key_prefix asserts are complements only — not sufficient for D-12 alone"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
      provides: "In-crate isolation Authorization proofs and/or harness driving real spawn+sample"
      contains: p7_isolation
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "Optional external proofs via public harness; missing-slot no-wrong-backend"
      contains: p7_isolation
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
      provides: "Test seam exports or cfg(test) harness for child outbound Authorization"
      contains: p7_
  key_links:
    - from: test seam → handle_subagent_request / child sample
      to: mock HTTP Authorization header
      via: "BOTH directions must assert Authorization equals child fixture token and base_url host matches child provider"
      pattern: "xai-fake-token|codex-fake-token|Authorization"
    - from: empty child slot
      to: send_failure / tool error
      via: "no mock server hit on child host"
      pattern: "bum login --provider"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Prove cross-provider child routing isolation and fail-closed missing-slot behavior with dual fake tokens (per D-09..D-12, AGENT-02/04/05/06). First establish a **test seam** so proofs can drive real child sampling/Authorization (review HIGH). Complements Plan 03 pure gate with observable route/Authorization proofs in **both** directions (D-12 hard bar — no one-direction waiver).

Purpose: Lock never-cross-slot for subagent spawn both directions without live OAuth.
Output: Test seam + green p7_isolation_* Authorization both dirs + parent stability after real spawn + missing-slot no-outbound.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-REVIEWS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-04-SUMMARY.md
@crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
@crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-09 | Child SamplingConfig from child model route |
| D-10 | Child bearer from child slot only |
| D-11 | Parent model unchanged |
| D-12 | Dual fake-token proofs **both** directions |
| D-06 | No parent fallback on missing slot |
| D-07 | Login-shaped error, no wrong-backend request |

## Scope boundaries

- Prefer in-crate module under `src/agent/subagent/tests/` for Authorization proofs that need pub(crate) paths; external `tests/cross_provider_subagent.rs` may call a **public** harness if one is exposed.
- Full live multi-turn agent E2E is Phase 9 — here automated resolve/spawn/mock is enough for AGENT-04/06.
- **Both** Grok→Codex and Codex→Grok Authorization proofs are mandatory completion criteria (addresses review MEDIUM: do not weaken D-12 to one direction).
- Concurrent parent/child turn clobber is best-effort smoke if harness allows; not a reason to skip directional isolation.
- Fixture tokens only.

## Cargo verify hygiene

- Unique filters: `p7_isolation`, `p7_missing`, `p7_parent_model`, `p7_seam` (if separate).
- Discovery ≥1 before execute.
- Prefer `--lib` for in-crate tests; `--test cross_provider_subagent` for external.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — test seam for child outbound Authorization + dual-direction isolation + real-spawn parent stability</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs, crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (dual fake tokens, mock Authorization, never_cross_slot)
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs (private resolve_model_override_to_config ~1006)
    - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs (pub(crate) handle_subagent_request ~59)
    - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs (existing unit harness patterns)
    - Plan 03 SUMMARY for available helpers
    - 07-PATTERNS.md dual fake token section
    - 07-REVIEWS.md Plan 05 HIGH test seam + both-dir + parent stability
  </read_first>
  <behavior>
    - Test seam: in-crate tests can invoke spawn/sample path that produces outbound HTTP with Authorization; OR public test harness module documents the same
    - p7_isolation_grok_parent_codex_child_route: dual fixtures → Codex child base_url host AND Authorization contains codex-fake-token not xai-fake-token (MANDATORY)
    - p7_isolation_codex_parent_grok_child_route: inverse xAI child Authorization xai-fake-token not codex-fake-token (MANDATORY)
    - p7_isolation_never_cross_slot_on_child_seed: child api_key/key_prefix never equals the other provider's token when both present (complement)
    - p7_parent_model_unchanged_after_cross_provider_spawn: parent model_id stable across **real spawn** (session/ChatState assert) — resolve-only proxy insufficient
    - p7_isolation_reasoning_effort_medium_on_child_config: Tool effort medium on child SamplingConfig when model supports it
  </behavior>
  <action>
    Implement D-09..D-12 automated proofs with an explicit test seam (AGENT-02, AGENT-04, AGENT-06). Addresses review HIGH test seam + MEDIUM both-dir + parent stability.

    1. **Test seam (mandatory first deliverable):** Choose one (document in SUMMARY):
       - **Preferred:** put isolation Authorization tests in `src/agent/subagent/tests/` (or sibling module) where pub(crate) handle_subagent_request and private helpers are reachable; mirror provider_routing mock HTTP patterns in-crate; OR
       - Expose a deliberate `pub` / `pub(crate)` testing harness (e.g. `subagent::test_support::spawn_child_and_capture_outbound`) that external `tests/cross_provider_subagent.rs` can call to force one child sample against mock base_url and return captured Authorization + host.
       Do **not** leave proofs only as external tests calling private resolve_model_override_to_config (uncompilable).
    2. Dual-slot auth.json fixtures with distinct fake tokens (xai-fake-token / codex-fake-token).
    3. **Both directions mandatory (D-12):**
       - Grok parent → Codex child: mock HTTP Authorization equals Codex fixture; base_url host matches Codex route.
       - Codex parent → Grok child: mock HTTP Authorization equals xAI fixture; base_url host matches xAI route.
       - Completing only one direction is **not** acceptance — do not document a one-direction waiver.
    4. Complements (allowed in addition, never instead of step 3): key_prefix inequality; seeded SamplingConfig fields after spawn.
    5. **Parent stability:** after real handle_subagent_request / harness spawn succeeds, assert parent session model_id (or equivalent session handle field) unchanged — not a resolve-only helper that never mutates parent state by construction.
    6. Effort medium proof ties AGENT-03/06 automated path to child config (not live LLM).
    7. Never log full secrets beyond equality with known fixture constants.
  </action>
  <verify>
    <automated>set -euo pipefail; list_lib=$(cargo test -p xai-grok-shell --lib p7_isolation -- --list 2>/dev/null || true); list_int=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null || true); echo "$list_lib$list_int" | grep -q 'p7_isolation_grok_parent_codex'; echo "$list_lib$list_int" | grep -q 'p7_isolation_codex_parent_grok'; n_lib=$(echo "$list_lib" | grep -c 'p7_isolation' || true); n_int=$(echo "$list_int" | grep -c 'p7_isolation' || true); test "$((n_lib + n_int))" -ge 2; if [ "$n_lib" -ge 1 ]; then cargo test -p xai-grok-shell --lib p7_isolation -- --nocapture; fi; if [ "$n_int" -ge 1 ]; then cargo test -p xai-grok-shell --test cross_provider_subagent p7_isolation -- --nocapture; fi; n_parent_lib=$(cargo test -p xai-grok-shell --lib p7_parent_model -- --list 2>/dev/null | grep -c 'p7_parent_model' || true); n_parent_int=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_parent_model' || true); test "$((n_parent_lib + n_parent_int))" -ge 1; if [ "$n_parent_lib" -ge 1 ]; then cargo test -p xai-grok-shell --lib p7_parent_model -- --nocapture; else cargo test -p xai-grok-shell --test cross_provider_subagent p7_parent_model -- --nocapture; fi</automated>
  </verify>
  <done>
    Test seam lands; BOTH cross-provider directions assert mock HTTP Authorization against child fixture token; base_url matches child; parent model stability after real spawn; effort medium on child config when applicable. Resolve-only alone is not acceptance. Verify cannot pass with only one Authorization direction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — missing child slot fails closed with no wrong-backend request both directions</name>
  <files>crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs</files>
  <read_first>
    - Plan 03 gate behavior / send_failure messages (pre-worktree)
    - provider_routing mock server patterns for "no request" asserts
    - 07-RESEARCH.md Pitfall 2 empty Codex key
  </read_first>
  <behavior>
    - p7_missing_child_codex_no_wrong_backend_request: parent xAI usable, codex slot empty, spawn Codex child → failure contains bum login --provider codex; mock Codex host receives zero requests
    - p7_missing_child_xai_no_wrong_backend_request: inverse for empty xAI slot when parent is Codex
    - Complements pure gate tests — proves no parent-token probe to wrong host as primary UX
  </behavior>
  <action>
    Strengthen AGENT-05/06 automated fail-closed proofs (D-05..D-07).

    1. Empty child slot fixtures; dual mock servers or single mock that would catch wrong-host traffic if parent fallback occurred.
    2. Invoke spawn/preflight path via the Plan 05 test seam (shell and/or Task eager if unit-reachable).
    3. Assert error suggestion/login text; assert mock request count 0 on child host.
    4. Do not accept mid-turn 401 as success criterion.
    5. Prefer also asserting no worktree created when gate fails if harness can observe Plan 03 ordering.
  </action>
  <verify>
    <automated>set -euo pipefail; n_lib=$(cargo test -p xai-grok-shell --lib p7_missing -- --list 2>/dev/null | grep -c 'p7_missing' || true); n_int=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_missing' || true); test "$((n_lib + n_int))" -ge 1; if [ "$n_lib" -ge 1 ]; then cargo test -p xai-grok-shell --lib p7_missing -- --nocapture; fi; if [ "$n_int" -ge 1 ]; then cargo test -p xai-grok-shell --test cross_provider_subagent p7_missing -- --nocapture; fi</automated>
  </verify>
  <done>
    Missing child provider fails before wrong-backend traffic; login hint present both directions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dual fake tokens in fixtures | Distinct constants; equality asserts detect cross-slot bugs |
| Mock HTTP | Must not record real secrets; fixture tokens only |
| Test seam surface | Public harness must not expose production token material beyond fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Elevation of Privilege | Child Authorization | high | mitigate | Dual-direction tests fail if parent token appears on child host — both dirs mandatory |
| T-07-04 | Spoofing | Empty slot spawn | high | mitigate | No-outbound-request asserts + login error |
| T-07-01 | Information Disclosure | test output | medium | mitigate | Compare to fixture constants; avoid printing full auth.json |
| T-07-14 | Bypass | Unreachable private resolve in external tests | high | mitigate | In-crate/public test seam required before claiming D-12 |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
p7_isolation both dirs + p7_missing + p7_parent_model green via seam-capable target; D-12 Authorization both directions mandatory.
</verification>

<success_criteria>
- AGENT-02/04 dual-direction isolation proven with mock HTTP Authorization **both ways** (D-12 hard bar)
- AGENT-05 no wrong-backend on missing slot
- AGENT-06 automated path (model+effort) covered without live LLM
- Test seam documented in SUMMARY
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-05-SUMMARY.md` when done
</output>
