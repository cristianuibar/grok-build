---
phase: 07-cross-provider-multi-agent-orchestration
plan: 05
type: execute
wave: 4
depends_on:
  - 07-03
  - 07-04
files_modified:
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - AGENT-02
  - AGENT-04
  - AGENT-05
  - AGENT-06
must_haves:
  truths:
    - "Grok parent → Codex child: child base_url AND outbound request Authorization (or equivalent header) match Codex fixture token — not parent xAI (D-09, D-10, D-12)"
    - "Codex parent → Grok child: child base_url AND outbound request Authorization match xAI fixture token — not parent Codex (D-09, D-10, D-12)"
    - "Dual fake tokens never cross-slot on child SamplingConfig seed (D-06, D-10)"
    - "Parent model_id unchanged after successful cross-provider spawn (D-11)"
    - "Missing child slot fails spawn with login-shaped error and produces no wrong-backend outbound request (D-05, D-06, D-07)"
    - "Child reasoning_effort medium applied on sampling config when Tool override set (AGENT-03/06 automated path)"
    - "Fixture tokens only — no live OAuth"
    - "Resolve-only key_prefix asserts are complements only — not sufficient for D-12 alone"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "Dual-direction isolation with mock HTTP Authorization + missing-slot no-wrong-backend proofs"
      contains: p7_isolation
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Optional shared constants/helpers reuse; not a substitute for subagent proofs"
      contains: never_cross_slot
  key_links:
    - from: resolve_model_override_to_config / spawn seed
      to: child request Authorization header on mock HTTP
      via: "mock HTTP Authorization (or equivalent outbound header) must equal child fixture token; base_url host matches child provider — both directions preferred; ≥1 direction mandatory for D-12"
      pattern: "xai-fake-token|codex-fake-token|Authorization"
    - from: empty child slot
      to: send_failure / tool error
      via: "no mock server hit on child host"
      pattern: "bum login --provider"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Prove cross-provider child routing isolation and fail-closed missing-slot behavior with dual fake tokens in automated tests (per D-09..D-12, AGENT-02/04/05/06). Complements Plan 03 pure gate with observable route/Authorization proofs.

Purpose: Lock never-cross-slot for subagent spawn both directions without live OAuth.
Output: Green p7_isolation_* (and related) harness tests on cross_provider_subagent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-04-SUMMARY.md
@crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
@crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-09 | Child SamplingConfig from child model route |
| D-10 | Child bearer from child slot only |
| D-11 | Parent model unchanged |
| D-12 | Dual fake-token proofs both directions |
| D-06 | No parent fallback on missing slot |
| D-07 | Login-shaped error, no wrong-backend request |

## Scope boundaries

- Prefer extending cross_provider_subagent.rs; only touch provider_routing.rs for shared helpers if needed.
- Full live multi-turn agent E2E is Phase 9 — here automated resolve/spawn/mock is enough for AGENT-04/06.
- Full MvpAgent spawn is preferred; if constrained, still require mock HTTP Authorization (or equivalent outbound header) for ≥1 cross-provider direction per D-12, plus resolve/seed complements both directions and spawn gate no-outbound on missing slot. Document which layer each test hits in SUMMARY.
- Concurrent parent/child turn clobber is best-effort smoke if harness allows; not a reason to skip directional isolation.

## Cargo verify hygiene

- Unique filters: `p7_isolation`, `p7_missing`, `p7_parent_model`.
- Discovery ≥1 before execute.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — dual-direction child route isolation (Grok↔Codex)</name>
  <files>crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (dual fake tokens, mock Authorization, never_cross_slot)
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs (resolve_model_override_to_config)
    - Plan 03 SUMMARY for available test hooks
    - 07-PATTERNS.md dual fake token section
  </read_first>
  <behavior>
    - p7_isolation_grok_parent_codex_child_route: with dual fixtures, Codex child yields Codex base_url host AND outbound mock HTTP Authorization (or equivalent request header) containing codex-fake-token — not xai-fake-token (D-12)
    - p7_isolation_codex_parent_grok_child_route: inverse — xAI child Authorization contains xai-fake-token — not codex-fake-token (D-12)
    - p7_isolation_never_cross_slot_on_child_seed: child api_key/key_prefix never equals the other provider's token when both present (complement unit)
    - p7_parent_model_unchanged_after_cross_provider_spawn: parent model_id stable across spawn (if spawn harness available; else resolve-only parent handle unchanged)
    - p7_isolation_reasoning_effort_medium_on_child_config: Tool effort medium appears on child SamplingConfig.reasoning_effort when model supports it
  </behavior>
  <action>
    Implement D-09..D-12 automated proofs (AGENT-02, AGENT-04, AGENT-06 path). D-12 requires request-level Authorization — resolve-only key_prefix is not sufficient alone.

    1. Dual-slot auth.json fixtures with distinct fake tokens (names containing xai-fake-token / codex-fake-token).
    2. Both directions: parent context as xAI model with Codex child slug and parent Codex with Grok child slug.
    3. D-12 bar (mandatory): for each direction (prefer both; at least one direction if harness limits force a single mock path, document the gap in SUMMARY and still complete the other direction via resolve+seed plus the Authorization direction that runs):
       - Force a child sample against mock HTTP (reuse provider_routing mock Authorization patterns).
       - Assert outbound Authorization (or equivalent bearer header) equals the child provider fixture token constant — not the parent provider token.
       - Assert child request base_url / host matches the child provider route (not parent).
    4. Complements (allowed in addition, never instead of step 3): resolve_model_override_to_config base_url + key_prefix inequality; child session seeded SamplingConfig fields after handle_subagent_request / spawn path.
    5. Never log or assert full secrets in failure messages beyond equality with known fixture constants.
    6. Effort medium proof ties AGENT-03/06 automated path to child config (not live LLM).
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_isolation' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_isolation -- --nocapture; cargo test -p xai-grok-shell --test cross_provider_subagent p7_parent_model -- --nocapture</automated>
  </verify>
  <done>
    Both cross-provider directions proven isolated; at least one direction (prefer both) asserts mock HTTP Authorization against child fixture token per D-12; base_url matches child; parent model stability asserted; effort medium on child config when applicable. Resolve-only key_prefix alone is not acceptance.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — missing child slot fails closed with no wrong-backend request</name>
  <files>crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - Plan 03 gate behavior / send_failure messages
    - provider_routing mock server patterns for "no request" asserts
    - 07-RESEARCH.md Pitfall 2 empty Codex key
  </read_first>
  <behavior>
    - p7_missing_child_codex_no_wrong_backend_request: parent xAI usable, codex slot empty, spawn Codex child → failure contains bum login --provider codex; mock Codex host receives zero requests
    - p7_missing_child_xai_no_wrong_backend_request: inverse for empty xAI slot when parent is Codex
    - Complements pure gate tests — proves no parent-token probe to wrong host as primary UX
  </behavior>
  <action>
    Strengthen AGENT-05/06 automated fail-closed proofs (D-05..D-07).

    1. Empty child slot fixtures; dual mock servers or single mock that would catch wrong-host traffic if parent fallback occurred.
    2. Invoke spawn/preflight path (shell and/or Task eager if unit-reachable).
    3. Assert error suggestion/login text; assert mock request count 0 on child host.
    4. Do not accept mid-turn 401 as success criterion.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_missing' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_missing -- --nocapture</automated>
  </verify>
  <done>
    Missing child provider fails before wrong-backend traffic; login hint present both directions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dual fake tokens in fixtures | Distinct constants; equality asserts detect cross-slot bugs |
| Mock HTTP | Must not record real secrets; fixture tokens only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Elevation of Privilege | Child Authorization | high | mitigate | Dual-direction tests fail if parent token appears on child host |
| T-07-04 | Spoofing | Empty slot spawn | high | mitigate | No-outbound-request asserts + login error |
| T-07-01 | Information Disclosure | test output | medium | mitigate | Compare to fixture constants; avoid printing full auth.json |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
p7_isolation + p7_missing + p7_parent_model filters green under cross_provider_subagent.
</verification>

<success_criteria>
- AGENT-02/04 dual-direction isolation proven with mock HTTP Authorization (D-12 hard bar)
- AGENT-05 no wrong-backend on missing slot
- AGENT-06 automated path (model+effort) covered without live LLM
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-05-SUMMARY.md` when done
</output>
