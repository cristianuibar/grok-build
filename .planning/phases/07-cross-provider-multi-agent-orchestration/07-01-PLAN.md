---
phase: 07-cross-provider-multi-agent-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
must_haves:
  truths:
    - "Wave 1 compile-safe harness: p7_ discovery lists ≥1 tools and ≥1 shell green tests before later waves (Nyquist scaffolding)"
    - "All Plan 01 p7_ tests compile against TODAY's APIs and stay GREEN (no intentional-red under the p7_ filter)"
    - "Tools green contracts assert CURRENT Task behavior: reasoning_effort remains None on SubagentRuntimeOverrides after execute (gap lock until Plan 02)"
    - "Shell green contracts: dual-fixture smoke, pure missing_provider_gate_error login hint, existing Tool unknown-model rejection regression, resolve isolation stubs, same-provider friction-free gate helper"
    - "Smoke path compiles and runs (fixture tokens only; no live OAuth)"
    - "Phase gate / Plans 02–06 only execute green p7_ filters — no expected-red commit protocol; product behavior lands green in its owning plan"
  artifacts:
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
      provides: "p7_ Task tool unit contracts that compile today (effort-None gap lock + model wiring regression)"
      contains: p7_
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "Integration harness scaffold for cross-provider gate + isolation (all green)"
      contains: p7_
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
      provides: "Optional pure p7_ unit anchors near existing effort/resume tests"
      contains: p7_
  key_links:
    - from: Wave 1 p7_ green scaffold
      to: Plans 02–06 GREEN product proofs
      via: "unique p7_ prefixes become phase-gate discovery targets; later plans ADD green tests (never leave failing p7_)"
      pattern: "p7_"
    - from: cross_provider_subagent harness
      to: missing_provider_gate_error + public Phase 4 resolvers
      via: "fixtures mirror model_switch_gate / provider_routing dual fake tokens"
      pattern: "xai-fake-token|codex-fake-token|bum login --provider"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Create wave 1 **compile-safe green** automated harness (`p7_` prefix) that locks Nyquist discovery for AGENT-01..06 before product waves (per D-12, D-14, RESEARCH Validation Architecture; **addresses review HIGH: Plan 01 compile/RED protocol + p7_ poison**).

Purpose: Scaffold filters and dual-token harness without poisoning later `cargo test … p7_` runs or breaking compile on a missing `TaskToolInput.reasoning_effort` field.
Output: Tools + shell p7_ green tests (fixture tokens only) with ≥1 discoverable match per crate subgroup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-REVIEWS.md
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
@crates/common/xai-tool-types/src/task.rs
@crates/codegen/xai-grok-shell/tests/model_switch_gate.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | Omit `Task.model` → inherit parent model/provider |
| D-02 | Explicit model via `TaskModelValidator` / catalog; cross-provider allowed |
| D-03 | Expose `reasoning_effort` on Task; wire `SubagentRuntimeOverrides.reasoning_effort` |
| D-04 | Effort vocab `low`/`medium`/`high`/`xhigh` (+ aliases); invalid rejects fail-closed |
| D-05 | Usable child-provider credentials checked at spawn time (before bg launch) |
| D-06 | Fail closed — never parent bearer/backend fallback |
| D-07 | Error names provider + `bum login --provider {xai\|codex}` |
| D-08 | Same-provider usable spawn: no extra gate friction |
| D-09 | Child SamplingConfig from child model → provider → route |
| D-10 | Child turns use child provider bearer only |
| D-11 | Parent model/provider unchanged by spawn |
| D-12 | Dual fake-token automated isolation proofs |
| D-13 | NL orchestration via Task schema/docs (model + effort) |
| D-14 | AGENT-01 same-provider regression is a hard gate |
| D-15 | `resume_from` pins source model (ignore model override) |
| D-16 | Out of scope: workflow engine, cost dashboards, Phase 8 rebrand, Phase 9 live E2E |

**This plan covers:** compile-safe green scaffold + current-behavior locks for D-01..D-15 discovery. Product green for schema/gate/isolation is Plans 02–05.

## Expected-green protocol (HARD — review cycle 1)

**Addresses review HIGH: intentional-red poisons `p7_`.**

1. **No intentional-red under `p7_`.** Every test named `p7_*` that lands in this plan must compile and **pass** on current mainline APIs.
2. **Do not reference `TaskToolInput.reasoning_effort`** (field does not exist yet — Plan 02 adds it). Any test that needs that field lives entirely in Plan 02 (add field + tests + wire in one plan so cargo always compiles).
3. **Do not use `#[should_panic]` / failing asserts** to encode future product behavior. Document planned names for Plans 02–05 in SUMMARY / VALIDATION inventory only.
4. **No `#[ignore]` expected-red placeholders** under a filter the phase gate will `discover` as `p7_` — ignored tests still inflate `--list` counts and invite vacuous execute.
5. **Later plans** add new green `p7_*` tests in the same commit wave that implements the behavior (or after the API exists in an earlier task of that plan). Plan 06 phase gate requires **all green** — never “all green except intentional red.”
6. Historical smoke name may keep `p7_wave0_harness_smoke_*` stem for filter stability; frontmatter `wave: 1` remains authoritative.

## Scope boundaries

- Do **not** implement schema field, shell gate, eager preflight, or dual-token Authorization product work (Plans 02–05).
- Do **not** require live OAuth; use `xai-fake-token*` / `codex-fake-token*` only.
- Do **not** invent a workflow engine or TUI spawn modal.
- Prefer patterns from `model_switch_gate.rs` + `provider_routing.rs` + Task unit tests.
- New integration target: `cross_provider_subagent` under shell `tests/`.

## Cargo verify hygiene

- Unique Phase 7 green prefix: `p7_`.
- Never unfiltered `cargo test -p xai-grok-shell --lib` as the only gate.
- Chain with `set -euo pipefail`; discovery assert ≥1 before execute.
- Plan 01 verify must leave **zero failing** `p7_` tests in tools or shell.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN scaffold — Task tool p7_ contracts on CURRENT APIs (no reasoning_effort field)</name>
  <files>crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs</files>
  <read_first>
    - .planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md (Task schema + test analogs)
    - crates/common/xai-tool-types/src/task.rs (TaskToolInput fields end at model then task_id ~96–110 — NO reasoning_effort today)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs (`model_threads_to_runtime_overrides`, runtime_overrides hardcode reasoning_effort: None ~305–316)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs (SubagentRuntimeOverrides.reasoning_effort)
    - 07-REVIEWS.md Plan 01 HIGH compile/RED notes
  </read_first>
  <behavior>
    - p7_task_model_threads_to_runtime_overrides_still_sets_model: existing capture-backend path with explicit model still threads model into SubagentRequest (regression lock; GREEN today)
    - p7_reasoning_effort_override_is_none_until_schema_lands: after Task execute with model set (no effort API yet), runtime_overrides.reasoning_effort is None — documents current hardcode gap; stays GREEN; Plan 02 replaces/extends with positive wire tests after field exists
    - p7_task_tool_input_schema_includes_model_property: schemars schema properties include model (GREEN today; Plan 02 adds sibling schema test for reasoning_effort after field lands)
    - MUST NOT construct TaskToolInput { reasoning_effort: ... } — will not compile (review HIGH)
  </behavior>
  <action>
    Add compiling `#[cfg(test)]` unit tests with `p7_` prefix in task/mod.rs (per D-03 gap lock, D-13 schema surface readiness). Mirror capture-backend harness used by model_threads_to_runtime_overrides.

    Addresses review concern: Plan 01 compile/RED protocol — tests must compile against public APIs that exist today.

    1. Gap-lock effort: run Task execute with explicit model + run_in_background false; capture SubagentRequest; assert runtime_overrides.reasoning_effort.is_none() (current hardcode). This is a green documentation of the gap Plan 02 closes — do not assert Some(effort) here.
    2. Model wire regression: assert model override + provenance Tool still work (rename/wrap existing pattern under p7_ if helpful).
    3. Schema: assert TaskToolInput schemars properties contain model. Do **not** assert reasoning_effort property exists until Plan 02.
    4. Do **not** implement TaskToolInput.reasoning_effort, sanitize, or production wire here.
    5. In SUMMARY, list Plan 02-owned green names (schema includes reasoning_effort, wire medium, invalid reject, omit stays None) as inventory only — not as failing tests.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-tools --lib p7_ -- --list 2>/dev/null | grep -c 'p7_' || true); test "$n" -ge 1; cargo test -p xai-grok-tools --lib p7_ -- --nocapture</automated>
  </verify>
  <done>
    Tools crate lists ≥1 p7_ tests; all listed p7_ tests pass; zero references to TaskToolInput.reasoning_effort; no intentional failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN scaffold — shell cross_provider_subagent harness + pure p7_ contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/model_switch_gate.rs (BUM_HOME sandbox, dual fake tokens, p6_ discovery)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (XAI_FAKE / CODEX_FAKE, Authorization asserts)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (missing_provider_gate_error, ModelSwitchMissingProviderError.suggestion)
    - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs (task_model_override_error already rejects Tool unknown models ~28–43; effort warn-and-ignore ~472–486)
    - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs (reasoning_effort_explicit_overrides_role; resume pin tests)
    - 07-REVIEWS.md Plan 01 unknown-model + red-poison notes
  </read_first>
  <behavior>
    - p7_wave0_harness_smoke_compiles_and_runs: sandbox + dual fixture auth.json readable (GREEN forever)
    - p7_missing_provider_gate_error_suggests_bum_login_for_empty_codex: pure missing_provider_gate_error Some when Codex + empty slot; suggestion contains bum login --provider codex (GREEN via Phase 6 public API)
    - p7_missing_provider_gate_error_none_when_slot_usable: usable Codex slot → None (GREEN)
    - p7_spawn_same_provider_no_extra_friction_when_parent_usable: same-provider usable path → gate None (D-08 pure helper lock; GREEN)
    - p7_tool_unknown_model_rejected_by_existing_task_model_override_error: regression of EXISTING Tool-provenance rejection via task_model_override_error / catalog path — assert fails closed TODAY (do not claim parent-fallback is the Tool path; addresses review MEDIUM stale premise)
    - p7_resolve_route_isolates_base_url_key_prefix_both_directions: public Phase 4 resolvers or catalog-driven route helpers for a Grok model and gpt-5.6-sol; base_url hosts and key_prefix differ (GREEN if public; else document defer to Plan 05 in-crate seam — do not call private resolve_model_override_to_config from external test)
    - Optional lib unit anchors only if they stay green without new product code
  </behavior>
  <action>
    Create `crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs` integration harness (per D-05..D-12, D-14) modeled on model_switch_gate / provider_routing.

    Addresses review concerns: compile-safe scaffold; Tool unknown-model is existing reject (not future parent-fallback rework).

    1. Smoke GREEN: BUM_HOME sandbox, dual-slot auth fixture with xai-fake-token / codex-fake-token, assert read_provider_auth_store works. serial_test if env mutation required.
    2. Pure decision tests call public missing_provider_gate_error: suggestion exact `bum login --provider {id}`; provider id present. These stay green and become spawn-path complements after Plan 03.
    3. Tool unknown-model: frame as **regression of existing** task_model_override_error / Task-path reject (handle_request.rs already rejects Tool explicit unknown before the non-Tool fallback block). Do not write a RED “will stop inheriting parent” test based on stale fallback premise. If external test cannot call pub(super) task_model_override_error, assert via public catalog/TaskModelValidator path or a thin lib unit under subagent/tests; keep GREEN.
    4. Isolation stubs: only public APIs from external integration test. Full D-12 mock HTTP Authorization is Plan 05 (with in-crate/public test seam). Do not call private resolve_model_override_to_config from tests/.
    5. Same-provider pure anchor: missing_provider_gate_error None when slot usable (full lifecycle spawn/resume is Plan 06).
    6. Do not implement handle_request gate, worktree reorder, or Task preflight here.
    7. SUMMARY inventory: list Plan 03/04/05-owned green names (pre-worktree gate, eager preflight, dual Authorization) without landing failing tests.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_ -- --nocapture; cargo test -p xai-grok-shell --test cross_provider_subagent p7_wave0_harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    cross_provider_subagent lists ≥1 p7_ tests; entire p7_ filter green; smoke green; no intentional-red; Tool unknown-model framed as existing rejection regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → auth.json | Fake tokens only; never real OAuth secrets in tree |
| Wave 1 green harness → CI | Must not land failing p7_ tests that poison later waves |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | harness fixtures | medium | mitigate | Use xai-fake-token* / codex-fake-token* only; never log full Authorization bodies in asserts |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
Tools p7_ discovery ≥1 and all green; shell cross_provider_subagent p7_ discovery ≥1 and all green; smoke green; product inventory documented for Plans 02–05 without failing tests.
</verification>

<success_criteria>
- Wave 1 compile-safe green harness exists for AGENT-01..06 filter discovery
- Unique p7_ prefixes for phase gate; zero intentional-red under p7_
- No product green scope creep; no TaskToolInput.reasoning_effort references
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md` when done
</output>
