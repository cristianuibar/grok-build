---
phase: 07-cross-provider-multi-agent-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
must_haves:
  truths:
    - "Wave 1 RED harness: p7_ discovery lists ≥1 tools contract and ≥1 shell contract for AGENT-01..06 before green waves (Nyquist contract scaffolding)"
    - "Tools RED contracts cover Task.reasoning_effort schema presence, override wiring, and invalid-effort reject"
    - "Shell RED contracts cover spawn missing-provider decision, Tool unknown-model fail-closed, dual-direction resolve isolation stubs, and same-provider regression anchors"
    - "Smoke path compiles and runs (fixture tokens only; no live OAuth)"
    - "No product green implementation required in this plan — intentional RED until Plans 02–05"
  artifacts:
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
      provides: "p7_ Task tool unit contracts for schema/effort/wiring (RED until Plan 02)"
      contains: p7_
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "Integration harness for cross-provider spawn gate + isolation (smoke green + RED contracts)"
      contains: p7_
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
      provides: "Optional pure p7_ unit anchors near existing effort/resume tests"
      contains: p7_
  key_links:
    - from: Wave 1 p7_ RED filters
      to: Plans 02–06 GREEN
      via: "unique p7_ prefixes become phase-gate discovery targets"
      pattern: "p7_"
    - from: cross_provider_subagent harness
      to: missing_provider_gate_error + resolve_model_override_to_config
      via: "fixtures mirror model_switch_gate / provider_routing dual fake tokens"
      pattern: "xai-fake-token|codex-fake-token|bum login --provider"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Create wave 1 RED automated contracts (`p7_` prefix; Nyquist contract scaffolding formerly called “Wave 0” in research) that encode AGENT-01..06 product behavior before implementation (per D-12, D-14, RESEARCH Validation Architecture). Frontmatter `wave: 1` is authoritative — this is the first execution wave. Smoke compiles green; contract tests are allowed RED until later waves.

Purpose: Lock Nyquist filters and prevent false-green empty discovery for Phase 7.
Output: Tools + shell p7_ harnesses (fixture tokens only) that discover with ≥1 match per subgroup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
@crates/common/xai-tool-types/src/task.rs
@crates/codegen/xai-grok-shell/tests/model_switch_gate.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | Omit `Task.model` → inherit parent model/provider |
| D-02 | Explicit model via `TaskModelValidator` / catalog; cross-provider allowed |
| D-03 | Expose `reasoning_effort` on Task; wire `SubagentRuntimeOverrides.reasoning_effort` |
| D-04 | Effort vocab `low`/`medium`/`high`/`xhigh` (+ aliases); invalid rejects fail-closed |
| D-05 | Usable child-provider credentials checked at spawn time (before bg launch) |
| D-06 | Fail closed — never parent bearer/backend fallback |
| D-07 | Error names provider + `bum login --provider {xai\|codex}` |
| D-08 | Same-provider usable spawn: no extra gate friction |
| D-09 | Child SamplingConfig from child model → provider → route |
| D-10 | Child turns use child provider bearer only |
| D-11 | Parent model/provider unchanged by spawn |
| D-12 | Dual fake-token automated isolation proofs |
| D-13 | NL orchestration via Task schema/docs (model + effort) |
| D-14 | AGENT-01 same-provider regression is a hard gate |
| D-15 | `resume_from` pins source model (ignore model override) |
| D-16 | Out of scope: workflow engine, cost dashboards, Phase 8 rebrand, Phase 9 live E2E |

**This plan covers:** test contracts for D-01..D-15 (RED anchors). No green product wiring.

## Scope boundaries

- Do **not** implement schema field, shell gate, or eager preflight production code (Plans 02–04).
- Do **not** require live OAuth; use `xai-fake-token*` / `codex-fake-token*` only.
- Do **not** invent a workflow engine or TUI spawn modal.
- Prefer extending patterns from `model_switch_gate.rs` + `provider_routing.rs` + Task unit tests.
- New integration target name: `cross_provider_subagent` under shell `tests/`.

## Cargo verify hygiene

- Unique Phase 7 prefix: `p7_`.
- Never use unfiltered `cargo test -p xai-grok-shell --lib` as the only gate.
- Chain with `set -euo pipefail`; discovery assert ≥1 before relying on a filter.
- Intentional RED is OK for contract tests; document which names stay RED until which plan.
- Naming: execution wave = 1 (this plan). Prose “RED harness / Nyquist contract scaffolding” replaces ambiguous “Wave 0” labels.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Task tool p7_ contracts for effort schema, wiring, invalid reject</name>
  <files>crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs</files>
  <read_first>
    - .planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md (Task schema + test analogs)
    - crates/common/xai-tool-types/src/task.rs (TaskToolInput.model field shape ~96–104)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs (`task_tool_input_schema_includes_model`, `model_threads_to_runtime_overrides`, runtime_overrides hardcode reasoning_effort: None ~305–316)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs (SubagentRuntimeOverrides.reasoning_effort)
  </read_first>
  <behavior>
    - p7_task_tool_input_schema_includes_reasoning_effort: schemars JSON schema properties include reasoning_effort with description listing product tokens
    - p7_reasoning_effort_threads_to_runtime_overrides: Task execute with model + reasoning_effort medium threads Some("medium") into SubagentRuntimeOverrides (and provenance Tool)
    - p7_invalid_reasoning_effort_rejected_before_spawn: nonsense effort returns ToolError invalid_arguments listing accepted tokens; no SubagentRequest spawn observed
    - p7_omitted_reasoning_effort_stays_none_on_overrides: omit effort → overrides.reasoning_effort is None (inherit path)
    - Existing model_threads_to_runtime_overrides remains valid; either update its None assert in Plan 02 or keep parallel p7_ for the new field
  </behavior>
  <action>
    Add compiling `#[cfg(test)]` unit tests with `p7_` prefix in task/mod.rs (per D-03, D-04, D-13). Mirror existing capture-backend harness used by model_threads_to_runtime_overrides.

    1. Schema test: serialize schemars schema_for TaskToolInput; assert properties.reasoning_effort exists and description mentions medium (and product effort guidance). Today this fails until Plan 02 adds the field — either #[should_panic]/ignore is forbidden; prefer assert that fails cleanly (RED) or gate behind a helper that Plan 02 makes pass. Standard RED-harness pattern: write the assert; leave RED until Plan 02.
    2. Wiring test: build Task tool input with reasoning_effort Some("medium"), run_in_background false, capture SubagentRequest; assert runtime_overrides.reasoning_effort == Some("medium") and model_override_provenance Tool. RED while hardcode None remains.
    3. Invalid effort test: pass reasoning_effort Some("not-a-real-effort"); expect Err invalid_arguments whose message lists accepted tokens (low/medium/high/xhigh or ReasoningEffort vocabulary). RED until Plan 02 eager parse reject.
    4. Omitted effort: assert None on overrides (should already be green if hardcode None — keep as regression lock for D-01-style omit inherit).
    5. Do not implement TaskToolInput.reasoning_effort or production wire here. Minimal compile-only stubs only if a test cannot name the field yet — prefer writing tests against the intended API so Plan 02 turns them green without rewriting names.
  </action>
  <verify>
    <automated>set -euo pipefail; list=$(cargo test -p xai-grok-tools --lib p7_ -- --list 2>/dev/null | grep -E 'p7_' || true); echo "$list"; test "$(echo "$list" | grep -c 'p7_' || true)" -ge 1</automated>
  </verify>
  <done>
    Tools crate lists ≥1 p7_ tests; schema/wiring/invalid-effort contracts exist and are RED or partial-green only where current code already satisfies omit-None.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — shell cross_provider_subagent harness + pure p7_ spawn contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/model_switch_gate.rs (BUM_HOME sandbox, dual fake tokens, p6_ discovery)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (XAI_FAKE / CODEX_FAKE, Authorization asserts)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (missing_provider_gate_error, ModelSwitchMissingProviderError.suggestion)
    - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs (model_unknown parent fallback ~435–449; effort warn-and-ignore ~472–486)
    - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs (reasoning_effort_explicit_overrides_role; resume pin tests)
    - .planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md (Wave 0 Gaps)
  </read_first>
  <behavior>
    - p7_wave0_harness_smoke_compiles_and_runs: sandbox + dual fixture auth.json readable (must stay GREEN; name keeps wave0 stem for historical filter stability)
    - p7_spawn_missing_provider_gate_blocks_codex_child_when_codex_slot_empty: pure or harness decision — missing_provider_gate_error Some when Codex child + empty slot; message/suggestion contains bum login --provider codex (RED until Plan 03 wires spawn path)
    - p7_spawn_missing_provider_allows_when_slot_usable: usable Codex slot → gate None (green via pure helper reuse)
    - p7_spawn_same_provider_no_extra_friction_when_parent_usable: same-provider path does not invent missing-provider when slot usable (D-08)
    - p7_tool_unknown_model_does_not_inherit_parent_provider: Tool provenance explicit unknown model must not silently adopt parent SamplingConfig (RED until Plan 03 fail-closed)
    - p7_resolve_model_override_isolates_base_url_and_key_prefix_both_directions: xAI vs Codex resolved configs differ on base_url + key_prefix (may be partially green via existing resolve_model_override_to_config)
    - p7_parent_model_unchanged_after_cross_provider_spawn_contract: contract documents parent model_id stability (RED until spawn path exercised)
    - Optional lib unit: p7_invalid_effort_tool_provenance_fails_closed near existing effort tests (RED until Plan 03)
  </behavior>
  <action>
    Create `crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs` integration harness (per D-05..D-12, D-14) modeled on model_switch_gate / provider_routing:

    1. Smoke GREEN: ensure BUM_HOME sandbox, write dual-slot auth fixture with xai-fake-token / codex-fake-token, assert read_provider_auth_store works. Use serial_test if env mutation required.
    2. Pure decision tests may call public `missing_provider_gate_error` directly (already green from Phase 6) — still name them p7_ to lock spawn-oriented copy expectations: suggestion exact `bum login --provider {id}`; human message may use spawn wording once Plan 03 lands (for this RED wave, assert suggestion + provider id at minimum).
    3. RED spawn-path contracts: if full handle_subagent_request / MvpAgent spawn is too heavy for this plan, write pure helpers + document which RED names Plan 03/05 must green via real spawn. Prefer at least one path that will call production spawn/gate after Plan 03 (same pattern as p6_missing_provider_apply_*).
    4. Both-direction isolation stubs: resolve_model_override_to_config or public Phase 4 resolvers for a Grok model and gpt-5.6-sol (or catalog Codex id); assert base_url hosts and credential key_prefix differ; never assert full tokens. (Full D-12 mock HTTP Authorization is Plan 05 — stubs here are fine.)
    5. Same-provider regression anchor: name a p7_same_provider_* test that either wraps an existing subagent unit path or asserts missing_provider_gate_error None when slot usable (full resume/roles/personas stay Plan 06).
    6. Do not implement handle_request gate or Task preflight here.
  </action>
  <verify>
    <automated>set -euo pipefail; list=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -E 'p7_' || true); echo "$list"; test "$(echo "$list" | grep -c 'p7_' || true)" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_wave0_harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    cross_provider_subagent lists ≥1 p7_ tests; smoke green; missing-provider / isolation / fail-closed contracts named for Plans 03–05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → auth.json | Fake tokens only; never real OAuth secrets in tree |
| Wave 1 RED harness → CI | Intentional failures must not be skipped silently without plan notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | harness fixtures | medium | mitigate | Use xai-fake-token* / codex-fake-token* only; never log full Authorization bodies in asserts |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
Tools p7_ discovery ≥1; shell cross_provider_subagent p7_ discovery ≥1; smoke green; remaining contracts documented RED.
</verification>

<success_criteria>
- Wave 1 RED harness exists for AGENT-01..06 filters
- Unique p7_ prefixes for phase gate
- No product green scope creep
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md` when done
</output>
