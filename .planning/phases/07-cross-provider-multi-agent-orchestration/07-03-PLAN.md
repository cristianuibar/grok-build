---
phase: 07-cross-provider-multi-agent-orchestration
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
  - crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
autonomous: true
requirements:
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
must_haves:
  truths:
    - "After effective child model resolve, spawn runs missing_provider_gate_error for child's catalog provider before spawn_session_on_thread (D-05)"
    - "Missing/unusable child OAuth slot fails closed via send_failure with provider name + bum login --provider hint — never parent bearer rewrite (D-06, D-07)"
    - "Same-provider spawn with usable slot has no extra missing-provider friction (D-08)"
    - "BYOK has_own_credentials skips OAuth-slot gate (same semantics as Phase 6)"
    - "Tool provenance invalid reasoning_effort fails closed (send_failure) — no warn-and-ignore (D-04)"
    - "Tool provenance explicit effort on model that lacks reasoning-effort support fails closed with clear message (discretion hard-fail)"
    - "Tool provenance unknown explicit model fails closed — no parent SamplingConfig fallback (D-06, RESEARCH pitfall)"
    - "resume_from still pins source model and ignores caller model override (D-15)"
    - "Child SamplingConfig still built via resolve_model_override_to_config / resolve path — provider-aware base_url + slot (D-09)"
    - "Parent session model is not mutated by spawn (D-11)"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
      provides: "Authoritative spawn gate + Tool effort/unknown-model fail-closed"
      contains: missing_provider_gate_error
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
      provides: "Shared usable-slot helper and/or resolve fail-closed helpers if extracted"
      contains: resolve_model_override_to_config
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "GREEN p7_ spawn gate + Tool fail-closed contracts"
      contains: p7_spawn_missing_provider
  key_links:
    - from: handle_subagent_request effective model
      to: missing_provider_gate_error
      via: "after resolve + resume pin; before credential seed / spawn_session_on_thread"
      pattern: "missing_provider_gate_error"
    - from: ModelOverrideProvenance::Tool invalid effort
      to: send_failure
      via: "ReasoningEffort parse Err or unsupported model + explicit effort"
      pattern: "send_failure"
    - from: Tool unknown model
      to: send_failure
      via: "replace parent fallback block for Tool provenance"
      pattern: "model_unknown"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Implement the authoritative shell spawn path: missing-provider credential gate, Tool effort fail-closed, Tool unknown-model fail-closed, preserve resume pin and provider-aware child resolve (per D-04..D-11, D-15). Greens shell p7_ gate contracts from Plan 01 that do not require full dual-token wire Authorization (Plan 05).

Purpose: Single authority so Task and harness cannot diverge on fail-closed spawn policy (AGENT-02/03/04/05 core).
Output: handle_subagent_request gate + effort reject + no parent fallback for Tool explicit model.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md
@crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
@crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/auth/status.rs
@crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-02 | Explicit model resolve stays catalog-wide (no parent-provider filter) |
| D-04 | Invalid effort reject for Tool path in shell |
| D-05 | Spawn-time usable credential check |
| D-06 | No parent bearer/backend fallback |
| D-07 | Login CLI hint in failure message |
| D-08 | Same-provider no extra friction |
| D-09 | Child route from child model |
| D-11 | Parent model unchanged |
| D-15 | Resume model pin |

**Discretion (document in code comments lightly):**
- Authoritative gate lives in `handle_subagent_request` (Task eager mirror is Plan 04).
- Extract shared `provider_oauth_slot_usable` from model_switch if clean; otherwise duplicate thin adapter on SubagentSpawnContext (prefer extract to avoid drift).
- Explicit effort on model without reasoning-effort support: **hard fail** for Tool provenance; harness/role soft paths may keep existing skip when effort only from role default and model lacks support.
- Error code: tool/string via send_failure is sufficient; may reuse suggestion from ModelSwitchMissingProviderError; optional SUBAGENT_SPAWN_MISSING_PROVIDER not required if suggestion text matches.

## Scope boundaries

- Do **not** implement TaskProviderCredentialGate / background eager preflight (Plan 04).
- Do **not** require mock HTTP Authorization proofs (Plan 05) — pure resolve + gate + unit send_failure is enough here.
- Do **not** call parent set_session_model on spawn.
- Do **not** log full tokens — key_prefix only.
- Fixture tokens only.

## Cargo verify hygiene

- `cargo test -p xai-grok-shell --test cross_provider_subagent p7_`
- Narrow lib filters `p7_` under subagent tests; use `--lib` when bare package list is poisoned by unrelated integration targets (Phase 6 lesson).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — authoritative missing-provider spawn gate after model resolve</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs, crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs, crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs (provider_oauth_slot_usable + missing_provider_gate_error call ~24–97)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (missing_provider_gate_error, ModelSwitchMissingProviderError)
    - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs (resolve_effective_model_config ~424–488; credential seed ~761+; spawn_session_on_thread)
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs (resolve_model_override_to_config; send_failure)
    - 07-PATTERNS.md spawn gate placement section
  </read_first>
  <behavior>
    - p7_spawn_missing_provider_gate_blocks_codex_child_when_codex_slot_empty GREEN on real spawn/handle path or pure helper composition that production also calls
    - p7_spawn_missing_provider_allows_when_slot_usable GREEN
    - p7_spawn_same_provider_no_extra_friction_when_parent_usable GREEN
    - BYOK skip: has_own_credentials true → no missing-provider failure
    - Gate uses catalog ModelEntry.info.provider only
  </behavior>
  <action>
    Implement D-05..D-08 in handle_subagent_request (authoritative).

    1. After effective model id/config is fully resolved (including resume pin re-resolve), look up catalog entry for effective_model_id; determine ModelProvider and has_own_credentials.
    2. Compute slot usable with the same semantics as model_switch::provider_oauth_slot_usable (disk store via read_provider_auth_store + provider_slot_usable; xAI may consult live AuthManager; Codex never inferred from xAI AuthManager alone). Prefer extracting a shared pub(crate) helper used by both model_switch and subagent spawn to prevent drift.
    3. Call missing_provider_gate_error(provider, model_id, has_own_credentials, slot_usable). If Some(err):
       - send_failure with human message adapted for spawn (e.g. Cannot spawn subagent with model '{id}': no usable {ProviderLabel} credentials. Run: {suggestion}) where suggestion is err.suggestion (`bum login --provider {id}`).
       - return before worktree side effects that are expensive if possible; **must** return before spawn_session_on_thread and before seeding child credentials from a wrong/empty route.
    4. Same-provider usable: gate returns None — no new friction.
    5. Green Plan 01 p7_spawn_missing_provider_* tests; strengthen harness to call production path if Wave 0 only had pure helpers.
    6. Do not rewrite parent credentials into child slot on failure or success.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_spawn_missing_provider' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_spawn_missing_provider -- --nocapture; cargo test -p xai-grok-shell --test cross_provider_subagent p7_spawn_same_provider -- --nocapture</automated>
  </verify>
  <done>
    Spawn fails closed on missing child provider with login hint; usable same-provider and dual-login paths not blocked by gate false positives.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Tool effort fail-closed + unknown model no parent fallback</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - handle_request.rs model_unknown parent fallback ~435–449
    - handle_request.rs effort apply warn-and-ignore ~472–486
    - handle_request.rs task_model_override_error (Tool-only) ~28–44
    - subagent/tests/mod.rs reasoning_effort_explicit_overrides_role
    - xai-grok-sampling-types ReasoningEffort FromStr
  </read_first>
  <behavior>
    - Tool provenance + invalid effort string → send_failure; no spawn (D-04)
    - Tool provenance + valid effort on supported model → effective_sampling_config.reasoning_effort set (AGENT-03)
    - Tool provenance + explicit effort on unsupported model → send_failure with clear message (discretion hard-fail)
    - Harness provenance may retain warn/skip for role-default edge cases if needed — do not loosen Tool path
    - Tool provenance + unknown explicit model → send_failure catalog-style error; no parent config inherit (D-06)
    - resume pin path unchanged (D-15)
    - Existing reasoning_effort_explicit_overrides_role stays green
  </behavior>
  <action>
    Strengthen shell fail-closed edges (per D-04, D-06, RESEARCH pitfalls 2–4).

    1. Effort block: when effective_runtime.reasoning_effort is Some:
       - Always parse with ReasoningEffort::from_str first.
       - On parse Err: if provenance is Tool (or always for safety on explicit override), send_failure with accepted tokens; return. Stop warn-and-ignore for Tool.
       - On parse Ok: if model_supports_reasoning_effort, apply to effective_sampling_config; else if Tool provenance, send_failure ("model does not support reasoning_effort"); if Harness/role-only, discretion soft-skip OK.
    2. model_unknown block: when provenance is Tool and not resume, send_failure with task/catalog error (reuse task_model_override_error / task_model_error_for_catalog) instead of parent fallback. Harness internal unknown may keep prior behavior only if needed for legacy — prefer fail-closed for explicit overrides generally; **must** fail-closed for Tool.
    3. Ensure resolve_model_override_to_config remains the path for successful explicit models (D-09); do not simplify to parent-only keys.
    4. Add/green p7_invalid_effort_tool_provenance_fails_closed and p7_tool_unknown_model_does_not_inherit_parent_provider.
    5. Confirm no set_session_model on parent from this path (D-11).
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-shell --lib p7_ -- --nocapture 2>/dev/null || true; cargo test -p xai-grok-shell --test cross_provider_subagent p7_tool -- --nocapture; cargo test -p xai-grok-shell --lib reasoning_effort_explicit -- --nocapture</automated>
  </verify>
  <done>
    Tool invalid effort and unknown model fail closed; valid effort applies; resume + role effort tests still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SubagentRequest → handle_subagent_request | Untrusted model/effort from tools or harness |
| Auth store → spawn gate | Usable decision must not leak token material into errors |
| Child SamplingConfig seed | Must use child provider slot only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Spoofing | Cross-provider spawn with empty child slot | high | mitigate | missing_provider_gate_error before spawn; no parent bearer fallback (D-05/D-06) |
| T-07-05 | Elevation of Privilege | Parent token on child backend | high | mitigate | resolve_model_override_to_config dual-key; gate blocks empty slot spawn |
| T-07-06 | Information Disclosure | send_failure messages | high | mitigate | CLI suggestion only; never include access tokens or auth.json body |
| T-07-07 | Tampering | Client-supplied provider claim | high | mitigate | Provider from catalog entry after resolve only |
| T-07-08 | Bypass | Background Task without shell gate | medium | mitigate | Shell remains authoritative; Plan 04 adds eager preflight for UX timing |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
cross_provider_subagent p7_spawn_missing_provider + p7_tool filters green; role effort regression green.
</verification>

<success_criteria>
- AGENT-05 shell authority complete
- AGENT-02/04 resolve path fail-closed at spawn
- AGENT-03 shell apply fail-closed for Tool effort
- D-04..D-09, D-11, D-15 implemented in shell
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md` when done
</output>
