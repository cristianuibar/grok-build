---
phase: 07-cross-provider-multi-agent-orchestration
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
  - crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/auth/status.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
autonomous: true
requirements:
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
must_haves:
  truths:
    - "Authoritative missing-provider gate runs AFTER effective child model is known and BEFORE worktree create/rehydrate and other expensive durable pre-spawn side effects (D-05; review HIGH gate-after-worktree)"
    - "Missing/unusable child OAuth slot fails closed via send_failure with provider name + bum login --provider hint — never parent bearer rewrite (D-06, D-07)"
    - "Missing-provider failure creates neither isolated worktree nor child session (review suggestion)"
    - "Same-provider spawn with usable slot has no extra missing-provider friction (D-08)"
    - "BYOK has_own_credentials skips OAuth-slot gate (same semantics as Phase 6)"
    - "provider_slot_usable is a pure helper with explicit auth_path / live_xai_auth inputs — not MvpAgent-bound private switch helper only (review MEDIUM)"
    - "Tool provenance invalid reasoning_effort fails closed (send_failure) — no warn-and-ignore (D-04)"
    - "Tool provenance explicit effort: supported fixture (e.g. Sol) applies; unsupported fixture hard-fails with clear message (discretion + review MEDIUM fixtures)"
    - "Tool provenance unknown explicit model: harden/assert EXISTING task_model_override_error reject — do not rework a stale parent-fallback premise for Tool path (D-06)"
    - "resume_from still pins source model and ignores caller model override (D-15)"
    - "Child SamplingConfig still built via resolve_model_override_to_config / resolve path — provider-aware base_url + slot (D-09)"
    - "Parent session model is not mutated by spawn (D-11)"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
      provides: "Pre-worktree spawn gate + Tool effort fail-closed; early model preflight helper"
      contains: missing_provider_gate_error
    - path: crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
      provides: "Calls shared pure provider_slot_usable (no longer sole private MvpAgent-only copy)"
      contains: provider_slot_usable
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "GREEN p7_ spawn gate + no-worktree-on-fail + Tool reject contracts"
      contains: p7_spawn_missing_provider
  key_links:
    - from: handle_subagent_request early effective model resolve
      to: missing_provider_gate_error
      via: "side-effect-free preflight resolve → gate → THEN worktree create/rehydrate → spawn_session_on_thread"
      pattern: "missing_provider_gate_error"
    - from: ModelOverrideProvenance::Tool invalid effort
      to: send_failure
      via: "ReasoningEffort parse Err or unsupported model + explicit effort"
      pattern: "send_failure"
    - from: Tool unknown model
      to: task_model_override_error / send_failure
      via: "existing Tool reject path; regression tests only unless harness gap found"
      pattern: "task_model_override_error"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Implement the authoritative shell spawn path: **pre-worktree** missing-provider credential gate, pure provider_slot_usable helper, Tool effort fail-closed with supported/unsupported fixtures, Tool unknown-model regression of existing reject, preserve resume pin and provider-aware child resolve (per D-04..D-11, D-15). Greens shell p7_ gate contracts that do not require full dual-token wire Authorization (Plan 05).

Purpose: Single authority so Task and harness cannot diverge on fail-closed spawn policy (AGENT-02/03/04/05 core); gate before expensive durable side effects (review HIGH).
Output: handle_subagent_request early gate + effort reject + pure usable helper + no parent fallback for Tool explicit model.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-REVIEWS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md
@crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
@crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
@crates/codegen/xai-grok-shell/src/agent/config.rs
@crates/codegen/xai-grok-shell/src/auth/status.rs
@crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-02 | Explicit model resolve stays catalog-wide (no parent-provider filter) |
| D-04 | Invalid effort reject for Tool path in shell |
| D-05 | Spawn-time usable credential check |
| D-06 | No parent bearer/backend fallback |
| D-07 | Login CLI hint in failure message |
| D-08 | Same-provider no extra friction |
| D-09 | Child route from child model |
| D-11 | Parent model unchanged |
| D-15 | Resume model pin |

**Discretion (document lightly):**
- Authoritative gate lives in `handle_subagent_request` (Task eager mirror is Plan 04).
- Extract pure `provider_slot_usable(auth_path, provider, live_xai_auth: Option&lt;...&gt;)` (or equivalent explicit inputs) shared by model_switch and subagent — **not** a private MvpAgent-only helper (review MEDIUM).
- Explicit effort on model without reasoning-effort support: **hard fail** for Tool provenance; harness/role soft paths may keep existing skip when effort only from role default and model lacks support.
- Error code: tool/string via send_failure is sufficient; reuse suggestion from ModelSwitchMissingProviderError.

## Scope boundaries

- Do **not** implement TaskProviderCredentialGate / background eager preflight (Plan 04).
- Do **not** require mock HTTP Authorization proofs (Plan 05) — pure resolve + gate + unit send_failure is enough here; optionally start a thin pub(crate) test helper if needed for Plan 05 seam (do not own dual-token wire here).
- Do **not** call parent set_session_model on spawn.
- Do **not** log full tokens — key_prefix only.
- Fixture tokens only.
- Do **not** rework Tool unknown-model path on a stale “falls back to parent” premise if task_model_override_error already rejects (review MEDIUM).

## Cargo verify hygiene

- `cargo test -p xai-grok-shell --test cross_provider_subagent p7_`
- Narrow lib filters `p7_` under subagent tests; use `--lib` when bare package list is poisoned.
- All Plan 03 p7_ tests must be green (no intentional-red).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — pure provider_slot_usable + pre-worktree missing-provider spawn gate</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs, crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs, crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs, crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/auth/status.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs (private provider_oauth_slot_usable ~24–47 — MvpAgent-bound; extract pure form)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (missing_provider_gate_error, ModelSwitchMissingProviderError)
    - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs (worktree setup ~217–366 BEFORE model resolve ~423+; gate must move earlier)
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs (resolve_model_override_to_config; send_failure; resolve precedence including role pins)
    - 07-PATTERNS.md spawn gate placement section
    - 07-REVIEWS.md Plan 03 HIGH gate-after-worktree + pure helper
  </read_first>
  <behavior>
    - p7_spawn_missing_provider_gate_blocks_codex_child_when_codex_slot_empty GREEN — production path or composition production also calls
    - p7_spawn_missing_provider_allows_when_slot_usable GREEN
    - p7_spawn_same_provider_no_extra_friction_when_parent_usable GREEN
    - p7_spawn_missing_provider_creates_neither_worktree_nor_child_session: missing slot → fail; no worktree path created/rehydrated for this request; no child session spawn (review suggestion)
    - BYOK skip: has_own_credentials true → no missing-provider failure
    - Gate uses catalog ModelEntry.info.provider only
    - Pure provider_slot_usable unit-tested with explicit path + optional live auth inputs
  </behavior>
  <action>
    Implement D-05..D-08 in handle_subagent_request with **correct ordering** (addresses review HIGH: gate after worktree).

    1. **Extract pure helper:** Move usable-slot logic out of MvpAgent-private `provider_oauth_slot_usable` into a shared pure function (e.g. in config.rs or auth/status.rs) with explicit inputs: auth store path (or store snapshot), ModelProvider, optional live xAI AuthManager handle for refreshable xAI fallback. Codex never inferred from xAI AuthManager alone. model_switch and subagent both call this pure helper.
    2. **Early side-effect-free model preflight:** Before worktree create/rehydrate (today ~217+), resolve the **effective child model id + catalog provider + has_own_credentials** using the same precedence rules as later full resolve (explicit override, resume pin, role/config pins, omit→parent inherit). Extract a pure/preflight helper that returns the effective model decision without mutating session or creating worktrees. Reuse its output later rather than resolving twice with divergent logic.
    3. **Gate before expensive durable side effects:** Call missing_provider_gate_error(provider, model_id, has_own_credentials, slot_usable). If Some(err):
       - send_failure with spawn-oriented human message + suggestion `bum login --provider {id}`
       - return **before** worktree create/rehydrate, pending insertion that assumes spawn, credential seed, and spawn_session_on_thread
    4. After gate None: proceed with existing worktree + full resolve path; ensure full resolve agrees with preflight effective model (or re-check gate only if something legitimately changes — prefer single resolve).
    5. Same-provider usable: gate returns None — no new friction.
    6. Green Plan 01 p7_* gate helpers; strengthen harness to exercise production ordering (assert no worktree on missing provider when testable).
    7. Do not rewrite parent credentials into child slot on failure or success.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_spawn_missing_provider' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_spawn_missing_provider -- --nocapture; cargo test -p xai-grok-shell --test cross_provider_subagent p7_spawn_same_provider -- --nocapture; cargo test -p xai-grok-shell --lib provider_slot_usable -- --nocapture 2>/dev/null || cargo test -p xai-grok-shell --lib p7_provider_slot -- --nocapture</automated>
  </verify>
  <done>
    Spawn fails closed on missing child provider with login hint **before** worktree create/rehydrate; pure provider_slot_usable shared; usable same-provider and dual-login paths not blocked by false positives.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Tool effort fail-closed (supported+unsupported fixtures) + unknown-model existing-reject regression</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - handle_request.rs task_model_override_error (Tool-only) ~28–44 — already rejects unknown Tool models
    - handle_request.rs model_unknown parent fallback ~435–449 — principally non-Tool/internal paths
    - handle_request.rs effort apply warn-and-ignore ~472–486
    - subagent/tests/mod.rs reasoning_effort_explicit_overrides_role
    - xai-grok-sampling-types ReasoningEffort FromStr
    - 07-REVIEWS.md Plan 03 unknown-model stale premise + unsupported-effort fixtures
  </read_first>
  <behavior>
    - Tool provenance + invalid effort string → send_failure; no spawn (D-04)
    - Tool provenance + valid effort on **supported** model fixture (e.g. Codex Sol / catalog model with reasoning-effort support) → effective_sampling_config.reasoning_effort set (AGENT-03)
    - Tool provenance + explicit effort on **unsupported** model fixture → send_failure with clear message (discretion hard-fail) — both fixtures required
    - Harness provenance may retain warn/skip for role-default edge cases if needed — do not loosen Tool path
    - Tool provenance + unknown explicit model → existing task_model_override_error / catalog reject; regression test asserts fail-closed; do not invent a rework of parent fallback for Tool if already rejected
    - resume pin path unchanged (D-15)
    - Existing reasoning_effort_explicit_overrides_role stays green
  </behavior>
  <action>
    Strengthen shell fail-closed edges (per D-04, D-06, RESEARCH pitfalls). Addresses review: scope unknown-model to existing reject; add supported+unsupported effort fixtures.

    1. Effort block: when effective_runtime.reasoning_effort is Some:
       - Always parse with ReasoningEffort::from_str first (accepts canonical tokens from Plan 02 Task wire).
       - On parse Err: if provenance is Tool (or always for safety on explicit override), send_failure with accepted tokens; return. Stop warn-and-ignore for Tool.
       - On parse Ok: if model_supports_reasoning_effort, apply to effective_sampling_config; else if Tool provenance, send_failure ("model does not support reasoning_effort"); if Harness/role-only, discretion soft-skip OK.
    2. Unknown model: **Harden/assert existing Tool rejection** via task_model_override_error (already early in handle_subagent_request). Add/green p7_tool_unknown_model_* as regression that Tool explicit unknown still fails closed with catalog-style error. Only touch the non-Tool parent-fallback block if a concrete harness-internal explicit-override gap is proven — do not change Tool path “to stop fallback” based on a stale premise.
    3. Ensure resolve_model_override_to_config remains the path for successful explicit models (D-09); do not simplify to parent-only keys.
    4. Add/green p7_invalid_effort_tool_provenance_fails_closed (filter p7_invalid_effort under --lib). Add tests for supported Sol-like fixture + unsupported fixture under p7_invalid_effort or sibling names discoverable by Plan 03 verify / VALIDATION.
    5. Confirm no set_session_model on parent from this path (D-11).
  </action>
  <verify>
    <automated>set -euo pipefail; n_effort=$(cargo test -p xai-grok-shell --lib p7_invalid_effort -- --list 2>/dev/null | grep -c 'p7_invalid_effort' || true); test "$n_effort" -ge 1; cargo test -p xai-grok-shell --lib p7_invalid_effort -- --nocapture; n_tool=$(cargo test -p xai-grok-shell --test cross_provider_subagent -- --list 2>/dev/null | grep -c 'p7_tool' || true); test "$n_tool" -ge 1; cargo test -p xai-grok-shell --test cross_provider_subagent p7_tool -- --nocapture; cargo test -p xai-grok-shell --lib reasoning_effort_explicit -- --nocapture</automated>
  </verify>
  <done>
    Tool invalid effort and unsupported-effort hard-fail green with supported+unsupported fixtures; Tool unknown-model existing reject regression green; valid effort applies; resume + role effort tests still green. Named filters discover ≥1 without swallow.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SubagentRequest → handle_subagent_request | Untrusted model/effort from tools or harness |
| Auth store → spawn gate | Usable decision must not leak token material into errors |
| Child SamplingConfig seed | Must use child provider slot only |
| Worktree FS | Must not create worktrees when gate fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Spoofing | Cross-provider spawn with empty child slot | high | mitigate | missing_provider_gate_error before worktree + spawn; no parent bearer fallback (D-05/D-06) |
| T-07-05 | Elevation of Privilege | Parent token on child backend | high | mitigate | resolve_model_override_to_config dual-key; gate blocks empty slot spawn |
| T-07-06 | Information Disclosure | send_failure messages | high | mitigate | CLI suggestion only; never include access tokens or auth.json body |
| T-07-07 | Tampering | Client-supplied provider claim | high | mitigate | Provider from catalog entry after resolve only |
| T-07-08 | Bypass | Background Task without shell gate | medium | mitigate | Shell remains authoritative; Plan 04 adds eager preflight for UX timing |
| T-07-12 | Tampering | Worktree side effects before gate | medium | mitigate | Early model preflight + gate before worktree create/rehydrate |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
cross_provider_subagent p7_spawn_missing_provider + p7_tool filters green; lib p7_invalid_effort green; role effort regression green. Gate ordering before worktree proven. No swallowed cargo exit codes.
</verification>

<success_criteria>
- AGENT-05 shell authority complete with pre-worktree gate
- AGENT-02/04 resolve path fail-closed at spawn
- AGENT-03 shell apply fail-closed for Tool effort with fixtures
- D-04..D-09, D-11, D-15 implemented in shell
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md` when done
</output>
