---
phase: 07-cross-provider-multi-agent-orchestration
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
  - crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
autonomous: true
requirements:
  - AGENT-05
  - AGENT-02
must_haves:
  truths:
    - "Default run_in_background Task path fails closed on missing child provider BEFORE returning the started notice (D-05, AGENT-05)"
    - "Eager preflight resolves the SAME effective child model/provider the shell will use — including omit-model inherit and role/config pins — not bare slug-only Fn(model_slug) (D-05, D-01, D-08; review HIGH)"
    - "Eager preflight uses same usable semantics as shell gate (refreshable OK; empty slot fails; BYOK skip) (D-05, D-08)"
    - "Error text names provider and includes bum login --provider {xai|codex} (D-07)"
    - "TaskProviderCredentialGate (or equivalent Resources resource) injected from shell when building session tool resources (parallel to TaskModelValidator)"
    - "Shell handle_subagent_request remains authoritative for harness-direct spawns (D-05 dual layer)"
    - "Same-provider spawn with usable creds does not add preflight friction (D-08)"
    - "All Task test fixtures that insert TaskModelValidator also insert the credential gate when fail-closed-on-missing-resource policy requires it (review MEDIUM resource migration)"
  artifacts:
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
      provides: "TaskProviderCredentialGate resource taking effective-model preflight request (not slug-only)"
      contains: TaskProviderCredentialGate
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
      provides: "Eager credential preflight before background spawn using full Task context"
      contains: TaskProviderCredentialGate
    - path: crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs
      provides: "Inject gate resource with shell effective-model resolve closure"
      contains: TaskProviderCredentialGate
  key_links:
    - from: Task execute (bg default)
      to: TaskProviderCredentialGate.preflight(request)
      via: "after model validator + effort parse; before tokio::spawn fire-and-forget; request carries model Option + subagent_type + parent model context"
      pattern: "TaskProviderCredentialGate"
    - from: agent_rebuild resource install
      to: Plan 03 pure resolve + missing_provider_gate_error / provider_slot_usable
      via: "closure computes effective child model/provider same precedence as shell; no tools crate reading auth.json"
      pattern: "update_resource"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Close the background-default UX hole for AGENT-05: eager Task-layer credential preflight so missing child provider returns a tool error before the "started" notice (per D-05, D-07, RESEARCH Pitfall 1). Preflight must use the **same effective child model/provider** shell will use (omit-model inherit + role pins) — not slug-only (review HIGH). Shell gate from Plan 03 stays authoritative.

Purpose: Background fire-and-forget must not report success when spawn will fail closed; UX gate must not disagree with shell authority.
Output: Effective-model-aware TaskProviderCredentialGate + Task eager check + shell injection + fixture migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-REVIEWS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-02-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
@crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs
@crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-05 | Spawn-time usable check before background launch |
| D-01 | Omit model inherit — preflight must still resolve effective model |
| D-07 | Login CLI hint on tool error |
| D-08 | Same-provider no extra friction |

**Discretion (review-aligned):** Prefer a preflight **request struct** (or multi-arg Fn) over `Fn(&str)` slug-only. Shell closure owns catalog + auth + parent model + role pin resolution using the same pure helpers Plan 03 extracted for early gate. If role/config pins require shell-only state that cannot be mirrored, prefer a thin SubagentBackend.preflight_spawn RPC — but default to Resources gate with enough injected context (parent model id, models list, auth accessors, role→model map) so omit-model and role pins agree with shell. **Do not** document “inherit-only skip” as acceptable completion for same-provider-if-somehow-missing when parent model is available in session rebuild.

## Scope boundaries

- Do **not** reimplement shell gate logic differently — shell injection should call the same pure missing_provider_gate_error / provider_slot_usable / effective-model preflight helpers from Plan 03.
- Do **not** open auth.json inside xai-grok-tools (keep I/O in shell closure).
- Do **not** add TUI QuestionView for Task spawn (tool error is primary).
- No live OAuth.

## Cargo verify hygiene

- Tools unit tests with capture backend + injected gate resource.
- Prefer p7_eager_ (tools) and p7_credential_gate (shell injection) prefixes.
- Never trailing `|| true` on required cargo execute paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — effective-model TaskProviderCredentialGate + eager preflight before bg spawn</name>
  <files>crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs, crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs</files>
  <read_first>
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs (TaskModelValidator register_resource pattern ~768–793)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs (eager model validation ~275–285; run_in_background spawn ~328–348; Plan 02 effort parse placement; omit model → None)
    - 07-RESEARCH.md Pitfall 1 background success-before-gate
    - 07-REVIEWS.md Plan 04 HIGH slug-only insufficient + resource fixtures
  </read_first>
  <behavior>
    - p7_eager_missing_provider_rejects_before_background_started: run_in_background true + gate returns Some(msg) → Err containing bum login --provider; mock backend spawn NOT called
    - p7_eager_missing_provider_allows_when_gate_none: gate None → spawn proceeds
    - p7_eager_omit_model_preflight_uses_effective_parent_model: omit Task.model → preflight request still carries enough context that gate evaluates parent/effective model provider (not skipped solely because slug is None)
    - p7_eager_gate_receives_subagent_type_for_role_pins: preflight request includes subagent_type so shell can apply role model pins
    - p7_eager_gate_unavailable_resource_policy: explicit model + missing resource → validation_unavailable fail-closed (mirror TaskModelValidator); migrate fixtures
    - Existing type/model/effort eagers still run first
  </behavior>
  <action>
    Implement D-05 background-safe preflight at Task boundary with **effective-model-aware** contract (addresses review HIGH).

    1. In types.rs, define a small preflight request type (e.g. TaskCredentialPreflightRequest) carrying at least: optional explicit model slug, subagent_type, and any parent-model hint the tool already has (or Option for inherit). TaskProviderCredentialGate wraps Arc&lt;dyn Fn(TaskCredentialPreflightRequest) -&gt; Option&lt;String&gt; + Send + Sync&gt; (or equivalent method) with new/error_for_preflight — **not** slug-only Fn(&amp;str). register_resource under grok_build.
    2. In task/mod.rs after model validation + effort parse, **always** build the preflight request from the Task input (explicit model after resume soft-ignore, subagent_type, omit-model inherit signal):
       - Load TaskProviderCredentialGate from Resources.
       - If missing and (explicit model present OR inherit path that still needs a usability check): return ToolError::custom validation_unavailable (mirror model validator missing) — fail closed; do not silently skip omit-model when resource absent if session should have injected it.
       - If present and preflight returns Some(msg): return ToolError::invalid_arguments(msg) (msg must include login hint from shell).
    3. Critical timing: this check must run **before** the run_in_background true branch that tokio::spawns fire-and-forget and returns started text.
    4. Unit tests inject gate stubs that inspect the **request** (assert omit-model still invokes gate with inherit/parent context; assert subagent_type forwarded) and return Some/None without real auth.
    5. **Fixture migration:** every unit test setup that inserts TaskModelValidator must also insert TaskProviderCredentialGate (None-returning stub is fine for unrelated tests). Grep TaskModelValidator::new / update_resource / test harness builders in task/mod.rs and update all.
    6. Do not duplicate ReasoningEffort logic here (already Plan 02).
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-tools --lib p7_eager -- --list 2>/dev/null | grep -c 'p7_eager' || true); test "$n" -ge 1; cargo test -p xai-grok-tools --lib p7_eager -- --nocapture; cargo test -p xai-grok-tools --lib p7_ -- --nocapture</automated>
  </verify>
  <done>
    Background Task path returns tool error on gated effective model before started notice; omit-model and subagent_type reach preflight; spawn backend not invoked on gate failure; fixtures migrated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — shell injects effective-model gate using Plan 03 pure resolve + usable semantics</name>
  <files>crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs, crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs (TaskModelValidator injection ~311–317)
    - Plan 03 pure provider_slot_usable + early effective-model preflight helpers
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs (model resolve precedence including role pins ~768–829)
    - crates/codegen/xai-grok-shell/src/agent/config.rs (find_model_by_id, has_own_credentials)
    - 07-REVIEWS.md Plan 04 semantic drift risk
  </read_first>
  <behavior>
    - Session rebuild installs TaskProviderCredentialGate alongside TaskModelValidator
    - Closure: from preflight request → resolve **effective** child model (explicit, else role pin, else parent inherit) → catalog provider → missing_provider_gate_error → Some(spawn-oriented message with bum login --provider) or None
    - Empty Codex slot + gpt child model → Some; dual usable → None
    - Omit model + parent on Codex with empty xAI is not incorrectly gated as cross-provider unless effective model is xAI
    - Role pin that selects a different provider than bare Task.model absence still gates the **pinned** provider
    - Named test p7_credential_gate_* proves injection + effective-model semantics
  </behavior>
  <action>
    Wire shell truth into the Task resource so UX preflight agrees with authoritative shell resolve (per D-05, D-01, D-07, D-08; addresses review HIGH/MEDIUM drift).

    1. Next to TaskModelValidator::new in agent_rebuild, update_resource TaskProviderCredentialGate::new with a closure capturing: models list, parent/current session model id, role→model pin map (or agent config used by shell resolve), auth accessors for provider_slot_usable / missing_provider_gate_error.
    2. Closure algorithm must reuse Plan 03 pure effective-model preflight helper (or call the same pure function) — do not invent a second precedence table that only looks at explicit slug.
    3. Message should align with Plan 03 spawn copy and include suggestion `bum login --provider {id}`.
    4. BYOK models return None from gate (has_own_credentials).
    5. If other session construction paths also inject TaskModelValidator, inject the credential gate there too (grep TaskModelValidator::new / update_resource).
    6. Add named tests under filter p7_credential_gate:
       - empty Codex + explicit gpt child → Some(login hint)
       - omit model + parent model provider usable → None
       - omit model + parent provider unusable → Some (same-provider-if-somehow-missing)
       - role pin to cross-provider model with empty target slot → Some even when Task.model omitted
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --lib p7_credential_gate -- --list 2>/dev/null | grep -c 'p7_credential_gate' || true); test "$n" -ge 1; cargo test -p xai-grok-shell --lib p7_credential_gate -- --nocapture; cargo test -p xai-grok-tools --lib p7_eager -- --nocapture</automated>
  </verify>
  <done>
    Production sessions inject effective-model-aware credential gate; p7_credential_gate discovers ≥1 and greens; Task eager path and shell gate agree on fail-closed policy including omit-model and role pins. No swallowed cargo exit codes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task tool Resources | Gate closure is trusted shell-injected; tools must not trust model-supplied provider strings |
| Background spawn channel | Preflight must complete before fire-and-forget |
| Effective model resolution | Tools must not invent provider labels; shell closure owns catalog authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Bypass | Background started before gate | high | mitigate | Eager Task preflight before tokio::spawn; shell gate remains second line |
| T-07-09 | Information Disclosure | Gate error message | medium | mitigate | Login CLI only; no tokens |
| T-07-10 | Tampering | Missing resource skip | medium | mitigate | Explicit/inherit path + missing gate resource → validation_unavailable fail-closed |
| T-07-13 | Bypass | Slug-only preflight ≠ shell effective model | high | mitigate | Preflight request + shell effective-model resolve shared with Plan 03 |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
p7_eager tools tests green; shell p7_credential_gate injection tests green; full tools p7_ still green.
</verification>

<success_criteria>
- AGENT-05 default background path fails closed with login prompt
- Dual-layer gate: Task eager (effective model) + shell authoritative
- Omit-model + role pins covered — not slug-only
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-04-SUMMARY.md` when done
</output>
