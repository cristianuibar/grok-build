---
phase: 07-cross-provider-multi-agent-orchestration
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
  - crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
autonomous: true
requirements:
  - AGENT-05
  - AGENT-02
must_haves:
  truths:
    - "Default run_in_background Task path fails closed on missing child provider BEFORE returning the started notice (D-05, AGENT-05)"
    - "Eager preflight uses same usable semantics as shell gate (refreshable OK; empty slot fails; BYOK skip) (D-05, D-08)"
    - "Error text names provider and includes bum login --provider {xai|codex} (D-07)"
    - "TaskProviderCredentialGate (or equivalent Resources Fn) injected from shell when building session tool resources (parallel to TaskModelValidator)"
    - "Shell handle_subagent_request remains authoritative for harness-direct spawns (D-05 dual layer)"
    - "Same-provider spawn with usable creds does not add preflight friction (D-08)"
    - "Omit model (inherit parent) preflight checks parent/child effective provider usability without inventing cross-provider when inherit stays same provider"
  artifacts:
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
      provides: "TaskProviderCredentialGate resource (Fn model slug → Option error msg)"
      contains: TaskProviderCredentialGate
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
      provides: "Eager credential preflight before background spawn"
      contains: TaskProviderCredentialGate
    - path: crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs
      provides: "Inject gate resource alongside TaskModelValidator"
      contains: TaskProviderCredentialGate
  key_links:
    - from: Task execute (bg default)
      to: TaskProviderCredentialGate.error_for(model)
      via: "after model validator + effort parse; before tokio::spawn fire-and-forget"
      pattern: "TaskProviderCredentialGate"
    - from: agent_rebuild resource install
      to: missing_provider_gate_error / provider_slot_usable
      via: "closure resolves model→provider→usable without tools crate reading auth.json directly"
      pattern: "update_resource"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Close the background-default UX hole for AGENT-05: eager Task-layer credential preflight so missing child provider returns a tool error before the "started" notice (per D-05, D-07, RESEARCH Pitfall 1). Shell gate from Plan 03 stays authoritative.

Purpose: Background fire-and-forget must not report success when spawn will fail closed.
Output: TaskProviderCredentialGate resource + Task eager check + shell injection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-02-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
@crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-05 | Spawn-time usable check before background launch |
| D-07 | Login CLI hint on tool error |
| D-08 | Same-provider no extra friction |

**Discretion:** Resources `Fn(&str) -> Option<String>` parallel to TaskModelValidator (RESEARCH open Q3 recommendation). Effective model for preflight: explicit Task.model if present; if omitted, use parent session model id from an existing resource if available, or skip cross-provider-only check only when inherit cannot be known — prefer injecting enough context that omit-model still gates if parent provider slot unusable (same-provider-if-somehow-missing). Do not require parent model resource if unavailable: document limitation and still gate all explicit cross-provider models.

## Scope boundaries

- Do **not** reimplement shell gate logic differently — shell injection should call the same pure missing_provider_gate_error / usable helpers.
- Do **not** open auth.json inside xai-grok-tools (keep I/O in shell closure).
- Do **not** add TUI QuestionView for Task spawn (tool error is primary).
- No live OAuth.

## Cargo verify hygiene

- Tools unit tests with capture backend + injected gate resource.
- Prefer p7_background_ or p7_eager_ prefixes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — TaskProviderCredentialGate resource + eager preflight before bg spawn</name>
  <files>crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs, crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs</files>
  <read_first>
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs (TaskModelValidator register_resource pattern ~768–793)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs (eager model validation ~275–285; run_in_background spawn ~328–348; Plan 02 effort parse placement)
    - 07-RESEARCH.md Pitfall 1 background success-before-gate
  </read_first>
  <behavior>
    - p7_eager_missing_provider_rejects_before_background_started: run_in_background true + gate returns Some(msg) → Err(invalid_arguments or custom) containing bum login --provider; mock backend spawn NOT called
    - p7_eager_missing_provider_allows_when_gate_none: gate None → spawn proceeds (bg or fg)
    - p7_eager_gate_unavailable_resource_policy: document chosen behavior — prefer fail closed with validation_unavailable if explicit model present and resource missing (safer) OR skip only when inherit-only; pick fail-closed for explicit model to match TaskModelValidator unavailable pattern
    - Existing type/model/effort eagers still run first
  </behavior>
  <action>
    Implement D-05 background-safe preflight at Task boundary.

    1. In types.rs, add TaskProviderCredentialGate wrapping Arc&lt;dyn Fn(&amp;str) -&gt; Option&lt;String&gt; + Send + Sync&gt; with new/error_for mirroring TaskModelValidator; register_resource under grok_build.
    2. In task/mod.rs after model validation + effort parse, when an effective model slug is known (explicit model after resume soft-ignore):
       - Load TaskProviderCredentialGate from Resources.
       - If missing and explicit model: return ToolError::custom validation_unavailable (mirror model validator missing) — fail closed for explicit cross-provider attempts.
       - If present and error_for(model) is Some(msg): return ToolError::invalid_arguments(msg) (msg must include login hint from shell).
    3. Critical timing: this check must run **before** the run_in_background true branch that tokio::spawns fire-and-forget and returns started text.
    4. Unit tests inject gate stubs that return Some/None without real auth.
    5. Do not duplicate ReasoningEffort logic here (already Plan 02).
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-tools --lib p7_eager -- --list 2>/dev/null | grep -c 'p7_eager' || true); test "$n" -ge 1; cargo test -p xai-grok-tools --lib p7_eager -- --nocapture; cargo test -p xai-grok-tools --lib p7_ -- --nocapture</automated>
  </verify>
  <done>
    Background Task path returns tool error on gated model before started notice; spawn backend not invoked on gate failure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — shell injects TaskProviderCredentialGate from real usable semantics</name>
  <files>crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs, crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/session/agent_rebuild.rs (TaskModelValidator injection ~311–317)
    - Plan 03 shared usable helper / missing_provider_gate_error
    - crates/codegen/xai-grok-shell/src/agent/config.rs (find_model_by_id, has_own_credentials)
  </read_first>
  <behavior>
    - Session rebuild installs TaskProviderCredentialGate alongside TaskModelValidator
    - Closure: resolve model id in available catalog → provider → missing_provider_gate_error → Some(spawn-oriented message with bum login --provider) or None
    - Empty Codex slot + gpt child model → Some; dual usable → None
    - Integration/unit proof that injection is present (resource update called / or end-to-end tool path if harness allows)
  </behavior>
  <action>
    Wire shell truth into the Task resource (per D-05, D-07, D-08).

    1. Next to TaskModelValidator::new in agent_rebuild, update_resource TaskProviderCredentialGate::new with a closure capturing models list + auth accessors needed for provider_oauth_slot_usable / missing_provider_gate_error.
    2. Message should align with Plan 03 spawn copy and include suggestion `bum login --provider {id}`.
    3. BYOK models return None from gate (has_own_credentials).
    4. If other session construction paths also inject TaskModelValidator, inject the credential gate there too (grep TaskModelValidator::new / update_resource).
    5. Add p7_ test proving injection semantics (pure closure unit test acceptable if full session rebuild is heavy): given catalog + empty codex store, error_for("gpt-5.6-sol") is Some containing login hint.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-shell --test cross_provider_subagent p7_ -- --nocapture; cargo test -p xai-grok-shell --lib p7_ -- --nocapture 2>/dev/null || cargo test -p xai-grok-shell --lib TaskProviderCredentialGate -- --nocapture 2>/dev/null || true</automated>
  </verify>
  <done>
    Production sessions inject credential gate using Phase 6 usable semantics; Task eager path and shell gate agree on fail-closed policy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task tool Resources | Gate closure is trusted shell-injected; tools must not trust model-supplied provider strings |
| Background spawn channel | Preflight must complete before fire-and-forget |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Bypass | Background started before gate | high | mitigate | Eager Task preflight before tokio::spawn; shell gate remains second line |
| T-07-09 | Information Disclosure | Gate error message | medium | mitigate | Login CLI only; no tokens |
| T-07-10 | Tampering | Missing resource skip | medium | mitigate | Explicit model + missing gate resource → validation_unavailable fail-closed |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
p7_eager tools tests green; shell injection tests green; full tools p7_ still green.
</verification>

<success_criteria>
- AGENT-05 default background path fails closed with login prompt
- Dual-layer gate: Task eager + shell authoritative
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-04-SUMMARY.md` when done
</output>
