---
phase: 07-cross-provider-multi-agent-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/common/xai-tool-types/src/task.rs
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
  - crates/codegen/xai-grok-agent/src/builder.rs
autonomous: true
requirements:
  - AGENT-03
  - AGENT-06
must_haves:
  truths:
    - "TaskToolInput exposes optional reasoning_effort in serde + schemars schema for model-facing Task tool (D-03, D-13)"
    - "Valid effort tokens thread into SubagentRuntimeOverrides.reasoning_effort as CANONICAL tokens (max→xhigh Display form) — not raw alias strings (D-03, AGENT-03)"
    - "Invalid effort is rejected at Task tool boundary with invalid_arguments listing accepted tokens — no silent clamp (D-04)"
    - "Omitted effort leaves overrides.reasoning_effort None so harness/role defaults still apply (D-01-style omit inherit)"
    - "Task description guidance mentions model + reasoning_effort for NL orchestration (D-13, AGENT-06)"
    - "All workspace TaskToolInput { ... } struct literals compile after the new field (~42 sites); full migration required (review MEDIUM)"
    - "resume_from continues to soft-ignore model override; document model pin (D-15)"
  artifacts:
    - path: crates/common/xai-tool-types/src/task.rs
      provides: "TaskToolInput.reasoning_effort Option<String> + schemars description"
      contains: reasoning_effort
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
      provides: "Wire sanitized canonical effort into SubagentRuntimeOverrides; eager invalid reject; full literal migration"
      contains: reasoning_effort
    - path: crates/codegen/xai-grok-agent/src/builder.rs
      provides: "task_effort_guidance appendix on build_task_description"
      contains: reasoning_effort
  key_links:
    - from: TaskToolInput.reasoning_effort
      to: SubagentRuntimeOverrides.reasoning_effort
      via: "Task execute path after sanitize + ReasoningEffort::from_str; store canonical Display; provenance Tool"
      pattern: "runtime_overrides: SubagentRuntimeOverrides"
    - from: invalid effort string
      to: ToolError::invalid_arguments
      via: "eager parse before background spawn"
      pattern: "invalid_arguments"
    - from: build_task_description
      to: parent model NL tool choice
      via: "schema + description list model and effort tokens"
      pattern: "task_effort_guidance|task_model_guidance"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Ship the model-facing Task contract for reasoning_effort: schema field, sanitize, wire into SubagentRuntimeOverrides with **canonical** tokens, fail-closed invalid tokens, full call-site migration, and light NL description guidance (per D-03, D-04, D-13). Adds Plan 02 green p7_ effort tests after the field exists (Plan 01 deliberately did not reference the field).

Purpose: AGENT-03 surface + AGENT-06 enablement without a workflow engine.
Output: TaskToolInput.reasoning_effort end-to-end into overrides + description text for parent models + workspace compile clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-REVIEWS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md
@crates/common/xai-tool-types/src/task.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
@crates/codegen/xai-grok-agent/src/builder.rs
@crates/codegen/xai-grok-sampling-types/src/types.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-03 | Schema field + wire overrides |
| D-04 | Invalid effort reject at Task boundary |
| D-13 | NL schema/docs via description guidance |
| D-15 | resume_from model pin unchanged; document in schema text |

**Discretion:** Parse with `ReasoningEffort::from_str` (accept catalog aliases including max→xhigh). Store **canonical** form after parse (e.g. Display/`to_string` of enum so max becomes xhigh) — shell re-parse sees stable tokens (addresses review MEDIUM). Only model-facing Task path this phase (not goal-orchestrator). Shell authoritative effort fail-closed remains Plan 03 for harness-direct overrides.

## Scope boundaries

- Do **not** implement spawn credential gate or TaskProviderCredentialGate (Plans 03–04).
- Do **not** change resume model pin behavior.
- Do **not** add workflow engine / TUI effort chrome.
- Update Plan 01 gap-lock test that asserted effort is always None when input now supplies medium.
- Full workspace `TaskToolInput { ... }` literal migration — not “selected tests only” (review MEDIUM).

## Cargo verify hygiene

- Prefer `cargo test -p xai-grok-tools --lib p7_` and targeted agent builder tests.
- Prefer named discovery ≥1 for agent effort-guidance filter — **no** `||` OR-chains that swallow a failed primary test (review LOW/MEDIUM).
- Never trailing `|| true` on cargo execute paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — TaskToolInput.reasoning_effort schema + full literal migration + canonical wire + invalid reject</name>
  <files>crates/common/xai-tool-types/src/task.rs, crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs</files>
  <read_first>
    - crates/common/xai-tool-types/src/task.rs (model field + sanitize_optional_arg; literals ~1126+)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs (execute path ~120–350; hardcode None; Plan 01 p7_ gap-lock tests; ~40 TaskToolInput literals)
    - crates/codegen/xai-grok-sampling-types ReasoningEffort FromStr vocabulary (max → Xhigh)
    - 07-PATTERNS.md Task schema delta section
    - 07-REVIEWS.md Plan 02 full-literal + canonicalize notes
  </read_first>
  <behavior>
    - p7_task_tool_input_schema_includes_reasoning_effort GREEN (add after field exists)
    - p7_reasoning_effort_threads_to_runtime_overrides GREEN for medium/high/xhigh/low with CANONICAL stored strings
    - p7_reasoning_effort_max_alias_canonicalizes_to_xhigh: input max → overrides Some("xhigh") (or Display form matching shell parse) not raw "max"
    - p7_invalid_reasoning_effort_rejected_before_spawn GREEN
    - p7_omitted_reasoning_effort_stays_none_on_overrides GREEN
    - Plan 01 p7_reasoning_effort_override_is_none_until_schema_lands either updated (omit path still None) or replaced so suite stays green
    - Sentinel/blank effort strings treated like omitted (sanitize_optional_arg)
    - Workspace compiles: every TaskToolInput struct literal updated
  </behavior>
  <action>
    Implement D-03 and D-04 on the Task tool path only. Addresses review: full call-site migration + canonical effort tokens.

    1. In TaskToolInput (xai-tool-types/task.rs), add optional `reasoning_effort: Option&lt;String&gt;` immediately after `model`, with schemars description covering product tokens low/medium/high/xhigh (mention alias max maps to xhigh), omit-to-inherit-default, reject-invalid, and prefer setting when user asks for effort. serde default + skip_serializing_if Option::is_none. Note resume_from: prior model used for model field; do not claim effort pin unless product soft-ignores effort on resume (discretion: allow effort on resume unless it conflicts — prefer pass-through).
    2. **Full literal migration (mandatory):** run `rg -n 'TaskToolInput \{'` across the workspace. Update **every** Rust struct literal that constructs TaskToolInput (tools tests, xai-tool-types tests, helpers like task_input(), etc. — ~42 sites). Add `reasoning_effort: None` (or meaningful Some in new tests). Do not stop at “selected tests.” KillTaskToolInput is a different type — skip those. Confirm `cargo check -p xai-tool-types -p xai-grok-tools` (and any other crate with literals) compiles.
    3. In task/mod.rs execute: sanitize effort via sanitize_optional_arg. Before building SubagentRequest / any background spawn:
       - If Some(raw), parse with ReasoningEffort::from_str (or shared helper). On Err, return ToolError::invalid_arguments with clear list of accepted tokens.
       - On Ok, set runtime_overrides.reasoning_effort = Some(**canonical** token) via the enum’s Display / stable product string (max→xhigh). Do **not** store the raw alias string.
    4. Replace hardcode `reasoning_effort: None` with the sanitized Option.
    5. Keep model validation order: depth → validate_type → model validator → **effort parse** → build request (credential preflight is Plan 04).
    6. Add green p7_ tests listed in behavior (field now exists — safe to construct). Update Plan 01 gap-lock test so the suite remains all-green under `p7_`.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-tools --lib p7_ -- --list 2>/dev/null | grep -c 'p7_' || true); test "$n" -ge 1; cargo test -p xai-grok-tools --lib p7_ -- --nocapture; cargo check -p xai-tool-types -p xai-grok-tools; test "$(rg -n 'TaskToolInput \{' --glob '*.rs' crates | wc -l)" -ge 1; cargo test -p xai-tool-types --lib -- --nocapture 2>/dev/null || cargo test -p xai-tool-types -- --nocapture</automated>
  </verify>
  <done>
    Task schema exposes reasoning_effort; valid values wire as canonical tokens; max→xhigh; invalid fail closed before spawn; omit stays None; all TaskToolInput literals migrate; tools p7_ all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — NL Task description effort guidance (AGENT-06 surface)</name>
  <files>crates/codegen/xai-grok-agent/src/builder.rs</files>
  <read_first>
    - crates/codegen/xai-grok-agent/src/builder.rs (task_model_guidance, build_task_description ~1250–1300; tests ~1456+)
    - 07-CONTEXT.md NL orchestration decision (D-13)
    - 07-REVIEWS.md Plan 02 verify OR-chain note
  </read_first>
  <behavior>
    - build_task_description includes guidance that parent may set model and reasoning_effort when user asks (e.g. Codex Sol medium-effort research)
    - Mentions product effort tokens
    - Does not claim a workflow engine
    - Existing model slug guidance tests still pass
    - Named test p7_build_task_description_includes_effort_guidance (or exact SUMMARY name) discoverable under filter p7_ or effort_guidance
  </behavior>
  <action>
    Per D-13 / AGENT-06, add light description appendix (not a new product surface):

    1. Add task_effort_guidance() (or inline constant) mirroring task_model_guidance style: when user requests a specific effort, set params.task.reasoning_effort to low|medium|high|xhigh; omit to use defaults; invalid rejected; max alias maps to xhigh if mentioned.
    2. Append after task_model_guidance in build_task_description.
    3. Add unit test with a stable name containing p7_ (preferred: p7_build_task_description_includes_effort_guidance) asserting description contains reasoning_effort guidance and medium (or product tokens).
    4. Do not invent multi-step workflow instructions beyond one-shot Task call guidance.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-agent --lib build_task_description -- --nocapture; n=$(cargo test -p xai-grok-agent --lib p7_ -- --list 2>/dev/null | grep -c 'p7_' || true); test "$n" -ge 1; cargo test -p xai-grok-agent --lib p7_ -- --nocapture</automated>
  </verify>
  <done>
    Parent-facing Task description documents model + reasoning_effort for NL orchestration; named p7_ builder test discovers ≥1 and greens; no OR-chain swallow.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model tool args → TaskToolInput | Untrusted strings for model and reasoning_effort |
| Task tool → SubagentRequest | Only sanitized validated canonical effort tokens may cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | reasoning_effort arg | medium | mitigate | Allowlist via ReasoningEffort::from_str; store canonical form; reject unknown tokens at Task boundary (D-04) |
| T-07-03 | Elevation of Privilege | unknown model + effort | medium | mitigate | Model still validated by TaskModelValidator; effort does not bypass catalog (D-02) |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
All xai-grok-tools p7_ filters green; agent p7_ description tests green with discovery ≥1; workspace TaskToolInput literals compile; no shell gate required yet.
</verification>

<success_criteria>
- AGENT-03 schema+wire complete at Task boundary with canonical tokens
- AGENT-06 description surface present
- D-03/D-04/D-13 implemented for tools path
- Full TaskToolInput call-site migration complete
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-02-SUMMARY.md` when done
</output>
