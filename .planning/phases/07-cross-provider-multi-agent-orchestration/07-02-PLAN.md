---
phase: 07-cross-provider-multi-agent-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/common/xai-tool-types/src/task.rs
  - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
  - crates/codegen/xai-grok-agent/src/builder.rs
autonomous: true
requirements:
  - AGENT-03
  - AGENT-06
must_haves:
  truths:
    - "TaskToolInput exposes optional reasoning_effort in serde + schemars schema for model-facing Task tool (D-03, D-13)"
    - "Valid effort tokens thread into SubagentRuntimeOverrides.reasoning_effort instead of hardcoding None (D-03, AGENT-03)"
    - "Invalid effort is rejected at Task tool boundary with invalid_arguments listing accepted tokens — no silent clamp (D-04)"
    - "Omitted effort leaves overrides.reasoning_effort None so harness/role defaults still apply (D-01-style omit inherit)"
    - "Task description guidance mentions model + reasoning_effort for NL orchestration (D-13, AGENT-06)"
    - "resume_from continues to soft-ignore model override; effort may still pass unless product soft-ignores it — document pin for model (D-15)"
  artifacts:
    - path: crates/common/xai-tool-types/src/task.rs
      provides: "TaskToolInput.reasoning_effort Option<String> + schemars description"
      contains: reasoning_effort
    - path: crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
      provides: "Wire sanitized effort into SubagentRuntimeOverrides; eager invalid reject"
      contains: reasoning_effort
    - path: crates/codegen/xai-grok-agent/src/builder.rs
      provides: "task_effort_guidance appendix on build_task_description"
      contains: reasoning_effort
  key_links:
    - from: TaskToolInput.reasoning_effort
      to: SubagentRuntimeOverrides.reasoning_effort
      via: "Task execute path after sanitize; provenance Tool"
      pattern: "runtime_overrides: SubagentRuntimeOverrides"
    - from: invalid effort string
      to: ToolError::invalid_arguments
      via: "eager parse before background spawn"
      pattern: "invalid_arguments"
    - from: build_task_description
      to: parent model NL tool choice
      via: "schema + description list model and effort tokens"
      pattern: "task_effort_guidance|task_model_guidance"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Ship the model-facing Task contract for reasoning_effort: schema field, sanitize, wire into SubagentRuntimeOverrides, fail-closed invalid tokens, and light NL description guidance (per D-03, D-04, D-13). Turns Plan 01 tools p7_ effort contracts GREEN.

Purpose: AGENT-03 surface + AGENT-06 enablement without a workflow engine.
Output: TaskToolInput.reasoning_effort end-to-end into overrides + description text for parent models.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-CONTEXT.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PATTERNS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md
@crates/common/xai-tool-types/src/task.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs
@crates/codegen/xai-grok-tools/src/implementations/grok_build/task/types.rs
@crates/codegen/xai-grok-agent/src/builder.rs
@crates/codegen/xai-grok-sampling-types/src/types.rs
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-03 | Schema field + wire overrides |
| D-04 | Invalid effort reject at Task boundary |
| D-13 | NL schema/docs via description guidance |
| D-15 | resume_from model pin unchanged; document in schema text |

**Discretion:** Parse with `ReasoningEffort::from_str` (accept catalog aliases including max→xhigh). Only model-facing Task path this phase (not goal-orchestrator). Shell authoritative effort fail-closed remains Plan 03 for harness-direct overrides.

## Scope boundaries

- Do **not** implement spawn credential gate or TaskProviderCredentialGate (Plans 03–04).
- Do **not** change resume model pin behavior.
- Do **not** add workflow engine / TUI effort chrome.
- Update existing `model_threads_to_runtime_overrides` assert that expects reasoning_effort is_none when tests pass medium.

## Cargo verify hygiene

- Prefer `cargo test -p xai-grok-tools --lib p7_` and targeted agent builder tests.
- Prefer `cargo test -p xai-grok-agent --lib build_task_description` / p7_ if added.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — TaskToolInput.reasoning_effort schema + sanitize + invalid reject + override wire</name>
  <files>crates/common/xai-tool-types/src/task.rs, crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs</files>
  <read_first>
    - crates/common/xai-tool-types/src/task.rs (model field + sanitize_optional_arg)
    - crates/codegen/xai-grok-tools/src/implementations/grok_build/task/mod.rs (execute path ~120–350; hardcode None; p7_ tests from Plan 01)
    - crates/codegen/xai-grok-sampling-types ReasoningEffort FromStr vocabulary
    - 07-PATTERNS.md Task schema delta section
  </read_first>
  <behavior>
    - p7_task_tool_input_schema_includes_reasoning_effort GREEN
    - p7_reasoning_effort_threads_to_runtime_overrides GREEN for medium/high/xhigh/low
    - p7_invalid_reasoning_effort_rejected_before_spawn GREEN
    - p7_omitted_reasoning_effort_stays_none_on_overrides GREEN
    - Sentinel/blank effort strings treated like omitted (sanitize_optional_arg)
  </behavior>
  <action>
    Implement D-03 and D-04 on the Task tool path only.

    1. In TaskToolInput (xai-tool-types/task.rs), add optional `reasoning_effort: Option<String>` immediately after `model`, with schemars description covering product tokens low/medium/high/xhigh (mention alias max if FromStr accepts it), omit-to-inherit-default, reject-invalid, and prefer setting when user asks for effort. serde default + skip_serializing_if Option::is_none. Note resume_from: prior model used for model field; do not claim effort pin unless product soft-ignores effort on resume (discretion: allow effort on resume unless it conflicts — prefer pass-through).
    2. In task/mod.rs execute: sanitize effort via sanitize_optional_arg. Before building SubagentRequest / any background spawn:
       - If Some(raw), parse with ReasoningEffort::from_str (or shared helper). On Err, return ToolError::invalid_arguments with clear list of accepted tokens (reuse parse error text or format listing low, medium, high, xhigh, and aliases).
       - On Ok, set runtime_overrides.reasoning_effort = Some(canonical token string) — prefer the raw sanitized token or canonical Display form; be consistent with shell parse later.
    3. Replace hardcode `reasoning_effort: None` with the sanitized Option.
    4. Keep model validation order: depth → validate_type → model validator → **effort parse** → build request (credential preflight is Plan 04).
    5. Green all Plan 01 tools p7_ effort tests; fix any pre-existing unit test that assumed effort always None when input lacked the field.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-tools --lib p7_ -- --list 2>/dev/null | grep -c 'p7_' || true); test "$n" -ge 1; cargo test -p xai-grok-tools --lib p7_ -- --nocapture</automated>
  </verify>
  <done>
    Task schema exposes reasoning_effort; valid values wire into overrides; invalid values fail closed before spawn; omit stays None.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — NL Task description effort guidance (AGENT-06 surface)</name>
  <files>crates/codegen/xai-grok-agent/src/builder.rs</files>
  <read_first>
    - crates/codegen/xai-grok-agent/src/builder.rs (task_model_guidance, build_task_description ~1250–1300; tests ~1456+)
    - 07-CONTEXT.md NL orchestration decision (D-13)
  </read_first>
  <behavior>
    - build_task_description includes guidance that parent may set model and reasoning_effort when user asks (e.g. Codex Sol medium-effort research)
    - Mentions product effort tokens
    - Does not claim a workflow engine
    - Existing model slug guidance tests still pass
  </behavior>
  <action>
    Per D-13 / AGENT-06, add light description appendix (not a new product surface):

    1. Add `task_effort_guidance()` (or inline constant) mirroring task_model_guidance style: when user requests a specific effort, set params.task.reasoning_effort to low|medium|high|xhigh; omit to use defaults; invalid rejected.
    2. Append after task_model_guidance in build_task_description.
    3. Add unit test (p7_build_task_description_includes_effort_guidance or extend existing) asserting description contains reasoning_effort guidance and medium (or product tokens).
    4. Do not invent multi-step workflow instructions beyond one-shot Task call guidance.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-agent --lib build_task_description -- --nocapture; cargo test -p xai-grok-agent --lib p7_ -- --nocapture 2>/dev/null || cargo test -p xai-grok-agent --lib task_effort -- --nocapture 2>/dev/null || cargo test -p xai-grok-agent --lib effort_guidance -- --nocapture</automated>
  </verify>
  <done>
    Parent-facing Task description documents model + reasoning_effort for NL orchestration; builder tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model tool args → TaskToolInput | Untrusted strings for model and reasoning_effort |
| Task tool → SubagentRequest | Only sanitized validated effort tokens may cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | reasoning_effort arg | medium | mitigate | Allowlist via ReasoningEffort::from_str; reject unknown tokens at Task boundary (D-04) |
| T-07-03 | Elevation of Privilege | unknown model + effort | medium | mitigate | Model still validated by TaskModelValidator; effort does not bypass catalog (D-02) |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
All xai-grok-tools p7_ filters green; agent description tests green; no shell gate required yet.
</verification>

<success_criteria>
- AGENT-03 schema+wire complete at Task boundary
- AGENT-06 description surface present
- D-03/D-04/D-13 implemented for tools path
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-02-SUMMARY.md` when done
</output>
