---
phase: 07-cross-provider-multi-agent-orchestration
plan: 06
type: execute
wave: 5
depends_on:
  - 07-02
  - 07-03
  - 07-04
  - 07-05
files_modified:
  - .planning/phases/07-cross-provider-multi-agent-orchestration/07-VALIDATION.md
  - .planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/rest.rs
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
must_haves:
  truths:
    - "Same-provider subagent spawn/resume/roles/personas regression suite stays green (D-14, AGENT-01)"
    - "resume_from model pin tests still pass (D-15)"
    - "Phase VALIDATION.md maps AGENT-01..06 to concrete p7_ / existing filters with per-subgroup discovery ≥1"
    - "PHASE-GATE.md runs discover+execute for every required subgroup (not aggregate p7_ only)"
    - "No workflow engine, cost dashboard, Phase 8 rebrand, or live OAuth required (D-16)"
    - "All AGENT-01..06 automated proofs green under fixture tokens"
  artifacts:
    - path: .planning/phases/07-cross-provider-multi-agent-orchestration/07-VALIDATION.md
      provides: "Nyquist requirement → test map for Phase 7"
      contains: AGENT-01
    - path: .planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md
      provides: "Runnable phase gate with per-subgroup discovery"
      contains: discover
  key_links:
    - from: ROADMAP Phase 7 success criteria
      to: cargo filters p7_ + existing subagent suite
      via: "VALIDATION.md maps each AGENT-id to automated command"
      pattern: "AGENT-0|p7_"
    - from: AGENT-01 hard gate
      to: existing subagent resume/roles/effort tests + p7_same_provider
      via: "phase gate includes non-p7 regression names where needed"
      pattern: "resume_model_pinning|reasoning_effort_explicit|p7_same_provider"
---

## Phase Goal

**As a** bum user with dual-provider sessions, **I want to** spawn a child subagent on another provider with explicit model and reasoning effort, **so that** cross-provider multi-agent work uses the child's credentials and backend without regressing same-provider subagents.

<objective>
Close Phase 7 with AGENT-01 same-provider hard regression green and a written validation map + automated phase gate covering AGENT-01..06 (per D-14, D-15, D-16). No new product features beyond regression renames if required for discovery.

Purpose: Prove roadmap success criteria before Phase 8 without false-green empty filters.
Output: 07-VALIDATION.md + 07-PHASE-GATE.md + green aggregate cargo sequence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-RESEARCH.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-01-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-02-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-03-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-04-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-05-SUMMARY.md
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-PHASE-GATE.md
</context>

## Locked decisions (this plan)

| ID | Coverage |
|----|----------|
| D-14 | AGENT-01 same-provider hard regression gate |
| D-15 | resume pin still green |
| D-16 | Explicit exclusions documented |

## Required subgroups (each must discover ≥1)

**Tools (`xai-grok-tools --lib`):**
- `p7_task_tool_input_schema` or `p7_` schema filter covering reasoning_effort
- `p7_reasoning_effort` (wire + invalid)
- `p7_eager` (background preflight)

**Agent (`xai-grok-agent --lib`):**
- effort guidance / build_task_description filter from Plan 02 SUMMARY

**Shell integration (`--test cross_provider_subagent`):**
- `p7_wave0_harness_smoke` (or smoke still present)
- `p7_spawn_missing_provider`
- `p7_isolation`
- `p7_missing`
- `p7_parent_model`
- `p7_same_provider` (if present)

**Shell lib / subagent unit (use `--lib` if bare package list poisoned):**
- `reasoning_effort_explicit_overrides_role` (AGENT-01 effort path)
- `resume_model_pinning` (D-15 / AGENT-01 resume)
- any additional `p7_` unit filters landed in Plans 03–05

**Optional regression:** existing Task model_threads tests remain green under tools lib.

## Discovery assert pattern (mandatory)

```bash
set -euo pipefail
discover() {
  local pkg="$1" filter="$2"; shift 2
  local extra=("$@")
  local n
  n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true)
  test "$n" -ge 1
  cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture
}
```

Aggregate `grep -c p7_` alone is insufficient for Task 2 verify.

## Scope boundaries

- Documentation under `.planning/phases/07-.../` primarily.
- Allowed: one-line test renames for discoverability; fix only test name drift — no Phase 8 rebrand or workflow engine.
- Do not require live dual-login E2E (Phase 9 OPS-06).

<tasks>

<task type="auto">
  <name>Task 1: AGENT-01 regression green + write 07-VALIDATION.md</name>
  <files>.planning/phases/07-cross-provider-multi-agent-orchestration/07-VALIDATION.md, crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs, crates/codegen/xai-grok-shell/src/agent/subagent/tests/rest.rs</files>
  <read_first>
    - .planning/ROADMAP.md Phase 7 success criteria
    - 07-RESEARCH.md Validation Architecture table
    - All 07-0N-SUMMARY.md for actual test names
    - subagent tests: reasoning_effort_explicit_overrides_role; resume_model_pinning_overrides_default_resolution
  </read_first>
  <action>
    Per D-14 / AGENT-01:

    1. Run existing same-provider subagent suite filters (resume pin, roles/effort override, spawn smoke if present). Fix only regressions introduced by Plans 02–05 — do not expand product scope.
    2. If same-provider coverage is thin under p7_, add thin p7_same_provider_* anchors that call existing helpers or assert inherit-model path still works (omit model → parent provider) without cross-provider fixtures.
    3. Write 07-VALIDATION.md mapping each AGENT-01..06 and ROADMAP success criterion to concrete cargo filters from SUMMARYs. Include wave table: Wave1=01, Wave2=02+03, Wave3=04, Wave4=05, Wave5=06.
    4. Document fixture-only policy and D-16 exclusions (no workflow engine, no cost dashboards, no Phase 8 rebrand, no live OAuth).
    5. Document per-subgroup discovery ≥1 requirement.
    6. Set status/wave_0 fields appropriately when maps match greened names.
  </action>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/07-cross-provider-multi-agent-orchestration/07-VALIDATION.md; rg -n "AGENT-01|AGENT-02|AGENT-03|AGENT-04|AGENT-05|AGENT-06|p7_|discover" .planning/phases/07-cross-provider-multi-agent-orchestration/07-VALIDATION.md; cargo test -p xai-grok-shell --lib reasoning_effort_explicit -- --nocapture; cargo test -p xai-grok-shell --lib resume_model_pinning -- --nocapture</automated>
  </verify>
  <done>
    VALIDATION.md maps AGENT-01..06 to real filters; same-provider resume/effort regressions green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase gate runbook with per-subgroup discovery + execute aggregate</name>
  <files>.planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md</files>
  <read_first>
    - 07-VALIDATION.md (Task 1)
    - Plan 01–05 verify blocks and SUMMARYs
    - 06-PHASE-GATE.md discover helper pattern
  </read_first>
  <action>
    Create 07-PHASE-GATE.md with copy-paste sequence covering **each required subgroup** from this plan (not single aggregate p7_).

    1. Tools: p7_ schema/effort/eager filters via --lib.
    2. Agent: description/effort guidance filter.
    3. Shell cross_provider_subagent: p7_spawn_missing_provider, p7_isolation, p7_missing, p7_parent_model, p7_same_provider (if present), smoke.
    4. Shell lib: reasoning_effort_explicit, resume_model_pinning, any p7_ unit filters.
    5. Prefer --lib for shell unit when bare package --list is poisoned by unrelated integration targets (Phase 6 lesson).
    6. Run the aggregate sequence; fix only filter name drift in the gate doc. Record pass timestamp.
    7. Explicit: no unfiltered cargo test -p xai-grok-shell --lib as sole gate; no live OAuth.
  </action>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md; rg -n "discover|test \"\\\$n\" -ge 1|AGENT-01|p7_" .planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md; discover() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); test "$n" -ge 1; cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture; }; discover xai-grok-tools p7_ --lib; discover xai-grok-shell p7_spawn_missing_provider --test cross_provider_subagent; discover xai-grok-shell p7_isolation --test cross_provider_subagent; discover xai-grok-shell p7_missing --test cross_provider_subagent; discover xai-grok-shell reasoning_effort_explicit --lib; discover xai-grok-shell resume_model_pinning --lib</automated>
  </verify>
  <done>
    Phase gate document exists; every required subgroup discovers ≥1 and executes green for AGENT-01..06 automated proofs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase gate CI | Fixture-only; must not require secret env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Bypass | Empty discovery false-green | medium | mitigate | Per-subgroup discover ≥1 before execute (Phase 6 lesson) |
| T-07-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
VALIDATION + PHASE-GATE present; discover+execute green for tools p7_, shell isolation/gate, AGENT-01 resume/effort.
</verification>

<success_criteria>
- AGENT-01 hard regression green
- AGENT-01..06 mapped and gated
- D-14/D-15/D-16 documented and enforced by exclusions
</success_criteria>

<output>
Create `.planning/phases/07-cross-provider-multi-agent-orchestration/07-06-SUMMARY.md` when done
</output>
