---
phase: 12-codex-depth-attribution-polish
plan: "08"
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
  - .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [ID-02, OPS-04]
must_haves:
  truths:
    - "The committed-diff audit accepts exactly the two legitimate committed Phase 12 review reports while continuing to reject unrecognized review-like and planning-like paths."
    - "The exact Phase 12 gap-plan and summary paths needed for this closure are admitted without converting any review, plan, or summary rule into an open-ended wildcard."
    - "The complete credential-free Phase 12 gate is GREEN at the final handed-off HEAD after the 12-08 summary metadata commit, with forbidden-content scanning unchanged for review artifacts."
    - "12-VALIDATION.md records current-HEAD commands, date, commit identity, results, and completion flags truthfully without changing its 18-row ownership map."
  artifacts:
    - path: .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
      provides: "Exact committed-diff allowances and non-vacuous fail-closed allowlist fixtures"
    - path: .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
      provides: "Refreshed current-HEAD credential-free closure evidence and re-verification handoff"
    - path: .planning/phases/12-codex-depth-attribution-polish/12-08-SUMMARY.md
      provides: "Committed plan close-out metadata present before the terminal read-only handoff verification"
  key_links:
    - from: .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
      to: .planning/phases/12-codex-depth-attribution-polish/12-REVIEW.md
      via: "Exact is_allowed_phase_diff_file case arm; file remains in committed_diff content scanning"
      pattern: "12-REVIEW.md"
    - from: .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
      to: .planning/phases/12-codex-depth-attribution-polish/12-REVIEW-FIX.md
      via: "Exact is_allowed_phase_diff_file case arm; file remains in committed_diff content scanning"
      pattern: "12-REVIEW-FIX.md"
    - from: .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
      to: .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
      via: "Current-HEAD complete-gate and validation-final evidence"
      pattern: "12-PHASE-GATE.md"
---

<objective>
Close the sole Phase 12 verification gap by teaching the committed-diff audit about the exact post-execution review artifacts, proving adjacent names still fail closed, and refreshing closure evidence from a reproducible credential-free GREEN run.

Purpose: Restore truth between the fail-closed gate and the 18/18 Nyquist record without changing product, user documentation, runtime, transport, OAuth, tool, notice, or user-facing behavior.
Output: A narrowly corrected Phase 12 gate, adversarial path fixtures, truthful validation evidence, and a current-HEAD re-verification handoff.
</objective>

<execution_context>
@/home/cristian/.codex/gsd-core/workflows/execute-plan.md
@/home/cristian/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-codex-depth-attribution-polish/12-CONTEXT.md
@.planning/phases/12-codex-depth-attribution-polish/12-RESEARCH.md
@.planning/phases/12-codex-depth-attribution-polish/12-07-SUMMARY.md
@.planning/phases/12-codex-depth-attribution-polish/12-VERIFICATION.md
@.planning/phases/12-codex-depth-attribution-polish/12-REVIEW.md
@.planning/phases/12-codex-depth-attribution-polish/12-REVIEW-FIX.md
@.planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
@.planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md

<requirement_note>
ROADMAP assigns Phase 12 product-honesty/D-10 deepening rather than new canonical requirement IDs. This closure preserves the already-delivered ID-02 and OPS-04 evidence; it changes only the planning gate and its validation record.
</requirement_note>

<gap_trace>
12-VERIFICATION.md reports one actionable gap: the complete gate rejects committed 12-REVIEW.md and 12-REVIEW-FIX.md, making the existing full/static PASS evidence and true completion flags non-reproducible. No product behavior gap, manual UAT gap, or deferred item is actionable here.
</gap_trace>
</context>

<artifacts_produced>

- An exact-path allowlist amendment for `12-REVIEW.md`, `12-REVIEW-FIX.md`, `12-08-PLAN.md`, and `12-08-SUMMARY.md`; no review/plan/summary glob is introduced.
- A fixture gate that proves the four legitimate paths pass and a fixed non-empty set of adjacent review-like, plan-like, summary-like, and cross-phase paths fail.
- Refreshed `12-VALIDATION.md` evidence containing the actual date, tested commit SHA, exact commands, observed result, and truthful single-occurrence completion flags.
- `12-08-SUMMARY.md` after execution; no product, user documentation, runtime, notice, or Rust source artifact changes, with only standard planning-metadata bookkeeping permitted during close-out.

</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrowly admit review artifacts and lock the allowlist boundary</name>
  <read_first>
    .planning/phases/12-codex-depth-attribution-polish/12-VERIFICATION.md
    .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
    .planning/phases/12-codex-depth-attribution-polish/12-REVIEW.md
    .planning/phases/12-codex-depth-attribution-polish/12-REVIEW-FIX.md
    .planning/phases/12-codex-depth-attribution-polish/12-07-SUMMARY.md
  </read_first>
  <files>.planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md</files>
  <behavior>
    - Exact legitimate paths `12-REVIEW.md`, `12-REVIEW-FIX.md`, `12-08-PLAN.md`, and `12-08-SUMMARY.md` are classified as allowed.
    - Nearby names such as `12-REVIEW-DRAFT.md`, `12-REVIEW-NOTES.md`, `12-REVIEW-FIX-2.md`, `12-09-PLAN.md`, `12-09-SUMMARY.md`, and a review path from another phase are classified as rejected.
    - Review report contents continue through the existing committed-diff forbidden-content scan; an executable invariant reads the exact pathspec array used by that scanner and proves its exclusion subset contains exactly `12-PHASE-GATE.md`, with no review path or review wildcard.
  </behavior>
  <action>First add a fixture function around `is_allowed_phase_diff_file` and make `--verify-scaffold` execute it. The fixture must use explicit expected-allowed and expected-rejected arrays, assert both arrays have their pinned nonzero counts, fail on the first inverted classification, and print the observed counts on success. Run the fixture before changing the allowlist and preserve its failing output as RED evidence: the two committed review paths must initially be rejected. Then extend the case statement with four literal paths only: the two committed review reports named in the verification gap and the exact Plan 08 plan/summary execution artifacts. Do not introduce a review-name wildcard, a numeric plan range beyond 08, a phase-directory catch-all, or any generic planning-file extension allowance. In the same RED/GREEN cycle, define one shared content-scan pathspec array/helper and make `committed_diff_gate` pass that exact array to `git diff`; do not leave a second independently maintained exclusion list. Add an executable fixture, invoked by `--verify-scaffold`, that reads the shared scanner input, extracts exclusion pathspecs, asserts there is exactly one, asserts it equals `:(exclude)$GATE_PATH`, and explicitly fails if either `12-REVIEW.md`, `12-REVIEW-FIX.md`, or any review-matching wildcard is excluded. Assert the positive `.` pathspec is still present as well. This fixture must exercise the same array/helper consumed by the production forbidden-content scanner, not a copied representation or prose grep. Preserve all WebSocket, stock-originator, tool-rename, derived-code, and notice-trigger patterns after the shared diff capture. Update help/scaffold messaging only enough to make both fixtures discoverable; do not change Rust, product docs, notices, runtime files, the pinned Phase 12 base, or any credential/network rule. Preserve all unrelated dirty and untracked files.</action>
  <acceptance_criteria>
    - The fixture is RED before the exact case-arm update and GREEN afterward.
    - Exactly four newly required execution/report paths are accepted, with no wildcard covering arbitrary review, plan, summary, or phase files.
    - At least six explicit adjacent/cross-phase paths are rejected and the fixture reports nonzero allowed/rejected counts.
    - The executable scanner-pathspec fixture proves the actual forbidden-content scan excludes exactly `12-PHASE-GATE.md`, includes `.`, and excludes neither review report nor a review wildcard.
    - Shell syntax and the full scaffold audit pass without provider credentials or network access.
  </acceptance_criteria>
  <verify>
    <automated>bash -n .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md &amp;&amp; bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md --verify-scaffold</automated>
  </verify>
  <done>The gate recognizes only the legitimate review and Plan 08 artifacts, explicit adversarial planning/review-like paths remain fail-closed, and the shared scanner-pathspec fixture proves both review reports remain inside forbidden-content scanning.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild truthful validation evidence from the complete credential-free gate</name>
  <read_first>
    .planning/phases/12-codex-depth-attribution-polish/12-VERIFICATION.md
    .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
    .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
    .planning/phases/12-codex-depth-attribution-polish/12-REVIEW-FIX.md
  </read_first>
  <files>.planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md, .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md</files>
  <action>After Task 1 is committed, capture `git rev-parse HEAD` and run the complete gate exactly as the verifier did: `bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md`. Do not use `--static` alone as closure evidence and do not substitute historical Phase 10 live evidence. If the complete command is nonzero, keep or set `wave_0_complete` and `nyquist_compliant` false, record the actual failing command/result, and stop rather than claiming closure. Only from an exit-zero complete run, refresh the stale final evidence in `12-VALIDATION.md`: keep exactly 18 existing task rows and their ownership, update rows `12-07-02` and `12-07-03` with the exact commands/results and current date, and add a compact re-verification record with the tested commit SHA, credential-free status, and observed fixture/static/Rust counts copied from command output. Keep each completion flag present exactly once and true only after GREEN evidence. Update the gate's latest-evidence comment only with values actually observed in this run; never fabricate a changed-path or test count. Do not modify any production, Rust, user documentation, notice, review, roadmap, state, or unrelated working-tree file.</action>
  <acceptance_criteria>
    - The complete default gate, not merely a component mode, exits zero after Task 1 is committed.
    - Validation still contains exactly 18 unique rows with unchanged Plan 01–07 ownership and no pending, red, or flaky row.
    - Rows 12-07-02 and 12-07-03 no longer cite stale pre-review closure evidence; they cite the actual current execution date and exact commands.
    - The evidence identifies the tested commit SHA and explicitly states that no OAuth credential or provider network access was used.
    - `wave_0_complete: true` and `nyquist_compliant: true` each occur exactly once and are supported by the new GREEN run.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md --validation-final &amp;&amp; test "$(awk -F '\174' '$2 ~ /^ 12-/ {n++} END {print n+0}' .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md)" -eq 18 &amp;&amp; test "$(rg -c '^wave_0_complete: true$' .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md)" -eq 1 &amp;&amp; test "$(rg -c '^nyquist_compliant: true$' .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md)" -eq 1</automated>
  </verify>
  <done>The 18/18 Nyquist map and both completion flags are truthful again, backed by a complete credential-free GREEN run with an identifiable tested commit.</done>
</task>

<task type="auto">
  <name>Task 3: Commit close-out metadata, then prove the exact read-only handoff HEAD</name>
  <read_first>
    .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
    .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
    .planning/phases/12-codex-depth-attribution-polish/12-VERIFICATION.md
  </read_first>
  <files>.planning/phases/12-codex-depth-attribution-polish/12-08-SUMMARY.md</files>
  <action>This is a terminal close-out contract that deliberately runs after the normal task commits, not an ordinary per-task commit. After Task 2's evidence refresh commit succeeds, complete the execute-plan metadata sequence first: create `12-08-SUMMARY.md` from the standard template using the already observed Task 1 and Task 2 results, perform any normal STATE/ROADMAP/REQUIREMENTS bookkeeping authorized by execute-plan, and make the single normal `docs(12-08): complete ... plan` metadata commit containing the summary and applicable planning metadata. The summary must state that the exact final handoff SHA is reported by the executor after the post-summary check; do not embed a guessed SHA or a claim that the post-summary run already passed. After that metadata commit, capture `FINAL_HANDOFF_SHA=$(git rev-parse HEAD)` and verify `git rev-parse HEAD` still equals it immediately before each command. Run `bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md`, then run `bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md --validation-final`. This post-summary portion is strictly read-only: do not edit, stage, commit, auto-fix, update planning metadata, regenerate the summary, or run any command that writes repository files afterward. Do not create a separate Task 3 commit; the metadata commit is its required commit boundary. If either command fails or HEAD changes, return failure with the captured SHA, exact command, and first failing boundary; do not amend the summary or create a corrective commit. On GREEN, return `FINAL_HANDOFF_SHA` and both successful commands in the executor's PLAN COMPLETE handoff. The execute-phase orchestrator must treat that returned SHA as the verified handoff commit and must not append another repository commit before handing off for verification.</action>
  <acceptance_criteria>
    - Task 2's committed evidence refresh and the committed `12-08-SUMMARY.md` are both part of `FINAL_HANDOFF_SHA` before the final run starts.
    - The complete default gate exits zero at exactly `FINAL_HANDOFF_SHA` and exercises both Rust and static/committed-diff boundaries.
    - The final validation audit exits zero and proves 18 green rows plus exactly one true occurrence of each completion flag.
    - The executor return records `FINAL_HANDOFF_SHA` and the two exact successful commands; the summary does not self-reference a SHA created by its own commit.
    - No repository write or commit occurs after `FINAL_HANDOFF_SHA` is captured and tested.
    - Unrelated dirty and untracked files remain untouched.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md &amp;&amp; bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md --validation-final</automated>
  </verify>
  <done>The final metadata commit, including `12-08-SUMMARY.md`, is reproducibly GREEN at the exact SHA returned to the orchestrator, with no later repository mutation.</done>
</task>

</tasks>

<closeout_order_override>
Task 3 is the plan's terminal post-summary verification hook. The executor MUST use this exact order: (1) execute and commit Task 1; (2) execute and commit Task 2; (3) generate the standard summary and apply only normal execute-plan planning-metadata bookkeeping; (4) create the single normal summary/planning-metadata commit; (5) capture `FINAL_HANDOFF_SHA`; (6) run Task 3's complete gate and `--validation-final` commands read-only while HEAD remains exactly `FINAL_HANDOFF_SHA`; (7) return PLAN COMPLETE with that SHA and both command results. Steps 5–7 replace any ordinary pre-summary execution or per-task commit treatment for Task 3. The executor MUST NOT rerun summary generation, metadata updates, acceptance auto-fixes, task commits, or any other repository-writing close-out step after step 4. If post-summary verification fails, return a failed handoff at the unchanged SHA; do not mutate the repository to make the return look successful.
</closeout_order_override>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history → committed-diff audit | Repository paths and added content are untrusted inputs to the phase scope gate. |
| Gate output → validation record | A stale, partial, or component-only result must not become completion evidence. |
| Test runner → developer environment | Closure must remain credential-free, network-free, check-only, and non-destructive to unrelated work. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-13 | Tampering | `is_allowed_phase_diff_file` | high | mitigate | Add literal report/Plan 08 paths only and run pinned allowed/rejected fixtures; forbid catch-all review, plan, summary, and directory patterns. |
| T-12-14 | Tampering | `committed_diff_gate` content scan | high | mitigate | Preserve the sole gate-script exclusion and prove review reports remain in forbidden-content scanning. |
| T-12-15 | Repudiation | `12-VALIDATION.md` closure evidence | medium | mitigate | Record exact command, date, tested commit SHA, observed counts, and exit-zero result; rerun after the evidence commit. |
| T-12-16 | Information Disclosure | Credential-free test runner | medium | mitigate | Retain `NO_LIVE_OAUTH_OR_NETWORK=true`, use only local fixtures/tests, and block any substitution with live-provider evidence. |
| T-12-17 | Denial of Service | Unrelated dirty/untracked workspace state | low | accept | The committed-range audit ignores unrelated worktree state; tasks avoid cleanup, mass formatting, checkout, reset, and deletion. |
</threat_model>

<source_audit>

| SOURCE | ID / Item | Plan | Status | Notes |
|--------|-----------|------|--------|-------|
| GOAL | Honest bum attribution and explicit Codex parity boundaries | 12-08 | COVERED | Restores reproducible proof; no product behavior is changed. |
| REQ | ID-02 product/user-facing identity remains bum | 12-08 | COVERED | Existing behavior evidence is rerun; no product or docs edit. |
| REQ | OPS-04 productive Codex tool path remains supported | 12-08 | COVERED | Complete gate reruns existing focused local behavior tests. |
| RESEARCH | Credential-free non-vacuous static/Rust close | 12-08 | COVERED | Full gate plus pinned allowlist fixtures and current-HEAD rerun. |
| RESEARCH | Verify notices and committed phase scope without churn | 12-08 | COVERED | Existing content scanning remains intact; notices are not edited. |
| CONTEXT | Automated focused close without repeated live dual-login | 12-08 | COVERED | All work is local and credential-free. |
| CONTEXT | Keep WS and broad tool-name parity deferred | 12-08 | COVERED | Existing forbidden-content scans remain active for admitted review artifacts. |
| CONTEXT | Notices change only for substantial derived code | 12-08 | COVERED | No derived implementation lands; notice-trigger scan is preserved. |
| CONTEXT | No runtime/wire behavior change | 12-08 | COVERED | Only gate and validation planning artifacts are modified. |
| VERIFICATION GAP | Exact review artifacts rejected; validation evidence stale | 12-08 | COVERED | Tasks 1–3 close both missing items and prepare re-verification. |
| DEFERRED | WebSocket transport, broad tool renames, additional live dual-login | none | EXCLUDED | Explicitly deferred; no closure task implements them. |

</source_audit>

<verification>
Run the scaffold/allowlist and shared scanner-pathspec fixtures first, then the complete credential-free gate and validation-final audit after the gate edit. After all implementation commits and the normal summary/planning-metadata commit, capture the final SHA and rerun the complete gate plus validation-final read-only at that exact HEAD. A component-only or pre-summary GREEN result is insufficient, and no repository write or commit may follow the final run.
</verification>

<success_criteria>
- Exact committed review and Plan 08 artifacts are allowed; unexpected adjacent/cross-phase planning/review-like artifacts are demonstrably rejected.
- Review artifact contents remain inside all existing forbidden-content checks.
- The complete gate and final validation audit are GREEN at the exact committed HEAD handed to the verifier, including `12-08-SUMMARY.md`.
- Validation contains 18 unchanged ownership rows, current reproducible evidence, and exactly one true occurrence of each completion flag.
- No product, user documentation, runtime, Rust, notice, OAuth, transport, or tool-schema changes; beyond the scoped gate, validation, and summary artifacts, only standard execute-plan planning-metadata bookkeeping is permitted, and unrelated worktree files remain untouched.
</success_criteria>

<output>
Create and commit `.planning/phases/12-codex-depth-attribution-polish/12-08-SUMMARY.md` before the terminal read-only verification. Do not write the self-referential final SHA into the summary. After the metadata commit, run the complete gate and `--validation-final` at the unchanged HEAD, then return that final tested SHA and the exact successful commands in the executor handoff without any later repository write or commit.
</output>
