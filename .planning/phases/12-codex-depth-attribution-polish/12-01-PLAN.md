---
phase: 12-codex-depth-attribution-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-pager/src/docs.rs
  - crates/codegen/xai-grok-agent/src/config.rs
  - .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
autonomous: true
requirements: [ID-02, OPS-04]
must_haves:
  truths:
    - "The full embedded documentation inventory has a non-vacuous product-identity contract before prose is changed."
    - "The existing Codex preset keeps Codex:apply_patch while bum's default preset keeps its established edit tool."
    - "Phase verification requires no OAuth credentials or provider network access."
  artifacts:
    - path: crates/codegen/xai-grok-pager/src/docs.rs
      provides: "p12_embedded_docs_use_bum_product_identity and p12_capability_disclosure_is_embedded_and_complete"
    - path: crates/codegen/xai-grok-agent/src/config.rs
      provides: "p12_codex_toolset_identity"
    - path: .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md
      provides: "Credential-free discover-before-execute phase gate"
  key_links:
    - from: crates/codegen/xai-grok-pager/src/docs.rs
      to: crates/codegen/xai-grok-pager/docs/user-guide/02-authentication.md
      via: "get_howto_doc(\"Authentication\") capability markers"
      pattern: "p12_capability_disclosure_is_embedded_and_complete"
    - from: crates/codegen/xai-grok-agent/src/config.rs
      to: crates/codegen/xai-grok-tools/src/implementations/codex/apply_patch/tool.rs
      via: "ToolConfig identity produced by the existing preset builder"
      pattern: "Codex:apply_patch"
---

<objective>
Create the Wave 0 regression contracts that define honest bum identity, the embedded capability disclosure, and the existing Codex patch-tool boundary before documentation implementation begins.

Purpose: Make the Phase 12 changes test-driven and non-vacuous without changing runtime or wire behavior.
Output: Two pager documentation tests, one agent toolset test, and the initial Phase 12 gate runbook.
</objective>

<execution_context>
@/home/cristian/.codex/gsd-core/workflows/execute-plan.md
@/home/cristian/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-codex-depth-attribution-polish/12-CONTEXT.md
@.planning/phases/12-codex-depth-attribution-polish/12-RESEARCH.md
@.planning/phases/12-codex-depth-attribution-polish/12-PATTERNS.md
@.planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md

<requirement_note>
The Phase 12 roadmap supplies narrative labels rather than new IDs in REQUIREMENTS.md. A spec-less probe could not source new canonical Phase 12 IDs. This plan deepens existing ID-02 and OPS-04 and derives its remaining must-haves from ROADMAP, CONTEXT, and RESEARCH without inventing requirement IDs.
</requirement_note>

<assumption_delta_decision>
Primary noun: `bum product identity and documented provider capability contract`.
Decision: `no-change` — optional parity enhancements do not move the identity/configuration anchor.
</assumption_delta_decision>

<interfaces>
From `crates/codegen/xai-grok-pager/src/docs.rs`: `USER_GUIDE`, `REFERENCE_DOCS`, and `get_howto_doc(title)` are the complete compiled documentation seams. Tests belong in the existing `#[cfg(test)] mod tests`.

From `crates/codegen/xai-grok-agent/src/config.rs`: `toolset_for_preset("codex")` resolves the Codex preset; `ToolConfig::from(&codex::ApplyPatchTool).id` and `ToolConfig::from(&grok_build::SearchReplaceTool).id` provide canonical IDs.
</interfaces>
</context>

## Artifacts this phase produces

- Rust test `docs::tests::p12_embedded_docs_use_bum_product_identity`.
- Rust test `docs::tests::p12_capability_disclosure_is_embedded_and_complete`.
- Rust test `config::tests::p12_codex_toolset_identity`.
- New file `.planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md` with inventory, allowlist, static checks, focused commands, and the no-live-UAT rule.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing embedded-document identity and disclosure contracts</name>
  <read_first>
    crates/codegen/xai-grok-pager/src/docs.rs
    .planning/phases/12-codex-depth-attribution-polish/12-PATTERNS.md
    .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
  </read_first>
  <files>crates/codegen/xai-grok-pager/src/docs.rs</files>
  <behavior>
    - `p12_embedded_docs_use_bum_product_identity` iterates `USER_GUIDE.iter().chain(REFERENCE_DOCS.iter())`, includes each filename in failures, rejects semantic stock-product/executable/global-home claims, and preserves classified provider/model, lineage, legal, internal, hosted-domain, and project-local compatibility references.
    - `p12_capability_disclosure_is_embedded_and_complete` resolves Authentication through `get_howto_doc` and asserts stable markers for non-stock identity, HTTP/SSE, deferred WebSocket transport, bum originator, tool mapping, and apply_patch shape.
  </behavior>
  <action>Add both p12 tests beside the existing documentation inventory tests. Implement the allowlist as explicit semantic categories rather than banning provider words globally. Do not edit any Markdown in this task. The tests must compile and fail against the current stale embedded prose, establishing RED before Wave 2. This implements the locked Product Attribution and Codex Parity Scope decisions.</action>
  <acceptance_criteria>
    - Both named tests are discoverable under the pager library target.
    - The inventory test covers all 22 registered USER_GUIDE entries plus both REFERENCE_DOCS entries.
    - The tests fail on current docs for meaningful missing/legacy content, not because of compilation or an empty test filter.
  </acceptance_criteria>
  <verify>
    <automated>identity_count=$(cargo test -p xai-grok-pager --lib -- --list | awk -F': ' '$1 ~ /::p12_embedded_docs_use_bum_product_identity$/ && $2 == "test" { n++ } END { print n + 0 }'); disclosure_count=$(cargo test -p xai-grok-pager --lib -- --list | awk -F': ' '$1 ~ /::p12_capability_disclosure_is_embedded_and_complete$/ && $2 == "test" { n++ } END { print n + 0 }'); test "$identity_count" -eq 1 && test "$disclosure_count" -eq 1 && if cargo test -p xai-grok-pager --lib p12_ -- --nocapture; then echo 'expected Wave 0 RED'; exit 1; else echo 'Wave 0 RED confirmed'; fi</automated>
  </verify>
  <done>The two non-vacuous pager contracts exist and are RED only because the current embedded documentation has not yet been corrected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the existing Codex and bum edit-tool identities</name>
  <read_first>
    crates/codegen/xai-grok-agent/src/config.rs
    crates/codegen/xai-grok-tools/src/implementations/codex/apply_patch/tool.rs
    crates/codegen/xai-grok-tools/THIRD_PARTY_NOTICES.md
  </read_first>
  <files>crates/codegen/xai-grok-agent/src/config.rs</files>
  <behavior>
    - The `codex` preset contains the canonical ID emitted by `codex::ApplyPatchTool` and it equals `Codex:apply_patch`.
    - The `grok-build` preset retains the canonical `grok_build::SearchReplaceTool` ID.
    - The assertion uses the real preset builder and does not introduce a new patch tool, alias, or rename.
  </behavior>
  <action>Add `p12_codex_toolset_identity` to the existing config test module, deriving IDs through `ToolConfig::from` and resolving presets through `toolset_for_preset`. Keep production preset composition untouched. This validates the locked existing-tool decision and OPS-04 without claiming stock Codex CLI wire parity.</action>
  <acceptance_criteria>
    - The test is discovered exactly once and passes.
    - Production code and tool schemas are unchanged.
    - No competing patch tool or broad tool rename is introduced.
  </acceptance_criteria>
  <verify>
    <automated>count=$(cargo test -p xai-grok-agent --lib -- --list | awk -F': ' '$1 ~ /::p12_codex_toolset_identity$/ && $2 == "test" { n++ } END { print n + 0 }'); test "$count" -eq 1 && cargo test -p xai-grok-agent --lib p12_codex_toolset_identity -- --nocapture</automated>
  </verify>
  <done>The preset boundary is protected by a focused green regression using canonical in-tree tool IDs.</done>
</task>

<task type="auto">
  <name>Task 3: Create the credential-free Phase 12 gate contract</name>
  <read_first>
    .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md
    .planning/phases/12-codex-depth-attribution-polish/12-VALIDATION.md
    .planning/phases/12-codex-depth-attribution-polish/12-RESEARCH.md
  </read_first>
  <files>.planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md</files>
  <action>Create `12-PHASE-GATE.md` as an executable Bash script (shebang plus executable bit) with `set -euo pipefail`. Its exact CLI is `bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md [MODE] [FILES...]`. Implement and document these modes before any consuming plan runs: no mode runs the complete Rust-plus-static phase gate; `--rust` runs the exact-discovery Rust matrix; `--static` runs inventory, attribution, notice, committed-diff, and check-only formatting gates; `--docs-files FILE...` runs the shared identity classifier on every supplied file and rejects zero files; and `--verify-scaffold` checks executable status, Bash syntax, every mode dispatch label, exact inventories, `discover_exact`, and the no-network invariant. Also implement the staged Nyquist modes consumed only by Plan 12-07 Task 3: `--validation-pre-final` read-only asserts exactly 18 unique expected IDs, exact plan/wave ownership, first 17 rows GREEN, row `12-07-03` pending, and both flags false; `--validation-finalize-row` first reruns that preflight and then atomically changes only row `12-07-03` to file-exists checked, dated `PASS` evidence naming the preflight command, and GREEN; `--validation-post-row` read-only asserts all 18 unique expected rows have exact ownership, checked artifacts, `PASS` evidence, GREEN status, zero pending/red/flaky rows, and both flags still false; `--validation-set-wave0` requires `--validation-post-row`, changes only `wave_0_complete` from its single false line to a single true line, and asserts `nyquist_compliant` remains a single false line; `--validation-set-nyquist` requires the post-row audit plus exactly one true wave flag and one false Nyquist flag, changes only the Nyquist flag to true, and asserts both true lines occur exactly once; `--validation-final` is read-only and reasserts the complete 18-row map, correct ownership, zero pending/red/flaky rows, and both single true flags. Every mutating mode must fail closed unless the required prior state is exact, write via a temporary file plus atomic rename, and refuse a repeated transition.

At creation time, pin the pre-Phase-12 `HEAD` SHA as `PHASE12_BASE_REF`; final diff checks must use the committed range from that literal SHA through `HEAD`, never the whole dirty worktree. Implement exact discover-before-execute counts for both p12 crate filters and every existing filtered apply-patch/trusted-originator command, pinning the observed expected counts in the script. Enumerate exactly 22 registered guides, two references, and the README/index entry points. Implement the shared per-file identity classifier used by `--docs-files`: require bum identity where applicable; reject stock-product subject claims, stock executable commands, and user-global stock-home patterns; allow only contextual provider/model, legal, lineage, internal identifier, hosted-domain, and project-local compatibility references. Add independent positive capability and notice assertions, `CODEX_ORIGINATOR` verification, and a committed Phase 12 diff allowlist that fails on WebSocket implementation, broad tool renames, competing patch tools, stock originator identity, unplanned files, or new copied/derived code without notice updates. Include `cargo fmt --all -- --check` and an explicit no-live-OAuth/no-network rule. Treat the two notice files as verify-only unless substantial copied or derived code lands. Do not add transport, auth, or runtime implementation steps.</action>
  <acceptance_criteria>
    - The runbook distinguishes required product identity from allowed provider/model/lineage/legal/internal references.
    - Empty filtered test runs cannot pass the gate.
    - The gate checks for stale docs, notice ancestry, bum originator, tool identity, and absence of out-of-scope implementation changes using the phase diff.
    - Every documented mode is implemented in the dispatch, `--verify-scaffold` proves the scaffold and modes are executable, and the staged validation modes enforce the final-row-before-flags ordering without a circular dependency.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/12-codex-depth-attribution-polish/12-PHASE-GATE.md --verify-scaffold</automated>
  </verify>
  <done>A reproducible and credential-free gate exists before documentation implementation starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded Markdown → product `/docs` | Repository prose becomes compiled user-facing product guidance. |
| Agent preset → model-visible tool schema | Preset composition defines mutation capabilities presented to the model. |
| Shell trusted route → provider request metadata | Existing tests must keep reserved Codex metadata route-scoped and bum-owned. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Spoofing | Embedded docs | high | mitigate | Add full-inventory identity and non-stock disclaimer contracts before prose edits. |
| T-12-02 | Tampering | Codex patch preset | high | mitigate | Lock the existing canonical apply-patch ID and default bum edit-tool separation through the real preset builder. |
| T-12-03 | Information Disclosure | Trusted route metadata | high | mitigate | Reuse the existing originator/non-leakage regressions in the phase gate; do not alter runtime code. |
| T-12-04 | Information Disclosure | Test fixtures | medium | mitigate | Gate uses source fixtures only and explicitly forbids live credentials and provider network checks. |
</threat_model>

<verification>
Wave 0 is complete when the pager contracts are discoverable and RED for documentation reasons, the toolset contract is green, and the gate runbook is non-vacuous.
</verification>

<success_criteria>
- Tests precede all prose changes they validate.
- No production, transport, OAuth, or tool implementation changes land.
- The phase has deterministic automated checks for identity, capability disclosure, tool naming, notices, and metadata containment.
</success_criteria>

<output>
Create `.planning/phases/12-codex-depth-attribution-polish/12-01-SUMMARY.md` when done.
</output>
