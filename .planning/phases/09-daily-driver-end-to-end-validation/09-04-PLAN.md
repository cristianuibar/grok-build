---
phase: 09-daily-driver-end-to-end-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
  - crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/auth
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
  - crates/codegen/xai-grok-tools/src
autonomous: false
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "Operator completes dual OAuth under BUM_HOME and records usable status without secrets (per D-04, D-12, D-15)"
    - "OPS-03 live xAI productive read + edit/shell PASSes on real backend (per D-05, D-16)"
    - "OPS-04 live GPT-5.6 productive read + edit/shell PASSes; capability gaps documented if any (per D-05, D-10)"
    - "OPS-05 same-process mid-session switch with productive turns both sides PASSes (per D-06)"
    - "OPS-06 both spawn directions PASSes with structured evidence: parent model, child model, effort, result returned, parent after (per D-07; review C1-M6)"
    - "Every OPS row classified PASS | PRODUCT BLOCKER | AUTH/ACCOUNT BLOCKED | PROVIDER OUTAGE/LIMIT (C1-L5); only PASS feeds Plan 05 GREEN"
    - "Sign-off fields filled: who, when UTC, binary/commit, model IDs (per D-09)"
    - "Any OPS-blocking bugs fixed in-phase with fail-closed/routing/chrome/tool-path only; larger work deferred (per D-03, D-13, D-14)"
    - "Task 3 records every actual product path changed in SUMMARY when conditional may-modify files are touched (C1-M5)"
    - "After product may-modify fixes, automated verify discover+executes affected p6/p7/p8 residual filters — not only p9_ (C2-M2)"
  artifacts:
    - path: .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
      provides: "Filled live checklist rows + outcome taxonomy + OPS-06 structured evidence + operator sign-off (redacted)"
      contains: "Sign-off"
  key_links:
    - from: Live dual login
      to: productive tool turns + switch + spawn
      via: "single TUI session preferred; fill each OPS row"
      pattern: "OPS-0"
    - from: UAT fail
      to: in-phase blocker fix
      via: "same seams as Phases 5–8 (auth gate, routing, residual chrome, tools)"
      pattern: "blocker"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** complete real coding sessions on both xAI and GPT-5.6 with mid-session switch and cross-provider subagents, **so that** bum is proven usable as my default coding agent.

<objective>
Execute the live dual-login UAT matrix (human operator) for OPS-03..06, fill 09-UAT.md with redacted evidence, and apply only in-phase blocker fixes if daily-driver bar fails (per D-03, D-05..D-10, D-13, D-15, D-16).

Purpose: Human half of hybrid proof — closes Phase 7 deferred live multi-turn dual-login NL E2E into OPS-06.
Output: Signed 09-UAT.md rows; optional minimal product fixes for true OPS blockers; SUMMARY with pass/fail notes (no secrets).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
@.planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh
@.planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-02-SUMMARY.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-03-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-VERIFICATION.md
</context>

## Locked decisions (this plan)

| ID | How this plan honors it |
|----|-------------------------|
| D-04 | Real dual OAuth on operator machine only |
| D-05..D-08 | Full session matrix with default models |
| D-09 / D-10 | Evidence + honest gaps |
| D-03 / D-13 | Blocker-only product code |
| D-14 | No deferred PROJECT outs while "fixing" |
| D-15 / D-16 | Human operator; no fixture waive |

## Blocker fix policy (HARD)

If UAT finds a failure that violates OPS-03..06:

| Allowed in-phase | Not allowed |
|------------------|-------------|
| Fail-closed / login-hint copy alignment (Phases 5–7) | Public install channel |
| Routing / credential slot wrong-provider bugs | Crate rename / mass env rename |
| Residual user-facing chrome that breaks daily-driver identity (clap help examples if UAT treats as blocker — not mass GROK_*) | Full catalog matrix / soak |
| Tool-path breakage for read/edit/shell daily bar | Custom agentic workflows |
| Re-run automated residual after fix | Treating auth expiry as product bug without re-auth attempt |

Larger feature work → document as deferred idea / later milestone; do not expand Phase 9 scope.

## Outcome taxonomy (HARD — C1-L5)

Every OPS-03..06 row (and the overall UAT close) must use exactly one of:

| Outcome | Meaning | Feeds Plan 05 GREEN? |
|---------|---------|----------------------|
| PASS | Live bar met on real backend with redacted evidence | Yes (required for all four) |
| PRODUCT BLOCKER | Product defect in fail-closed/routing/chrome/tool-path — Task 3 may fix in-phase | No until fixed + re-UAT PASS |
| AUTH/ACCOUNT BLOCKED | Token expiry, missing account, consent — re-auth then retry; not a product bug without re-auth attempt | No (do not waive) |
| PROVIDER OUTAGE/LIMIT | Provider 5xx, rate limit, regional outage — not a product defect | No (document; do not claim product fix needed) |

Only **PASS** on all four OPS rows may feed Plan 05 hybrid GREEN. Do not collapse outage/auth into PASS or into PRODUCT BLOCKER without evidence.

## Conditional may-modify source paths (C1-M5)

Task 3 may touch product code only under D-03/D-13. Declared may-modify set (not mandatory edits):

- `crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs` — switch / missing-provider gate
- `crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs` — spawn gate / child routing
- `crates/codegen/xai-grok-shell/src/agent/config.rs` — missing-provider / login-hint copy
- `crates/codegen/xai-grok-shell/src/auth/` — dual-slot store/status presentation (non-secret)
- `crates/codegen/xai-grok-pager/src/app/cli.rs` — residual user-facing chrome
- `crates/codegen/xai-grok-pager-bin/src/main.rs` — CLI composition / login routing
- `crates/codegen/xai-grok-tools/src/` — daily tool-path breakage only

If any of these change: list **every actual path** in 09-04-SUMMARY.md and ensure files_modified reporting matches. If no product fix: SUMMARY states may-modify unused.

## OPS-06 structured evidence (HARD — C1-M6)

For **each** spawn direction, 09-UAT.md must record redacted fields:

| Field | Required |
|-------|----------|
| parent_model | yes |
| child_model | yes |
| effort | yes (or explicit n/a if UI has no effort control) |
| result_returned | yes/no — child completed and returned to parent (start-only is FAIL) |
| parent_model_after | yes — must match parent_model |
| error_class | if failed: PRODUCT BLOCKER / AUTH/ACCOUNT BLOCKED / PROVIDER OUTAGE/LIMIT |

A child that starts but never returns is **not** PASS.

## Scope boundaries

- Agent prepares binary build, re-runs automated residual, and drafts redacted notes — **Cristian** completes interactive login and TUI matrix (D-15).
- Do not commit auth.json, tokens, or raw secret transcripts (D-12).
- Do not mark PASS if network/account fails — stop/re-auth; classify AUTH/ACCOUNT BLOCKED or PROVIDER OUTAGE/LIMIT (D-16, C1-L5).
- Use disposable workspace from Plan 03 UAT (C1-M3); record initial/final git status in notes.
- Complete CLI chrome preflight before matrix; residual Grok product chrome → blocker decision for Task 3 (C1-M4).

## Review dispositions (cycle 1)

| ID | Disposition |
|----|-------------|
| C1-M5 | Incorporated — conditional may-modify paths + SUMMARY path recording |
| C1-M6 | Incorporated — structured OPS-06 evidence fields |
| C1-L5 | Incorporated — PASS / PRODUCT BLOCKER / AUTH/ACCOUNT BLOCKED / PROVIDER OUTAGE/LIMIT |

## Review dispositions (cycle 2)

| ID | Disposition |
|----|-------------|
| C2-M2 | Incorporated — Task 3 automated verify discover+executes path-mapped p6/p7/p8 residual filters after may-modify fixes (not only p9_) |

## Post-fix residual map (HARD — C2-M2)

When Task 3 touches any may-modify product path, automated residual recheck MUST discover+execute the matching filters (not prose-only). Always re-run `p9_` after UAT finalize.

| Changed path class | Required residual filters (discover+execute) |
|--------------------|-----------------------------------------------|
| `model_switch.rs` / `config.rs` / `auth/` | shell `--test model_switch_gate` `p6_dual_login`; `p6_missing_provider`; shell `--test provider_routing` `switch_changes_next_sample_route` |
| `handle_request.rs` (spawn) | shell `--lib` list both `p7_isolation_*` dirs; execute aggregate `p7_isolation` once; `p7_spawn_missing_provider`; `p7_parent_model` |
| `xai-grok-tools/src/` | tools `--lib` `p7_eager` |
| `cli.rs` / `pager-bin/src/main.rs` | shell `--lib` `p8_telemetry`; pager-bin `--bin bum` `p8_no_auto_update`; `p8_sentry` |
| No product change (may-modify unused) | still discover+execute `p9_` + `p6_dual_login` + aggregate `p7_isolation` (UAT finalize safety net) |

Union of all touched classes when multiple paths change. Chain with `&&` / `set -euo pipefail`.

<tasks>

<task type="auto">
  <name>Task 1: Agent preflight — build binary + confirm automated half still green</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh
    - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
    - .planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
  </read_first>
  <action>
    1. Run scripts/uat-preflight.sh (or equivalent) to print steps and ensure target/debug/bum builds via cargo build -p xai-grok-pager-bin.
    2. Re-confirm automated residual still green for the critical subset (at minimum p9_ + p6_dual_login + p7_isolation both dirs + home_isolation) using discover pattern from Plan 02 — chain with && only. Prefer list-only for direction names then aggregate p7_isolation once if reusing Plan 02 hygiene.
    3. Record binary version/commit into 09-UAT.md Preflight notes (redacted env only). Confirm disposable workspace + chrome preflight rows exist blank for operator. Do not invent live PASS marks.
    4. Ensure 09-UAT.md tables are ready for operator fill: outcome taxonomy column (C1-L5), OPS-06 structured fields blank (C1-M6), Pass/outcome columns blank.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo build -p xai-grok-pager-bin && test -x target/debug/bum && discover() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); test "$n" -ge 1; cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture; }; discover xai-grok-shell p9_ --test cross_provider_subagent && discover xai-grok-shell p6_dual_login --test model_switch_gate && discover xai-grok-shell p7_isolation_grok_parent_codex --lib && discover xai-grok-shell p7_isolation_codex_parent_grok --lib && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture && rg -n "parent_model|result_returned|PRODUCT BLOCKER|PROVIDER OUTAGE|disposable|chrome" .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md | head -30</automated>
  </verify>
  <done>
    Binary ready; automated residual still green; UAT ready with taxonomy + OPS-06 structure for human matrix without live claims yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human live dual-login OPS-03..06 matrix (Cristian)</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md</files>
  <what-built>
    Automated residual green (Plans 01–02); required 09-UAT.md checklist + preflight script (Plan 03) with disposable workspace and chrome preflight; built target/debug/bum ready for live dual OAuth.
  </what-built>
  <action>
    Blocking human checkpoint (D-15, D-16): Cristian runs the live dual-login matrix and fills 09-UAT.md. Agent does not simulate live OAuth or mark fixture green as live pass. Follow how-to-verify steps; classify every row with outcome taxonomy (C1-L5); fill structured OPS-06 evidence (C1-M6); on completion fill Sign-off; on PRODUCT BLOCKER hand redacted notes to Task 3.
  </action>
  <how-to-verify>
    1. **Disposable workspace (C1-M3):** create/use worktree or fixture file; record path + initial `git status --short`.
    2. Run phase preflight (or follow 09-UAT.md Preflight): export BUM_HOME if isolating; ./target/debug/bum login; ./target/debug/bum login --provider codex; ./target/debug/bum auth status (confirm both usable — do not paste tokens into chat/git).
    3. **CLI chrome preflight (C1-M4):** run `./target/debug/bum --help` and note login/auth/switch labels. If residual product-visible Grok chrome breaks daily-driver identity, mark chrome preflight BLOCKER DECISION for Task 3 — do not ignore.
    4. Prefer TUI: ./target/debug/bum. Optional headless only if TUI blocked for a specific row. Work only in the disposable workspace for edit/shell.
    5. **OPS-03:** On xAI model (e.g. grok-build) complete read + edit or shell on real backend; mark outcome PASS or other taxonomy value + model id + notes.
    6. **OPS-04:** On GPT-5.6 (e.g. gpt-5.6-sol) complete read + edit or shell; document capability gaps if any Grok-only tools fail while daily tools work.
    7. **OPS-05:** In the **same** CLI process, productive xAI turn → switch model to GPT-5.6 → productive Codex turn (optional reverse); no CLI restart.
    8. **OPS-06 (C1-M6):** Spawn Grok parent → Codex child **and** Codex parent → Grok child (NL or Task with explicit model + effort). For each direction fill: parent_model, child_model, effort, result_returned (must be yes for PASS), parent_model_after (must match parent). Start-without-return is not PASS.
    9. Classify each OPS row: PASS | PRODUCT BLOCKER | AUTH/ACCOUNT BLOCKED | PROVIDER OUTAGE/LIMIT (C1-L5). Re-auth before calling product bug on token expiry. Outages are not product defects.
    10. Final `git status --short` in disposable workspace; cleanup note. Fill Sign-off: operator, date UTC, binary/commit, models, gaps, secrets committed = No, chrome outcome.
  </how-to-verify>
  <verify>
    <automated>set -euo pipefail; U=.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md; test -f "$U" && rg -n "OPS-03|OPS-04|OPS-05|OPS-06|Sign-off|Operator|parent_model|result_returned|PRODUCT BLOCKER|PROVIDER OUTAGE" "$U" | head -40</automated>
    <human-check>All four OPS rows have an outcome taxonomy value; PASS rows include model IDs; OPS-06 both dirs have structured evidence with result_returned; Sign-off completed without secrets</human-check>
  </verify>
  <done>
    09-UAT.md has live outcomes for OPS-03..06 (PASS or explicit non-pass taxonomy), OPS-06 structured evidence filled, operator sign-off filled, no secrets pasted.
  </done>
  <resume-signal>Type "approved" when all OPS-03..06 rows are PASS with sign-off filled, or describe failures/blockers with redacted notes and taxonomy (PRODUCT BLOCKER / AUTH/ACCOUNT BLOCKED / PROVIDER OUTAGE/LIMIT)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: In-phase blocker fixes (only if UAT fails OPS bar) + redacted UAT finalize</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md, crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs, crates/codegen/xai-grok-shell/src/agent/subagent/handle_request.rs, crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/auth/, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-tools/src/</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md (operator notes + taxonomy)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md (Pitfalls + product fix analog table)
    - Matching seam from Phases 5–8 for the failure class (auth gate / routing / chrome / tools)
  </read_first>
  <action>
    1. If human resumed with **approved** and all OPS rows PASS: sanitize 09-UAT.md (strip any accidental secrets); ensure sign-off complete; confirm OPS-06 structured fields present; document single-session multi-OPS if used; skip product code. SUMMARY records PASS and may-modify unused (C1-M5). Still run the no-product-change residual safety net from the Post-fix residual map (C2-M2).
    2. If human resumed with non-PASS outcomes: map each row via taxonomy (C1-L5). AUTH/ACCOUNT BLOCKED → request re-auth/retry, no product code. PROVIDER OUTAGE/LIMIT → document only, no product code, do not claim product defect. PRODUCT BLOCKER → apply only allowed in-phase fixes (D-03/D-13 table) on declared may-modify paths (C1-M5). Align error copy with Phase 5–7 product CLI login labels when friction is message-only. Residual Grok chrome from preflight blocker decision is in-scope only if it breaks daily-driver identity (C1-M4).
    3. After any product fix: list every actual file path changed in SUMMARY (C1-M5). **C2-M2 (HARD):** re-run residual via the Post-fix residual map — discover+execute the union of filters for every touched path class (p6_/p7_/p8_ as applicable) plus always `p9_`. Do not leave residual recheck as prose-only; the automated verify command must exercise these filters. Rebuild bum; ask operator to re-run only the failed UAT rows (another human-verify cycle if needed — document in SUMMARY). Addresses review concern C2-M2.
    4. Never expand into D-14 deferred work. Never commit secrets. Never mark live OPS green on fixtures alone (D-16). Never reclassify OUTAGE/AUTH as PASS without a successful live re-run.
    5. Final secrets scan on 09-UAT.md: JWT/private-key regex plus ensure no auth.json contents or opaque token dumps; confirm auth.json not staged; token-like path guard on tracked files.
  </action>
  <verify>
    <automated>set -euo pipefail; U=.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md; test -f "$U" && rg -n "OPS-03|OPS-04|OPS-05|OPS-06|Sign-off|Operator|parent_model|result_returned|PASS|PRODUCT BLOCKER|AUTH/ACCOUNT|PROVIDER OUTAGE" "$U" | head -50 && ! rg -n "eyJ[A-Za-z0-9_-]{20,}\\.[A-Za-z0-9_-]{10,}|BEGIN (RSA |OPENSSH )?PRIVATE" "$U" && ! git ls-files | rg -q '(^|/)(auth\.json)$' && ! git ls-files | rg -qi '(^|/)([^/]*token[^/]*|credentials\.json|\.oauth)($|/)' && discover() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); echo "discover $pkg $filter (${extra[*]:-}): n=$n"; test "$n" -ge 1; cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture; }; list_only() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); echo "list_only $pkg $filter (${extra[*]:-}): n=$n"; test "$n" -ge 1; }; CHANGED=$(git diff --name-only HEAD 2>/dev/null; git diff --cached --name-only 2>/dev/null; git status --porcelain 2>/dev/null | awk '{print $NF}'); need_p6=0; need_p7=0; need_tools=0; need_p8=0; echo "$CHANGED" | rg -q 'model_switch\.rs|agent/config\.rs|/auth/' && need_p6=1 || true; echo "$CHANGED" | rg -q 'handle_request\.rs' && need_p7=1 || true; echo "$CHANGED" | rg -q 'xai-grok-tools/' && need_tools=1 || true; echo "$CHANGED" | rg -q 'pager/src/app/cli\.rs|pager-bin/src/main\.rs' && need_p8=1 || true; discover xai-grok-shell p9_ --test cross_provider_subagent; discover xai-grok-shell p6_dual_login --test model_switch_gate; if [ "$need_p6" -eq 1 ]; then discover xai-grok-shell p6_missing_provider --test model_switch_gate; discover xai-grok-shell switch_changes_next_sample_route --test provider_routing; fi; list_only xai-grok-shell p7_isolation_grok_parent_codex --lib; list_only xai-grok-shell p7_isolation_codex_parent_grok --lib; discover xai-grok-shell p7_isolation --lib; if [ "$need_p7" -eq 1 ]; then discover xai-grok-shell p7_spawn_missing_provider --lib; discover xai-grok-shell p7_parent_model --lib; fi; if [ "$need_tools" -eq 1 ]; then discover xai-grok-tools p7_eager --lib; fi; if [ "$need_p8" -eq 1 ]; then discover xai-grok-shell p8_telemetry --lib; discover xai-grok-pager-bin p8_no_auto_update --bin bum; discover xai-grok-pager-bin p8_sentry --bin bum; fi</automated>
  </verify>
  <done>
    09-UAT.md filled, redacted, and taxonomy-complete; OPS-06 structured evidence present; either all OPS rows PASS with sign-off, or remaining non-PASS rows use AUTH/OUTAGE/PRODUCT labels correctly; any product fixes list every path in SUMMARY and re-verify automated residual via path-mapped p6/p7/p8 filters plus p9_ (C2-M2); no secrets committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → IdP / model APIs | Live tokens only on operator machine |
| UAT notes → git | Redaction required before commit |
| Child spawn → parent credentials | Must not cross-send bearers (live + residual) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Information Disclosure | UAT notes / commits | high | mitigate | Redact tokens; forbid auth.json in git; pre-push scan in verify |
| T-09-09 | Information Disclosure | cross-provider spawn | high | mitigate | Live OPS-06 both dirs + residual p7 isolation already green |
| T-09-10 | Spoofing | fixture waive | high | mitigate | Checkpoint requires live evidence; D-16 |
| T-09-11 | Tampering | stock auto-update mid-UAT | medium | mitigate | Prior p8 residual; quiet fork holds |
| T-09-SC | Tampering | cargo installs | low | accept | No new packages unless blocker demands existing dep only |
</threat_model>

<verification>
Signed UAT for OPS-03..06 (or explicit blocked status); automated residual still green; secrets absent from docs.
</verification>

<success_criteria>
- Live dual-login matrix executed by human with outcome taxonomy per row
- All four OPS rows PASS with model IDs, OPS-06 structured evidence, and sign-off OR documented AUTH/OUTAGE/PRODUCT non-pass (not silent waive)
- Blocker fixes (if any) stay within D-03/D-13 and record every changed path
- Post-fix residual recheck discover+executes affected p6/p7/p8 filters, not only p9_ (C2-M2)
</success_criteria>

<output>
Create `.planning/phases/09-daily-driver-end-to-end-validation/09-04-SUMMARY.md` when done
</output>
