---
phase: 09-daily-driver-end-to-end-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
autonomous: false
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "Operator completes dual OAuth under BUM_HOME and records usable status without secrets (per D-04, D-12, D-15)"
    - "OPS-03 live xAI productive read + edit/shell PASSes on real backend (per D-05, D-16)"
    - "OPS-04 live GPT-5.6 productive read + edit/shell PASSes; capability gaps documented if any (per D-05, D-10)"
    - "OPS-05 same-process mid-session switch with productive turns both sides PASSes (per D-06)"
    - "OPS-06 both spawn directions PASSes with model+effort and parent model stability notes (per D-07)"
    - "Sign-off fields filled: who, when UTC, binary/commit, model IDs (per D-09)"
    - "Any OPS-blocking bugs fixed in-phase with fail-closed/routing/chrome/tool-path only; larger work deferred (per D-03, D-13, D-14)"
  artifacts:
    - path: .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
      provides: "Filled live checklist rows + operator sign-off (redacted notes only)"
      contains: "Sign-off"
  key_links:
    - from: Live dual login
      to: productive tool turns + switch + spawn
      via: "single TUI session preferred; fill each OPS row"
      pattern: "OPS-0"
    - from: UAT fail
      to: in-phase blocker fix
      via: "same seams as Phases 5–8 (auth gate, routing, residual chrome, tools)"
      pattern: "blocker"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** complete real coding sessions on both xAI and GPT-5.6 with mid-session switch and cross-provider subagents, **so that** bum is proven usable as my default coding agent.

<objective>
Execute the live dual-login UAT matrix (human operator) for OPS-03..06, fill 09-UAT.md with redacted evidence, and apply only in-phase blocker fixes if daily-driver bar fails (per D-03, D-05..D-10, D-13, D-15, D-16).

Purpose: Human half of hybrid proof — closes Phase 7 deferred live multi-turn dual-login NL E2E into OPS-06.
Output: Signed 09-UAT.md rows; optional minimal product fixes for true OPS blockers; SUMMARY with pass/fail notes (no secrets).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
@.planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh
@.planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-02-SUMMARY.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-03-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-VERIFICATION.md
</context>

## Locked decisions (this plan)

| ID | How this plan honors it |
|----|-------------------------|
| D-04 | Real dual OAuth on operator machine only |
| D-05..D-08 | Full session matrix with default models |
| D-09 / D-10 | Evidence + honest gaps |
| D-03 / D-13 | Blocker-only product code |
| D-14 | No deferred PROJECT outs while "fixing" |
| D-15 / D-16 | Human operator; no fixture waive |

## Blocker fix policy (HARD)

If UAT finds a failure that violates OPS-03..06:

| Allowed in-phase | Not allowed |
|------------------|-------------|
| Fail-closed / login-hint copy alignment (Phases 5–7) | Public install channel |
| Routing / credential slot wrong-provider bugs | Crate rename / mass env rename |
| Residual user-facing chrome that breaks daily-driver identity (clap help examples if UAT treats as blocker — not mass GROK_*) | Full catalog matrix / soak |
| Tool-path breakage for read/edit/shell daily bar | Custom agentic workflows |
| Re-run automated residual after fix | Treating auth expiry as product bug without re-auth attempt |

Larger feature work → document as deferred idea / later milestone; do not expand Phase 9 scope.

## Scope boundaries

- Agent prepares binary build, re-runs automated residual, and drafts redacted notes — **Cristian** completes interactive login and TUI matrix (D-15).
- Do not commit auth.json, tokens, or raw secret transcripts (D-12).
- Do not mark PASS if network/account fails — stop/re-auth (D-16).

<tasks>

<task type="auto">
  <name>Task 1: Agent preflight — build binary + confirm automated half still green</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh
    - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
    - .planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
  </read_first>
  <action>
    1. Run scripts/uat-preflight.sh (or equivalent) to print steps and ensure target/debug/bum builds via cargo build -p xai-grok-pager-bin.
    2. Re-confirm automated residual still green for the critical subset (at minimum p9_ + p6_dual_login + p7_isolation both dirs + home_isolation) using discover pattern from Plan 02 — chain with && only.
    3. Record binary version/commit into 09-UAT.md Preflight notes (redacted env only). Do not invent live PASS marks.
    4. Ensure 09-UAT.md tables are ready for operator fill (blank Pass columns).
  </action>
  <verify>
    <automated>set -euo pipefail; cargo build -p xai-grok-pager-bin && test -x target/debug/bum && discover() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); test "$n" -ge 1; cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture; }; discover xai-grok-shell p9_ --test cross_provider_subagent && discover xai-grok-shell p6_dual_login --test model_switch_gate && discover xai-grok-shell p7_isolation_grok_parent_codex --lib && discover xai-grok-shell p7_isolation_codex_parent_grok --lib && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Binary ready; automated residual still green; UAT ready for human matrix without live claims yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human live dual-login OPS-03..06 matrix (Cristian)</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md</files>
  <what-built>
    Automated residual green (Plans 01–02); required 09-UAT.md checklist + preflight script (Plan 03); built target/debug/bum ready for live dual OAuth.
  </what-built>
  <action>
    Blocking human checkpoint (D-15, D-16): Cristian runs the live dual-login matrix and fills 09-UAT.md. Agent does not simulate live OAuth or mark fixture green as live pass. Follow how-to-verify steps; on completion fill Sign-off; on product blockers hand redacted notes to Task 3.
  </action>
  <how-to-verify>
    1. Run phase preflight (or follow 09-UAT.md Preflight): export BUM_HOME if isolating; ./target/debug/bum login; ./target/debug/bum login --provider codex; ./target/debug/bum auth status (confirm both usable — do not paste tokens into chat/git).
    2. Prefer TUI: ./target/debug/bum. Optional headless only if TUI blocked for a specific row.
    3. **OPS-03:** On xAI model (e.g. grok-build) complete read + edit or shell on real backend; mark Pass + model id + notes.
    4. **OPS-04:** On GPT-5.6 (e.g. gpt-5.6-sol) complete read + edit or shell; document capability gaps if any Grok-only tools fail while daily tools work.
    5. **OPS-05:** In the **same** CLI process, productive xAI turn → switch model to GPT-5.6 → productive Codex turn (optional reverse); no CLI restart.
    6. **OPS-06:** Spawn Grok parent → Codex child **and** Codex parent → Grok child (NL or Task with explicit model + effort); confirm child results return and parent model unchanged; both directions required.
    7. Fill Sign-off: operator, date UTC, binary/commit, models, gaps, secrets committed = No.
    8. If a step fails due to expired tokens: re-auth and retry before calling product bug. If product bug blocks OPS: note exact symptom for Task 3.
  </how-to-verify>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md && rg -n "OPS-03|OPS-04|OPS-05|OPS-06|Sign-off|Operator" .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md | head -30</automated>
    <human-check>All four OPS rows marked Pass with model IDs and Sign-off completed without secrets in the file</human-check>
  </verify>
  <done>
    09-UAT.md has live Pass for OPS-03..06 (or explicit fail notes for Task 3), operator sign-off filled, no secrets pasted.
  </done>
  <resume-signal>Type "approved" when all OPS-03..06 rows PASS and sign-off is filled, or describe failures/blockers with redacted notes</resume-signal>
</task>

<task type="auto">
  <name>Task 3: In-phase blocker fixes (only if UAT fails OPS bar) + redacted UAT finalize</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md (operator notes)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md (Pitfalls + product fix analog table)
    - Matching seam from Phases 5–8 for the failure class (auth gate / routing / chrome / tools)
  </read_first>
  <action>
    1. If human resumed with **approved** and all OPS rows PASS: sanitize 09-UAT.md (strip any accidental secrets); ensure sign-off complete; document single-session multi-OPS if used; skip product code. SUMMARY records PASS.
    2. If human resumed with **blockers**: classify each against OPS-03..06. Apply only allowed in-phase fixes (D-03/D-13 table above). Align error copy with Phase 5–7 product CLI login labels when friction is message-only.
    3. After any product fix: re-run relevant automated residual filters (affected p6_/p7_/p8_/p9_) with discover+execute && chains; rebuild bum; ask operator to re-run only the failed UAT rows (another human-verify cycle if needed — document in SUMMARY).
    4. Never expand into D-14 deferred work. Never commit secrets. Never mark live OPS green on fixtures alone (D-16).
    5. If blocker is environment/account only: leave row FAIL or blocked; do not waive.
  </action>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md && rg -n "OPS-03|OPS-04|OPS-05|OPS-06|Sign-off|Operator|Pass" .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md | head -40 && ! rg -n "eyJ[A-Za-z0-9_-]{20,}\\.[A-Za-z0-9_-]{10,}|BEGIN (RSA |OPENSSH )?PRIVATE" .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md && discover() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); test "$n" -ge 1; cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture; }; discover xai-grok-shell p9_ --test cross_provider_subagent</automated>
  </verify>
  <done>
    09-UAT.md filled and redacted; either all OPS rows PASS with sign-off, or remaining fails are environment-blocked with explicit notes; any product fixes re-verified automated; no secrets committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → IdP / model APIs | Live tokens only on operator machine |
| UAT notes → git | Redaction required before commit |
| Child spawn → parent credentials | Must not cross-send bearers (live + residual) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Information Disclosure | UAT notes / commits | high | mitigate | Redact tokens; forbid auth.json in git; pre-push scan in verify |
| T-09-09 | Information Disclosure | cross-provider spawn | high | mitigate | Live OPS-06 both dirs + residual p7 isolation already green |
| T-09-10 | Spoofing | fixture waive | high | mitigate | Checkpoint requires live evidence; D-16 |
| T-09-11 | Tampering | stock auto-update mid-UAT | medium | mitigate | Prior p8 residual; quiet fork holds |
| T-09-SC | Tampering | cargo installs | low | accept | No new packages unless blocker demands existing dep only |
</threat_model>

<verification>
Signed UAT for OPS-03..06 (or explicit blocked status); automated residual still green; secrets absent from docs.
</verification>

<success_criteria>
- Live dual-login matrix executed by human
- All four OPS rows PASS with model IDs and sign-off OR documented environment block (not silent waive)
- Blocker fixes (if any) stay within D-03/D-13
</success_criteria>

<output>
Create `.planning/phases/09-daily-driver-end-to-end-validation/09-04-SUMMARY.md` when done
</output>
