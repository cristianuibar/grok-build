---
phase: 09-daily-driver-end-to-end-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
  - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
autonomous: true
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "09-PHASE-GATE.md composes discover+execute for full P0/P1 prior subgroups + p9_ (per D-01, D-11; review C1-M2)"
    - "Every required automated subgroup has discovery n≥1 then green execute — no empty filters (per D-02 automated half)"
    - "Task 2 automated verify is the sole SoT and runs the full claimed inventory (C1-M2) — not a subset"
    - "Isolation: discover both direction names, execute aggregate p7_isolation once (C1-L3); home_isolation uses discover-before-execute (C1-L2)"
    - "Automated half runs green without live OAuth or network secrets (per D-04, D-12)"
    - "Human sign-off section present but not satisfied yet — automated-only is not full gate GREEN (per D-02, D-16)"
    - "Cargo hygiene: one TESTNAME filter per invocation; multi-filter chains use && only"
  artifacts:
    - path: .planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
      provides: "Runnable hybrid gate: automated discover+execute + human UAT checklist section"
      contains: "discover"
    - path: .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
      provides: "Cross-links PHASE-GATE commands; automated status notes after run"
      contains: "09-PHASE-GATE"
  key_links:
    - from: OPS-05 residual
      to: model_switch_gate p6_dual_login + provider_routing switch_changes_next_sample_route
      via: "PHASE-GATE P0 block"
      pattern: "p6_dual_login"
    - from: OPS-06 residual
      to: p7_isolation both dirs + p7_eager + p7_spawn_missing_provider
      via: "PHASE-GATE P0 block"
      pattern: "p7_isolation_codex_parent_grok"
    - from: Quiet-fork residual
      to: p8_* + home_isolation
      via: "PHASE-GATE P1 block"
      pattern: "home_isolation"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** complete real coding sessions on both xAI and GPT-5.6 with mid-session switch and cross-provider subagents, **so that** bum is proven usable as my default coding agent.

<objective>
Write and execute the automated half of the Phase 9 hybrid gate: compose critical prior p6/p7/p8 filters + thin p9_ into 09-PHASE-GATE.md with per-subgroup discover≥1 then execute (per D-01, D-02, D-11; reuse 07/08 gate product — do not invent a new framework).

Purpose: Prove regression residual for routing/switch/spawn/quiet-fork before human live UAT.
Output: 09-PHASE-GATE.md + automated-half green run recorded; human UAT section present but unsigned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-PATTERNS.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-01-SUMMARY.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md
@.planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-PHASE-GATE.md
</context>

## Locked decisions (this plan)

| ID | How this plan honors it |
|----|-------------------------|
| D-01 | Automated half of hybrid proof composed and run |
| D-02 | Document that full GREEN needs human UAT too |
| D-04 / D-12 | Fixture-only automated; no secrets |
| D-11 | Prior critical gates + p9_; green-only; no intentional-red |
| D-14 | No deferred PROJECT scope |

## Green-only gate protocol

1. Prefer `--lib` or explicit `--test <name>` — never bare package list as sole gate (Phase 6 lesson).
2. Per-subgroup: list count ≥1 then execute; never aggregate-only `grep -c p9_` as sole proof.
3. One positional TESTNAME per cargo test; chain with `&&` only.
4. Automated-half GREEN does **not** claim OPS live rows PASS (D-16).

## Scope boundaries

- Do **not** run or fake live dual-login UAT here.
- Do **not** re-run entire 06/07/08 gates unless time allows optional broad residual — minimum is RESEARCH full P0 + P1 inventory + p9_ (C1-M2).
- Do **not** add product features; if a required filter is missing (discover n=0), diagnose target/filter mismatch and restore the original greened name from prior SUMMARYs first — do not invent intentional-red. Prefer renaming PHASE-GATE to match reality over adding new thin wrappers (final-gate policy is even stricter in Plan 05 / C1-M7).

## Full P0/P1 automated inventory (SoT — C1-M2)

Task 1 PHASE-GATE document **and** Task 2 `<automated>` verify must both cover this inventory. Prefer executing the documented PHASE-GATE script block as the sole runtime SoT; the verify command must not be a subset.

| Group | Package / target | Filter | Mode |
|-------|------------------|--------|------|
| P2 | xai-grok-shell --test cross_provider_subagent | p9_ | discover+execute |
| P0 OPS-05 | xai-grok-shell --test model_switch_gate | p6_dual_login | discover+execute |
| P0 OPS-05 | xai-grok-shell --test model_switch_gate | p6_missing_provider | discover+execute |
| P0 OPS-05 | xai-grok-shell --test provider_routing | switch_changes_next_sample_route | discover+execute |
| P0 OPS-06 | xai-grok-shell --lib | p7_isolation_grok_parent_codex | discover only (direction name present) |
| P0 OPS-06 | xai-grok-shell --lib | p7_isolation_codex_parent_grok | discover only (direction name present) |
| P0 OPS-06 | xai-grok-shell --lib | p7_isolation | discover+execute once (aggregate runs both dirs — C1-L3) |
| P0 OPS-06 | xai-grok-tools --lib | p7_eager | discover+execute |
| P0 OPS-06 | xai-grok-shell --lib | p7_spawn_missing_provider | discover+execute |
| P0 OPS-06 | xai-grok-shell --lib | p7_parent_model | discover+execute |
| P1 quiet | xai-grok-shell --lib | p8_telemetry | discover+execute |
| P1 quiet | xai-grok-pager-bin --bin bum | p8_no_auto_update | discover+execute |
| P1 quiet | xai-grok-pager-bin --bin bum | p8_sentry | discover+execute |
| P1 quiet | xai-grok-pager-bin --test home_isolation | hermetic or home (≥1 name) | discover then execute (C1-L2) |

## Review dispositions (cycle 1)

| ID | Disposition |
|----|-------------|
| C1-M2 | Incorporated — full inventory in PHASE-GATE + Task 2 verify SoT |
| C1-L2 | Incorporated — home_isolation discover-before-execute |
| C1-L3 | Incorporated — discover both dirs; execute aggregate p7_isolation once |

<tasks>

<task type="auto">
  <name>Task 1: Write 09-PHASE-GATE.md automated discover+execute + human placeholder</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md (final inventory from Plan 01)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-01-SUMMARY.md (actual p9_ names)
    - .planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md (discover helper + composition; isolation discover-dirs / execute-aggregate pattern)
    - .planning/phases/08-quiet-fork-rebrand-polish/08-PHASE-GATE.md (p8 + home_isolation discover-before-execute)
  </read_first>
  <action>
    Create 09-PHASE-GATE.md with frontmatter-style header notes (phase 9, requirements OPS-03..06, fixture_only for automated, green_only true, human_uat_required true) and a runnable bash sequence that is the sole automated SoT (C1-M2):

    1. set -euo pipefail plus the canonical discover() helper from 07/08/VALIDATION (list ≥1 then execute). Provide a list-only discovery helper for direction-name checks that must not re-execute (C1-L3).
    2. **P2 thin smoke:** discover xai-grok-shell p9_ --test cross_provider_subagent (per D-11).
    3. **P0 OPS-05 residual (full):** discover+execute model_switch_gate p6_dual_login; p6_missing_provider; provider_routing switch_changes_next_sample_route (per D-06 automated residual). All three required — do not omit p6_missing_provider or switch_changes_next_sample_route (C1-M2).
    4. **P0 OPS-06 residual (full, isolation hygiene C1-L3):** list/discover shell --lib p7_isolation_grok_parent_codex (n≥1); list/discover shell --lib p7_isolation_codex_parent_grok (n≥1); then discover+execute aggregate shell --lib p7_isolation **once** (do not re-execute the two direction-specific filters after aggregate). Also discover+execute tools --lib p7_eager; shell --lib p7_spawn_missing_provider; shell --lib p7_parent_model. Prefer --lib for isolation (Phase 7 in-crate seam). Optional P1: --test cross_provider_subagent p7_spawn_same_provider if discoverable.
    5. **P1 quiet residual (full):** shell --lib p8_telemetry; pager-bin --bin bum p8_no_auto_update; p8_sentry (all three required — C1-M2). **home_isolation (C1-L2):** discover-before-execute — list hermetic or home filter names under --test home_isolation with n≥1, then execute (mirror 08-PHASE-GATE); never bare execute without discovery.
    6. **Human section (docs, not cargo):** checklist rows for OPS-03..06 signed PASS required; fail if UAT missing or fixture-only claims; secrets policy (D-12). End automated section with explicit line that automated half GREEN still requires human UAT for full gate (D-02).
    7. Forbidden notes: unfiltered shell --lib sole gate; live multi-turn as automated; CI OAuth secrets; marking live OPS from fixtures; subset verify that omits claimed inventory filters.
  </action>
  <verify>
    <automated>set -euo pipefail; G=.planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md; test -f "$G" && for tok in discover p9_ p6_dual_login p6_missing_provider switch_changes_next_sample_route p7_isolation p7_isolation_grok_parent_codex p7_isolation_codex_parent_grok p7_eager p7_spawn_missing_provider p7_parent_model p8_telemetry p8_no_auto_update p8_sentry home_isolation human_uat OPS-03 OPS-06; do test "$(rg -c -- "$tok" "$G" || true)" -ge 1 || { echo "missing $tok"; exit 1; }; done && test "$(rg -c 'human' "$G")" -ge 1</automated>
  </verify>
  <done>
    PHASE-GATE exists with discover helper, full P0/P1/p9_ automated blocks (including p6_missing_provider, switch_changes_next_sample_route, p7_spawn_missing_provider, p7_parent_model, p8_no_auto_update, p8_sentry), isolation discover-dirs/execute-aggregate once, home_isolation discover-before-execute, and human UAT required section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute full automated half (SoT) and sync VALIDATION status notes</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md, .planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-PHASE-GATE.md
    - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
  </read_first>
  <action>
    1. Run the automated half of 09-PHASE-GATE.md end-to-end as the sole SoT (C1-M2). Prefer bash -euo pipefail against the documented script block rather than a hand-picked subset. From repo root.
    2. On any discover n=0 or execute failure: diagnose (wrong --lib vs --test target, filter rename). Fix PHASE-GATE to match prior greened names; never skip a P0/P1 inventory row silently. Prefer restore/rename over inventing new wrappers.
    3. Isolation hygiene (C1-L3): confirm both direction names listed n≥1 then only one aggregate p7_isolation execute. Home isolation (C1-L2): discovery n≥1 for hermetic|home before execute.
    4. Record automated-half results in 09-02 SUMMARY (commands + pass for every inventory row). Update 09-VALIDATION.md automated Status column notes to green for residual rows that passed; keep live UAT Status pending (per D-16).
    5. Do not set nyquist_compliant true yet (requires Plan 05 human sign-off).
    6. Confirm no secrets written to docs.
  </action>
  <verify>
    <automated>set -euo pipefail; discover() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); echo "discover $pkg $filter (${extra[*]:-}): n=$n"; test "$n" -ge 1; cargo test -p "$pkg" "${extra[@]}" "$filter" -- --nocapture; }; list_only() { local pkg="$1" filter="$2"; shift 2; local extra=("$@"); local n; n=$(cargo test -p "$pkg" "${extra[@]}" -- --list 2>/dev/null | grep -c "$filter" || true); echo "list_only $pkg $filter (${extra[*]:-}): n=$n"; test "$n" -ge 1; }; discover xai-grok-shell p9_ --test cross_provider_subagent && discover xai-grok-shell p6_dual_login --test model_switch_gate && discover xai-grok-shell p6_missing_provider --test model_switch_gate && discover xai-grok-shell switch_changes_next_sample_route --test provider_routing && list_only xai-grok-shell p7_isolation_grok_parent_codex --lib && list_only xai-grok-shell p7_isolation_codex_parent_grok --lib && discover xai-grok-shell p7_isolation --lib && discover xai-grok-tools p7_eager --lib && discover xai-grok-shell p7_spawn_missing_provider --lib && discover xai-grok-shell p7_parent_model --lib && discover xai-grok-shell p8_telemetry --lib && discover xai-grok-pager-bin p8_no_auto_update --bin bum && discover xai-grok-pager-bin p8_sentry --bin bum && list_only xai-grok-pager-bin hermetic --test home_isolation && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture</automated>
  </verify>
  <done>
    Full P0/P1/p9_ inventory green including p6_missing_provider, switch_changes_next_sample_route, p7_spawn_missing_provider, p7_parent_model, p8_no_auto_update, p8_sentry; isolation directions listed then aggregate once; home_isolation discover-before-execute; VALIDATION notes updated without claiming live OPS pass; SUMMARY records the full run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase gate CI → network | Automated half must not require live IdP or model APIs |
| Docs → phase GREEN claim | Automated green must not imply OPS live pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Tampering | empty discover filters | medium | mitigate | Per-subgroup n≥1 before execute |
| T-09-04 | Spoofing | fixture-as-live | high | mitigate | Human section required; D-16 language in gate footer |
| T-09-05 | Information Disclosure | gate logs | medium | mitigate | Fixture tokens only; no auth dumps |
| T-09-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
PHASE-GATE automated sequence green; dual-direction p7 isolation named; human half still required for full GREEN.
</verification>

<success_criteria>
- Runnable 09-PHASE-GATE.md exists and automated half passes the full P0/P1/p9_ inventory (C1-M2)
- Isolation discover-dirs + execute aggregate once; home_isolation discover-before-execute (C1-L2/L3)
- Reuses 07/08 discover product (no new framework)
- Live OPS rows remain pending until UAT
</success_criteria>

<output>
Create `.planning/phases/09-daily-driver-end-to-end-validation/09-02-SUMMARY.md` when done
</output>
