---
phase: 09-daily-driver-end-to-end-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
  - .planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh
  - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
autonomous: true
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "09-UAT.md is a required (not advisory) live dual-login checklist covering OPS-03..06 (per D-02, D-05..D-09, D-16)"
    - "Preflight covers dual login, auth status without secrets, binary build, BUM_HOME isolation options (per D-04, D-15)"
    - "Disposable UAT worktree/fixture file required with initial and final git status --short recorded (review C1-M3)"
    - "CLI chrome preflight required (bum --help, auth/login/switch labels); residual product-visible Grok chrome is an explicit blocker decision (C1-M4)"
    - "Session matrix encodes productive tool bar, mid-session switch, both spawn directions, default models (per D-05..D-08)"
    - "Sign-off fields require operator, date UTC, commit/binary, model IDs, capability gaps, secrets=no (per D-09, D-10, D-12)"
    - "Secrets preflight guards auth.json/token paths and phase-diff scan, not only JWT/private-key regex (C1-L4)"
    - "Optional preflight script prints steps and never stores tokens (per D-12)"
  artifacts:
    - path: .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
      provides: "Required live dual-login UAT checklist with OPS-03..06 tables + sign-off"
      contains: "OPS-06"
    - path: .planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh
      provides: "Non-secret preflight helper (BUM_HOME + checklist printer)"
      contains: "BUM_HOME"
  key_links:
    - from: 09-UAT.md OPS rows
      to: 09-VERIFICATION.md (Plan 05)
      via: "signed PASS rows become live evidence"
      pattern: "Sign-off"
    - from: uat-preflight.sh
      to: bum login / auth status / TUI
      via: "operator runs printed steps; script never auto-marks pass"
      pattern: "auth status"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** complete real coding sessions on both xAI and GPT-5.6 with mid-session switch and cross-provider subagents, **so that** bum is proven usable as my default coding agent.

<objective>
Produce the required live dual-login UAT runbook and a non-secret preflight helper so Cristian can execute OPS-03..06 under real OAuth (per D-05..D-10, D-15, D-16; PATTERNS UAT skeleton elevated from advisory → required).

Purpose: Human half preparation while Plan 02 lands automated residual — both needed for hybrid GREEN.
Output: 09-UAT.md + scripts/uat-preflight.sh; VALIDATION human rows point at UAT sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-PATTERNS.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-01-SUMMARY.md
@.planning/phases/02-multi-slot-credentials-xai-oauth/02-VERIFICATION.md
</context>

## Locked decisions (this plan)

| ID | How this plan honors it |
|----|-------------------------|
| D-05 | OPS-03/04 tables require read + edit/shell |
| D-06 | OPS-05 same-process /model switch |
| D-07 | OPS-06 both spawn directions mandatory |
| D-08 | Default models grok-build + gpt-5.6-sol (record actual if different) |
| D-09 / D-10 | Sign-off + gap documentation fields |
| D-12 | Secrets policy in checklist + preflight |
| D-15 | Checklist for Cristian; agent does not fake live pass |
| D-16 | Gate rule: fixture green does not count |

## Scope boundaries

- Do **not** execute live OAuth as the agent in this plan (Plan 04).
- Do **not** store tokens, write real auth.json into the repo, or auto-mark UAT PASS.
- Do **not** implement deferred PROJECT outs.
- Prefer TUI daily path in instructions; headless `-p`/`-m` allowed per row if TUI blocked (CONTEXT discretion).
- Live edit/shell steps must target a **disposable** worktree or fixture file — never the primary development checkout as the only workspace (C1-M3).

## Review dispositions (cycle 1)

| ID | Disposition |
|----|-------------|
| C1-M3 | Incorporated — disposable worktree/fixture + initial/final git status |
| C1-M4 | Incorporated — CLI chrome preflight + residual Grok chrome blocker decision |
| C1-L4 | Incorporated — auth.json/token path + phase-diff secret scan |

<tasks>

<task type="auto">
  <name>Task 1: Write required 09-UAT.md dual-login checklist</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-PATTERNS.md (UAT structure skeleton)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md (session matrix + evidence)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md (live CLI preflight; pitfalls)
    - crates/codegen/xai-grok-models/default_models.json (confirm grok-build / gpt-5.6-sol ids)
    - crates/codegen/xai-grok-pager/src/app/cli.rs (user-facing help/chrome strings for preflight anchors)
  </read_first>
  <action>
    Create 09-UAT.md as a **required** gate checklist (not advisory) with:

    1. Header: Operator Cristian; environment ~/.bum or temp BUM_HOME; secrets policy; gate rule that fixture green does not count (D-16). Cite D-05..D-10, D-15, D-16.
    2. **Disposable workspace (C1-M3, required):** instruct operator to use a disposable git worktree or throwaway fixture directory/file for all live edit/shell steps. Require recording: path to disposable workspace; initial `git status --short` (or fixture baseline); final `git status --short` after matrix; cleanup verification. Forbid treating the primary bum/grok-build checkout as the only edit target without an explicit disposable path.
    3. **Preflight — binary and auth:** build bum (cargo build -p xai-grok-pager-bin); network + both accounts; optional isolated BUM_HOME; bum login; bum login --provider codex; bum auth status (usable both — no token dump); note binary version/commit.
    4. **Preflight — CLI chrome (C1-M4, required):** run `target/debug/bum --help`, sample login/auth status output chrome, and note `/model` (or switch) labels. Record whether product-facing strings present as bum (not residual stock Grok product chrome / ~/.grok-only guidance that would confuse daily-driver identity). Decision rule: residual product-visible Grok chrome that breaks daily-driver identity → explicit **blocker decision** for Plan 04 Task 3 (allowed in-phase chrome fix per D-03); do not silently ignore. Document PASS vs BLOCKER DECISION row in preflight table.
    5. **Preflight — secrets (C1-L4):** checklist requires (a) no tracked/staged auth.json under phase dir or repo; (b) no opaque token / credential-like dumps in UAT notes; (c) operator will scan phase diff before commit, not only JWT/private-key shapes. Reference git ls-files guard pattern for auth.json and token-like paths.
    6. OPS-03 table: TUI prefer; xAI model (default grok-build); read real file in disposable workspace; edit or shell productive change on real backend (D-05).
    7. OPS-04 table: GPT-5.6 catalog entry (default gpt-5.6-sol); read + edit/shell; capability gaps field (D-10).
    8. OPS-05 table: same CLI process; xAI productive turn → model switch to GPT-5.6 → Codex productive turn; optional reverse; no restart (D-06).
    9. OPS-06 table: Grok parent → Codex child **and** Codex parent → Grok child (NL or Task with model+effort); parent model unchanged after child; both rows mandatory (D-07). Include placeholders for structured evidence fields Plan 04 will fill (parent model, child model, effort, result returned, parent after — C1-M6 cross-ref).
    10. Notes: single dual-login session may cover multiple OPS rows when natural — still fill each row (CONTEXT discretion).
    11. Sign-off table: operator, date UTC, binary/commit, models under test, gaps, secrets committed? No, disposable workspace path, chrome preflight outcome (D-09, D-12, C1-M3, C1-M4).
    12. Anti-patterns: environment skip ≠ pass; one OPS-06 direction insufficient; no secret paste; no mock headless as live pass; no live edits only in primary checkout without disposable path; no ignoring residual Grok chrome without blocker decision.
  </action>
  <verify>
    <automated>set -euo pipefail; U=.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md; test -f "$U" && for tok in OPS-03 OPS-04 OPS-05 OPS-06 Preflight Sign-off gpt-5.6-sol grok-build disposable 'git status' chrome secret auth.json fixture D-0; do test "$(rg -c -- "$tok" "$U" || true)" -ge 1 || { echo "missing $tok"; exit 1; }; done && test "$(rg -c 'OPS-06' "$U")" -ge 1 && test "$(rg -c 'Sign-off' "$U")" -ge 1</automated>
  </verify>
  <done>
    09-UAT.md exists with preflight (auth + chrome + secrets + disposable workspace), four OPS tables (both spawn dirs), sign-off, and required-gate language.
  </done>
</task>

<task type="auto">
  <name>Task 2: Non-secret uat-preflight script + VALIDATION human row links</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh, .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md
    - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md Manual / Human Verifications section
    - .planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md Live UAT CLI preflight snippet
  </read_first>
  <action>
    1. Create scripts/uat-preflight.sh under the phase dir (executable bit in git if practical): bash set -euo pipefail; optionally default BUM_HOME to $HOME/.bum or accept env override; print numbered preflight steps mirroring 09-UAT.md including disposable workspace + git status baseline guidance (C1-M3) and CLI chrome checks (C1-M4); optionally run cargo build -p xai-grok-pager-bin if binary missing; print path to target/debug/bum; optionally print `bum --help` head for chrome inspection without auto-marking pass; **never** cat auth.json tokens; never curl live APIs with secrets; never auto-mark UAT pass (D-12, D-15).
    2. Script must refuse to write secrets into the phase dir. Optional: print remapped BUM_HOME for isolation using mktemp guidance without persisting credentials into the repo.
    3. **Secrets hygiene helpers (C1-L4):** script may print (not execute as hard fail unless safe) suggested checks: git ls-files filter for auth.json / token-like paths; remind operator to scan phase diff before commit. Never dump credential file contents.
    4. Update 09-VALIDATION.md Manual / Human Verifications Instructions column to point at 09-UAT.md §OPS-03..06 and mention the preflight script path, disposable workspace, and chrome preflight.
    5. Do not execute live login in this task.
  </action>
  <verify>
    <automated>set -euo pipefail; S=.planning/phases/09-daily-driver-end-to-end-validation/scripts/uat-preflight.sh; U=.planning/phases/09-daily-driver-end-to-end-validation/09-UAT.md; test -f "$S" && test -f "$U" && bash -n "$S" && rg -n "09-UAT|uat-preflight|OPS-03|OPS-04|disposable|chrome" .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md | head -30 && for tok in disposable chrome auth.json; do test "$(rg -c -- "$tok" "$S" || true)" -ge 1 || { echo "preflight missing $tok"; exit 1; }; done && ! rg -n "BEGIN (RSA |OPENSSH )?PRIVATE|eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{10,}" "$S" "$U" && ! git ls-files | rg -q '(^|/)(auth\.json)$'</automated>
  </verify>
  <done>
    Preflight script syntax-valid, non-secret, and covers disposable workspace + chrome + secrets path guards; VALIDATION human rows link to 09-UAT; checklist ready for Plan 04 operator execute.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator machine → IdP | Real OAuth only on human path; not encoded in scripts |
| UAT docs → git | Redacted notes only; no tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06 | Information Disclosure | UAT checklist / preflight | high | mitigate | Explicit secrets policy; script never dumps auth.json; auth.json/token path guards + phase-diff scan (C1-L4); pre-push language in UAT |
| T-09-06b | Tampering | live UAT on primary checkout | medium | mitigate | Disposable worktree/fixture + initial/final git status (C1-M3) |
| T-09-07 | Elevation of Privilege | fake auto-pass | medium | mitigate | Script cannot mark UAT PASS; Plan 04 human sign-off only |
| T-09-SC | Tampering | cargo installs | low | accept | No new packages |
</threat_model>

<verification>
UAT checklist complete; preflight bash -n clean; VALIDATION links human rows to UAT; no secrets in artifacts.
</verification>

<success_criteria>
- Required dual-login UAT runbook ready for Cristian with disposable workspace + chrome preflight
- Preflight helper non-secret with path-level secret guards
- Live evidence fields match D-09/D-10/D-12
</success_criteria>

<output>
Create `.planning/phases/09-daily-driver-end-to-end-validation/09-03-SUMMARY.md` when done
</output>
