---
phase: 09-daily-driver-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
  - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
autonomous: true
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - "09-VALIDATION.md dual-maps OPS-03..06 to automated residual commands AND required live UAT rows (per D-01, D-02, D-16)"
    - "Thin p9_ fixture smoke discovers ≥1 and stays GREEN — no live network, no intentional-red (per D-11)"
    - "Dual-slot fixture residual under temp BUM_HOME is readable for both providers without ambient stock homes (per D-04, D-12)"
    - "Login-hint residual still points at product CLI provider login for empty slots (per D-03 fail-closed residual)"
    - "Wave 0 inventory rows for VALIDATION + p9_ marked complete; nyquist frontmatter updated toward wave_0_complete"
  artifacts:
    - path: .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
      provides: "Finalized Nyquist dual auto/human map for OPS-03..06 with P0/P1 re-run inventory"
      contains: "p9_"
    - path: crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
      provides: "Green-only p9_ daily-driver residual smoke (fixture tokens)"
      contains: "p9_"
  key_links:
    - from: OPS-03..06 live rows
      to: 09-UAT.md (Plan 03) + signed VERIFICATION (Plan 05)
      via: "VALIDATION human table Required — never fixture-green alone (D-16)"
      pattern: "live UAT required"
    - from: p9_ discovery filter
      to: 09-PHASE-GATE.md automated half (Plan 02)
      via: "discover ≥1 then execute under green-only protocol"
      pattern: "p9_"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** complete real coding sessions on both xAI and GPT-5.6 with mid-session switch and cross-provider subagents, **so that** bum is proven usable as my default coding agent.

<objective>
Finalize Wave 0/1 validation inventory and land thin green-only `p9_` fixture residual smoke so later PHASE-GATE discovery is non-vacuous (per D-01 hybrid methodology, D-11 automated scope, RESEARCH Validation Architecture, Phase 7/8 green-only protocol).

Purpose: Establish the dual auto/human map and a discoverable `p9_` filter before composing prior-gate re-runs or live UAT.
Output: Updated 09-VALIDATION.md + 1–3 green `p9_*` tests under shell cross_provider harness; zero intentional-red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-PATTERNS.md
@.planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md
@crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs
@.planning/phases/08-quiet-fork-rebrand-polish/08-01-PLAN.md
@.planning/phases/07-cross-provider-multi-agent-orchestration/07-PHASE-GATE.md
</context>

## Locked decisions (traceability)

CONTEXT section locks assigned stable IDs for plan coverage:

| ID | Decision |
|----|----------|
| D-01 | Hybrid proof: automated prior-gate residual + thin p9_ fixtures **plus** live dual-login human UAT |
| D-02 | Phase gate GREEN requires both automated green **and** signed human checklist for all four OPS rows |
| D-03 | Fix blocking bugs in-phase only (fail-closed / routing / chrome / tool-path); larger work deferred |
| D-04 | Live UAT under `~/.bum` or temp `BUM_HOME`; no CI live OAuth secrets |
| D-05 | OPS-03/04 minimum: productive read + edit/shell per provider on real backend |
| D-06 | OPS-05: same CLI process switch xAI ↔ GPT-5.6 without restart |
| D-07 | OPS-06: both live spawn directions (Grok→Codex and Codex→Grok) with model+effort |
| D-08 | Models under test: one current xAI daily + one GPT-5.6 catalog entry (e.g. Sol) — not full matrix |
| D-09 | Live evidence: structured UAT + VERIFICATION rows with who/when/model IDs |
| D-10 | Document provider capability gaps honestly; daily tools still required |
| D-11 | Automated: re-run critical prior gates + thin p9_; green-only; no live network for auto half |
| D-12 | Never commit tokens, auth.json, or secret transcripts |
| D-13 | Validation-first — product code only for OPS blockers found in UAT |
| D-14 | Out of scope: install channel, crate rename, mass env rename, custom workflows, full catalog, enterprise IdP |
| D-15 | Cristian runs live UAT; agent prepares checklists/scripts and lands automated green first |
| D-16 | Live OAuth required for green OPS rows — no fixture waive; network/account fail blocks human path |

**This plan covers:** D-01/D-11 inventory scaffolding, D-12 fixture hygiene, dual residual anchors for D-05..D-07 automated half. Live UAT / gate close are later plans.

## Expected-green protocol (HARD)

1. **No intentional-red under `p9_`.** Every `p9_*` test must compile and **pass** on current mainline APIs.
2. **No live network** in `p9_` tests — fixture tokens only (`xai-fake-token*` / `codex-fake-token*`).
3. **No `#[ignore]` expected-red placeholders** under `p9_`.
4. Prefer **thin residual** over re-implementing full p6/p7 Authorization harnesses (re-run those in Plan 02).

## Scope boundaries

- Do **not** implement live OAuth automation, CI secrets, or product feature work.
- Do **not** invent a new verification product — finalize existing VALIDATION draft only.
- Do **not** implement deferred PROJECT outs (D-14).
- Do **not** mark live OPS rows green in VALIDATION (status stays pending until Plan 05).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN thin p9_ dual-slot residual smoke (fixture-only)</name>
  <files>crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/cross_provider_subagent.rs (ensure_sandbox, write_auth_document, p7_missing_provider_gate_error_*, fixture constants)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md (Pattern 3 thin p9_, Validation Architecture)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-PATTERNS.md (p9_ naming + OnceLock hygiene)
  </read_first>
  <behavior>
    - p9_fixture_dual_slot_auth_document_readable: under process sandbox BUM_HOME, write dual-slot auth.json with distinct fake tokens; read_provider_auth_store shows both providers present/usable material without live network
    - p9_login_hint_residual_suggests_bum_provider_login: empty-slot missing_provider_gate_error still suggests product CLI provider login for Codex (and optionally xAI) — residual lock for daily-driver fail-closed copy
    - Optional third: p9_route_hosts_differ_for_xai_and_codex_models — pure resolve_provider_route host/slot differ for grok-build vs gpt-5.6-sol (thin residual only if cheap; skip if already covered and time-bound)
  </behavior>
  <action>
    1. Extend cross_provider_subagent.rs with 1–3 green tests named under the p9_ prefix (per D-11). Reuse ensure_sandbox + write_auth_document + sample_oidc; do not introduce a second BUM_HOME mutation mid-process (OnceLock hygiene).
    2. Use fixture token strings that remain fake-only (prefer xai-fake-token-p9 / codex-fake-token-p9 or reuse existing p7 fakes). Never read real ~/.bum credentials.
    3. Dual-slot residual must assert both providers appear in the auth document / store under the sandbox path after write.
    4. Login-hint residual must assert suggestion text uses the product CLI provider form (same shape as existing p7_missing_provider_gate_error_suggests_* — reassert under p9_ for phase discovery, or factor shared assert if duplication is noisy; prefer thin dedicated p9_ names for discoverability).
    5. Do not add live HTTP, intentional-red, or #[ignore] under p9_. Do not re-implement p7_isolation Authorization mock harness.
  </action>
  <verify>
    <automated>set -euo pipefail; n=$(cargo test -p xai-grok-shell --test cross_provider_subagent p9_ -- --list 2>/dev/null | grep -c 'p9_' || true); test "$n" -ge 1 && cargo test -p xai-grok-shell --test cross_provider_subagent p9_ -- --nocapture</automated>
  </verify>
  <done>
    cargo discover lists ≥1 p9_ under cross_provider_subagent; all p9_ tests green; fixture-only; no secrets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize 09-VALIDATION.md dual auto/human inventory</name>
  <files>.planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md (current draft)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-RESEARCH.md (recommended automated re-run inventory P0/P1/P2)
    - .planning/phases/09-daily-driver-end-to-end-validation/09-CONTEXT.md (locked hybrid bar)
    - SUMMARY of Task 1 actual p9_ test names after they land
  </read_first>
  <action>
    1. Update 09-VALIDATION.md so every OPS-03..06 row has both Automated residual command(s) and Live UAT proof columns (per D-01, D-02, D-16). Keep human rows Required (not advisory).
    2. Map concrete filters for P0/P1 inventory from RESEARCH (p6_dual_login, p6_missing_provider, switch_changes_next_sample_route, p7_isolation both dirs, p7_eager, p7_spawn_missing_provider, p7_parent_model, p8_telemetry, p8_no_auto_update, p8_sentry, home_isolation, and p9_ from Task 1).
    3. Record actual greened p9_ test names and the cargo command: cargo test -p xai-grok-shell --test cross_provider_subagent p9_ -- --nocapture.
    4. Mark Wave 0 items for VALIDATION + p9_ complete; leave PHASE-GATE / UAT / VERIFICATION as pending for later plans. Set frontmatter wave_0_complete appropriately for inventory readiness (nyquist_compliant remains false until hybrid gate closes in Plan 05).
    5. Explicit exclusions block: D-14 deferred PROJECT outs; no CI live secrets (D-04); no intentional-red (D-11); live OPS rows cannot green on fixtures alone (D-16).
    6. Cite D-NN coverage for each OPS row in notes or a short coverage table.
  </action>
  <verify>
    <automated>set -euo pipefail; test -f .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md && rg -n "OPS-03|OPS-04|OPS-05|OPS-06|p9_|p6_dual_login|p7_isolation|live UAT required|D-0" .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md | head -50 && test "$(rg -c 'p9_' .planning/phases/09-daily-driver-end-to-end-validation/09-VALIDATION.md)" -ge 1</automated>
  </verify>
  <done>
    VALIDATION dual-maps all four OPS reqs to auto residual + required live UAT; p9_ named; exclusions documented; Wave 0 inventory closed for this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture tests → CI | Must never require live OAuth or real tokens |
| Auth fixtures → disk | Temp BUM_HOME only; no ambient ~/.bum writes in p9_ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | p9_ fixtures / logs | medium | mitigate | Fake tokens only; no real auth.json; never commit secrets (D-12) |
| T-09-02 | Spoofing | VALIDATION live rows | high | mitigate | Dual columns; live OPS cannot claim pass from fixtures (D-16) |
| T-09-SC | Tampering | cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
p9_ discover+execute green; VALIDATION inventory dual-maps OPS-03..06 with D-NN citations.
</verification>

<success_criteria>
- Thin green-only p9_ residual exists and is discoverable
- 09-VALIDATION.md ready as source of truth for Plan 02 gate composition and Plan 03 UAT
- No product scope creep / no deferred PROJECT outs
</success_criteria>

<output>
Create `.planning/phases/09-daily-driver-end-to-end-validation/09-01-SUMMARY.md` when done
</output>
