---
phase: 06-mid-session-switch-missing-provider-gate
plan: 05
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - MOD-03
  - MOD-06
must_haves:
  truths:
    - "With both providers usable (fixture dual tokens), mid-session switch Grok↔GPT succeeds without MODEL_SWITCH_MISSING_PROVIDER (MOD-03 free movement)"
    - "Successful switch changes next-sample route (base URL + credential slot) — reuse/extend switch_changes_next_sample_route"
    - "Same-provider switch with usable creds has no missing-provider friction"
    - "Session conversation history is not wiped by model_switch apply on success (D-06)"
    - "Mid-turn switch remains next-turn-only: apply does not cancel in-flight turn by default (D-06)"
    - "BYOK has_own_credentials models switch without OAuth slot (MOD-06 BYOK)"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Dual-login free switch + missing vs usable matrix integration proofs"
      contains: switch_changes_next_sample_route
  key_links:
    - from: dual fixture auth.json
      to: model_switch::apply success
      via: "both slots usable → free Grok↔GPT switch"
      pattern: "providers.xai|providers.codex|switch"
    - from: apply success
      to: next sample SamplingConfig
      via: "Phase 4 route rebuild on next turn"
      pattern: "base_url|credential"
---

## Phase Goal

**As a** bum user mid-session, **I want to** switch models freely and get a clear fail-closed login path when a provider is missing, **so that** I never depend on silent mid-turn 401 as the primary UX and never restart the CLI to change models.

<objective>
Prove MOD-03 free dual-provider mid-session switching and MOD-06 edge cases (same-provider, BYOK, history/next-turn) with automated fixture tests (per D-06). Complements Plan 01 block tests with positive free-switch proofs.

Purpose: Roadmap success criterion 3 — both providers logged in → free Grok↔GPT movement in one session.
Output: Extended provider_routing (and optional pager) tests green with fake tokens only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-CONTEXT.md
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-RESEARCH.md
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs
@crates/codegen/xai-grok-shell/src/session/acp_session_impl/model_switch.rs
@.planning/phases/04-provider-aware-request-routing/04-CONTEXT.md
</context>

## Locked decisions (this plan)

| ID | Applies |
|----|---------|
| D-02 | Usable dual fixtures include refreshable = usable |
| D-06 | Mid-turn next-only; keep history |
| MOD-03 | Switch anytime; next turn uses new model |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual-usable free Grok↔GPT switch + same-provider no friction</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (switch_changes_next_sample_route ~995–1068, dual fake tokens, harness)
    - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs (fixture store patterns if needed)
    - .planning/phases/06-mid-session-switch-missing-provider-gate/06-RESEARCH.md (Validation Architecture MOD-03/06 free switch)
  </read_first>
  <behavior>
    - dual_login_free_switch_xai_to_codex_no_missing_provider: both slots usable → apply/switch succeeds (not MISSING_PROVIDER)
    - dual_login_free_switch_codex_to_xai_no_missing_provider: reverse direction
    - same_provider_codex_switch_with_usable_creds: Sol→Terra (or two Codex catalog ids) succeeds without missing-provider
    - switch_changes_next_sample_route remains PASS (regression)
  </behavior>
  <action>
    Extend provider_routing integration proofs (per MOD-03, D-02).

    1. Using existing dual fake-token fixture style, add named tests that perform mid-session model switches across providers and assert success / route change, and assert error is not MODEL_SWITCH_MISSING_PROVIDER.
    2. Same-provider Codex switch with only codex slot filled (xai empty) still succeeds when staying on Codex models.
    3. Reuse public test helpers from the harness; explicit auth file paths; no live OAuth.
    4. Prefer one filter-friendly name prefix free_switch_ or dual_login_ for verify.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing dual_login -- --nocapture && cargo test -p xai-grok-shell --test provider_routing free_switch -- --nocapture && cargo test -p xai-grok-shell --test provider_routing switch_changes_next_sample_route -- --nocapture && cargo test -p xai-grok-shell --test provider_routing same_provider -- --nocapture</automated>
  </verify>
  <done>
    Dual-usable free cross-provider and same-provider switches proven; Phase 4 route switch still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BYOK skip + history / next-turn continuity contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/config.rs (has_own_credentials)
    - crates/codegen/xai-grok-shell/src/session/acp_session_impl/model_switch.rs (history not rewritten)
    - crates/codegen/xai-grok-shell/src/agent/handlers/model_switch.rs (no cancel on switch)
    - .planning/phases/06-mid-session-switch-missing-provider-gate/06-CONTEXT.md (D-06 mid-turn)
  </read_first>
  <behavior>
    - byok_own_credentials_skips_oauth_missing_provider: model with own api_key/env_key switches even when that provider OAuth slot empty
    - successful_switch_does_not_clear_conversation_history: assert session history/message list length preserved across apply (shell session handle or documented session command contract)
    - mid_turn_switch_is_next_sample_only: successful cross-provider switch updates prepared sampling for next sample without aborting session actor (D-06; shell-side proof in provider_routing — pager cancel UI is out of this plan’s file ownership)
  </behavior>
  <action>
    Lock D-06 and BYOK (RESEARCH A3) **in shell tests only** (do not edit pager task_result.rs — Plan 02 owns that file for MissingProvider QuestionView tests).

    1. BYOK fixture: catalog entry with has_own_credentials true; empty OAuth for that provider; switch must not return MODEL_SWITCH_MISSING_PROVIDER (if not already covered in Plan 01, re-assert here at integration level).
    2. History: assert handle_set_session_model / apply path does not clear chat history (inspect existing session state fields available in test harness). If harness cannot spawn full session cheaply, add a focused integration assertion that documents the non-wipe contract via session command side effects — do not invent Phase 7 multi-agent tests.
    3. Mid-turn / next-turn-only: assert switch success rebuilds next-sample route while session remains active (Phase 4 semantics). Do not add cancel-turn behavior.
    4. Do not implement subagent credential inheritance (Phase 7). Do not modify model_switch.rs production code unless a one-line test-only export is required — prefer public apply path via existing harness.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell --test provider_routing byok -- --nocapture && cargo test -p xai-grok-shell --test provider_routing history -- --nocapture && cargo test -p xai-grok-shell --test provider_routing dual_login -- --nocapture</automated>
  </verify>
  <done>
    BYOK, history preserve, and next-turn continuity contracts covered for MOD-03/D-06 in shell integration tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → CI | Fake tokens only; never real ChatGPT secrets |
| Dual-slot fixtures | Prove isolation: xAI token not used for Codex route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Elevation of Privilege | Dual free switch tests | medium | mitigate | Assert route uses target provider credentials/base_url (Phase 4 patterns) not parent/other slot |
| T-06-16 | Information Disclosure | Test logs | low | accept | Fake tokens; no production credentials |
| T-06-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
provider_routing dual_login/free_switch/same_provider/byok/history filters + switch_changes_next_sample_route green; pager switch_model non-cancel assertion green.
</verification>

<success_criteria>
- MOD-03 dual free switch proven
- D-06 history + next-turn continuity locked
- BYOK skip proven
</success_criteria>

<output>
Create `.planning/phases/06-mid-session-switch-missing-provider-gate/06-05-SUMMARY.md` when done
</output>
