---
phase: 06-mid-session-switch-missing-provider-gate
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - crates/codegen/xai-grok-pager/src/app/app_view.rs
  - crates/codegen/xai-grok-pager/src/slash/commands/model.rs
  - crates/codegen/xai-grok-pager/src/slash/command.rs
autonomous: true
requirements:
  - MOD-06
must_haves:
  truths:
    - "Full mixed catalog remains listed when a provider is logged out — no auth-based filtering of GPT or Grok rows (D-04)"
    - "Rows whose provider lacks usable credentials show exact badge text needs login (dim/secondary; not bold accent)"
    - "Expired-but-refreshable provider shows no needs-login badge (usable semantics match D-02 / shell)"
    - "Badged rows remain fully selectable (selecting runs shell gate)"
    - "build_model_items / suggest_args does not open auth.json on every keystroke — uses AppView dual-slot usable cache"
  artifacts:
    - path: crates/codegen/xai-grok-pager/src/app/app_view.rs
      provides: "Dual-slot provider auth usable snapshot cache"
      contains: usable
    - path: crates/codegen/xai-grok-pager/src/slash/commands/model.rs
      provides: "build_model_items needs login badge"
      contains: needs login
  key_links:
    - from: AppView usable cache
      to: build_model_items
      via: "provider from model meta + cache flags → display badge"
      pattern: "needs login|provider"
    - from: apply_auth_meta / pure setter
      to: AppView usable cache
      via: "refresh snapshot without FS in suggest_args"
      pattern: "provider_auth|usable"
---

## Phase Goal

**As a** bum user mid-session, **I want to** switch models freely and get a clear fail-closed login path when a provider is missing, **so that** I never depend on silent mid-turn 401 as the primary UX and never restart the CLI to change models.

<objective>
Add light picker auth cues: dual-slot usable cache on AppView and `needs login` badge on model rows without filtering the mixed catalog (per D-04, D-08, RESEARCH A4).

Purpose: Users see which models will gate before selecting; catalog stays complete (Phase 3).
Output: Badge on unusable-provider rows; cache refresh on auth lifecycle; unit tests for display strings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-UI-SPEC.md
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-PATTERNS.md
@.planning/phases/06-mid-session-switch-missing-provider-gate/06-RESEARCH.md
@crates/codegen/xai-grok-pager/src/slash/commands/model.rs
@crates/codegen/xai-grok-pager/src/app/app_view.rs
@crates/codegen/xai-grok-pager/src/slash/command.rs
@crates/codegen/xai-grok-shell/src/auth/status.rs
</context>

## Locked decisions (this plan)

| ID | Applies |
|----|---------|
| D-02 | Badge usable semantics = refreshable OK (same as shell) |
| D-04 | Full mixed catalog + light needs login badge |
| D-08 | Exact badge text needs login; secondary/dim color; Phase 3 (current) suffix preserved |

## Discretion lock (badge surface)

ArgItem has no right_label today (PATTERNS). Use display suffix ` · needs login` (UI-SPEC allowed MVP). Prefer not expanding ArgItem unless right_label is a small additive field already used elsewhere with low blast radius. Do not replace description with the badge.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppView dual-slot provider usable cache + refresh hooks</name>
  <files>crates/codegen/xai-grok-pager/src/app/app_view.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/app/app_view.rs (apply_auth_meta, auth lifecycle fields)
    - crates/codegen/xai-grok-shell/src/auth/status.rs (store_usable / AuthStatusReport if public)
    - .planning/phases/06-mid-session-switch-missing-provider-gate/06-RESEARCH.md (Pitfall 6)
  </read_first>
  <behavior>
    - provider_auth_usable defaults: safe false/unknown until refreshed (fail-closed badge may over-show until refresh — acceptable; prefer accurate refresh on startup/auth)
    - pure setter/refresh helper updates xai and codex flags independently
    - cache read is pure for consumers (no FS in getter)
  </behavior>
  <action>
    Add lightweight dual-slot usable snapshot on AppView (per D-02, D-04, TEA purity). Do **not** edit dispatch/auth.rs in this plan (Plan 03 owns that file for deferred retry) — expose a public/crate refresh helper on AppView that auth lifecycle can call later.

    1. Fields such as provider_auth_usable_xai / provider_auth_usable_codex (bool) or a small map keyed by wire id. Document that these are display-only hints; shell gate remains authoritative.
    2. Implement AppView::set_provider_auth_usable / refresh_provider_auth_usable_from_report (or equivalent) that apply_auth_meta can call when AuthMeta/status is present. Prefer booleans only — never store tokens. Startup one-shot via existing auth meta path is OK; never open auth.json inside slash suggest_args.
    3. Unit-test field updates by calling the pure setter with injected bools.
    4. Optional follow-up wiring of AuthComplete → refresh may land in Plan 03 if needed; this plan must ship the cache API + apply_auth_meta hook sufficient for badge tests.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-pager provider_auth_usable -- --nocapture && cargo check -p xai-grok-pager</automated>
  </verify>
  <done>
    AppView holds dual-slot usable flags refreshed on auth lifecycle without per-keystroke FS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_model_items needs login badge without filtering catalog</name>
  <files>crates/codegen/xai-grok-pager/src/slash/commands/model.rs, crates/codegen/xai-grok-pager/src/slash/command.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/slash/commands/model.rs (build_model_items, suggest_args, tests)
    - crates/codegen/xai-grok-pager/src/slash/command.rs (ArgItem, AppCtx / CommandExecCtx fields)
    - .planning/phases/06-mid-session-switch-missing-provider-gate/06-UI-SPEC.md (Auth badge on picker rows)
    - Phase 3 model meta provider stamp (info.meta provider string from ACP)
  </read_first>
  <behavior>
    - build_model_items_badges_unusable_provider: unusable codex → GPT rows display contains exact substring needs login; xAI usable rows without badge
    - build_model_items_never_filters_by_auth: count of items equals available models when one provider unusable
    - current_and_needs_login_suffix: {name} (current) · needs login when both apply
    - usable_provider_no_badge: refreshable/usable → no needs login text
  </behavior>
  <action>
    Badge display (per D-04, D-08, RESEARCH A4).

    1. Thread usable cache into suggest context: extend AppCtx / CommandExecCtx with provider usable flags (or read from a passed snapshot). Update call sites that construct ctx for model command tests.
    2. build_model_items: for each model, resolve provider from ACP model meta provider field (xai|codex); if that slot unusable, append ` · needs login` (exact lowercase, no punctuation). Preserve (current) suffix rules from Phase 3. Do not filter rows. Do not put badge into description.
    3. Unit tests construct ModelState with mixed catalog + meta.provider and inject usable flags.
    4. Settings dynamic enum badge optional if same list builder is shared; only required for /model slash surface this phase unless settings reuses build_model_items.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-pager needs_login -- --nocapture && cargo test -p xai-grok-pager build_model_items -- --nocapture</automated>
  </verify>
  <done>
    Mixed list intact; needs login badge on unusable-provider rows only; tests lock exact copy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth disk → AppView cache | Display hints only; must not log token material when refreshing |
| suggest_args → user | Badge strings are static; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Information Disclosure | cache refresh path | medium | mitigate | Refresh usable booleans only; never store/display access or refresh tokens in AppView |
| T-06-13 | Bypass | Trusting badge instead of shell gate | low | accept | Badge is advisory; shell gate (Plan 01) remains authority — documented on field |
| T-06-14 | Tampering | Client meta.provider for badge | low | accept | Badge mislabel possible if meta wrong; switch still gated by catalog provider in shell |
| T-06-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
needs_login + build_model_items + provider_auth_usable tests green; catalog count unchanged under partial auth.
</verification>

<success_criteria>
- D-04 badge + full list
- TEA purity preserved for suggest_args
</success_criteria>

<output>
Create `.planning/phases/06-mid-session-switch-missing-provider-gate/06-04-SUMMARY.md` when done
</output>
