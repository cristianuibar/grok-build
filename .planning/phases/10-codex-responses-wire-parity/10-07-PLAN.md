---
phase: 10-codex-responses-wire-parity
plan: 07
type: execute
wave: 3
depends_on:
  - 10-06
files_modified:
  - crates/codegen/xai-grok-shell/tests/common/mod.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
  - crates/codegen/xai-grok-shell/src/session/helpers/session_compact.rs
  - crates/codegen/xai-grok-shell/src/session/acp_session_tests/auth_error_no_retry_tests.rs
  - crates/codegen/xai-grok-shell/src/session/acp_session_tests/cancel_running_task_tests.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
autonomous: true
requirements:
  - OPS-04
must_haves:
  truths:
    - "Every listed shell test/fixture SamplerConfig literal compiles with an explicit disabled typed profile or a default-preserving struct update."
    - "Existing fixture intent for provider routing, compaction, authentication errors, cancellation, and subagent tests is retained."
    - "No fixture silently opts into the trusted profile or changes request/body expectations before the trusted OAuth gate is tested."
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/provider_routing.rs
      provides: "Provider-routing fixture compatibility with disabled profile"
      contains: "responses_wire_profile"
    - path: crates/codegen/xai-grok-shell/tests/common/mod.rs
      provides: "Shared shell test fixture compatibility with disabled profile"
      contains: "responses_wire_profile"
  key_links:
    - from: "Shell test and fixture constructors"
      to: "SamplerConfig typed profile"
      via: "explicit disabled field or default-preserving struct update"
      pattern: "responses_wire_profile"
---

## Plan user story

**As a** bum daily-driver user, **I want to** complete GPT-5.6 turns and Grok↔GPT-5.6 switches reliably, **so that** I can keep coding in one CLI without a restart.

<objective>
Update all known shell test and fixture `SamplerConfig` literals for the typed profile without changing the behavior each test was built to prove.

Purpose: make the profile-field rollout compile-safe and keep provider-routing, session, auth, cancellation, and subagent regressions trustworthy.
Output: bounded fixture-only compatibility updates plus focused compile/test checks.
</objective>

<context>
@.planning/phases/10-codex-responses-wire-parity/10-CONTEXT.md
@.planning/phases/10-codex-responses-wire-parity/10-RESEARCH.md
@.planning/phases/10-codex-responses-wire-parity/10-PATTERNS.md
@.planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
@.planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
</context>

## Locked decisions honored

- The typed `SamplerConfig` profile remains disabled in fixtures unless a later explicit trusted-profile test elects it; no header sentinel replaces it.
- Each fixture keeps the exact model, provider, endpoint, credential, backend, retry, and expected-response semantics it existed to test.
- Bum session identity remains primary; optional Codex headers are request metadata reserved for Plan 04.
- Phase 11 reasoning policy and Phase 12 WebSocket/tool/attribution work remain deferred.

<tasks>

<task type="auto">
  <name>Task 1: Update shared and provider-routing test fixtures with disabled profile compatibility</name>
  <files>crates/codegen/xai-grok-shell/tests/common/mod.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/common/mod.rs
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
    - .planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
  </read_first>
  <action>
    Update every explicit `SamplerConfig` literal in the shared test helpers and provider-routing integration tests with a disabled profile field or a safe `..SamplerConfig::default()` struct update. Preserve each fixture's model/provider/base URL/credential/backend/header and expected route semantics. Do not make a provider-routing fixture imply trusted Codex behavior merely to satisfy the new field.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-shell --lib && rg -n "responses_wire_profile" crates/codegen/xai-grok-shell/tests/common/mod.rs crates/codegen/xai-grok-shell/tests/provider_routing.rs</automated>
  </verify>
  <acceptance_criteria>
    Shared and provider-routing fixtures compile, retain their existing routing assertions, and have no implicit trusted-profile opt-in.
  </acceptance_criteria>
  <done>Shared shell fixtures and provider-routing tests are compatible without semantic drift.</done>
</task>

<task type="auto">
  <name>Task 2: Update session-compaction, auth-error, and cancellation test literals without weakening their assertions</name>
  <files>crates/codegen/xai-grok-shell/src/session/helpers/session_compact.rs, crates/codegen/xai-grok-shell/src/session/acp_session_tests/auth_error_no_retry_tests.rs, crates/codegen/xai-grok-shell/src/session/acp_session_tests/cancel_running_task_tests.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/session/helpers/session_compact.rs
    - crates/codegen/xai-grok-shell/src/session/acp_session_tests/auth_error_no_retry_tests.rs
    - crates/codegen/xai-grok-shell/src/session/acp_session_tests/cancel_running_task_tests.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
  </read_first>
  <action>
    Add the disabled profile field to each explicit session helper, auth-error, and cancellation `SamplerConfig` literal, preserving compaction values, error classification, no-retry behavior, cancellation timing, credentials, and backend choices. Use a default struct update only when every intentional non-default value remains explicit. Do not modify the test assertions beyond what is necessary to construct the updated type.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-shell --lib && rg -n "responses_wire_profile" crates/codegen/xai-grok-shell/src/session/helpers/session_compact.rs crates/codegen/xai-grok-shell/src/session/acp_session_tests/auth_error_no_retry_tests.rs crates/codegen/xai-grok-shell/src/session/acp_session_tests/cancel_running_task_tests.rs</automated>
  </verify>
  <acceptance_criteria>
    The listed session/auth/cancellation tests compile with the profile disabled and retain the semantic purpose of their existing error, retry, and cancellation checks.
  </acceptance_criteria>
  <done>Session-oriented fixtures are field-complete without hiding test failures behind a changed profile value.</done>
</task>

<task type="auto">
  <name>Task 3: Update subagent test support and verify the shell fixture matrix compiles</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/subagent/tests/mod.rs
    - crates/codegen/xai-grok-shell/tests/common/mod.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
    - .planning/phases/10-codex-responses-wire-parity/10-06-PLAN.md
  </read_first>
  <action>
    Update every subagent-test `SamplerConfig` literal with the disabled typed profile while retaining parent/child model choice, provider routing, auth fixtures, and response expectations. Run the focused shell test compilation matrix after Tasks 1-2 so any overlooked literal is found before Plan 04 alters trusted header behavior.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib --no-run && cargo test -p xai-grok-shell --test provider_routing --no-run</automated>
  </verify>
  <acceptance_criteria>
    Subagent test support compiles with the profile field, no fixture opts in implicitly, and the named shell fixture matrix has no missing-field compile failures.
  </acceptance_criteria>
  <done>All enumerated shell test/fixture literal fallout is resolved before trusted-header behavior is introduced.</done>
</task>

</tasks>

<threat_model>
## L1 threat model

| Threat ID | Boundary | Risk | Mitigation and proof |
|---|---|---|---|
| T-10-01 | Test fixture -> sampler profile | A fixture defaults to trusted behavior and lets profile leakage evade route-isolation tests. | Explicit disabled/default-preserving values in every listed fixture, preservation of existing semantic fields, and focused compilation. |
</threat_model>

## Spec-less probe assumptions

No Phase 10 SPEC supplies Edge Coverage or Prohibitions. The OPS-04 testable truth is that every listed fixture constructs the new typed configuration without changing what it asserts; numerical precision is inapplicable.

## Artifacts this phase produces

- Disabled-profile compatibility updates in exactly the six listed shell test/fixture paths.
- Focused shell library and provider-routing compilation evidence.
- A trustworthy fixture baseline for Plan 04 trusted-profile/header tests.

<verification>
Run the task-level shell test compilation commands and `cargo fmt --all -- --check`. Do not treat compilation as live OPS-04 success.
</verification>

<success_criteria>
- Every declared shell test/fixture literal compiles with the typed profile.
- Existing fixture intent stays intact and trusted behavior remains opt-in.
- No same-wave file overlaps with Plans 02 or 06 are introduced.
</success_criteria>

<output>
Create `.planning/phases/10-codex-responses-wire-parity/10-07-SUMMARY.md` when execution completes.
</output>
