---
phase: 10-codex-responses-wire-parity
plan: 06
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/codegen/xai-grok-shell/src/tools/config.rs
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
  - crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
  - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
  - crates/codegen/xai-grok-shell/src/trace_classifier/mod.rs
  - crates/codegen/xai-grok-shell/src/test_support/lsp_runtime.rs
autonomous: true
requirements:
  - OPS-04
must_haves:
  truths:
    - "Every listed shell production/config-support SamplerConfig literal compiles with the typed profile and preserves its prior intentional semantic fields."
    - "All listed production/config-support paths leave the profile disabled until the later trusted first-party Codex OAuth activation plan elects it."
    - "No URL/header sentinel substitutes for the typed SamplerConfig field."
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
      provides: "Compatible reconstructed SamplerConfig literal before trusted-header behavior is added"
      contains: "responses_wire_profile"
    - path: crates/codegen/xai-grok-shell/src/agent/config.rs
      provides: "Compatible shell model/config SamplerConfig literal"
      contains: "responses_wire_profile"
  key_links:
    - from: "Shell production/config constructors"
      to: "SamplerConfig typed profile"
      via: "explicit disabled field or default-preserving struct update"
      pattern: "responses_wire_profile"
---

## Plan user story

**As a** bum daily-driver user, **I want to** complete GPT-5.6 turns and Grok↔GPT-5.6 switches reliably, **so that** I can keep coding in one CLI without a restart.

<objective>
Make every known shell production/config-support `SamplerConfig` literal compatible with the typed profile before trusted Codex header behavior is introduced.

Purpose: preserve current xAI, BYOK, custom-endpoint, tool, subagent, trace, credential, and LSP-runtime behavior while the new field is disabled.
Output: bounded shell production/config literal updates with focused compile evidence; no header or route-policy behavior changes.
</objective>

<context>
@.planning/phases/10-codex-responses-wire-parity/10-CONTEXT.md
@.planning/phases/10-codex-responses-wire-parity/10-RESEARCH.md
@.planning/phases/10-codex-responses-wire-parity/10-PATTERNS.md
@.planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
@.planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
</context>

## Locked decisions honored

- The typed `SamplerConfig` profile remains the only ownership mechanism; it is not replaced by a header sentinel or URL heuristic.
- Each literal preserves its current semantic fields. `..SamplerConfig::default()` is permitted only when it retains every intentionally set model, endpoint, auth, backend, header, timeout, retry, and reasoning value.
- Bum session identity remains primary; optional Codex headers are request metadata and are intentionally deferred to Plan 04 after this compatibility work.
- Phase 11 reasoning policy and Phase 12 WebSocket/tool/attribution work remain deferred.

<tasks>

<task type="auto">
  <name>Task 1: Update tools and agent configuration constructors without changing routing semantics</name>
  <files>crates/codegen/xai-grok-shell/src/tools/config.rs, crates/codegen/xai-grok-shell/src/agent/config.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/tools/config.rs
    - crates/codegen/xai-grok-shell/src/agent/config.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
    - .planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
  </read_first>
  <action>
    Find the explicit `SamplerConfig` constructors in the tools and agent configuration paths and add the typed profile with its disabled value. Where converting to a struct update is clearer, use `..SamplerConfig::default()` only after keeping every existing non-default field explicit. Preserve model selection, base URL, API backend, credential route, extra headers, context window, stream-tool setting, and reasoning effort exactly as before. Do not add trusted headers, provider detection, or request-body shaping in these paths.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-shell --lib</automated>
  </verify>
  <acceptance_criteria>
    Both configuration paths compile with the typed field and retain their prior route/auth/backend values. The profile is disabled and no new header or provider-guessing branch appears.
  </acceptance_criteria>
  <done>Tool and agent configuration constructors are field-complete and behavior-preserving.</done>
</task>

<task type="auto">
  <name>Task 2: Update session reconstruction and credential-provider literals as compatibility-only work</name>
  <files>crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs, crates/codegen/xai-grok-shell/src/auth/credential_provider.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
    - crates/codegen/xai-grok-shell/src/auth/credential_provider.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
    - .planning/phases/10-codex-responses-wire-parity/10-04-PLAN.md
  </read_first>
  <action>
    Add the disabled typed profile to every explicit `SamplerConfig` literal in session reconstruction and credential-provider paths. Preserve current credentials, bearer resolver, account-header trust gate, proxy-derived headers, compaction values, model/backend facts, and retry behavior. This task must not add originator, session/thread/client-request metadata, profile enablement, or encrypted-error behavior; Plan 04 owns those trusted-Codex semantics after the literal baseline is complete.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-shell --lib</automated>
  </verify>
  <acceptance_criteria>
    Session and credential constructors compile with the profile explicitly disabled and retain all pre-existing semantic fields. The existing trusted account-ID behavior is untouched, while no new Codex identity header is introduced.
  </acceptance_criteria>
  <done>Header-behavior work has a clean, compiling session/credential configuration baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Update subagent, trace, and LSP runtime support constructors with default-preserving values</name>
  <files>crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs, crates/codegen/xai-grok-shell/src/trace_classifier/mod.rs, crates/codegen/xai-grok-shell/src/test_support/lsp_runtime.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs
    - crates/codegen/xai-grok-shell/src/trace_classifier/mod.rs
    - crates/codegen/xai-grok-shell/src/test_support/lsp_runtime.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
  </read_first>
  <action>
    Update each named production/support `SamplerConfig` literal with the disabled profile or a safe default-preserving struct update. Keep subagent provider routing, trace classification inputs, and LSP runtime fixture behavior exactly intact. Do not use this compatibility task to opt any child/session/runtime into trusted Codex wire policy.
  </action>
  <verify>
    <automated>cargo check -p xai-grok-shell --lib && rg -n "responses_wire_profile" crates/codegen/xai-grok-shell/src/agent/subagent/mod.rs crates/codegen/xai-grok-shell/src/trace_classifier/mod.rs crates/codegen/xai-grok-shell/src/test_support/lsp_runtime.rs</automated>
  </verify>
  <acceptance_criteria>
    All three listed paths compile, preserve their intentional configuration values, and use a disabled profile unless Plan 04 later enables it at the trusted OAuth boundary.
  </acceptance_criteria>
  <done>All enumerated shell production/config-support literal fallout is resolved without a behavior change.</done>
</task>

</tasks>

<threat_model>
## L1 threat model

| Threat ID | Boundary | Risk | Mitigation and proof |
|---|---|---|---|
| T-10-01 | Shell constructor -> sampler profile | A missing or non-disabled literal makes the trusted profile leak to an unrelated production path. | Enumerate every listed production/config-support literal, preserve their intentional fields, and compile with disabled/default-preserving values. |
</threat_model>

## Spec-less probe assumptions

No Phase 10 SPEC supplies Edge Coverage or Prohibitions. The OPS-04 testable truth here is compile-safe disabled-profile propagation through every listed shell production/config support constructor; numerical precision is inapplicable.

## Artifacts this phase produces

- Disabled-profile compatibility updates in exactly the seven listed shell production/config-support paths.
- Focused shell compile evidence proving every affected constructor remains field-complete.
- A clean prerequisite for Plan 04 trusted-header/profile activation.

<verification>
Run the task-level shell checks, then `cargo fmt --all -- --check`. Do not claim live Codex wire success from this compatibility-only plan.
</verification>

<success_criteria>
- Every declared shell production/config-support literal compiles with the typed profile.
- Defaults preserve existing non-Codex behavior until the trusted OAuth gate explicitly enables the profile.
- No header sentinel, URL heuristic, or early trusted-header behavior replaces the typed field.
</success_criteria>

<output>
Create `.planning/phases/10-codex-responses-wire-parity/10-06-SUMMARY.md` when execution completes.
</output>
