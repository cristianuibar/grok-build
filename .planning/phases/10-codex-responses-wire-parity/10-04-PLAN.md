---
phase: 10-codex-responses-wire-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 10-01
  - 10-06
  - 10-07
files_modified:
  - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
  - crates/codegen/xai-grok-shell/src/session/acp_session_tests/codex_reconstruct_refresh_tests.rs
  - crates/codegen/xai-grok-sampling-types/src/error.rs
autonomous: true
requirements:
  - OPS-04
  - OPS-05
must_haves:
  truths:
    - "Only first-party ChatGPT/Codex SessionToken OAuth reconstruction enables the trusted profile and Codex request identity metadata."
    - "Trusted requests use originator bum and stable session-local UUID values for session-id, thread-id, and x-client-request-id."
    - "BYOK, custom endpoints, xAI, and non-Codex routes receive none of the trusted profile or Codex-only identity headers."
    - "ChatGPT-Account-ID remains trusted-host and availability-gated."
    - "Both encrypted_content and encrypted content variants are recognized case-insensitively only for HTTP 400 and share one bounded friendly-recovery predicate."
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
      provides: "Trusted Codex profile and session-local request identity injection"
      contains: "originator"
    - path: crates/codegen/xai-grok-sampling-types/src/error.rs
      provides: "Shared encrypted-content HTTP-400 predicate"
      contains: "is_encrypted_content_error"
  key_links:
    - from: "Codex SessionToken OAuth trust gate"
      to: "SamplerConfig profile and extra_headers"
      via: "reconstruct_full_config and is_first_party_codex_url"
      pattern: "codex_session_oauth"
---

## Plan user story

**As a** bum daily-driver user, **I want to** complete GPT-5.6 turns and Grok↔GPT-5.6 switches reliably, **so that** I can keep coding in one CLI without a restart.

<objective>
Activate the Phase 10 trusted wire profile and request identity only at the existing first-party ChatGPT/Codex OAuth boundary, and centralize encrypted-content mismatch classification.

Purpose: complete trusted Codex interoperability without leaking identity metadata or recovery behavior to xAI, BYOK, or custom endpoints.
Output: shell-owned trusted profile/header construction, isolation tests, and a single 400-gated encrypted-content predicate used by friendly recovery.
</objective>

<context>
@.planning/phases/10-codex-responses-wire-parity/10-CONTEXT.md
@.planning/phases/10-codex-responses-wire-parity/10-RESEARCH.md
@.planning/phases/10-codex-responses-wire-parity/10-PATTERNS.md
@.planning/phases/10-codex-responses-wire-parity/10-VALIDATION.md
@.planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-06-PLAN.md
@.planning/phases/10-codex-responses-wire-parity/10-07-PLAN.md
</context>

## Locked decisions honored

- Reuse `is_first_party_codex_url` plus Codex `SessionToken` OAuth as the exclusive trust gate; do not add URL guessing to sampler code.
- Use bum's existing session identity as the stable local source for optional UUID metadata. These headers are request metadata, not a replacement identity or third-party state store.
- Send `originator: bum`, never the upstream CLI value, and do not add telemetry, attestation, installation, or beta headers.
- Keep `ChatGPT-Account-ID` under the current trusted-and-known behavior. Phase 11/12 deferrals remain unchanged.

<tasks>

<task type="auto">
  <name>Task 1: Enable the profile and trusted request metadata only during first-party Codex OAuth reconstruction</name>
  <files>crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs, crates/codegen/xai-grok-shell/src/session/acp_session_tests/codex_reconstruct_refresh_tests.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
    - crates/codegen/xai-grok-shell/src/session/acp_session_tests/codex_reconstruct_refresh_tests.rs
    - crates/codegen/xai-grok-shell/src/agent/config.rs
    - crates/codegen/xai-grok-sampler/src/config.rs
    - .planning/phases/10-codex-responses-wire-parity/10-01-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-06-PLAN.md
    - .planning/phases/10-codex-responses-wire-parity/10-07-PLAN.md
  </read_first>
  <action>
    In `SessionActor::reconstruct_full_config`, reuse the existing `provider == Codex`, `AuthType::SessionToken`, and `is_first_party_codex_url` gate to enable the new trusted profile. Under exactly that gate, preserve current `ChatGPT-Account-ID` behavior and add `originator: bum`, `session-id`, `thread-id`, and `x-client-request-id`. Derive the three ID values from the stable `self.session_info.id` UUID for the actor lifetime, not from a new value per request. Extend the reconstruction fixture matrix: trusted Codex OAuth receives the profile and identity headers, its values are parseable/equal across repeat reconstruction, and it does not identify as the upstream CLI; BYOK, custom endpoint, and xAI/non-Codex controls receive none of the trusted headers/profile.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-shell --lib codex_reconstruct</automated>
  </verify>
  <acceptance_criteria>
    Trusted OAuth config has `originator` equal to bum, three equal parseable UUID header values, the trusted profile, and existing account-ID behavior. Tests prove that no Codex-only metadata/profile appears in BYOK, custom-host, or xAI/non-Codex configurations.
  </acceptance_criteria>
  <done>The shell, not the sampler, decides trusted Codex wire activation and request metadata using the existing fail-closed trust boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize encrypted-content mismatch recognition and route shell recovery through it</name>
  <files>crates/codegen/xai-grok-sampling-types/src/error.rs, crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs</files>
  <read_first>
    - crates/codegen/xai-grok-sampling-types/src/error.rs
    - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
    - crates/codegen/xai-grok-sampler/src/retry.rs
    - .planning/phases/10-codex-responses-wire-parity/10-RESEARCH.md
  </read_first>
  <action>
    Expand the shared `SamplingError::is_encrypted_content_error` logic so it is case-insensitive, accepts both underscore and spaced encrypted-content wording, and remains true only for HTTP 400 API errors. If the shell's error-info representation cannot call that method directly, expose one shared message/status predicate from sampling-types and make the method delegate to it; make `sampler_turn.rs` call that same predicate rather than maintaining a second substring test. Keep the shell's user-facing path bounded and terminal for the recognized 400; do not retry arbitrary 400s or alter generic retry policy. Add focused tests for both spellings, casing, wrong status, and unrelated messages.
  </action>
  <verify>
    <automated>cargo test -p xai-grok-sampling-types encrypted_content_error --lib && cargo test -p xai-grok-shell --lib encrypted_content</automated>
  </verify>
  <acceptance_criteria>
    Both accepted spellings and casing variations return true only at HTTP 400. Unrelated 400 messages and non-400 failures return false. The shell recovery branch uses the shared predicate and does not keep a second independent encrypted-content matcher.
  </acceptance_criteria>
  <done>Encrypted-content mismatch handling is consistent, bounded, and isolated to the specific documented 400 class.</done>
</task>

</tasks>

<threat_model>
## L1 threat model

| Threat ID | Boundary | Risk | Mitigation and proof |
|---|---|---|---|
| T-10-08 | Credential/endpoint route -> outbound headers | Codex OAuth account or identity metadata leaks to BYOK, custom hosts, or xAI. | Reuse existing first-party SessionToken gate; route-matrix tests assert absence outside it. |
| T-10-09 | Session state -> request metadata | Per-request IDs break continuity or external identity values are persisted/copied. | Reuse one bum session UUID, assert parseability/equality, and send only metadata headers. |
| T-10-10 | API error -> friendly recovery | Broad text matching recovers unrelated failures or repeats a 400 indefinitely. | Centralize a status-gated predicate and keep recognized handling terminal/bounded. |
</threat_model>

## Spec-less probe assumptions

No SPEC Edge Coverage exists. OPS-04 and OPS-05 are testable through trusted-route isolation, exact header/profile assertions, and 400 predicate cases; numerical precision is inapplicable because UUID validation is parseability/equality, not a numeric tolerance.

## Artifacts this phase produces

- Trusted-Codex profile activation in `SessionActor::reconstruct_full_config`.
- Bum request identity metadata injection and route-isolation regressions.
- A single shared encrypted-content 400 predicate consumed by both sampling types and shell recovery.

<verification>
Run Codex reconstruction tests, encrypted-content predicate tests, relevant shell recovery tests, and `cargo fmt --all -- --check`. Repeat reconstruction within one fixture to prove stable metadata values.
</verification>

<success_criteria>
- Codex identity headers/profile are present only on trusted first-party SessionToken OAuth routes.
- ChatGPT account behavior remains trusted-host gated.
- Encrypted-content spelling variations receive one bounded, shared classification path.
</success_criteria>

<output>
Create `.planning/phases/10-codex-responses-wire-parity/10-04-SUMMARY.md` when execution completes.
</output>
