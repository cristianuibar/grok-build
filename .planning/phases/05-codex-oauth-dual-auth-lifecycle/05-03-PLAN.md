---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/codex/mod.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/browser.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/device.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/claims.rs
  - crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs
  - crates/codegen/xai-grok-shell/src/auth/mod.rs
  - crates/codegen/xai-grok-shell/src/auth/flow.rs
  - crates/codegen/xai-grok-shell/src/auth/model.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
autonomous: true
requirements:
  - AUTH-02
user_setup:
  - service: ChatGPT account
    why: "Live browser smoke optional only; CI uses mock IdP / injected tokens"
    env_vars: []
    dashboard_config:
      - task: "Optional: enable device code in ChatGPT workspace security if using --device-auth live"
        location: "ChatGPT workspace security settings"
must_haves:
  truths:
    - "User can complete Codex ChatGPT OAuth via injected mock token-exchange path that persists only providers.codex under temp BUM_HOME"
    - "Authorize URL builder includes public client_id, S256 challenge, redirect http://localhost:1455/auth/callback (or bound port), offline_access scope, originator bum"
    - "Browser loopback binds 127.0.0.1:1455 with 1457 fallback; redirect URI host string is localhost not 127.0.0.1"
    - "Device flow uses Codex deviceauth usercode/token endpoints (not xAI RFC8628 device grant)"
    - "chatgpt_account_id from id_token maps to GrokAuth.organization_id; email claims map when present"
    - "bum login --provider codex routes to Codex flow; bare bum login remains xAI-only"
    - "Re-login codex overwrites only codex slot; seeded xAI remains"
    - "Success copy uses Signed in to Codex (UI-SPEC); never prints tokens"
    - "codex_login_persists_slot GREEN in auth_codex_lifecycle"
    - "codex_authorize_url_includes_pkce_and_localhost_callback GREEN"
    - "codex_device_endpoints_use_deviceauth GREEN"
    - "No Platform API-key primary path; no ~/.codex import"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/codex/mod.rs
      provides: "Codex OAuth constants + login entry"
      contains: CODEX_CLIENT_ID
    - path: crates/codegen/xai-grok-shell/src/auth/codex/browser.rs
      provides: "axum loopback PKCE login"
      contains: auth/callback
    - path: crates/codegen/xai-grok-shell/src/auth/codex/device.rs
      provides: "deviceauth usercode/token poll"
      contains: deviceauth
    - path: crates/codegen/xai-grok-shell/src/auth/codex/claims.rs
      provides: "id_token claims → email/account id"
      contains: chatgpt_account
    - path: crates/codegen/xai-grok-shell/src/auth/flow.rs
      provides: "run_cli_login provider dispatch"
      contains: provider
  key_links:
    - from: run_cli_login(--provider codex)
      to: codex browser/device login
      via: "provider match → persist via mutate_provider_store_or_prune(PROVIDER_CODEX)"
      pattern: "PROVIDER_CODEX|run_cli_login|codex"
    - from: token exchange success
      to: providers.codex GrokAuth
      via: "AuthMode::Oidc + refresh_token + organization_id + expires_at"
      pattern: "organization_id|refresh_token|AuthMode::Oidc"
    - from: authorize URL
      to: auth.openai.com oauth/authorize
      via: "PKCE S256 + state + localhost redirect"
      pattern: "code_challenge|localhost|1455|auth/callback"
---

## Phase Goal

**As a** bum user, **I want to** log in to ChatGPT/Codex via OAuth into the bum store, **so that** GPT models can use my ChatGPT subscription credentials without touching xAI credentials.

<objective>
Implement in-tree Codex ChatGPT OAuth (browser PKCE + device-code) and CLI `bum login --provider codex` that persists only `providers.codex` (AUTH-02; D-01, D-02, D-04, D-09, D-10, D-11, D-12). Vertical slice: CLI → Codex flow → multi-slot store.

Purpose: Fill the reserved Codex slot with real OAuth lifecycle; Phase 4 routing already selects the slot.
Output: Mockable Codex login path + CLI provider dispatch; codex_login_persists_slot GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-02-SUMMARY.md
@crates/codegen/xai-grok-shell/src/auth/oidc/login.rs
@crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs
@crates/codegen/xai-grok-shell/src/auth/device_code.rs
@crates/codegen/xai-grok-shell/src/auth/flow.rs
@crates/codegen/xai-grok-shell/src/auth/storage.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | `--provider codex\|xai`; bare login xAI-only |
| D-02 | Browser PKCE default; device flags for headless |
| D-04 | Re-login overwrites only targeted slot |
| D-09 | No stock import; `$BUM_HOME/auth.json` only |
| D-10 | In-tree mirror: ports 1455/1457, localhost redirect, public client_id |
| D-11 | Persist tokens + chatgpt_account_id → organization_id |
| D-12 | ChatGPT OAuth primary — not Platform API key |
| D-13 | Mock IdP tests; no live ChatGPT required |

**Discretion locks:**
- Module placement: `auth/codex/{mod,browser,device,claims}.rs`
- Scope key constant: `CODEX_AUTH_SCOPE = "https://auth.openai.com::app_EMoamEEZ73f0CkXaXp7hrann"` (issuer::client_id parity with xAI)
- Share `generate_pkce` via `pub(crate)` export from `oidc/protocol.rs` (or identical helper in codex)
- `originator=bum` on authorize URL
- Skip `obtain_api_key` Platform exchange (COVERAGE OPT-OUT)
- Inject-able HTTP / token-exchange hooks for tests (wiremock or trait/callback) so CI never hits auth.openai.com

## Codex OAuth constants (must match RESEARCH)

| Constant | Value |
|----------|-------|
| Issuer | `https://auth.openai.com` |
| client_id | `app_EMoamEEZ73f0CkXaXp7hrann` |
| Ports | 1455 preferred, 1457 fallback |
| Redirect | `http://localhost:{port}/auth/callback` (host **localhost**) |
| Bind | `127.0.0.1:{port}` |
| Token | `{issuer}/oauth/token` |
| Device usercode | `{issuer}/api/accounts/deviceauth/usercode` |
| Device token | `{issuer}/api/accounts/deviceauth/token` |
| Device verify | `{issuer}/codex/device` |
| Scopes | `openid profile email offline_access api.connectors.read api.connectors.invoke` |

## UI-SPEC copy (login)

- Start: `Signing in with Codex...` / `Open this URL to sign in:` + indented URL
- Success: `✓ Signed in to Codex as {email}` or without email
- Port bind fail: UI-SPEC bind error suggesting `--device-auth`
- Never print tokens in errors

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codex module constants, PKCE URL, claims, mock exchange persist</name>
  <files>crates/codegen/xai-grok-shell/src/auth/codex/mod.rs, crates/codegen/xai-grok-shell/src/auth/codex/browser.rs, crates/codegen/xai-grok-shell/src/auth/codex/claims.rs, crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs, crates/codegen/xai-grok-shell/src/auth/mod.rs, crates/codegen/xai-grok-shell/src/auth/model.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md (constants, authorize/token shapes, persist mapping)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md (codex/browser, claims, PKCE)
    - crates/codegen/xai-grok-shell/src/auth/oidc/login.rs (axum loopback pattern)
    - crates/codegen/xai-grok-shell/src/auth/oidc/protocol.rs (generate_pkce, exchange_code, state)
    - crates/codegen/xai-grok-shell/src/auth/jwt.rs
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (mutate_provider_store_or_prune)
  </read_first>
  <behavior>
    - codex_authorize_url_includes_pkce_and_localhost_callback: URL contains client_id, S256, localhost, auth/callback, offline_access, originator bum
    - codex_login_persists_slot: inject tokens → providers.codex Oidc entry selectable; seeded xAI unchanged (AUTH-02)
    - claims map chatgpt_account_id into organization_id and email when present
  </behavior>
  <action>
    Build Codex OAuth core (D-10, D-11, D-12, D-13; COVERAGE authorize/token/claims INTEGRATE).

    1. Create `auth/codex/mod.rs` with issuer, client_id, ports, scopes, CODEX_AUTH_SCOPE constants; re-export login entrypoints.
    2. Export or share PKCE generation from oidc protocol (`pub(crate)` visibility) — do not hand-roll crypto.
    3. Implement authorize URL builder matching openai/codex shape (RESEARCH). Validate state on callback.
    4. Implement `claims.rs` using existing insecure JWT decode pattern for exp/email/chatgpt_account_id (field names as upstream id_token provides; map account id → organization_id).
    5. Implement browser PKCE loopback in `browser.rs` with axum (not tiny_http): bind 127.0.0.1:1455 then 1457; redirect URI string uses localhost host and `/auth/callback` path; form POST token exchange; persist via `mutate_provider_store_or_prune(..., PROVIDER_CODEX, ...)` writing GrokAuth AuthMode::Oidc with key=access, refresh_token, expires_at, oidc_issuer, oidc_client_id, organization_id, email.
    6. Provide a test-facing inject path (e.g. `persist_codex_tokens_for_test` or mock exchange function) so lifecycle tests GREEN without opening real ports when possible; also unit-test authorize URL builder without network.
    7. Do not implement Platform obtain_api_key. Do not read ~/.codex. Do not clear xAI on codex login.
  </action>
  <acceptance_criteria>
    - auth/codex modules compile and are wired in auth/mod.rs
    - Authorize URL unit test GREEN
    - codex_login_persists_slot GREEN via mock/inject path
    - Seeded xAI sibling preserved on codex persist
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_authorize_url_includes_pkce_and_localhost_callback -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_login_persists_slot -- --nocapture</automated>
  </verify>
  <done>
    Codex OAuth core persists only providers.codex with claims mapping.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex device-code flow + CLI login --provider dispatch</name>
  <files>crates/codegen/xai-grok-shell/src/auth/codex/device.rs, crates/codegen/xai-grok-shell/src/auth/flow.rs, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/device_code.rs (xAI device UX — copy shape only)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md (device-code + login success copy)
    - crates/codegen/xai-grok-shell/src/auth/flow.rs (run_cli_login, report_signed_in)
    - crates/codegen/xai-grok-pager/src/app/cli.rs
    - crates/codegen/xai-grok-pager-bin/src/main.rs (Command::Login arm)
  </read_first>
  <behavior>
    - codex_device_endpoints_use_deviceauth GREEN: URL builders / constants for usercode + token use Codex deviceauth paths (issuer api/accounts/deviceauth/*), never xAI DEVICE_GRANT_TYPE
    - run_cli_login with provider codex + device_auth selects Codex device path
    - bare login still xAI; --provider xai explicit same as bare
    - report_signed_in includes provider label per UI-SPEC (Signed in to {ProviderLabel})
    - Unknown provider errors with UI-SPEC unknown provider copy
  </behavior>
  <action>
    Complete AUTH-02 vertical slice (D-01, D-02, D-04; UI-SPEC).

    1. Implement `codex/device.rs` against Codex deviceauth usercode + token poll APIs (RESEARCH/PATTERNS). CLI stderr copy matches UI-SPEC device-code block (indent 2 spaces, dim safety note, Waiting for authorization...).
    2. Extend `run_cli_login` signature with provider argument (Option or enum; None/xai → existing xAI path; codex → codex browser/device). Keep bare login xAI-only (D-01).
    3. Update `report_signed_in` (or provider-aware variant) to UI-SPEC: `✓ Signed in to {ProviderLabel} as {email}` / without email.
    4. Ensure clap `--provider` and pager-bin Login arm pass provider into shell (if Plan 01 already added fields, wire them; otherwise add now).
    5. Device flags: `--device-auth` / `--device-code` force device for the selected provider.
    6. GREEN `codex_device_endpoints_use_deviceauth` in auth_codex_lifecycle: assert device usercode/token endpoint construction contains deviceauth path segments and does not use xAI-only device grant strings. Prefer mock device poll → persist when cheap; URL construction contract is the AUTH-02 automated minimum for device.
    7. Invalidate nothing Phase-6-related. No dual logout here (Plan 04). Do not add more production files beyond Task 1 codex modules + flow/cli/pager-bin listed in files_modified.
  </action>
  <acceptance_criteria>
    - bum login --provider codex compiles end-to-end through pager-bin
    - codex_device_endpoints_use_deviceauth GREEN
    - Device module uses Codex deviceauth paths
    - Success copy includes Codex label when that path succeeds
    - Bare login still xAI
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager bum_login_provider_codex_parses -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_device_endpoints_use_deviceauth -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle codex_login_persists_slot -- --nocapture</automated>
  </verify>
  <done>
    CLI Codex login is a complete AUTH-02 slice (mock-proven persist + real flow code).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/loopback → token exchange | OAuth code + state on localhost callback |
| auth.openai.com → process | Untrusted IdP JSON; parse fail-closed |
| CLI provider arg → flow dispatch | Allow-list xai\|codex only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Spoofing | codex/browser callback | high | mitigate | Cryptographic OAuth state compare; reject missing/mismatched state |
| T-05-07 | Information Disclosure | login errors / report_signed_in | high | mitigate | Never print access/refresh tokens; safe_detail only; UI-SPEC |
| T-05-08 | Tampering | redirect URI | high | mitigate | Fixed allow-listed localhost redirect path/ports only |
| T-05-09 | Elevation of Privilege | cross-slot write | high | mitigate | mutate_provider_store_or_prune(PROVIDER_CODEX) only; sibling preserve tests |
| T-05-10 | Spoofing | public client_id reuse | medium | accept | Documented community/Codex CLI practice; product lock ChatGPT OAuth (STATE.md durability note) |
| T-05-SC | Tampering | package installs | low | accept | No new packages; axum/reqwest/sha2 already in tree |
</threat_model>

## Note on user_setup

Live ChatGPT is optional smoke only. Automated AUTH-02 is mock/fixture only (D-13).

<verification>
codex_login_persists_slot green; authorize URL green; clap provider parse green; no xAI wipe on codex login.
</verification>

<success_criteria>
- AUTH-02 satisfied for automated proofs: Codex OAuth persist under bum store only
- Bare login remains xAI; --provider codex selects Codex path
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-03-SUMMARY.md` when done
</output>
