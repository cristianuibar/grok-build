---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 04
type: execute
wave: 4
depends_on:
  - 05-02
  - 05-03
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/flow.rs
  - crates/codegen/xai-grok-shell/src/auth/status.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
  - crates/codegen/xai-grok-pager/src/slash/commands/logout.rs
  - crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs
  - crates/codegen/xai-grok-shell/src/extensions/auth.rs
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "CLI logout disk authority is blocking clear_provider_slot / clear_all_provider_slots — not AuthManager::remove_scope nonblocking lock"
    - "bum logout --provider codex clears only Codex on disk; xAI remains"
    - "bum logout --provider xai clears only xAI on disk (blocking); also invalidates in-memory xAI AuthManager when present"
    - "bum logout --all clears both under one lock-held clear_all_provider_slots"
    - "Bare bum logout prints UI-SPEC usage, exits non-zero, zero mutation"
    - "run_cli_auth_status returns string or writes injected Write — testable without global stdout races"
    - "bum auth status greppable paste-safe for both providers"
    - "TUI /logout fail-closed before ACP dual wipe OR provider-aware ACP; extensions/auth.rs updated; no silent dual clear"
    - "selective_logout_isolates, logout_all_clears_both, bare_logout_fail_closed, run_cli_auth_status GREEN"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/flow.rs
      provides: "run_cli_logout provider/all + run_cli_auth_status testable I/O"
      contains: run_cli_auth_status
    - path: crates/codegen/xai-grok-shell/src/extensions/auth.rs
      provides: "ACP logout dual-safe / fail-closed"
      contains: handle_logout
    - path: crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs
      provides: "TUI logout dispatch dual-safe"
      contains: Logout
  key_links:
    - from: run_cli_logout
      to: clear_provider_slot / clear_all_provider_slots
      via: "blocking disk authority; AuthManager::remove_scope only secondary xAI memory invalidate"
      pattern: "clear_provider_slot|clear_all_provider|remove_scope"
    - from: run_cli_auth_status
      to: status pure formatter
      via: "path-taking read; format to String or Write"
      pattern: "logged_in|usable|Auth status"
    - from: TUI /logout + extensions/auth.rs
      to: fail-closed or provider-aware clear
      via: "no silent dual wipe via perform_logout bare scope"
      pattern: "handle_logout|dispatch.*Logout|provider"
---

## Phase Goal

**As a** bum user, **I want to** log out of one provider and inspect auth status for both, **so that** I can manage dual sessions without accidentally wiping the other login.

<objective>
Ship dual-safe CLI logout (`--provider` / `--all` / bare fail-closed) and `bum auth status` (AUTH-03, AUTH-04; D-05, D-06), plus dual-safe TUI/ACP `/logout` (D-03). Addresses review: blocking clear as disk authority; atomic clear_all; extensions/auth.rs; injectable status I/O.

Purpose: Complete independent lifecycle management surfaces users touch daily.
Output: GREEN selective logout + status lifecycle tests; UI-SPEC copy compliance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md
@crates/codegen/xai-grok-shell/src/auth/flow.rs
@crates/codegen/xai-grok-shell/src/auth/status.rs
@crates/codegen/xai-grok-shell/src/auth/storage.rs
@crates/codegen/xai-grok-shell/src/auth/manager.rs
@crates/codegen/xai-grok-shell/src/extensions/auth.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
@crates/codegen/xai-grok-pager/src/slash/commands/logout.rs
@crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-03 | Minimal TUI; do not block on rich dual chrome |
| D-05 | Selective logout; `--all`; bare fail-closed |
| D-06 | `bum auth status` both providers; no secrets |
| D-09 | Never touch stock credential homes |

## UI-SPEC copy locks (must match)

**Bare logout body:**
```
Specify a provider or clear all:
  bum logout --provider xai
  bum logout --provider codex
  bum logout --all
```

**Success selective:** `Logged out of {ProviderLabel}` (+ email when known)  
**Success --all:** `Logged out of all providers`  
**Empty selective:** `No cached {ProviderLabel} session to log out of.`  
**Empty --all:** `No cached sessions to log out of.`  
**Status:** UI-SPEC key:value blocks; order xAI then Codex.

**TUI /logout:** Prefer description `Log out of a provider (CLI: bum logout --provider …)`; bare dual-wipe fail-closed.

**Local clear only:** no remote oauth/revoke (COVERAGE OPT-OUT).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual logout CLI — blocking disk authority, selective, atomic --all, bare fail-closed</name>
  <files>crates/codegen/xai-grok-shell/src/auth/flow.rs, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/flow.rs (perform_logout, run_cli_logout)
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (clear_provider_slot, clear_all_provider_slots)
    - crates/codegen/xai-grok-shell/src/auth/manager.rs (remove_scope nonblocking — NOT disk authority)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-04 logout)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
  </read_first>
  <behavior>
    - selective_logout_isolates GREEN (both directions)
    - logout_all_clears_both GREEN (atomic one-lock clear_all)
    - bare_logout_fail_closed GREEN
    - xAI path may still note XAI_API_KEY when env set; Codex path never invents Platform key warning
  </behavior>
  <action>
    Implement dual logout (D-05, AUTH-03). Addresses review: blocking clear_provider_slot is disk authority; atomic clear_all; do not use AuthManager::remove_scope as authoritative clear.

    1. Extend run_cli_logout with provider/all. Bare → UI-SPEC usage stderr + Err; **do not** call AuthManager::clear / remove_scope for bare.
    2. Selective: **blocking** `clear_provider_slot(auth_file, AuthProvider::…)`. For xAI, **after** successful disk clear, invalidate in-memory AuthManager / telemetry identity (secondary). For Codex, disk clear is sufficient until Plan 05 cache invalidate hooks exist (call invalidate if exported).
    3. `--all`: **one** `clear_all_provider_slots` under a single lock — not two sequential locked clears that race (Addresses review: non-atomic --all).
    4. Wire clap Logout + pager-bin.
    5. GREEN lifecycle tests with dual-slot fixtures on explicit paths.
    6. No Phase 6 gate. No silent dual-wipe on bare. No remote revoke.
  </action>
  <acceptance_criteria>
    - selective_logout_isolates, logout_all_clears_both, bare_logout_fail_closed GREEN
    - Disk clear does not depend on nonblocking remove_scope success
    - UI-SPEC usage string on bare
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle selective_logout_isolates -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle bare_logout_fail_closed -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle logout_all_clears_both -- --nocapture</automated>
  </verify>
  <done>
    AUTH-03 dual logout is CLI-complete with blocking disk authority and atomic --all.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bum auth status testable I/O + TUI/ACP dual-safe logout</name>
  <files>crates/codegen/xai-grok-shell/src/auth/flow.rs, crates/codegen/xai-grok-shell/src/auth/status.rs, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs, crates/codegen/xai-grok-pager/src/slash/commands/logout.rs, crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs, crates/codegen/xai-grok-shell/src/extensions/auth.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/status.rs
    - crates/codegen/xai-grok-shell/src/extensions/auth.rs (handle_logout → perform_logout)
    - crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs
    - crates/codegen/xai-grok-pager/src/slash/commands/logout.rs
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-04 TUI + status Write)
  </read_first>
  <behavior>
    - run_cli_auth_status: handler against dual fixture returns/writes UI-SPEC greppable paste-safe text
    - Unreadable store → UI-SPEC error without dumping contents
    - /logout description dual-safe; dispatch does not clear both silently
    - extensions/auth handle_logout does not dual-wipe without provider/all intent
  </behavior>
  <action>
    Status CLI + dual-safe TUI/ACP (D-06, D-03, AUTH-04). Addresses review: injectable Write/return string; include extensions/auth.rs.

    1. `run_cli_auth_status` formats via Plan 02 pure API; **return String** or write to `impl Write` parameter for tests — binary prints the result. On read failure, UI-SPEC unreadable message with path label — never dump file body.
    2. Wire Command::Auth { Status } through pager-bin.
    3. TUI dual-safety (pick one, implement fully):
       - **Preferred minimal (D-03):** fail-closed before ACP — dispatch/slash shows UI-SPEC message pointing to `bum logout --provider|--all` and does **not** emit Effect::Logout that clears credentials when both slots may exist; OR
       - **Provider-aware ACP:** extend LogoutParams with provider/all and update `extensions/auth.rs` handle_logout to call blocking clear APIs selectively.
    4. Must update **extensions/auth.rs** so ACP path cannot report ok while nonblocking remove_scope skipped disk (Addresses review). If fail-closed at TUI, ACP bare logout must also fail-closed or require params — document chosen contract in SUMMARY.
    5. GREEN run_cli_auth_status calling production handler (not pure formatter alone). Keep auth_status_format_paste_safe regression.
    6. No Phase 6 gate. Local clear only.
  </action>
  <acceptance_criteria>
    - bum auth status parse + testable handler present
    - run_cli_auth_status GREEN (handler path)
    - /logout + extensions/auth dual-safe (no silent dual wipe)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager bum_auth_status_parses -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle run_cli_auth_status -- --nocapture && cargo test -p xai-grok-shell --test auth_codex_lifecycle auth_status_format_paste_safe -- --nocapture</automated>
  </verify>
  <done>
    AUTH-04 status CLI and dual-safe TUI/ACP logout shipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI logout flags → disk mutation | Explicit user intent required for destructive clear |
| Status read → stdout | Must not leak secrets from auth.json |
| ACP ExtRequest logout → disk | Untrusted params; must not dual-wipe silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Tampering / Integrity | bare logout | high | mitigate | Fail-closed usage; bare_logout_fail_closed asserts zero mutation |
| T-05-12 | Information Disclosure | auth status CLI | high | mitigate | Paste-safe formatter; never dump auth.json |
| T-05-13 | Denial of Service | --all | medium | mitigate | Explicit flag; atomic clear_all under one lock |
| T-05-14 | Tampering | TUI/ACP /logout | high | mitigate | Fail-closed or provider-aware; extensions/auth.rs included |
| T-05-23 | Tampering | nonblocking remove_scope as authority | high | mitigate | Blocking clear_provider_slot is disk SoT for CLI logout |
| T-05-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
All AUTH-03/04 lifecycle named tests green; clap status/logout parses; ACP dual-safe.
</verification>

<success_criteria>
- Users can selectively log out and inspect dual status without secrets or cross-wipe; disk clear is blocking and atomic for --all
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-04-SUMMARY.md` when done
</output>
