---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "Wave 0 integration binary tests/auth_codex_lifecycle.rs compiles and --list discovers smoke + AUTH-02..05 contracts"
    - "auth_codex_lifecycle_harness_smoke PASSES without live ChatGPT or real tokens"
    - "Behavior-RED contracts encode codex_login_persists_slot, selective_logout_isolates, logout_all_clears_both, bare_logout_fail_closed, auth_status_format_paste_safe, codex_refresh_isolates, codex_invalid_grant_no_xai_wipe"
    - "Clap parse tests encode --provider codex|xai, bare login remains provider-None/xAI default, bare logout fail-closed parse or handler contract, auth status subcommand, --all vs --provider conflict"
    - "Phase 5 proofs run via cargo test --test auth_codex_lifecycle and pager clap filters — never cargo test -p xai-grok-shell --lib as a phase gate"
    - "Automated cargo gates use one positional TESTNAME per cargo test invocation; no bare | tail without pipefail"
    - "No production Codex OAuth, dual logout, status CLI, or refresher implementation in this plan — RED/scaffold only"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
      provides: "Wave 0 dual-auth lifecycle harness for AUTH-02..05"
      contains: auth_codex_lifecycle_harness_smoke
    - path: crates/codegen/xai-grok-pager/src/app/cli.rs
      provides: "Clap parse RED/scaffold tests for provider login/logout/status"
      contains: provider
  key_links:
    - from: tests/auth_codex_lifecycle.rs
      to: public shell auth + storage APIs (and later plan exports)
      via: "integration tests import public xai_grok_shell::auth surfaces; fixtures under tempfile BUM_HOME/auth.json"
      pattern: "auth_codex_lifecycle|PROVIDER_CODEX|providers"
    - from: cli.rs parse tests
      to: Command::Login / Logout / Auth
      via: "PagerArgs::try_parse_from([bum, ...])"
      pattern: "try_parse_from|Login|Logout|Auth|provider"
---

## Phase Goal

**As a** bum user, **I want to** log into both xAI and ChatGPT/Codex, manage them independently, and keep both sessions healthy, **so that** I can freely use Grok and GPT-5.6 models without credential clobber.

<objective>
Create the Wave 0 integration harness `tests/auth_codex_lifecycle.rs` with compiling smoke plus behavior-RED contracts for AUTH-02..05, and clap parse scaffolds for dual-provider CLI surface (per D-01, D-05, D-06, D-13). No production dual-auth implementation in this plan.

Purpose: Nyquist Wave 0 + locked verification path (mock/fixture tokens only; no live ChatGPT; no Phase 6 gate UX).
Output: `cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --list` succeeds; smoke green; lifecycle contracts present and RED where production is still xAI-only; clap tests present for planned CLI shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-shell/src/auth/model.rs
@crates/codegen/xai-grok-shell/src/auth/storage.rs
@crates/codegen/xai-grok-shell/src/auth/flow.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | `bum login --provider codex\|xai`; bare `bum login` remains xAI-only |
| D-02 | Codex browser PKCE default; `--device-auth` / `--device-code` for device |
| D-03 | CLI login is AUTH-02 hard requirement; minimal TUI non-blocking |
| D-04 | Re-login overwrites only that provider’s slot |
| D-05 | Selective logout `--provider`; `--all` explicit; bare logout fail-closed |
| D-06 | `bum auth status` both providers; greppable; never print secrets |
| D-07 | Independent refresh; permanent fail wipes only that slot |
| D-08 | One multi-slot store; per-slot ops (no second auth process) |
| D-09 | No import from `~/.codex` / stock grok; only `$BUM_HOME/auth.json` |
| D-10 | In-tree Codex OAuth (1455/1457, localhost redirect, public client_id) |
| D-11 | Persist access/refresh/expiry/account id under `providers.codex` |
| D-12 | ChatGPT OAuth primary — not Platform API key |
| D-13 | Automated mock/fixture proofs; live browser optional smoke only |

**This plan covers:** D-13 harness existence; D-01/D-05/D-06 as clap RED scaffolds; AUTH-02..05 as behavior-RED names only.

## Wave 0 harness strategy (mandatory)

- Create `crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs` only for shell contracts.
- Extend clap tests inside `crates/codegen/xai-grok-pager/src/app/cli.rs` (existing parse test module) — do not invent a second CLI crate.
- **Never** use `cargo test -p xai-grok-shell --lib` as a Phase 5 gate (Phase 4 lesson; prefer integration binaries).
- Prefer public APIs available today: `PROVIDER_XAI`, `PROVIDER_CODEX`, `read_provider_auth_store`, `select_provider_access_token`, nested auth.json fixtures (mirror `auth_multi_slot.rs`).
- Fake tokens only: e.g. `xai-fake-token`, `codex-fake-token` — no live ChatGPT.
- Do **not** implement production Codex OAuth, dual logout, status formatter, or refresher here.

## CI RED policy

- Wave 0 commits behavior-RED tests. Phase execution is sequential on the planning/execution branch: Plan 01 RED → Plans 02–06 GREEN before merge to mainline CI that runs all integration tests.
- Task gates for Plan 01 run `--list` + smoke (not the full RED binary).
- Do **not** use `#[ignore]` for core AUTH-02..05 contracts.

## Cargo verify hygiene (mandatory)

- One positional TESTNAME filter per `cargo test` invocation, or run the binary without a filter when listing.
- Chain independent cargo commands with `&&` only.
- Never pipe cargo through bare `| tail` without `set -o pipefail`. Prefer no pipe.

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| Wave 0 harness | `tests/auth_codex_lifecycle.rs` | integration binary |
| AUTH-02..05 RED contracts | same | named tests |
| Clap provider/logout/status scaffolds | `pager/.../cli.rs` | unit tests |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 auth_codex_lifecycle smoke + AUTH-02..05 behavior-RED contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md (Validation Architecture, Recommended Plan Wave Structure)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md (status format + secrets policy)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md (auth_codex_lifecycle section)
    - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs (fixture style)
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs (Wave 0 RED style)
    - crates/codegen/xai-grok-shell/src/auth/model.rs (PROVIDER_*, GrokAuth, select_provider_access_token)
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (read_provider_auth_store, write fixtures)
  </read_first>
  <behavior>
    - auth_codex_lifecycle_harness_smoke: tempfile nested auth.json with both providers readable via public APIs — PASSES on current main
    - codex_login_persists_slot: after mock/fixture Codex login path (Plan 03 GREEN), providers.codex holds Oidc selectable token and providers.xai unchanged — RED until Plan 03
    - selective_logout_isolates: logout codex leaves xAI token; logout xAI leaves codex — RED until Plan 04
    - logout_all_clears_both: --all clears both when both present — RED until Plan 04
    - bare_logout_fail_closed: bare logout does not mutate auth.json when both slots populated — RED until Plan 04
    - auth_status_format_paste_safe: status text contains greppable logged_in/usable for both xAI and Codex blocks; never contains fake token substrings — RED until Plan 02/04
    - codex_refresh_isolates: mock refresh updates only codex access/refresh; xAI key unchanged — RED until Plan 05
    - codex_invalid_grant_no_xai_wipe: permanent Codex failure clears/marks only codex slot — RED until Plan 05
  </behavior>
  <action>
    Wave 0 compiling harness + behavior-RED dual-auth lifecycle contracts only (D-13; RESEARCH Validation Architecture). No production dual-auth code.

    1. Create `crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs` as a Cargo integration test binary (auto-discovered under `tests/`).
    2. Module docs: Phase 5 dual auth lifecycle; public APIs preferred; no shell --lib gate; no live ChatGPT; no Phase 6 missing-provider gate; CI RED policy sequential until Plan 06; cite AUTH-02..05 and D-01..D-13.
    3. Mirror `auth_multi_slot.rs` style: tempfile auth.json, nested `version` + `providers.xai` / `providers.codex`, fake tokens, `GrokAuth` fields as needed.
    4. Implement `auth_codex_lifecycle_harness_smoke` so the binary compiles and lists. After this task, `cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --list` must exit 0.
    5. Encode each AUTH contract as a named test with intentional RED (`assert!(false, "Plan NN: …")` or failing production-path assertion). Prefer real public call sites once Plan 02+ exports exist; Wave 0 must compile without those exports.
    6. For status paste-safety: when asserting no secrets, check output does not contain `codex-fake-token` / `xai-fake-token` / `refresh_token` raw values after Plan 04 greens the formatter.
    7. Do not implement production storage mutate_provider, Codex OAuth, CLI logout dual, status CLI, or refresher. Do not fix shell --lib. Do not import ~/.codex (D-09).
  </action>
  <acceptance_criteria>
    - tests/auth_codex_lifecycle.rs exists
    - cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --list exits 0 and discovers smoke plus AUTH contract names listed in behavior
    - auth_codex_lifecycle_harness_smoke PASSES
    - At least one login/logout/status/refresh contract is behavior-RED (assertion fail), not a compile error
    - No production dual-auth files outside the new test file
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --list && cargo test -p xai-grok-shell --test auth_codex_lifecycle auth_codex_lifecycle_harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    Wave 0 shell integration binary compiles, lists, and holds behavior-RED AUTH-02..05 contracts for Plans 02–06.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clap parse scaffolds for --provider, bare logout, auth status</name>
  <files>crates/codegen/xai-grok-pager/src/app/cli.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/app/cli.rs (Command::Login/Logout, existing parse tests ~885+)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md (CLI login/logout/status copy)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md (cli.rs target shape)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <behavior>
    - bum_login_defaults_to_xai_without_provider_argument: bare bum login still parses; provider field absent/None (D-01) — lock existing green if possible
    - bum_login_provider_codex_parses: bum login --provider codex parses provider codex — RED until clap field lands Plan 03/04
    - bum_login_provider_xai_parses: bum login --provider xai parses
    - bum_logout_requires_provider_or_all: bare bum logout either fails clap or leaves a parse shape that handlers will fail-closed — document intended Plan 04 shape
    - bum_logout_all_parses: bum logout --all parses
    - bum_auth_status_parses: bum auth status parses to Auth/Status — RED until subcommand lands Plan 04
  </behavior>
  <action>
    Extend clap tests only (D-01, D-05, D-06). Prefer compile-safe scaffolds: if Command variants lack fields yet, write tests that currently fail compile is NOT allowed — use assert!(false, "Plan 04: add Command fields") only when parse cannot compile against target shape; better approach for Wave 0: add the clap fields as Option/bool with defaults that preserve bare-login behavior, with tests that lock parse contracts, OR keep pure-doc RED tests that call try_parse_from for flags that already parse as unknown and expect error until fields exist.

    Discretion lock for Wave 0: **prefer adding minimal clap surface early** so parse tests are real green locks without production shell handlers:
    - Login: `provider: Option&lt;String&gt;` with long `--provider` value_parser accepting `xai`/`codex` (or enum ProviderCli)
    - Logout: `provider: Option&lt;String&gt;`, `all: bool` with conflicts_with provider
    - Auth { Status } subcommand

    If adding clap fields in this task, pager-bin match arms must still compile: update match patterns to ignore new fields with `_` or pass-through stubs that call existing xAI-only handlers until Plan 03/04 rewire (minimal glue only — no Codex OAuth). Document that bare logout fail-closed behavior is Plan 04 even if parse accepts bare Logout for one release mid-phase.

    Prefer: clap fields + match arm compile fixes in pager-bin only if required for compile; otherwise keep fields and use `..` / ignore. Do **not** implement selective logout or Codex login body here.
  </action>
  <acceptance_criteria>
    - Named clap tests for provider login, auth status, logout --all present and discoverable
    - Bare bum login still parses as xAI-default (provider None)
    - xai-grok-pager-bin still cargo-checks if clap surface changed
    - No Codex OAuth network code; no dual-slot clear logic
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager bum_login_defaults_to_xai_without_provider_argument -- --nocapture</automated>
  </verify>
  <done>
    Clap surface scaffolds lock D-01/D-05/D-06 parse contracts for later handler plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → process | Fake tokens must not ship as production defaults |
| Clap CLI → shell auth handlers | Untrusted argv; provider ids must be allow-listed later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | tests/auth_codex_lifecycle.rs | low | accept | Fake tokens only; no real ChatGPT credentials in suite |
| T-05-02 | Elevation of Privilege | dual-slot RED contracts | high | mitigate | Encode never-cross-wipe isolation asserts for Plans 02–05 GREEN |
| T-05-SC | Tampering | cargo/npm installs | low | accept | No new packages this plan |
</threat_model>

<verification>
Wave 0: list + smoke green; RED contracts named; clap scaffolds present; no production dual-auth lifecycle.
</verification>

<success_criteria>
- auth_codex_lifecycle binary lists AUTH-02..05 contract names
- Smoke passes; remaining contracts intentionally RED or clap-locked
- Cargo hygiene: one TESTNAME per invocation in plan verifies
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-01-SUMMARY.md` when done
</output>
