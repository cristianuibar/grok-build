---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager-bin/src/main.rs
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "Wave 0 integration binary tests/auth_codex_lifecycle.rs compiles and --list discovers smoke + AUTH-02..05 contracts"
    - "auth_codex_lifecycle_harness_smoke PASSES without live ChatGPT or real tokens"
    - "Lifecycle contracts include reconstruct-path AUTH-05 names (mid-session refresh, concurrent single spend, fresh skip IdP, transient hard-unexpired/expired, account-header presence and trusted-host absences)"
    - "Clap parse tests encode --provider codex|xai, bare login xAI default, bare logout fail-closed shape, auth status, --all vs --provider; verify runs those named tests (not only bare-login)"
    - "BUM_HOME OnceLock hygiene documented in harness: explicit auth-file path APIs preferred; no multi-BUM_HOME mutation in one process"
    - "If clap surface changes, pager-bin main.rs match arms compile (files_modified includes main.rs)"
    - "Phase 5 proofs run via cargo test --test auth_codex_lifecycle and pager clap filters — never cargo test -p xai-grok-shell --lib as a phase gate"
    - "No production Codex OAuth, dual logout, status CLI, or refresher implementation in this plan — RED/scaffold only"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
      provides: "Wave 0 dual-auth lifecycle harness for AUTH-02..05"
      contains: auth_codex_lifecycle_harness_smoke
    - path: crates/codegen/xai-grok-pager/src/app/cli.rs
      provides: "Clap parse RED/scaffold tests for provider login/logout/status"
      contains: provider
  key_links:
    - from: tests/auth_codex_lifecycle.rs
      to: public shell auth + storage APIs (and later plan exports)
      via: "integration tests import public xai_grok_shell::auth surfaces; fixtures via explicit auth_file paths under tempfile"
      pattern: "auth_codex_lifecycle|PROVIDER_CODEX|providers"
    - from: cli.rs parse tests
      to: Command::Login / Logout / Auth + pager-bin match
      via: "PagerArgs::try_parse_from([bum, ...]); main.rs arms compile"
      pattern: "try_parse_from|Login|Logout|Auth|provider"
---

## Phase Goal

**As a** bum user, **I want to** log into both xAI and ChatGPT/Codex, manage them independently, and keep both sessions healthy, **so that** I can freely use Grok and GPT-5.6 models without credential clobber.

<objective>
Create the Wave 0 integration harness `tests/auth_codex_lifecycle.rs` with compiling smoke plus behavior-RED contracts for AUTH-02..05 (including reconstruct-path AUTH-05), and clap parse scaffolds for dual-provider CLI (per D-01, D-05, D-06, D-13). Addresses review concerns: BUM_HOME OnceLock hygiene, pager-bin clap compile surface, verify runs new parser tests. No production dual-auth implementation.

Purpose: Nyquist Wave 0 + locked verification path (mock/fixture tokens only; no live ChatGPT; no Phase 6 gate UX).
Output: harness lists; smoke green; lifecycle contracts RED where production is still xAI-only; clap tests present and verified by name.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
@crates/codegen/xai-grok-pager/src/app/cli.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
@crates/codegen/xai-grok-config/src/paths.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-01 | `bum login --provider codex\|xai`; bare `bum login` remains xAI-only |
| D-02 | Codex browser PKCE default; `--device-auth` / `--device-code` for device |
| D-03 | CLI login is AUTH-02 hard requirement; minimal TUI non-blocking |
| D-04 | Re-login overwrites only that provider’s slot |
| D-05 | Selective logout `--provider`; `--all` explicit; bare logout fail-closed |
| D-06 | `bum auth status` both providers; greppable; never print secrets |
| D-07 | Independent refresh; permanent fail wipes only that slot |
| D-08 | One multi-slot store; per-slot ops (no second auth process) |
| D-09 | No import from `~/.codex` / stock grok; only `$BUM_HOME/auth.json` |
| D-10 | In-tree Codex OAuth (1455/1457, localhost redirect, public client_id) |
| D-11 | Persist access/refresh/expiry/account id under `providers.codex` |
| D-12 | ChatGPT OAuth primary — not Platform API key |
| D-13 | Automated mock/fixture proofs; live browser optional smoke only |

**This plan covers:** D-13 harness existence; D-01/D-05/D-06 as clap RED scaffolds; AUTH-02..05 as behavior-RED names only.

## Wave 0 harness strategy (mandatory)

- Create `crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs` only for shell contracts.
- Extend clap tests inside `crates/codegen/xai-grok-pager/src/app/cli.rs`.
- **Never** use `cargo test -p xai-grok-shell --lib` as a Phase 5 gate.
- Prefer **explicit `auth_file: &Path` public APIs** for fixtures (addresses review: BUM_HOME OnceLock). Do not mutate `BUM_HOME` independently across tests in one process. If a future handler requires `grok_home()`, use a single process-wide sandbox set once or a separate subprocess — never race OnceLock.
- Fake tokens only: `xai-fake-token`, `codex-fake-token`, `codex-refresh-token` — no live ChatGPT.
- Do **not** implement production Codex OAuth, dual logout, status formatter, or refresher here.

## CI RED policy

- Wave 0 commits behavior-RED tests. Sequential: Plan 01 RED → Plans 02–06 GREEN before mainline CI that runs all integration tests.
- Task gates for Plan 01 run `--list` + smoke + named clap tests (not the full RED binary).
- Do **not** use `#[ignore]` for core AUTH-02..05 contracts.

## Cargo verify hygiene (mandatory)

- One positional TESTNAME filter per `cargo test` invocation, or run the binary without a filter when listing.
- Chain independent cargo commands with `&&` only.
- Never pipe cargo through bare `| tail` without `set -o pipefail`. Prefer no pipe.

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| Wave 0 harness | `tests/auth_codex_lifecycle.rs` | integration binary |
| AUTH-02..05 RED contracts | same | named tests |
| Clap provider/logout/status scaffolds | `pager/.../cli.rs` (+ `pager-bin/main.rs` if clap fields added) | unit tests + compile glue |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 auth_codex_lifecycle smoke + AUTH-02..05 behavior-RED contracts</name>
  <files>crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md (Validation Architecture)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-01 + 05-05 required contracts)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
    - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
    - crates/codegen/xai-grok-shell/src/auth/model.rs
    - crates/codegen/xai-grok-shell/src/auth/storage.rs
    - crates/codegen/xai-grok-config/src/paths.rs (OnceLock grok_home)
  </read_first>
  <behavior>
    - auth_codex_lifecycle_harness_smoke: tempfile nested auth.json with both providers readable via public path-taking APIs — PASSES on current main
    - codex_login_persists_slot: providers.codex holds Oidc token; providers.xai unchanged — RED until Plan 03
    - codex_authorize_url_includes_pkce_and_localhost_callback — RED until Plan 03
    - codex_device_endpoints_use_deviceauth — RED until Plan 03
    - codex_device_pending_slowdown_exchange_denied — RED until Plan 03 (device multi-step)
    - codex_oauth_state_mismatch_writes_nothing — RED until Plan 03
    - selective_logout_isolates / logout_all_clears_both / bare_logout_fail_closed — RED until Plan 04
    - auth_status_format_paste_safe / run_cli_auth_status — RED until Plan 02/04
    - auth_status_usable_expired_refreshable / auth_status_usable_expired_no_refresh — RED until Plan 02 (usable semantics)
    - codex_refresh_isolates / codex_invalid_grant_no_xai_wipe — RED until Plan 05
    - codex_reconstruct_refreshes_mid_session_expiry: after prepare stamps stale bearer, async reconstruct/ensure_fresh path spends RT and returns fresh bearer into request construction — RED until Plan 05 (AUTH-05 production path)
    - codex_concurrent_refresh_single_idp_spend — RED until Plan 05
    - codex_fresh_token_skips_idp — RED until Plan 05
    - codex_transient_fail_hard_unexpired_keeps_token / codex_transient_fail_hard_expired_no_credential — RED until Plan 05
    - chatgpt_account_id_header_on_codex — RED until Plan 05
    - chatgpt_account_id_header_absent_on_xai / chatgpt_account_id_header_absent_on_custom_endpoint — RED until Plan 05
  </behavior>
  <action>
    Wave 0 compiling harness + behavior-RED dual-auth lifecycle contracts only (D-13; review cycle 1). No production dual-auth code.

    1. Create `crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs` as a Cargo integration test binary.
    2. Module docs: Phase 5 dual auth; public path-taking APIs preferred; **OnceLock rule** — do not set different BUM_HOME values across tests in this binary; fixture paths are tempfile auth.json paths passed explicitly; no shell --lib gate; no live ChatGPT; no Phase 6 gate; CI RED sequential until Plan 06; cite AUTH-02..05 and D-01..D-13.
    3. Mirror multi_slot fixture style: nested version + providers.xai / providers.codex, fake tokens, GrokAuth fields as needed — always via explicit paths.
    4. Implement smoke so `cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --list` exits 0.
    5. Encode each AUTH contract as a named test. Prefer compile-safe RED assertions against existing public behavior when possible; use intentional fail with Plan-NN message only when the production seam does not exist yet. Do not leave unconditional placeholders that never mention the intended seam.
    6. AUTH-05 names must target **async reconstruct / ensure_fresh_codex_auth**, not ModelsManager prepare alone (Addresses review concern: wrong production hook).
    7. Do not implement production storage mutate, Codex OAuth, dual logout, status CLI, or refresher. Do not import ~/.codex (D-09).
  </action>
  <acceptance_criteria>
    - tests/auth_codex_lifecycle.rs exists
    - --list discovers smoke plus AUTH contract names in behavior
    - auth_codex_lifecycle_harness_smoke PASSES
    - At least one login/logout/status/refresh contract is behavior-RED (assertion fail), not a compile error
    - Module docs state OnceLock / explicit-path policy
    - No production dual-auth files outside the new test file
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --list && cargo test -p xai-grok-shell --test auth_codex_lifecycle auth_codex_lifecycle_harness_smoke -- --nocapture</automated>
  </verify>
  <done>
    Wave 0 shell integration binary compiles, lists reconstruct-path AUTH-05 contracts, and holds behavior-RED AUTH-02..05 names for Plans 02–06.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clap parse scaffolds + pager-bin compile for --provider, logout, auth status</name>
  <files>crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager-bin/src/main.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager/src/app/cli.rs (Command::Login/Logout, parse tests)
    - crates/codegen/xai-grok-pager-bin/src/main.rs (Command match arms ~1739+)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-01 clap/main.rs)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <behavior>
    - bum_login_defaults_to_xai_without_provider_argument: bare bum login provider None (D-01)
    - bum_login_provider_codex_parses / bum_login_provider_xai_parses
    - bum_logout_requires_provider_or_all (parse shape for fail-closed handler)
    - bum_logout_all_parses
    - bum_auth_status_parses
    - cargo check -p xai-grok-pager-bin succeeds if clap fields added
  </behavior>
  <action>
    Extend clap tests and compile surface (D-01, D-05, D-06). Addresses review concern: CLI scaffold must include pager-bin main.rs when clap shape changes; verify must run new parser tests not only bare-login.

    Discretion lock for Wave 0: **add minimal clap surface early** so parse tests are real locks without production shell handlers:
    - Login: provider Option with long `--provider` accepting xai/codex (typed enum preferred if cheap)
    - Logout: provider Option + all bool with conflicts_with provider
    - Auth { Status } subcommand

    When adding clap fields: update **pager-bin main.rs** match arms so the binary compiles (ignore new fields with `_` or pass-through stubs that call existing xAI-only handlers until Plans 03–04). Do **not** implement selective logout or Codex login body here.

    Alternative only if clap fields deferred entirely to Plans 03–04: leave main.rs untouched and keep parse tests that expect unknown-flag errors until those plans land — but then Plan 03/04 own both fields and main.rs. Prefer early fields + main.rs compile glue this plan.
  </action>
  <acceptance_criteria>
    - Named clap tests for provider login, auth status, logout --all present
    - Bare bum login still parses as xAI-default (provider None)
    - cargo check -p xai-grok-pager-bin green if clap surface changed
    - Verify command runs multiple new parser test names, not only bare-login
    - No Codex OAuth network code; no dual-slot clear logic
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-pager bum_login_defaults_to_xai_without_provider_argument -- --nocapture && cargo test -p xai-grok-pager bum_login_provider_codex_parses -- --nocapture && cargo test -p xai-grok-pager bum_logout_all_parses -- --nocapture && cargo test -p xai-grok-pager bum_auth_status_parses -- --nocapture && cargo check -p xai-grok-pager-bin</automated>
  </verify>
  <done>
    Clap surface scaffolds lock D-01/D-05/D-06 parse contracts; pager-bin compiles; verify exercises new parser names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → process | Fake tokens must not ship as production defaults |
| Clap CLI → shell auth handlers | Untrusted argv; provider ids must be allow-listed later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | tests/auth_codex_lifecycle.rs | low | accept | Fake tokens only; no real ChatGPT credentials in suite |
| T-05-02 | Elevation of Privilege | dual-slot RED contracts | high | mitigate | Encode never-cross-wipe isolation asserts for Plans 02–05 GREEN |
| T-05-SC | Tampering | cargo/npm installs | low | accept | No new packages this plan |
</threat_model>

<verification>
Wave 0: list + smoke green; reconstruct-path AUTH-05 names present; clap scaffolds verified by name; no production dual-auth lifecycle.
</verification>

<success_criteria>
- auth_codex_lifecycle binary lists AUTH-02..05 contract names including reconstruct/mid-session refresh
- Smoke passes; remaining contracts intentionally RED or clap-locked
- Cargo hygiene: one TESTNAME per invocation in plan verifies
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-01-SUMMARY.md` when done
</output>
