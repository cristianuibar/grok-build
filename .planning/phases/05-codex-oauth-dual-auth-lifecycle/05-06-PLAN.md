---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 06
type: execute
wave: 6
depends_on:
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
  - crates/codegen/xai-grok-pager/src/app/cli.rs
  - crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs
  - crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
  - crates/codegen/xai-grok-shell/src/agent/config.rs
  - crates/codegen/xai-grok-shell/src/auth/flow.rs
  - crates/codegen/xai-grok-shell/src/auth/storage.rs
  - crates/codegen/xai-grok-shell/src/auth/codex/refresh.rs
  - crates/codegen/xai-grok-shell/src/auth/refresh/codex_refresher.rs
  - crates/codegen/xai-grok-shell/src/extensions/auth.rs
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "Full auth_codex_lifecycle suite GREEN including reconstruct mid-session, concurrent refresh, usable semantics, device multi-step, state-mismatch"
    - "AUTH-05 gate hits real async ensure_fresh / reconstruct path — not prepare-only helpers"
    - "auth_multi_slot regression GREEN"
    - "provider_routing regression GREEN (MOD-04/MOD-05 + never_cross_slot)"
    - "Relevant pager clap parse tests green"
    - "No Phase 6 missing-provider switch gate UX shipped"
    - "No stock ~/.codex or ~/.grok credential import paths added"
    - "No Platform API-key primary Codex login path"
    - "COVERAGE.md decisions still accurate"
    - "cargo check -p xai-grok-shell and -p xai-grok-pager-bin succeed"
    - "cargo fmt --all --check and targeted clippy on touched crates succeed when runnable"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
      provides: "Phase 5 gate suite AUTH-02..05"
      contains: codex_reconstruct_refreshes_mid_session_expiry
    - path: .planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
      provides: "API coverage matrix for ChatGPT OAuth"
      contains: INTEGRATE
  key_links:
    - from: phase gate
      to: AUTH-02..05 + MOD-04/05 + AUTH-01 reg
      via: "integration binaries + reconstruct-path AUTH-05 proofs — never shell --lib as gate"
      pattern: "auth_codex_lifecycle|codex_reconstruct|auth_multi_slot|provider_routing"
---

## Phase Goal

**As a** bum user, **I want** dual OAuth lifecycle fully proven by automation, **so that** Phase 6 can build missing-provider UX on a trustworthy auth base.

<objective>
Phase 5 gate: full automated map green for AUTH-02..05 (including reconstruct-path AUTH-05 and concurrent refresh), regressions for multi-slot and provider routing, clap surfaces, confirm deferred Phase 6/7/import/Platform-key scope not shipped (D-13 + CONTEXT deferred). Addresses review: gate must hit real async reconstruct path; files_modified may include production modules for residual fixes; fmt/clippy.

Purpose: Seal dual auth lifecycle before mid-session gate work.
Output: All named lifecycle tests green; check builds; no scope creep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-01-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-02-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-03-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-04-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-05-SUMMARY.md
@crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-13 | Automated proofs primary; live smoke optional |
| Deferred | Phase 6 gate, Phase 7 multi-agent, stock import, Platform API key primary |

## Cargo verify hygiene

- Integration binaries only for shell gates — **never** `cargo test -p xai-grok-shell --lib` as phase gate
- One TESTNAME per cargo invocation when filtering; full binary OK without filter for suite gate
- No bare `| tail` without pipefail

## Gate residual fix policy

This plan may apply **minimal production fixes** in modules listed under `files_modified` when gate failures reveal gaps. Do not start Phase 6 UX. Prefer fixing ensure_fresh / reconstruct / clear / status / device paths over rewriting architecture.

<tasks>

<task type="auto">
  <name>Task 1: Full AUTH-02..05 lifecycle suite green including reconstruct AUTH-05</name>
  <files>crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs, crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs, crates/codegen/xai-grok-shell/src/agent/config.rs, crates/codegen/xai-grok-shell/src/auth/flow.rs, crates/codegen/xai-grok-shell/src/auth/storage.rs, crates/codegen/xai-grok-shell/src/auth/codex/refresh.rs, crates/codegen/xai-grok-shell/src/auth/refresh/codex_refresher.rs, crates/codegen/xai-grok-shell/src/extensions/auth.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-VALIDATION.md
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-06 gate)
    - Prior plan SUMMARYs 05-01..05-05 for remaining RED notes
  </read_first>
  <action>
    Phase gate hardening (D-13). Addresses review: AUTH-05 gate must hit real async reconstruct path; concurrent refresh required; production fixes allowed and listed.

    1. Run full `auth_codex_lifecycle` binary; fix residual RED stubs, flaky binds, missing asserts for AUTH-02..05.
    2. **Must pass** (not optional): `codex_reconstruct_refreshes_mid_session_expiry`, `codex_concurrent_refresh_single_idp_spend`, `codex_fresh_token_skips_idp`, transient fail pair, account header presence/absence, usable semantic tests, device multi-step, state-mismatch, selective logout, status handler.
    3. Confirm no `#[ignore]` on core contracts.
    4. If small production bugs surface, fix in listed modules only with minimal diffs — no Phase 6 UX.
    5. Verify COVERAGE.md still matches (revoke OPT-OUT; authorize/token/refresh/device/claims INTEGRATE).
  </action>
  <acceptance_criteria>
    - cargo test -p xai-grok-shell --test auth_codex_lifecycle exits 0
    - Reconstruct-path AUTH-05 contracts pass (not prepare-only substitutes)
    - Concurrent refresh single IdP spend passes
    - No Phase 6 gate code paths
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --nocapture</automated>
  </verify>
  <done>
    Dual auth lifecycle suite fully green including reconstruct AUTH-05 proofs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression matrix + clap + build/fmt/clippy + deferred-scope audit</name>
  <files>crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs, crates/codegen/xai-grok-pager/src/app/cli.rs, crates/codegen/xai-grok-pager/src/app/dispatch/auth.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md (deferred)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-REVIEWS.md (05-06)
  </read_first>
  <action>
    Regression + scope seal. Addresses review: pager clap/dispatch coverage; fmt/clippy; credential path assertions under product home.

    1. Run auth_multi_slot — must stay green.
    2. Run provider_routing — MOD-04/05 + never_cross_slot green.
    3. Run relevant pager clap tests: login provider, logout all, auth status.
    4. cargo check shell + pager-bin green.
    5. cargo fmt --all --check; cargo clippy -p xai-grok-shell -p xai-grok-pager -p xai-grok-pager-bin -- -D warnings if project allows, else non-fatal document in SUMMARY if pre-existing noise blocks -D.
    6. Scope audit: no new stock `/.codex` auth import for product login; no Platform API key as Codex primary; no Phase 6 “login to Codex to use GPT” gate strings as product path. Prefer assertions that credential paths resolve under explicit product-home/temp fixtures rather than broad greps alone.
    7. Document optional live smoke steps in SUMMARY human notes only.
  </action>
  <acceptance_criteria>
    - auth_multi_slot and provider_routing exit 0
    - pager clap tests for Phase 5 surface green
    - cargo check shell + pager-bin exit 0
    - Deferred scope audit clean
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_multi_slot -- --nocapture && cargo test -p xai-grok-shell --test provider_routing -- --nocapture && cargo test -p xai-grok-pager bum_login_provider_codex_parses -- --nocapture && cargo test -p xai-grok-pager bum_auth_status_parses -- --nocapture && cargo check -p xai-grok-shell && cargo check -p xai-grok-pager-bin && cargo fmt --all --check</automated>
    <human-check>
      <test>Optional live smoke: BUM_HOME temp dir; bum login --provider codex (browser); bum auth status; bum logout --provider codex; confirm xAI untouched if also logged in</test>
      <expected>Codex login succeeds for ChatGPT account; status shows Codex logged_in without tokens; selective logout leaves xAI</expected>
      <why_human>Live IdP + browser loopback not exercisable in CI mocks</why_human>
    </human-check>
  </verify>
  <done>
    Phase 5 sealed: dual OAuth lifecycle automated green + regressions + clap + optional live UAT note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → release confidence | Tests must not use real user tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Information Disclosure | phase gate tests | low | accept | Fixtures only; human smoke uses developer account |
| T-05-20 | Tampering | scope creep Phase 6/import | medium | mitigate | Explicit deferred audit in Task 2 |
| T-05-21 | Tampering | prepare-only AUTH-05 false green | high | mitigate | Gate requires reconstruct mid-session + concurrent spend tests |
| T-05-SC | Tampering | package installs | low | accept | No new packages in gate plan |
</threat_model>

<verification>
Full lifecycle (incl. reconstruct AUTH-05) + multi_slot + provider_routing + clap green; checks/fmt green; deferred scope clean.
</verification>

<success_criteria>
- ROADMAP Phase 5 success criteria 1–4 proven by automation including mid-session dual-session health
- Ready for Phase 6 mid-session gate work
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-06-SUMMARY.md` when done
</output>
