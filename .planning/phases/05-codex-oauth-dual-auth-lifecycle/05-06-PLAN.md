---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 06
type: execute
wave: 6
depends_on:
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
  - crates/codegen/xai-grok-shell/tests/provider_routing.rs
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
must_haves:
  truths:
    - "Full auth_codex_lifecycle suite GREEN (AUTH-02..05 contracts)"
    - "auth_multi_slot regression GREEN (AUTH-01 multi-slot sibling preserve)"
    - "provider_routing regression GREEN (MOD-04/MOD-05 dual route + never_cross_slot)"
    - "No Phase 6 missing-provider switch gate UX shipped"
    - "No stock ~/.codex or ~/.grok credential import paths added"
    - "No Platform API-key primary Codex login path"
    - "COVERAGE.md decisions still accurate for integrated vs opt-out OAuth surface"
    - "cargo check -p xai-grok-shell and -p xai-grok-pager-bin succeed"
  artifacts:
    - path: crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
      provides: "Phase 5 gate suite AUTH-02..05"
      contains: auth_codex_lifecycle_harness_smoke
    - path: .planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
      provides: "API coverage matrix for ChatGPT OAuth"
      contains: INTEGRATE
  key_links:
    - from: phase gate
      to: AUTH-02..05 + MOD-04/05 + AUTH-01 reg
      via: "integration binaries only — never shell --lib as gate"
      pattern: "auth_codex_lifecycle|auth_multi_slot|provider_routing"
---

## Phase Goal

**As a** bum user, **I want** dual OAuth lifecycle fully proven by automation, **so that** Phase 6 can build missing-provider UX on a trustworthy auth base.

<objective>
Phase 5 gate: full automated map green for AUTH-02..05, regressions for multi-slot and provider routing, confirm deferred Phase 6/7/import/Platform-key scope not shipped (D-13 + CONTEXT deferred). Optional human-check notes for live `bum login --provider codex` smoke only.

Purpose: Seal dual auth lifecycle before mid-session gate work.
Output: All named lifecycle tests green; check builds; no scope creep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/COVERAGE.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-01-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-02-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-03-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-04-SUMMARY.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-05-SUMMARY.md
@crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/tests/provider_routing.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-13 | Automated proofs primary; live smoke optional |
| Deferred | Phase 6 gate, Phase 7 multi-agent, stock import, Platform API key primary |

## Cargo verify hygiene

- Integration binaries only for shell gates — **never** `cargo test -p xai-grok-shell --lib` as phase gate
- One TESTNAME per cargo invocation when filtering; full binary OK without filter for suite gate
- No bare `| tail` without pipefail

<tasks>

<task type="auto">
  <name>Task 1: Full AUTH-02..05 lifecycle suite green + fix residual gaps</name>
  <files>crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs, crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md (Phase Requirements → Test Map)
    - Prior plan SUMMARYs 05-01..05-05 for remaining RED notes
  </read_first>
  <action>
    Phase gate hardening (D-13).

    1. Run full `auth_codex_lifecycle` binary; fix any remaining RED stubs, flaky binds, or missing asserts for AUTH-02..05 named contracts.
    2. Ensure smoke + login + selective logout + logout all + bare fail-closed + status paste-safe + refresh isolates + invalid_grant isolation all GREEN.
    3. Confirm no `#[ignore]` on core contracts.
    4. If small production bugs surface only at gate, fix in the owning modules (storage/codex/flow/refresh/config) with minimal diffs — do not start Phase 6 UX.
    5. Verify COVERAGE.md still matches shipped surface (revoke still OPT-OUT; authorize/token/refresh/device/claims INTEGRATE).
  </action>
  <acceptance_criteria>
    - cargo test -p xai-grok-shell --test auth_codex_lifecycle exits 0
    - All AUTH-02..05 contract names pass
    - No Phase 6 gate code paths
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle -- --nocapture</automated>
  </verify>
  <done>
    Dual auth lifecycle suite fully green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression matrix + build check + deferred-scope audit</name>
  <files>crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs, crates/codegen/xai-grok-shell/tests/provider_routing.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
    - crates/codegen/xai-grok-shell/tests/provider_routing.rs
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md (deferred)
  </read_first>
  <action>
    Regression + scope seal.

    1. Run `cargo test -p xai-grok-shell --test auth_multi_slot` — must stay green.
    2. Run `cargo test -p xai-grok-shell --test provider_routing` — MOD-04/05 + never_cross_slot green.
    3. `cargo check -p xai-grok-shell` and `cargo check -p xai-grok-pager-bin` green.
    4. Grep audit: no new readers of stock `/.codex` auth for product login; no marketing Platform API key as Codex primary; no Phase 6 “login to Codex to use GPT” gate strings as product path (Phase 6 owns that).
    5. Document optional live smoke steps in SUMMARY human notes only (not required for CI green).
  </action>
  <acceptance_criteria>
    - auth_multi_slot and provider_routing exit 0
    - cargo check shell + pager-bin exit 0
    - Deferred scope audit clean
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_multi_slot -- --nocapture && cargo test -p xai-grok-shell --test provider_routing -- --nocapture && cargo check -p xai-grok-shell && cargo check -p xai-grok-pager-bin</automated>
    <human-check>
      <test>Optional live smoke: BUM_HOME temp dir; bum login --provider codex (browser); bum auth status; bum logout --provider codex; confirm xAI untouched if also logged in</test>
      <expected>Codex login succeeds for ChatGPT account; status shows Codex logged_in yes without printing tokens; selective logout leaves xAI</expected>
      <why_human>Live IdP + browser loopback not exercisable in CI mocks</why_human>
    </human-check>
  </verify>
  <done>
    Phase 5 sealed: dual OAuth lifecycle automated green + regressions + optional live UAT note.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → release confidence | Tests must not use real user tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Information Disclosure | phase gate tests | low | accept | Fixtures only; human smoke uses developer account |
| T-05-20 | Tampering | scope creep Phase 6/import | medium | mitigate | Explicit deferred audit grep in Task 2 |
| T-05-SC | Tampering | package installs | low | accept | No new packages in gate plan |
</threat_model>

<verification>
Full lifecycle + multi_slot + provider_routing green; checks green; deferred scope clean.
</verification>

<success_criteria>
- ROADMAP Phase 5 success criteria 1–4 proven by automation
- Ready for Phase 6 mid-session gate work
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-06-SUMMARY.md` when done
</output>
