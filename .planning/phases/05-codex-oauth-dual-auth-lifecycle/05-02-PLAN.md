---
phase: 05-codex-oauth-dual-auth-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/codegen/xai-grok-shell/src/auth/storage.rs
  - crates/codegen/xai-grok-shell/src/auth/model.rs
  - crates/codegen/xai-grok-shell/src/auth/mod.rs
  - crates/codegen/xai-grok-shell/src/auth/status.rs
  - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  - crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "mutate_provider_store_or_prune updates only the named provider slot and preserves siblings"
    - "clear_provider_slot removes only that provider; deletes auth.json only when all slots empty"
    - "Codex writers never call write_auth_json / mutate_xai_store_or_prune for Codex persistence"
    - "Status pure model formats both xAI and Codex with logged_in/usable/account/plan and never includes access or refresh token material"
    - "auth_status_format_paste_safe GREEN for pure formatter against dual-slot fixture"
    - "auth_multi_slot sibling-preserve still green after new writers"
  artifacts:
    - path: crates/codegen/xai-grok-shell/src/auth/storage.rs
      provides: "provider-parameterized RMW + clear_provider_slot"
      contains: mutate_provider_store_or_prune
    - path: crates/codegen/xai-grok-shell/src/auth/status.rs
      provides: "pure AuthStatus report + paste-safe format (UI-SPEC)"
      contains: logged_in
    - path: crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
      provides: "GREEN isolation + status format contracts"
      contains: auth_status_format_paste_safe
  key_links:
    - from: mutate_provider_store_or_prune
      to: AuthDocument.providers
      via: "lock → still_live → read full document → mutate ONE provider key → write/prune"
      pattern: "mutate_provider_store|clear_provider|PROVIDER_CODEX|PROVIDER_XAI"
    - from: status formatter
      to: read_auth_document / read_provider_auth_store
      via: "pure inspect; no secrets in Display/format string"
      pattern: "logged_in|usable|account|plan"
---

## Phase Goal

**As a** bum user, **I want** dual-provider credentials stored and inspectable without cross-slot clobber, **so that** login/logout/status can manage xAI and Codex independently.

<objective>
Deliver provider-parameterized multi-slot storage ops (`mutate_provider_store_or_prune`, `clear_provider_slot`) and a pure paste-safe status model/formatter (per D-05, D-06, D-08, D-09). This is the dual-lifecycle foundation: later plans mount Codex OAuth and CLI on these primitives without inventing a second auth file.

Purpose: AUTH-03/AUTH-04 storage and status core GREEN before OAuth network paths.
Output: Slot-isolated writers + greppable status format; lifecycle tests for isolation/status green where applicable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-CONTEXT.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-RESEARCH.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md
@.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-01-PLAN.md
@crates/codegen/xai-grok-shell/src/auth/storage.rs
@crates/codegen/xai-grok-shell/src/auth/model.rs
@crates/codegen/xai-grok-shell/src/auth/mod.rs
@crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs
@crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
</context>

## Locked decisions (traceability)

| ID | Decision |
|----|----------|
| D-05 | Selective slot clear; never silent dual-wipe |
| D-06 | Status lists both providers; greppable; no secrets |
| D-08 | One store; per-slot operations |
| D-09 | Credentials only under `$BUM_HOME/auth.json` |
| D-11 | Codex slot shape ready for Oidc GrokAuth entries |
| D-13 | Automated proofs |

**Discretion locks this plan:**
- Status module path: new `auth/status.rs` (pure; no CLI I/O) — CLI wire in Plan 04
- Mutation result enum may alias or generalize `XaiStoreMutation` → `ProviderStoreMutation` (Unchanged / DocumentWritten / FileDeleted)
- Existing `mutate_xai_store_or_prune` may delegate to generic provider API with `PROVIDER_XAI` (no behavior change for xAI callers)

## Artifacts this plan produces

| Symbol | Location | Kind |
|--------|----------|------|
| `mutate_provider_store_or_prune` | `auth/storage.rs` | `pub(crate)` RMW |
| `mutate_provider_store_or_prune_with_lock` | `auth/storage.rs` | guard-held RMW |
| `clear_provider_slot` | `auth/storage.rs` | selective clear + prune |
| `ProviderAuthStatus` / format | `auth/status.rs` | pure model + Display/format |
| Isolation + status tests | `tests/auth_codex_lifecycle.rs` | GREEN |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provider-slot mutate/clear RMW (GREEN isolation)</name>
  <files>crates/codegen/xai-grok-shell/src/auth/storage.rs, crates/codegen/xai-grok-shell/src/auth/model.rs, crates/codegen/xai-grok-shell/src/auth/mod.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs, crates/codegen/xai-grok-shell/tests/auth_multi_slot.rs</files>
  <read_first>
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (`mutate_xai_store_or_prune*`, `mutate_auth_document*`, `persist_document_or_prune`, `apply_xai_slot`, `read_provider_auth_store`)
    - crates/codegen/xai-grok-shell/src/auth/model.rs (PROVIDER_*, AuthDocument, AuthStore, GrokAuth)
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-PATTERNS.md (storage.rs section)
    - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  </read_first>
  <behavior>
    - mutate_provider_codex_preserves_xai: seed both slots; mutate codex store; raw JSON xAI payload unchanged
    - mutate_provider_xai_preserves_codex: seed both; mutate xAI; codex unchanged
    - clear_provider_codex_leaves_xai: clear codex only; xAI remains; file not deleted
    - clear_last_provider_deletes_file: clear both sequentially → FileDeleted / file absent
    - concurrent_provider_mutations_preserve_both: optional extend auth_multi_slot if cheap
  </behavior>
  <action>
    Implement generic provider-slot writers (D-05, D-08, D-09; PATTERNS storage section).

    1. Add `mutate_provider_store_or_prune(auth_file, provider, f)` and `_with_lock` variant mirroring `mutate_xai_store_or_prune` but parameterized by provider id (`PROVIDER_XAI` | `PROVIDER_CODEX`). Remove empty provider keys; set `AUTH_DOCUMENT_VERSION`; use existing `persist_document_or_prune` / lock / `still_live` patterns.
    2. Add `clear_provider_slot(auth_file, provider)` that empties that provider’s store (or removes key) and prunes file only when all providers empty.
    3. Prefer rewiring `mutate_xai_store_or_prune` to call the generic helper with `PROVIDER_XAI` so one code path owns isolation (behavior-preserving for Phase 2 callers).
    4. Do **not** route Codex writes through `write_auth_json` / `apply_xai_slot` only (those are xAI-slot conveniences).
    5. Re-export from `auth/mod.rs` as needed for integration tests (`pub(crate)` or `pub` matching existing storage surface).
    6. GREEN lifecycle tests that call public/crate-visible APIs for mutate/clear isolation (replace Wave 0 RED stubs for storage isolation parts). Keep login/refresh tests RED.
    7. Run auth_multi_slot regression (sibling preserve) — must stay green.
    8. No OAuth HTTP; no CLI handlers; no ~/.codex import.
  </action>
  <acceptance_criteria>
    - mutate_provider_store_or_prune and clear_provider_slot exist and isolate slots
    - auth_codex_lifecycle isolation tests for mutate/clear GREEN
    - cargo test -p xai-grok-shell --test auth_multi_slot still green
    - No tokens logged in new code paths
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle mutate_provider -- --nocapture && cargo test -p xai-grok-shell --test auth_multi_slot -- --nocapture</automated>
  </verify>
  <done>
    Dual-slot storage mutations are provider-parameterized and proven isolated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure dual-provider status model + paste-safe format</name>
  <files>crates/codegen/xai-grok-shell/src/auth/status.rs, crates/codegen/xai-grok-shell/src/auth/mod.rs, crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs</files>
  <read_first>
    - .planning/phases/05-codex-oauth-dual-auth-lifecycle/05-UI-SPEC.md (status block format locked)
    - crates/codegen/xai-grok-shell/src/auth/storage.rs (read_auth_document / read_provider_auth_store)
    - crates/codegen/xai-grok-shell/src/auth/model.rs (select_provider_access_token, GrokAuth fields)
    - crates/codegen/xai-grok-shell/tests/auth_codex_lifecycle.rs
  </read_first>
  <behavior>
    - auth_status_format_paste_safe: dual fixture → format includes xAI then Codex blocks with logged_in/usable/account/plan keys; format string must not contain access token, refresh token, or key field values
    - auth_status_both_providers_always_listed: empty store still lists both with logged_in: no
    - auth_status_asymmetric_login: one slot populated → one yes one no
  </behavior>
  <action>
    Pure status inspect (D-06, UI-SPEC status contract).

    1. Create `auth/status.rs` with types for per-provider status (`logged_in`, `usable`, optional account/plan labels) and a function that builds status from an auth file path or AuthDocument.
    2. Field semantics (UI-SPEC): logged_in = session/token present; usable = present and not known-invalid; account = email or em-dash placeholder; plan = plan label or em-dash.
    3. Format output exactly as UI-SPEC greppable blocks (xAI first, blank line, Codex). Provider labels: `xAI` / `Codex` (not wire ids alone as headers).
    4. Never include access_token, refresh_token, key, or raw JWT material in format output.
    5. Wire module in `auth/mod.rs`. GREEN `auth_status_format_paste_safe` and related lifecycle tests against fixtures (no CLI yet).
    6. Usable may treat permanent-invalid sticky state if such a flag exists; otherwise derive from non-blank selectable token via `select_provider_access_token`. Document any temporary usable==logged_in approximation for Plan 05 permanent-fail refinement.
  </action>
  <acceptance_criteria>
    - status.rs exists; format matches UI-SPEC keys
    - auth_status_format_paste_safe GREEN
    - Format never contains fixture token substrings
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p xai-grok-shell --test auth_codex_lifecycle auth_status_format_paste_safe -- --nocapture</automated>
  </verify>
  <done>
    Paste-safe dual status pure API ready for CLI Plan 04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.json on disk → status formatter | File may contain secrets; formatter must not emit them |
| Provider id string → mutate/clear | Must only accept known slots or fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | auth/status.rs | high | mitigate | Paste-safe format tests assert no token substrings; only logged_in/usable/account/plan |
| T-05-04 | Tampering / Integrity | mutate_provider_store_or_prune | high | mitigate | Full-document lock RMW; sibling preserve tests; never rewrite whole map from single-slot mental model |
| T-05-05 | Denial of Service | clear_provider_slot bare misuse | medium | mitigate | Plan 04 fail-closed CLI; this plan only provides selective clear API |
| T-05-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Isolation mutate/clear green; status paste-safe green; auth_multi_slot regression green.
</verification>

<success_criteria>
- Codex and xAI slots can be mutated/cleared independently under lock
- Status text is greppable and secret-free for both providers
</success_criteria>

<output>
Create `.planning/phases/05-codex-oauth-dual-auth-lifecycle/05-02-SUMMARY.md` when done
</output>
