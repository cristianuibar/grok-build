# Phase 12.1 — Plan Check (goal-backward)

**Phase:** 12.1 — Close gap: ID-02 — fix bum version branding (INSERTED)  
**Checked:** 2026-07-22  
**Plans:** 2 (`12.1-01`, `12.1-02`)  
**Checker:** gsd-plan-checker (revision gate)  
**Verdict:** **PASS**

---

## Phase goal (source of truth)

From ROADMAP.md:

> Close the release-blocking ID-02 gap: `bum version` (and any remaining version printers) identify the product as **bum**, not stock `grok`

**Requirements:** ID-02  

**Success criteria:**

1. `bum version` prints a bum product label (not `grok …`) with version/hash still present
2. Grep/static gates show no remaining hard-coded `grok {version}` / product-label regressions on the version path
3. Live or hermetic probe reconfirms ID-02 for the version command after the fix

**Locked methodology (CONTEXT):** surgical version-path product-token rename + unit + hermetic + scoped static gates; dual-login UAT not required; no full residual rebrand / crate renames / JSON product field.

---

## Coverage matrix

| Requirement / criterion | Primary delivery | Regression gate | Status |
|-------------------------|------------------|-----------------|--------|
| SC-1 `bum version` → bum + version/hash | 12.1-01 T2 wire `format_version_cmd_line` + `display_version_with_commit` | 12.1-02 T1 hermetic starts_with `bum ` + contains `.` | Covered |
| SC-2 static residual on version path | 12.1-01 T2 post-wire `! rg '"grok {}"'` | 12.1-02 T2 formal static gate | Covered |
| SC-3 hermetic/live reconfirm | — | 12.1-02 T1 `hermetic_bum_version_product_token_is_bum` | Covered |
| ID-02 product token `bum` only (D-01/D-02) | 12.1-01 T1 const + pure formatter + unit | 12.1-02 hermetic | Covered |
| Keep version/hash/channel plumbing (D-03) | 12.1-01 T2 keeps `display_version_with_commit` + `channel_label` | hermetic `.` present | Covered |
| JSON unchanged (D-04) | 12.1-01 T2 leave JSON branch byte-for-byte | optional lightweight guard (discretion) | Covered |
| Scope: version path only (D-05/D-06) | both plans scope boundaries | deferred residuals excluded | Covered |
| Unit + hermetic + static (D-07/D-10/D-11) | 01 unit | 02 hermetic + static | Covered |
| No dual-login UAT (D-08) | 12.1-02 T2 VALIDATION Manual-Only empty / excluded | — | Covered |

**Requirement ID `ID-02` appears in both plans' `requirements` frontmatter.**

---

## Wave / dependency graph

```text
Wave 1: 12.1-01  (depends_on: [])
Wave 2: 12.1-02  (depends_on: ["12.1-01"])
```

| Check | Result |
|-------|--------|
| Acyclic | ✅ |
| No future refs | ✅ |
| Wave numbers consistent | ✅ (wave 2 = dep max + 1) |
| depends_on format | ✅ matches phase 11/12 style (`12.1-01`) |
| No same-wave file conflict | ✅ (01 owns main.rs; 02 owns home_isolation + VALIDATION) |

---

## Plan structure summary

| Plan | Wave | Tasks | Files (declared) | Autonomous | Structure |
|------|------|-------|------------------|------------|-----------|
| 01 | 1 | 2 (tdd + tdd) | `xai-grok-pager-bin/src/main.rs` | true | Valid — Files/Action/Verify/Done + `<automated>` |
| 02 | 2 | 2 (tdd + auto) | `home_isolation.rs`, `12.1-VALIDATION.md` | true | Valid — Files/Action/Verify/Done + `<automated>` |

Scope: 2 tasks/plan, 1–2 files/plan — well within budget.

---

## Dimension results

### 1. Requirement coverage — ✅ PASS

ID-02 is the sole roadmap requirement. Both plans claim it. Tasks cover product-token fix, version/hash keep, static residual, and hermetic reconfirm — maps 1:1 to success criteria 1–3.

### 2. Task completeness — ✅ PASS

All four tasks have `<files>`, `<action>`, `<verify>` with `<automated>`, and `<done>`. Actions name concrete symbols (`VERSION_CMD_PRODUCT_NAME`, `format_version_cmd_line`, `p8_bin_version_cmd_product_is_bum`, `hermetic_bum_version_product_token_is_bum`), line neighborhoods, and exact acceptance shapes.

### 3. Dependency correctness — ✅ PASS

See graph. Plan 02 correctly waits for plan 01 wire-up before hermetic/static acceptance.

### 4. Key links planned — ✅ PASS

| Link | Planned in |
|------|------------|
| `VERSION_CMD_PRODUCT_NAME` → `format_version_cmd_line` | 01 key_links + T1 action |
| `format_version_cmd_line` → `Command::Version` non-JSON println | 01 key_links + T2 action |
| `display_version_with_commit` + `channel_label` kept | 01 T2 action/behavior |
| Hermetic `CARGO_BIN_EXE_bum version` → live Version path | 02 key_links + T1 |
| Static `rg '"grok {}"'` scoped to pager-bin main.rs | 01 T2 verify + 02 T2 |

Unit-only green without wire is explicitly avoided (T1 helper first, T2 wire, plan 02 hermetic).

### 5. Scope sanity — ✅ PASS

Surgical gap-close only: composition-root version text + regression gates. No residual rebrand, crate renames, clap `--version` churn, JSON product field, or dual-login UAT. Matches CONTEXT boundary and user check criteria (“only version path + regression gate”).

### 6. Verification derivation — ✅ PASS

`must_haves.truths` are user/gate-observable (`bum version` product token, residual zero, hermetic stdout, JSON keys left alone). Artifacts and key_links support those truths.

### 7. Context compliance — ✅ PASS

CONTEXT locked decisions (version format, fix scope, regression gate) are implemented as D-01..D-08 / D-10 / D-11 in plan tables and task text.

| Locked area | Task evidence |
|-------------|----------------|
| Product label `bum` only | 01 T1 const + unit equality |
| Shape `bum {version}` | 01 T1 formatter + unit |
| Keep display_version + channel | 01 T2 wire |
| JSON no product field | 01 T2 leave JSON unchanged |
| Primary fix Version human path | 01 T2 only non-JSON branch |
| No full residual / no crate renames | both scope boundaries |
| Unit + hermetic + scoped static | 01 unit + 02 hermetic + static |
| Dual-login UAT not required | 02 T2 VALIDATION / D-08 |

**Deferred ideas excluded:** residual string sweep, crate renames, JSON product field, dual-login UAT — all restated as out of scope.

**Discretion resolved appropriately:** local `VERSION_CMD_PRODUCT_NAME`, pure formatter, `p8_bin_*` unit next to banner helpers, separate hermetic fn in `home_isolation.rs` — matches RESEARCH/PATTERNS recommendations.

### 7b. Scope reduction — ✅ PASS

No `v1` / `static for now` / `placeholder` / `wire later` language reducing locked decisions. Full product-token rename + dual automated gates as decided.

### 7c. Architectural tier compliance — ✅ PASS

| Capability | Map tier | Plan placement |
|------------|----------|----------------|
| Version human product label | Composition-root binary | 01 → `pager-bin` main.rs |
| Version/hash composition | Shared lib (keep) | 01 keeps `xai-grok-version` / update channel |
| Hermetic probe | Integration test | 02 → `home_isolation.rs` |
| Static residual gate | Planning/CI grep | 02 acceptance commands |

No capability moved to a less-trusted tier.

### 8. Nyquist compliance — ✅ PASS

| Task | Plan | Wave | Automated Command | Status |
|------|------|------|-------------------|--------|
| 01-01 | 01 | 1 | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` | ✅ |
| 01-02 | 01 | 1 | unit + `! rg '"grok {}"'` main.rs + scoped fmt | ✅ |
| 02-01 | 02 | 2 | `cargo test … --test home_isolation hermetic_bum_version_product -- --nocapture` | ✅ |
| 02-02 | 02 | 2 | static residual + full home_isolation + fmt + VALIDATION exists | ✅ |

- VALIDATION.md present (`12.1-VALIDATION.md`)
- No `<automated>MISSING</automated>` / no watch-mode
- Sampling: Wave 1 2/2 automated; Wave 2 2/2 automated
- Wave 0: infrastructure ready (existing cargo/test harness); product asserts are plan deliverables (TDD), not broken MISSING links
- `workflow.nyquist_validation` enabled in config

### 9. Cross-plan data contracts — ✅ PASS

Plan 01 produces wired binary behavior; plan 02 consumes stdout of `bum version` and source residual. No conflicting sanitize/transform pipelines.

### 10. CLAUDE.md compliance — ⏭️ SKIPPED (no `./CLAUDE.md`)

AGENTS.md conventions honored: per-crate tests, scoped fmt, no root Cargo.toml churn, product identity without crate renames.

### 11. Research resolution — ⚠️ WARNING (non-blocking)

`12.1-RESEARCH.md` has `## Open Questions` without `(RESOLVED)` suffix. All three questions have Recommendations and are operationally resolved in plans:

1. **Naming** → `p8_bin_version_cmd_product_is_bum` (plans 01)
2. **home_isolation vs new file** → separate fn in same file (plan 02)
3. **REQUIREMENTS checkbox** → plan 02 says re-verify in SUMMARY only; no checkbox ownership flip here

Not a goal-delivery gap; optional hygiene: rename section to `## Open Questions (RESOLVED)` with inline RESOLVED markers.

### 12. Pattern compliance — ✅ PASS

| File | Analog (PATTERNS.md) | Plan reference |
|------|----------------------|----------------|
| `main.rs` | same-file `format_pager_banner` + `p8_bin_*` + Version arm | 01 read_first + action |
| `home_isolation.rs` | same-file hermetic spawn | 02 read_first + action + PATTERNS |

Shared patterns (local PRODUCT_NAME const, pure helper unit test, hermetic env, static scoped grep, scoped fmt) appear in plan actions.

### Verify command format sanity — ✅ PASS

Automated verifies use `set -euo pipefail` (or `&&` chains) without `2>/dev/null || echo "0"` false-pass patterns. Static residual uses fail-closed `if rg …; then exit 1; fi` / `! rg`. No hard-coded magic test counts as sole acceptance.

Plan 02 **action** (not formal verify) uses `grep -c … || true` for discovery preflight; final `<automated>` re-runs real cargo tests without count-swallowing — acceptable.

### Numeric/factual claim authority — ✅ PASS

Live residual `"grok {}"` at main.rs ~1634 confirmed at check time. Plan numeric claims are shapes/filters, not stale RESEARCH counts.

---

## Issues

### Blockers

*None.*

### Warnings

```yaml
issues:
  - plan: null
    dimension: research_resolution
    severity: warning
    description: >
      12.1-RESEARCH.md Open Questions section lacks (RESOLVED) suffix / inline RESOLVED
      markers, though all three items have Recommendations and are implemented in plans.
    fix_hint: >
      Optional: retitle to "## Open Questions (RESOLVED)" and mark each item RESOLVED with
      the chosen answer (p8_bin naming; separate home_isolation test fn; REQUIREMENTS at verify).
  - plan: "12.1-02"
    dimension: task_completeness
    severity: info
    task: 2
    description: >
      Action preflight uses n=$(… | grep -c … || true) discovery; formal <automated> is
      clean. Prefer keeping discovery out of automated verify (already true).
    fix_hint: No plan change required.
```

---

## Goal-backward verdict

Starting hypothesis (plans fail goal) is **disproved** by:

1. Known live gap site is the only production edit target (`Command::Version` non-JSON in pager-bin main.rs).
2. Product token fix is wired (not helper-only) before hermetic acceptance.
3. All three ROADMAP success criteria have executable tasks + automated verify.
4. Locked CONTEXT decisions are fully scoped; deferred work is excluded.
5. Scope stays at version path + regression gate — suitable for an inserted gap-close phase.

**Plans verified. Run `/gsd:execute-phase 12.1` to proceed.**
