---
phase: 12.1-close-gap-id-02-fix-bum-version-branding
verified: 2026-07-22T06:21:56Z
status: passed
score: 3/3 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: false
---

# Phase 12.1: Close gap: ID-02 — fix bum version branding Verification Report

**Phase Goal:** Close the release-blocking ID-02 gap: `bum version` (and any remaining version printers) identify the product as **bum**, not stock `grok`
**Verified:** 2026-07-22T06:21:56Z
**Status:** passed
**Re-verification:** No — initial verification
**Mode:** mvp (roadmap goal is technical; PLANs supply user-story framing — verified against roadmap Success Criteria + plan user-story outcome)

## User Flow Coverage

User story (from 12.1-0N-PLAN): *As a bum daily-driver user, I want to see `bum version` identify the product as bum with version/hash still present, so that the release-blocking ID-02 branding gap is closed.*

| Step | Expected | Evidence | Status |
|------|----------|----------|--------|
| Invoke `bum version` | CLI accepts `version` subcommand | Live `target/debug/bum version` exit 0; hermetic `arg("version")` spawn | ✓ |
| Read product label | First stdout line starts with `bum ` not `grok ` | Live: `bum 0.1.220-alpha.4 (3ef08b0)`; hermetic test asserts `starts_with("bum ")` / `!starts_with("grok ")` | ✓ |
| Confirm version body | Version/hash digits present on same line | Live hash `(3ef08b0)` + dotted version; hermetic asserts `contains('.')` | ✓ |
| Outcome | ID-02 residual on version path closed without dual-login UAT | Unit + hermetic + static residual green; D-08 UAT excluded by design | ✓ |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | `bum version` prints a bum product label (not `grok …`) with version/hash still present | ✓ VERIFIED | Live: `target/debug/bum version` → `bum 0.1.220-alpha.4 (3ef08b0)`. Wire: `Command::Version` non-JSON uses `format_version_cmd_line(&display_version_with_commit(...))` at `main.rs:1641–1647`. Product const `VERSION_CMD_PRODUCT_NAME = "bum"` at `main.rs:94`. |
| 2 | Grep/static gates show no remaining hard-coded `grok {version}` / product-label regressions on the version path | ✓ VERIFIED | `rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` → zero matches. Human text path no longer formats with stock product token; only formatter uses product const. |
| 3 | Live or hermetic probe reconfirms ID-02 for the version command after the fix | ✓ VERIFIED | Live binary (above) + `cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product` → `hermetic_bum_version_product_token_is_bum ... ok`. Unit: `p8_bin_version_cmd_product_is_bum ... ok`. |

**Score:** 3/3 truths verified (0 present, behavior-unverified)

### Supporting plan truths (non-roadmap, all pass)

| Truth | Status | Evidence |
|-------|--------|----------|
| Version/hash/channel from `display_version_with_commit` + channel plumbing | ✓ VERIFIED | `main.rs:1643–1646` calls `display_version_with_commit(env!("VERSION_WITH_COMMIT"), channel_label())` |
| JSON `--json` keys remain `currentVersion` + `channel` only | ✓ VERIFIED | Live: `{"currentVersion":"0.1.220-alpha.4 (3ef08b0)","channel":"unknown"}`; JSON arm `main.rs:1634–1639` |
| Unit gate `p8_bin_version_cmd_product_is_bum` | ✓ VERIFIED | Green filter run (1 passed) |
| Dual-login UAT not required (D-08) | ✓ VERIFIED | CONTEXT/PLAN/VALIDATION explicit exclusion; no gap |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `crates/codegen/xai-grok-pager-bin/src/main.rs` | `VERSION_CMD_PRODUCT_NAME` + `format_version_cmd_line` + wired `Command::Version` | ✓ VERIFIED | Exists, substantive, wired into Version arm; live stdout proves data flow |
| `crates/codegen/xai-grok-pager-bin/src/main.rs` | `p8_bin_version_cmd_product_is_bum` unit gate | ✓ VERIFIED | Test present + green |
| `crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs` | `hermetic_bum_version_product_token_is_bum` | ✓ VERIFIED | Test present + green; env traps + asserts |
| `.planning/phases/12.1-.../12.1-VALIDATION.md` | Nyquist map for ID-02 gates | ✓ VERIFIED | status executed; unit/hermetic/static rows green |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `format_version_cmd_line` | `Command::Version` non-JSON println | `println!("{}", format_version_cmd_line(&display_version_with_commit(...)))` | ✓ WIRED | `main.rs:1641–1647` |
| `VERSION_CMD_PRODUCT_NAME` | `format_version_cmd_line` body | `format!("{VERSION_CMD_PRODUCT_NAME} {version_with_channel}")` | ✓ WIRED | `main.rs:94–98` |
| `CARGO_BIN_EXE_bum` / `arg("version")` | Command::Version text path | Hermetic env_clear + HOME/BUM_HOME + GROK/CODEX traps | ✓ WIRED | `home_isolation.rs:170–229` green |
| Static `rg` on pager-bin main.rs | stock product-token residual | expect zero matches | ✓ WIRED | zero matches on HEAD |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Version text line | `version_with_channel` | `display_version_with_commit(env!("VERSION_WITH_COMMIT"), channel_label())` | Yes — live hash `3ef08b0` and version `0.1.220-alpha.4` | ✓ FLOWING |
| Product token | `VERSION_CMD_PRODUCT_NAME` | local const `"bum"` | Yes — live prefix `bum ` | ✓ FLOWING |
| JSON payload | `currentVersion`, `channel` | `env!("VERSION_WITH_COMMIT")` + `channel_name()` | Yes — live JSON non-empty keys only | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Live product token + version/hash | `target/debug/bum version` | `bum 0.1.220-alpha.4 (3ef08b0)` | ✓ PASS |
| Live JSON shape | `target/debug/bum version --json` | keys `currentVersion`, `channel` only | ✓ PASS |
| Unit formatter | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` | 1 passed (`p8_bin_version_cmd_product_is_bum`) | ✓ PASS |
| Hermetic probe + isolation suite | `cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture` | 2 passed | ✓ PASS |
| Static residual | `rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` | zero matches | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| (none declared as `scripts/*/tests/probe-*.sh`) | n/a | Phase uses cargo unit/integration + static rg as gates | SKIP (covered by spot-checks) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| ID-02 | 12.1-01, 12.1-02 | Product UI chrome / user-facing strings present as **bum** — residual version path | ✓ SATISFIED (scoped residual) | Live + hermetic + unit + static on `Command::Version` path. Broader residual rebrand explicitly out of scope (D-06 / CONTEXT deferred). |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | No TBD/FIXME/XXX in phase-touched production/test paths reviewed | — | — |

Note: Incidental stock strings outside the version path (e.g. error text mentioning `grok-pager agent`) remain deferred residual rebrand (D-06) — not phase blockers.

### Human Verification Required

None. D-08 dual-login UAT explicitly excluded; all locked ID-02 behaviors for this gap have automated + live evidence.

### Gaps Summary

No gaps. Phase goal achieved on HEAD:

1. Human-readable `bum version` product token is **bum** with version/hash present.
2. Stock `"grok {}"` format residual absent from pager-bin `main.rs`.
3. Hermetic integration probe and unit gate reconfirm ID-02.

---

_Verified: 2026-07-22T06:21:56Z_
_Verifier: Claude (gsd-verifier)_
