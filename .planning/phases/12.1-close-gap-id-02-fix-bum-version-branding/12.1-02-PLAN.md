---
phase: 12.1-close-gap-id-02-fix-bum-version-branding
plan: 02
type: execute
wave: 2
depends_on:
  - 12.1-01
files_modified:
  - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
  - .planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-VALIDATION.md
autonomous: true
requirements:
  - ID-02
must_haves:
  truths:
    - "Hermetic spawn of bum version stdout first line starts with bum  and does not start with stock grok  (D-07, D-10)"
    - "Scoped static residual grep on version path is zero matches after plan 01 fix (D-11)"
    - "No dual-login live UAT required for this gap close (D-08)"
    - "Version/hash digits still present on hermetic stdout (D-03 keep)"
  artifacts:
    - path: crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
      provides: "hermetic_bum_version_product_token_is_bum integration gate"
      contains: "hermetic_bum_version_product_token_is_bum"
    - path: .planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-VALIDATION.md
      provides: "Nyquist map with green hermetic + static commands for ID-02"
      contains: "ID-02"
  key_links:
    - from: CARGO_BIN_EXE_bum version argv
      to: Command::Version text path (plan 01)
      via: "hermetic env_clear HOME BUM_HOME GROK_HOME CODEX_HOME traps"
      pattern: "arg(\"version\")"
    - from: static rg scoped to pager-bin main.rs
      to: stock product-token format residual
      via: "expect zero matches post-fix"
      pattern: "grok"
---

## Phase Goal

**As a** bum daily-driver user, **I want to** see `bum version` identify the product as bum with version/hash still present, **so that** the release-blocking ID-02 branding gap is closed.

<objective>
Lock the plan-01 product-token fix with a hermetic binary probe and a scoped static anti-regression gate (D-07, D-10, D-11). Dual-login UAT remains out of scope (D-08). Update phase VALIDATION map to mark ID-02 version gates automated.

Purpose: Unit-only green can miss a still-stock println; hermetic spawn + static residual grep close that hole.
Output: Green `home_isolation` product-token test + formal static gate + VALIDATION sampling updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-CONTEXT.md
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-RESEARCH.md
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-PATTERNS.md
@.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-01-PLAN.md
@crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
@crates/codegen/xai-grok-pager-bin/src/main.rs
</context>

## Locked decisions (this plan)

| ID | Decision |
|----|----------|
| D-07 | Unit + hermetic binary probe + scoped static greps |
| D-08 | Dual-login live UAT **not** required |
| D-10 | Hermetic `bum version` stdout starts with `bum ` / not stock product token |
| D-11 | Scoped static grep forbids reintroduction of stock product-token format on version path |
| D-03 | Version/hash still present (assert digits / `.` on hermetic first line) |
| D-04 | JSON shape unchanged — optional lightweight key-set guard only if cheap |
| D-06 | Do not expand into full residual rebrand greps |

## Scope boundaries

- Do **not** edit production code outside tests unless plan 01 left a compile break (should already be green).
- Do **not** add repo-wide unscoped `grok` greps as phase gates (deferred residuals intentionally remain).
- Do **not** run dual-login UAT or require network/OAuth (D-08).
- Do **not** flip REQUIREMENTS.md ID-02 checkbox ownership here if already Complete — note re-verification in SUMMARY only; optional one-line note in VALIDATION that live residual is closed by these gates.
- Do **not** workspace-wide fmt.

## Discretion (resolved)

- Separate test function `hermetic_bum_version_product_token_is_bum` in the same `home_isolation.rs` file (keeps isolation test focused).
- Optional shared spawn helper only if duplication is painful — not required.
- Optional JSON key-set assert co-located — allowed but not locked; skip if it bloats the task.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hermetic bum version product-token probe (D-07, D-10, D-03)</name>
  <files>crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs</files>
  <read_first>
    - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs (bum_bin + hermetic_temp_home_writes_only_under_bum_home env pattern ~14–134)
    - 12.1-PATTERNS.md hermetic recommended test
    - Plan 01 SUMMARY if present (confirm format_version_cmd_line wired)
  </read_first>
  <behavior>
    - hermetic_bum_version_product_token_is_bum: spawn CARGO_BIN_EXE_bum with arg version under isolated HOME/BUM_HOME and GROK_HOME/CODEX_HOME traps; exit success
    - First non-empty stdout line trimmed starts with "bum "
    - First line does not start with stock product token "grok "
    - First line contains '.' (version digits present)
  </behavior>
  <action>
    Per D-07, D-10, D-03 and 12.1-PATTERNS.md: add a new `#[test] fn hermetic_bum_version_product_token_is_bum` in `home_isolation.rs` that reuses the same hermetic spawn pattern as `hermetic_temp_home_writes_only_under_bum_home` (TempDir home, BUM_HOME, GROK_HOME/CODEX_HOME traps, telemetry/updater env knobs, env_clear + PATH restore, Windows USERPROFILE when cfg).

    Spawn `bum_bin()` with single arg `version` (not `--version`). Assert status success with stdout/stderr in panic message. Parse first line of stdout (trim). Assert starts_with `"bum "`. Assert does not start_with stock product token plus trailing space. Assert first line contains `'.'` so version body was not dropped.

    Do not weaken or remove existing isolation assertions in the original test. Do not require network. Do not touch production main.rs unless compile requires (should not). Prefer leaving isolation test and brand test as two functions.
  </action>
  <verify>
    <automated>set -euo pipefail; cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product -- --nocapture</automated>
  </verify>
  <done>
    Hermetic probe green: live `bum version` path product token is bum with version body present (D-10, D-03).
  </done>
</task>

<task type="auto">
  <name>Task 2: Scoped static residual gate + VALIDATION map (D-11, D-08)</name>
  <files>.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-VALIDATION.md</files>
  <read_first>
    - crates/codegen/xai-grok-pager-bin/src/main.rs Command::Version arm (confirm plan 01 wire-up)
    - 12.1-VALIDATION.md (seeded draft)
    - 12.1-RESEARCH.md Validation Architecture section
  </read_first>
  <action>
    Per D-11: run the scoped static residual gate against the version path owner only:

    ```
    set -euo pipefail
    # Precondition after plan 01: zero matches (command must fail to find matches)
    if rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs; then
      echo "FAIL: stock product-token format residual still present in pager-bin main.rs" >&2
      exit 1
    fi
    ```

    <!-- planner-discipline-allow: "grok {}" -->

    Also confirm unit + hermetic filters discover at least one test each:
    ```
    set -euo pipefail
    n=$(cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --list 2>/dev/null | grep -c p8_bin_version || true)
    test "$n" -ge 1
    n=$(cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product -- --list 2>/dev/null | grep -c hermetic_bum_version_product || true)
    test "$n" -ge 1
    cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture
    cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture
    cargo fmt -- --check crates/codegen/xai-grok-pager-bin/src/main.rs crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
    ```

    Update `12.1-VALIDATION.md`: set status/nyquist fields appropriately for executed gates (wave_0_complete true; mark per-task rows for 12.1-01-01, 12.1-01-02, 12.1-02-01, 12.1-02-02 automated commands File Exists ✅; note D-08 manual UAT excluded). Do not claim phase archive/milestone audit closed — that is verify/archive work. Document D-08: no dual-login UAT in Manual-Only (none required).

    Do not open full residual inventory. Do not edit non-version crates.
  </action>
  <verify>
    <automated>set -euo pipefail; if rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs; then echo FAIL residual; exit 1; fi; cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture; cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture; cargo fmt -- --check crates/codegen/xai-grok-pager-bin/src/main.rs crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs; test -f .planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-VALIDATION.md</automated>
  </verify>
  <done>
    Static residual gate zero on pager-bin main.rs; unit + full home_isolation green; VALIDATION maps ID-02 gates; D-08 UAT not required.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test process → hermetic HOME | Spawned `bum` must not write ambient developer `~/.bum` / `~/.grok` / `~/.codex` |
| Static grep → source tree | Scoped path only; unscoped greps would false-fail on deferred residuals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.1-03 | Tampering | home_isolation hermetic spawn | medium | mitigate | env_clear + HOME/BUM_HOME + GROK_HOME/CODEX_HOME traps; no ambient auth store touch |
| T-12.1-04 | Information Disclosure | version probe | low | mitigate | No dual-login UAT (D-08); version path does not load credentials for print |
| T-12.1-05 | Elevation of Privilege | N/A | low | accept | Local CLI version command; no privilege boundary crossed |
| T-12.1-SC | Tampering | package installs | low | accept | Zero new packages — no install surface |
</threat_model>

<verification>
Phase-level gate sequence (all must pass):
1. Unit: `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture`
2. Hermetic: `cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture`
3. Static: no match for stock product-token format residual under pager-bin main.rs
4. Scoped fmt check on touched Rust files
</verification>

<success_criteria>
1. Hermetic `bum version` first line starts with `bum ` and includes version digits (D-10, D-03)
2. Scoped static residual gate is green (D-11)
3. Full home_isolation suite still green (isolation + brand)
4. VALIDATION documents automated coverage for ID-02; D-08 UAT not required
</success_criteria>

<output>
Create `.planning/phases/12.1-close-gap-id-02-fix-bum-version-branding/12.1-02-SUMMARY.md` when done
</output>
