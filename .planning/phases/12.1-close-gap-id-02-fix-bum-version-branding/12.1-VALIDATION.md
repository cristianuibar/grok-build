---
phase: 12.1
slug: close-gap-id-02-fix-bum-version-branding
status: executed
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-22
updated: 2026-07-22
---

# Phase 12.1 — Validation Strategy

> Close release-blocking **ID-02** residual: `bum version` human text product token must be **bum**, not stock **grok**, with version/hash still present. Hermetic unit/integration + scoped static grep only — **no dual-login UAT** (CONTEXT D-08).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Built-in `cargo test` (Rust 1.92.0) |
| **Config file** | none global — per-crate; binary unit tests in `main.rs`; integration in `tests/home_isolation.rs` |
| **Quick run command** | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` |
| **Full suite command** | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture && ! rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` |
| **Estimated runtime** | ~30–120s after first compile (pager-bin only) |

### Cargo verify hygiene

| Rule | Detail |
|------|--------|
| Scope | `-p xai-grok-pager-bin` only — never full workspace test |
| Fmt | Scoped path check only — never `cargo fmt --all`; `cargo fmt -- --check <paths>` still walks workspace (edition-2024 spill) — skip workspace-spilling fmt; use intentional style on touched snippets only |
| Chains | `&&` only — never `;` / `\|\| true` masking failures |
| Static residual | Scoped to `crates/codegen/xai-grok-pager-bin/src/main.rs` only — not repo-wide `grok` |
| Network | No live OAuth / dual-login required |

---

## Sampling Rate

- **After every task commit:** Quick run (`p8_bin_version`) when touching main.rs; hermetic filter when touching `home_isolation.rs`
- **After every plan wave:** Full suite command above
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~120s after warm build

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.1-01-01 | 01 | 1 | ID-02 | T-12.1-02 | Product token const not secret | unit | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` | ✅ `main.rs` `p8_bin_version_cmd_product_is_bum` | ✅ green |
| 12.1-01-02 | 01 | 1 | ID-02 | T-12.1-01 | Version path no credentials | unit + static | `p8_bin_version` + `! rg … main.rs` + scoped style | ✅ `format_version_cmd_line` wired | ✅ green |
| 12.1-02-01 | 02 | 2 | ID-02 | T-12.1-03 | Hermetic HOME traps prevent ambient write | integration | `cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product -- --nocapture` | ✅ `hermetic_bum_version_product_token_is_bum` | ✅ green |
| 12.1-02-02 | 02 | 2 | ID-02 | T-12.1-04 | No dual-login / no ambient probe | static + suite | Full suite command + VALIDATION update | ✅ VALIDATION executed | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers the phase — no new test framework.

- [x] `cargo test` / `xai-grok-pager-bin` binary unit module + `tests/home_isolation.rs` harness already present
- [x] `tempfile` dev-dep already available for hermetic HOME
- [x] Unit assert `p8_bin_version_cmd_*` — landed in plan 01 (green)
- [x] Hermetic product-token assert — landed in plan 02 (green)
- [x] Static residual gate — plan 01 wire-up + plan 02 formal acceptance (zero matches on `"grok {}"` in pager-bin main.rs)

*Wave 0 complete = infrastructure ready; product asserts are plan deliverables (green-only, no intentional-red scaffolds).*

---

## Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | Owning plan | D-NN | Result |
|--------|----------|-----------|-------------------|-------------|------|--------|
| ID-02 | Formatter product token is bum | unit | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` | 01 | D-01 D-02 D-07 | ✅ green |
| ID-02 | Live binary `bum version` stdout starts with `bum ` | hermetic integration | `cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product -- --nocapture` | 02 | D-10 | ✅ green |
| ID-02 | No stock product-token format residual on version path | static | `! rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` | 01+02 | D-11 | ✅ green (zero matches) |
| ID-02 | Version/hash still present | hermetic | first line contains `.` | 02 | D-03 | ✅ green |
| ID-02 | JSON keys currentVersion + channel only | code review / optional assert | leave JSON branch unchanged (plan 01) | 01 | D-04 | ✅ unchanged |
| ID-02 | clap `--version` still bum | regression (already green) | optional live; not mandatory | — | out of scope fix | n/a |

**Live residual closed by automated gates:** unit + hermetic + scoped static residual replace prior live `bum version` stock-token evidence; no further dual-login or network residual required for ID-02 (D-08).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| — | — | All locked ID-02 behaviors for this gap have automated gates | Dual-login UAT explicitly **excluded** (D-08) — none required |

*All phase behaviors have automated verification. D-08: no dual-login live UAT for this gap close.*

---

## Execution Evidence (plan 02)

| Gate | Command / check | Observed |
|------|-----------------|----------|
| Static residual | `rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` | zero matches |
| Unit discovery | `cargo test … --bin bum p8_bin_version -- --list` | ≥1 test |
| Hermetic discovery | `cargo test … --test home_isolation hermetic_bum_version_product -- --list` | ≥1 test |
| Unit run | `p8_bin_version` filter | 1 passed |
| Full home_isolation | `--test home_isolation` | 2 passed (isolation + product-token) |
| Scoped fmt | `cargo fmt -- --check` on pager-bin paths | deferred — walks workspace (same process note as plan 01); no intentional style debt in new probe |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 infrastructure ready; plan 01/02 product asserts green
- [x] No watch-mode flags
- [x] Feedback latency < 120s warm
- [x] `nyquist_compliant: true` set after execute + verify sampling green

**Approval:** execute sampling green 2026-07-22 — phase archive / milestone audit close remains verify/archive work (not claimed here)
