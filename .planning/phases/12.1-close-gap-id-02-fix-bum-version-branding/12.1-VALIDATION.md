---
phase: 12.1
slug: close-gap-id-02-fix-bum-version-branding
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-07-22
---

# Phase 12.1 — Validation Strategy

> Close release-blocking **ID-02** residual: `bum version` human text product token must be **bum**, not stock **grok**, with version/hash still present. Hermetic unit/integration + scoped static grep only — **no dual-login UAT** (CONTEXT D-08).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Built-in `cargo test` (Rust 1.92.0) |
| **Config file** | none global — per-crate; binary unit tests in `main.rs`; integration in `tests/home_isolation.rs` |
| **Quick run command** | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` |
| **Full suite command** | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture && cargo test -p xai-grok-pager-bin --test home_isolation -- --nocapture && ! rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` |
| **Estimated runtime** | ~30–120s after first compile (pager-bin only) |

### Cargo verify hygiene

| Rule | Detail |
|------|--------|
| Scope | `-p xai-grok-pager-bin` only — never full workspace test |
| Fmt | Scoped path check only — never `cargo fmt --all` |
| Chains | `&&` only — never `;` / `\|\| true` masking failures |
| Static residual | Scoped to `crates/codegen/xai-grok-pager-bin/src/main.rs` only — not repo-wide `grok` |
| Network | No live OAuth / dual-login required |

---

## Sampling Rate

- **After every task commit:** Quick run (`p8_bin_version`) when touching main.rs; hermetic filter when touching `home_isolation.rs`
- **After every plan wave:** Full suite command above
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~120s after warm build

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.1-01-01 | 01 | 1 | ID-02 | T-12.1-02 | Product token const not secret | unit | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` | ❌ lands in 01 | ⬜ pending |
| 12.1-01-02 | 01 | 1 | ID-02 | T-12.1-01 | Version path no credentials | unit + static | `p8_bin_version` + `! rg … main.rs` + scoped fmt | ⚠️ main.rs exists; wire-up in 01 | ⬜ pending |
| 12.1-02-01 | 02 | 2 | ID-02 | T-12.1-03 | Hermetic HOME traps prevent ambient write | integration | `cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product -- --nocapture` | ❌ lands in 02 | ⬜ pending |
| 12.1-02-02 | 02 | 2 | ID-02 | T-12.1-04 | No dual-login / no ambient probe | static + suite | Full suite command + VALIDATION update | ✅ VALIDATION draft | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers the phase — no new test framework.

- [x] `cargo test` / `xai-grok-pager-bin` binary unit module + `tests/home_isolation.rs` harness already present
- [x] `tempfile` dev-dep already available for hermetic HOME
- [ ] Unit assert `p8_bin_version_cmd_*` — lands in plan 01 (not pre-stubbed intentional-red)
- [ ] Hermetic product-token assert — lands in plan 02
- [ ] Static residual gate — plan acceptance (plan 01 wire-up + plan 02 formal)

*Wave 0 complete = infrastructure ready; product asserts are plan deliverables (green-only, no intentional-red scaffolds).*

---

## Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | Owning plan | D-NN |
|--------|----------|-----------|-------------------|-------------|------|
| ID-02 | Formatter product token is bum | unit | `cargo test -p xai-grok-pager-bin --bin bum p8_bin_version -- --nocapture` | 01 | D-01 D-02 D-07 |
| ID-02 | Live binary `bum version` stdout starts with `bum ` | hermetic integration | `cargo test -p xai-grok-pager-bin --test home_isolation hermetic_bum_version_product -- --nocapture` | 02 | D-10 |
| ID-02 | No stock product-token format residual on version path | static | `! rg -n '"grok \{\}"' crates/codegen/xai-grok-pager-bin/src/main.rs` | 01+02 | D-11 |
| ID-02 | Version/hash still present | hermetic | first line contains `.` | 02 | D-03 |
| ID-02 | JSON keys currentVersion + channel only | code review / optional assert | leave JSON branch unchanged (plan 01) | 01 | D-04 |
| ID-02 | clap `--version` still bum | regression (already green) | optional live; not mandatory | — | out of scope fix |

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| — | — | All locked ID-02 behaviors for this gap have automated gates | Dual-login UAT explicitly **excluded** (D-08) |

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 infrastructure ready; plan 01/02 product asserts green
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s warm
- [ ] `nyquist_compliant: true` set after execute + verify sampling green

**Approval:** pending
