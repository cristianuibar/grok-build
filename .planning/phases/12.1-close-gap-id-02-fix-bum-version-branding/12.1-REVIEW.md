---
phase: 12.1-close-gap-id-02-fix-bum-version-branding
reviewed: 2026-07-22T06:35:00Z
depth: standard
files_reviewed: 2
files_reviewed_list:
  - crates/codegen/xai-grok-pager-bin/src/main.rs
  - crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 12.1: Code Review Report

**Reviewed:** 2026-07-22T06:35:00Z
**Depth:** standard
**Files Reviewed:** 2
**Status:** clean

## Summary

Phase 12.1 closes the ID-02 residual: human-readable `bum version` must identify the product as **bum**, not stock **grok**. The change set is intentionally tiny:

| File | What changed |
|------|----------------|
| `crates/codegen/xai-grok-pager-bin/src/main.rs` | `VERSION_CMD_PRODUCT_NAME` + pure `format_version_cmd_line`; `Command::Version` non-JSON path wires through the formatter; unit gate `p8_bin_version_cmd_product_is_bum` |
| `crates/codegen/xai-grok-pager-bin/tests/home_isolation.rs` | Hermetic spawn probe `hermetic_bum_version_product_token_is_bum` |

Reviewed against plan SUMMARYs and `12.1-VERIFICATION.md` (passed 3/3 must-haves). Adversarial pass focused on correctness of the product-token path, accidental scope bleed (JSON / clap / residual rebrand), and hermetic test reliability.

**Assessment:** Implementation is correct, scoped, and well-gated. No bugs, security issues, or maintainability defects that should block ship.

All reviewed files meet quality standards. No issues found.

## Narrative Findings (AI reviewer)

### Correctness

- `format_version_cmd_line` composes `{VERSION_CMD_PRODUCT_NAME} {version_with_channel}` — same shape as the previous stock `"grok {}"` path, only the product token changes.
- `Command::Version` non-JSON arm still sources version/hash/channel via `display_version_with_commit(env!("VERSION_WITH_COMMIT"), channel_label())`; JSON arm is untouched (`currentVersion` + `channel` only) — matches D-03 / D-04.
- Static residual: no `"grok {}"` remains in pager-bin `main.rs`.
- Unit test locks exact equality `bum 0.1.220-alpha.4 (f3eccc6)` plus starts-with / not-starts-with guards on the pure helper.

### Security

- No new network, auth, filesystem, or secret surface.
- Hermetic probe uses `env_clear()` + HOME/BUM_HOME + GROK_HOME/CODEX_HOME traps (child process only) — same isolation pattern as the existing home-isolation test. No credential leakage or path-traversal risk.

### Tests

- Hermetic probe asserts first non-empty stdout line starts with `bum `, rejects `grok `, and requires a version body (`.`).
- Separation of product-token probe from isolation writes test matches plan D-07 / D-10 and avoids coupling unrelated assertions.
- Weakness of `contains('.')` alone is acceptable here: unit gate already pins exact format; hermetic gate only needs to reconfirm the live binary path after composition-root wire-up.

### Out of scope (not findings)

- Residual non-version `grok` / `grok-pager` strings elsewhere in `main.rs` (e.g. agent subcommand error text) remain deferred rebrand per D-06 / CONTEXT.
- Clap long `--version` / crate package names intentionally unchanged (D-04 / D-06).
- Shared hermetic env-setup helper between the two `home_isolation` tests would be a style DRY preference only — not a defect for this gap close.

---

_Reviewed: 2026-07-22T06:35:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
